# 前端 → 后端 对接确认单（多议题）

> 提交方：微信小程序前端
> 日期：2026-06-15
> 处理方：后端数据库开发
>
> | 议题 | 主题 | 优先级 |
> | --- | --- | --- |
> | 议题一 | 「申请售后」点击无反应 | 高（用户无法发起售后申诉） |
> | 议题二 | 首页「仓库」角标改为未读提醒 | 中（需求语义变更，需后端支持） |

---

# 议题一：「申请售后」点击无反应

## 一、问题现象

「积分活动记录」页 →「消费记录」Tab → 每条记录的「申请售后」按钮，**点击后页面无任何反应**（不跳转、不报错）。

同一张卡片上的「查看详情」按钮工作正常。

---

## 二、根本原因（前端已完整排查）

前端链路代码均正确，唯一断点是「申请售后」按钮取不到记录主键 `consumption_record_id`，导致跳转条件不成立而静默失败。

### 数据流（纯文本）

```
后端 GET /api/v4/shop/consumption/me
        │  返回 data.records[]
        ▼
每条 record 是否包含字段 consumption_record_id ← 关键主键
        │
        ▼
前端 loadConsumptionRecords() 原样透传 (...record)
        │
        ▼
WXML 按钮 data-record-id="{{item.consumption_record_id}}"
        │
        ▼
点击 onApplyAfterSale(e)
   recordId = e.currentTarget.dataset.recordId
        │
        ├── 有值 → wx.navigateTo 跳转售后页 ✅
        └── 空值 → 中止，页面无反应 ❌（当前现象）
```

### 同卡片两个按钮的对比（关键佐证）

| 按钮 | 取值方式 | 结果 |
| --- | --- | --- |
| 查看详情 | `data-record="{{item}}"`（整个对象） | 对象一定存在 → 正常 ✅ |
| 申请售后 | `data-record-id="{{item.consumption_record_id}}"`（单个字段） | 字段缺失则为空 → 失败 ❌ |

**结论**：「查看详情」正常而「申请售后」无反应，说明后端 `consumption/me` 列表项**未返回 `consumption_record_id` 字段**，或该主键字段名与前端约定不一致。

---

## 三、需要后端确认的事项

| 编号 | 确认项 | 说明 |
| --- | --- | --- |
| Q1 | `GET /api/v4/shop/consumption/me` 返回的 `data.records[]` 中，每条记录**是否包含 `consumption_record_id` 字段**？ | 请提供一条真实返回 JSON 样例（金额可脱敏） |
| Q2 | 该接口若经过 `DataSanitizer` 处理，消费记录主键是**保留为 `consumption_record_id`，还是被改写成通用 `id`**？ | 前端售后入口、详情查询都依赖此主键 |
| Q3 | 售后申诉接口 `POST /api/v4/system/disputes`，当 `order_type=consumption` 时，`order_id` 的口径**就是 `consumption_record_id` 吗**？ | 需与列表返回主键字段对齐一致 |

---

## 四、期望后端提供的信息

1. `consumption/me` 一条真实记录的完整 JSON 字段清单（含主键字段名）。
2. 主键字段的最终命名（前端按「以后端为准」直接采用，不做映射）。
3. `order_id` 与消费记录主键的对应关系确认。

---

## 五、字段对齐约定

遵循「以后端数据库为准，前端适配后端」原则：

- 后端确认列表项主键字段名后，前端直接将 WXML 的 `data-record-id` 改为对应字段，**不做字段映射**。
- 全链路（后端 API → TS 逻辑层 → WXML 模板层）统一使用 snake_case 主键字段名。

---

## 六、前端已做的处理（本次）

- 文件：`packageUser/records/trade-upload-records/trade-upload-records.ts`
- 改动：`onApplyAfterSale` 由「静默失败」改为「缺主键时明确报错（Toast + 日志）」并为跳转增加失败回调，便于真机用 vConsole 复现定位。
- 质量检测：ESLint 通过、tsc --noEmit 通过、Prettier 通过。

---

## 七、相关接口与路径

| 用途 | 方法 | 路径 |
| --- | --- | --- |
| 消费记录列表 | GET | `/api/v4/shop/consumption/me` |
| 提交售后申诉 | POST | `/api/v4/system/disputes` |

---

# 议题二：首页「仓库」角标改为未读提醒

## 一、问题现象

首页「仓库」入口按钮上显示「99+」红色角标。用户进入仓库页查看后再退出，角标**仍然显示 99+**，没有清零。

## 二、根因分析（前端已排查，非故障）

当前角标取的是后端 `GET /api/v4/backpack/stats` 的 `total_items`（仓库可用物品**总数**），不是「未查看物品数」。

```
进入仓库页 → 只是查看列表（GET 查询），不消耗物品 → 物品总数不变
        ▼
返回首页 onShow → 重新请求 /backpack/stats
        ▼
后端返回 total_items 仍为 100+（用户没用掉物品）
        ▼
角标仍显示 99+ ← 符合当前实现，非 bug
```

**结论**：前端、Web 管理端、后端均无故障。这是角标**业务语义**与用户预期不一致——用户期望它是「未读提醒」（看过即清零），而当前实现是「物品总数」（用掉才减少）。

## 三、产品决策（已确认）

角标语义改为**未读提醒**：只显示「新获得、尚未查看过的物品数」，用户进入仓库查看后清零。

## 四、为什么前端无法独立实现

「未读/已读」是需要持久化的状态，必须由后端记录，前端自行计算会因换设备/重装而丢失，且违反「业务数据必须由后端权威提供」的项目规则。当前 `stats` 接口也没有「未读数」这个数据。

## 五、需要后端确认/提供的事项

| 编号 | 事项 | 说明 |
| --- | --- | --- |
| Q1 | 物品表（items 或等价表）增加「是否已查看」状态字段 | 建议 `is_viewed`（BOOLEAN）+ `first_viewed_at`（DATETIME，UTC 存储） |
| Q2 | `GET /api/v4/backpack/stats` 返回中增加未查看物品数字段 | 建议 `unviewed_items`（INT），专供首页角标使用 |
| Q3 | 新增「标记已读」接口 | 建议 `POST /api/v4/backpack/mark-viewed`，用户进入仓库列表页时调用，批量标记未读为已读 |
| Q4 | 「已查看」的判定口径 | 进入仓库列表页即算已读，还是需点开单个物品详情才算？请后端与产品商定 |

## 六、字段对齐约定

遵循「以后端数据库为准，前端适配后端」原则：

- 后端提供 `unviewed_items` 字段后，前端把首页角标数据源从 `total_items` 改为 `unviewed_items`，不做映射。
- 前端在仓库列表页加载时调用「标记已读」接口；接口字段命名以后端为准。
- 全链路统一 snake_case。

## 七、前端当前状态

- 暂不改动角标相关代码（改动即前端自造业务数据，违反规则）。
- 等后端提供 `unviewed_items` 字段与「标记已读」接口后再对接。

## 八、相关接口与路径

| 用途 | 方法 | 路径 |
| --- | --- | --- |
| 背包统计（角标数据源） | GET | `/api/v4/backpack/stats` |
| 标记物品已读（待新增） | POST | `/api/v4/backpack/mark-viewed`（建议） |
