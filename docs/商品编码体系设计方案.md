# 商品编码体系设计方案

> 版本：v1.4　|　创建日期：2026-06-29　|　施工完成：2026-07-06（北京时间）　|　状态：**后端 + Web 管理后台已全量施工落地并实连真实库验证通过**（落地清单与验证记录见 §16）；微信小程序完整对接说明见 §16.4
> v1.4 补记（2026-07-06）：补齐 §8.3 三项增强（手册 PDF 导出 5b / 导出清单二维码 5c / 扫码输码快速定位 5d）并修复商品导出 blob 下载 5e；均属 admin 前端，后端无需改动（所需数据 item_code/系列/价格/图片/编码搜索归一化早已具备，本轮实连真实库 + 真实 API 复核通过）。
> v1.5 补记（2026-07-06）：新增 §17「S1–S5 业务逻辑实施方案」——拍板 5 个场景全做、按 §13.7 顺序分期逐个上线（S1采购→S2批次→S4组合→S5分销→S3二手）；10 张表+外键+模型已就位，本方案补齐 Service/路由/幂等/前端，全程复用现网范式、零新增技术栈。**§17 为待施工方案，尚未开始编码**。
> 适用项目：天工商户营销平台（Node.js + Express + Sequelize + MySQL 8 + Redis）

## 1. 背景与目标

平台商品中心采用 EAV 三层模型（SPU `exchange_items` / SKU `exchange_item_skus` / 属性），目前 SPU 层缺少对外业务编码，仅有自增主键；SKU 层 `sku_code` 现有规则将属性含义编入编码，存在耦合与语义漂移风险。

核心业务诉求：**用户需根据产品手册上印刷的商品编码，到系统中手工搜索定位商品。**

| 目标 | 落地方式 |
|---|---|
| 长期维护成本低、零技术债 | 编码无意义，业务含义全部落在 EAV 属性表，编码永不"说谎" |
| 不耦合类目/属性 | 编码不含任何业务语义，类目或属性变更不影响编码 |
| 防枚举、不泄露经营数据 | 随机生成，竞品无法顺序爬取全量目录或推算上架速度 |
| 适合手册查找（人工读/输） | 全大写、去易混淆字符、分组展示、搜索容错归一化 |
| 零新增技术栈 | 复用现有 `crypto` 随机 + 唯一索引重试范式，不引入雪花/Redis 发号器 |

**核心铁律**：内部关联只认 BIGINT 自增主键；对外标识用随机无意义码；业务含义一律落在属性表，不编进编码本身。

## 2. 方案选型依据（行业对照）

| 标识类型 | 代表 | 形态 | 是否适合本项目 |
|---|---|---|---|
| 内部主键（机器流转） | 阿里 item_id、京东 SKU、腾讯雪花 | 12–19 位长数字 | 否，人无法手输 |
| 含义编码（人工编） | 小公司货号 `CB4385`、活动码 | 4–15 位含义串 | 否，类目变更即语义错误 |
| **对外随机短码（人读/输）** | **亚马逊 ASIN `B0xxxxxxxx`**、YouTube、Stripe | **前缀 + 随机无意义码** | **是，本项目采用** |

结论：本项目商品形态为半标品（手串等），编码需对外展示且供人工手册查找，同时是交易平台需防枚举。唯一同时满足的方案是"前缀 + 随机无意义码 + 全大写无歧义字符集"，与亚马逊 ASIN 范式一致。

## 3. 编码规范定义

### 3.1 字符集（全大写无歧义，30 字符）

```
23456789ABCDEFGHJKMNPQRSTUVWXYZ
```

剔除易混淆字符 `0 O I L 1`，全大写、不区分大小写，与项目现有 `RedemptionCodeGenerator` 字符集完全统一。

### 3.2 编码格式

| 用途 | 字段 | 存储规范形 | 展示形（分组） | 示例 |
|---|---|---|---|---|
| SPU 平台展示码 | `exchange_items.item_code` | `SP` + 12 位随机 | `SP-XXXX-XXXX-XXXX` | `SP-7K9M-Q3RW-X2NV` |
| SKU 平台展示码 | `exchange_item_skus.sku_code` | `SK` + 12 位随机 | `SK-XXXX-XXXX-XXXX` | `SK-4N8P-TX2H-9QYR` |
| 供应商货号 | `exchange_items.supplier_item_code` | 原样存储 | 原样 | `CB4385`、`F-090` |

- 前缀 `SP`/`SK` 仅标识层级（SPU/SKU），层级永不变，不携带业务含义，不违反无意义原则。
- 随机体 12 位 @ 30 字符集 = 30¹² ≈ 5.3×10¹⁷ 空间，撞码概率极低，唯一索引兜底 + 重试。
- 12 位可被 4 整除，分组工整为 4+4+4。

### 3.3 存储 / 展示 / 搜索的三态处理

| 形态 | 用途 | 例子 |
|---|---|---|
| 规范形（存储） | 数据库存储、唯一索引、内部匹配 | `SP7K9MQ3RWX2NV` |
| 展示形（输出） | 前端、产品手册、对账单 | `SP-7K9M-Q3RW-X2NV` |
| 输入归一化（搜索） | 用户输入 → 转大写 + 去分隔符/空格后匹配规范形 | 输入 `sp-7k9m...` / `sp 7k9m...` 均命中 |

### 3.5 多端场景与展示边界

本编码同一套贯穿线下手册与线上系统，但**各端展示策略不同**：

| 端 | 编码展示 | 编码搜索入口 | 说明 |
|---|---|---|---|
| 线下产品手册 | 印刷展示 `SP-XXXX-XXXX-XXXX` | —— | C 端顾客 + 内部人员均使用 |
| 微信小程序（C 端） | **不展示编码字段** | **保留：输编码搜商品** | 编码"只进不出"，作搜索输入但不作展示字段露出（同淘宝隐藏 item_id） |
| 管理后台 | 完整展示 SP/SK 码 | 支持 | 运营管理、导出手册清单 |

落地约束：
1. **小程序 C 端**：商品列表/详情**不渲染** `item_code` 字段；但搜索接口必须接受编码输入（经容错归一化后匹配），支持用户照手册输码定位商品。API 可返回 `item_code` 供搜索回显，但前端不作为商品信息字段展示。
2. **统一展示形**：凡展示编码处（手册、管理后台、对账单）一律输出带连字符展示形 `SP-XXXX-XXXX-XXXX`；数据库统一存无连字符规范形。禁止部分带横线部分不带。
3. **容错归一化**：用户输入无论大小写/横线/空格，搜索接口先归一化再匹配。
4. **手册编码由系统导出**：手册编码由管理后台自动生成并导出（商品名 + SP 码 + 图片清单），杜绝人工誊抄出错。
5. **可选增强**：手册编码旁可附二维码/条形码，支持扫码跳转与手工输入并存。

### 3.4 供应商货号说明

截图中的"商品端码"（`CB4385`/`F-090`）是供货方自有货号，属外部来源：**可重复、可空、不建唯一索引、不参与任何内部关联**，仅作采购对账参考字段。严禁用于系统内部关联或作为主键。

#### 为什么必须把它当"脏数据参考字段"而非标识

供应商货号天然**杂乱、格式不统一、且可能重复**，这正是本方案坚持"内部只认 BIGINT 主键、对外用自生成随机码"的核心理由：

| 现实问题 | 举例 | 本方案的处理 |
|---|---|---|
| 各供应商编码规则各异 | A 厂 `CB4385`、B 厂 `F-090`、C 厂 `2024-春-001` | 原样存 `VARCHAR(100)`，**不强制格式**、不归一化校验 |
| 不同供应商货号可能撞号 | A 厂和 B 厂都叫 `001` | **不建唯一索引**，允许重复；靠内部 `exchange_item_id` 区分 |
| 同一货号被复用/改版 | 旧款下架后新款沿用 `F-090` | 我方 `item_code` 不变不复用，与供应商改版解耦 |
| 可能为空/缺失 | 小作坊无正式货号 | `allowNull`，留空不影响任何流程 |
| 含中文/空格/特殊符号 | `天然-巴西·A级` | 当纯文本存储，不参与匹配/排序键 |

落地约束：
- **只读参考**：仅用于采购对账、与供货方沟通时人工比对，**不参与** SPU/SKU 关联、库存、定价、搜索主链路。
- **不做唯一约束、不做去重**：重复是正常现象，系统不报错、不合并。
- **辅助查询（本次做）**：运营可"按供应商货号找货"，做成**模糊查询且明确提示可能多条命中**（因可重复），结果仍以我方 `item_code`/`item_name` 为准定位。详见 §3.8。
- **供应商维度（本次做，多供应商）**：新增独立 `suppliers` 主数据表 + `exchange_item_suppliers` 关联表（一个 SPU 可挂多个供应商，多源采购）；货号 `supplier_item_code` 落在关联表上（每个供应商对同一 SPU 有各自货号）。详见 §3.8 与 §4.5。

一句话：**供应商货号是"他们的语言"，我方系统只把它当便签贴着看；所有内部逻辑只认我方自己生成的、干净唯一的 `item_code`/`sku_code`。**

### 3.8 供应商维度 + 货号辅助查询（本次新增，多供应商模型）

> 实测：现网 `merchants` 表是"核销商家"（带 `commission_rate`/结算账户，用于券核销），**与"供货商"语义完全不同**，不可复用。故新增独立 `suppliers` 表承载供货维度。
>
> 拍板结论：**一个商品可对应多个供应商**（多源采购），故采用「`suppliers` 主数据 + `exchange_item_suppliers` 多对多关联」，货号挂在关联行上。

数据模型（多对多）：

| 表 | 关键字段 | 说明 |
|---|---|---|
| `suppliers` | `supplier_id` PK、`supplier_name` UNIQUE | 供应商主数据；名称**加唯一约束**防重复建档 |
| `exchange_item_suppliers` | `exchange_item_id` + `supplier_id` + `supplier_item_code` | SPU↔供应商关联行；每行存该供应商对此 SPU 的货号 |

字段约束：
- `exchange_item_suppliers.supplier_item_code`：NULL、**可重复**、不建唯一约束（脏数据如实存），**加普通索引**加速辅助查询。
- `(exchange_item_id, supplier_id)`：建唯一约束（同一 SPU 同一供应商只挂一行，避免重复关联）。
- SPU 不强制挂供应商：自营/未知来源可不建任何关联行（等价"供应商可空"）。
- 可选标记 `is_primary`：多供应商时标主供货商（展示/默认对账用），非必需。

辅助查询能力（管理端）：
1. **按货号模糊查**：`exchange_item_suppliers.supplier_item_code LIKE %kw%`，结果列表标注"共 N 条命中"，每条带 `供应商名 + 我方 item_code + 商品名`，让运营据我方码精确定位。
2. **按供应商筛货**：选定 `supplier_id` 列出其全部供货商品。
3. **组合定位**：`(supplier_id + 货号)` 组合查询，把"不同供应商撞号"区分开。
4. **冲突提示**：当同一供应商对**多个 SPU** 用了相同 `supplier_item_code` 时，列表给非阻断的"⚠️ 该供应商存在重复货号"提示，供人工核对（系统不自动合并/拒绝）。

### 3.6 系列连号编码（双轨制）

业务需求：同一产品系列需要一组**可读、连续**的编码，用于（B）人能看出"某系列第几个"、（C）手册/包装印连号成套、（D）实物批次/防伪连号。

连号与随机互斥（连号必然有序可枚举），故采用**双轨制**：一个商品同时拥有两个编码，各司其职。

| 轨道 | 字段 | 形态 | 是否连号 | 用途 |
|---|---|---|---|---|
| 随机主码 | `item_code` | `SP-XXXX-XXXX-XXXX` 随机 | 否 | 对外标识、系统关联、小程序搜索、防枚举 |
| 可读系列号 | `series_code` + `series_seq` | `SLNB-001` 系列码+连续序号 | 是 | 系列归类、手册/包装成套印刷、实物批次 |

示例：

```
玫瑰石英手串  item_code=SP-7K9M-Q3RW-X2NV  系列号=SLNB-001
黑曜石手串    item_code=SP-4N8P-TX2H-9QYR  系列号=SLNB-002   ← 主码无关联，系列号连续
紫水晶手串    item_code=SP-A3CD-FH5J-K6MN  系列号=SLNB-003
```

设计要点：
- **随机主码不变**：`item_code` 仍随机，防枚举能力不受影响。
- **系列号是含义码 + 连号**：会暴露单系列规模（如 `SLNB-001~050` 即 50 款），但这正是主动印给用户看的成套信息，非机密；主码仍保密。
- **序号在系列内连续**：`series_seq` 按系列独立递增，展示为 `series_code` + 补零（默认 3 位）。
- **序号空洞**：商品下架/删除后序号不回收（保持连续可追溯），允许出现空号。
- 系列归属可空：未归入任何系列的商品 `series_id` 为 NULL，仅有随机主码。

#### 数据模型

新增 `product_series` 表 + `exchange_items` 两个字段：

| 表/字段 | 类型 | 说明 |
|---|---|---|
| `product_series.series_id` | BIGINT PK AI | 系列主键 |
| `product_series.series_code` | VARCHAR(20) UNIQUE | 可读系列码（如 `SLNB`），全大写 |
| `product_series.series_name` | VARCHAR(100) | 系列名称 |
| `product_series.next_seq` | INT | 系列内下一个序号（发号用，事务内自增） |
| `product_series.status` | ENUM(active/inactive) | 状态 |
| `exchange_items.series_id` | BIGINT NULL FK | 所属系列，可空 |
| `exchange_items.series_seq` | INT NULL | 系列内连续序号 |

系列号展示形 = `series_code` + `-` + `series_seq` 补零，如 `SLNB-001`。`(series_id, series_seq)` 建唯一索引保证系列内不重号。

### 3.7 前瞻性扩展字段（现在预留，将来启用）

为避免将来加列改动迁移，本次预留两类外部/实物标识字段，**当前留空不强制使用**：

| 字段 | 所在层 | 类型 | 说明 |
|---|---|---|---|
| `barcode` | SKU `exchange_item_skus` | VARCHAR(20) NULL | 国际标准条码（UPC/EAN-13/GTIN），对接商超/海外/扫码枪。全球唯一标准码，区别于供应商货号 |
| `nfc_uid` | 实物 `items` | VARCHAR(64) NULL | NFC 芯片硬件 UID，实物防伪载体 |
| `anti_fake_code` | 实物 `items` | VARCHAR(32) NULL | 防伪码（可独立于 tracking_code），用于扫码验真 |
| `verify_count` | 实物 `items` | INT NOT NULL DEFAULT 0 | 被验真次数（判断盗刷） |
| `first_verified_at` | 实物 `items` | DATETIME NULL | 首次验真时间 |

设计约束：
- **条码挂 SKU 层**：条码对应具体规格，与国际惯例一致；区别于 `supplier_item_code`（供应商内部货号）。
- **防伪挂实物层**：防伪是"每一件实物"的事，依托已有 `items.tracking_code`（一物一码）。
- **仅预留字段，验真流程为后续路线图**：扫码验真接口、防盗刷规则、NFC 读写为独立子系统，本次不实现，待业务启动时再设计。
- 预留字段全部 NULL/默认值，不影响现有逻辑。

## 4. 数据库变更（Sequelize Migration）

### 4.1 `exchange_items` 新增 `item_code` 列

```sql
ALTER TABLE exchange_items
  ADD COLUMN item_code VARCHAR(14) NULL COMMENT '平台商品展示码(SPU,无意义随机码 SP+12位规范形)' AFTER exchange_item_id,
  ADD UNIQUE INDEX uk_item_code (item_code);
```

- `item_code` 先建为 `NULL`（兼容存量数据未回填），回填完成后可在后续迁移收紧为 `NOT NULL`。
- **供应商货号不在本表加列**：因拍板「多供应商」，货号 `supplier_item_code` 落在 `exchange_item_suppliers` 关联表（见 §4.5），`exchange_items` 不再新增 `supplier_item_code`/`supplier_id`。

### 4.2 `exchange_item_skus.sku_code` 重构

保持 `UNIQUE` 约束不变，列宽由 `VARCHAR(100)` 收窄至 `VARCHAR(14)`，生成规则改为 `SK` + 12 位随机规范形。

同时预留国际条码字段：

```sql
ALTER TABLE exchange_item_skus
  ADD COLUMN barcode VARCHAR(20) NULL COMMENT '国际标准条码(UPC/EAN/GTIN,预留,可空)' AFTER sku_code;
```

### 4.4 实物防伪预留字段 + 批次列（`items`）

```sql
ALTER TABLE items
  ADD COLUMN nfc_uid          VARCHAR(64) NULL COMMENT 'NFC芯片UID(防伪,预留)',
  ADD COLUMN anti_fake_code   VARCHAR(32) NULL COMMENT '防伪码(扫码验真,预留)',
  ADD COLUMN verify_count     INT NOT NULL DEFAULT 0 COMMENT '被验真次数',
  ADD COLUMN first_verified_at DATETIME NULL COMMENT '首次验真时间',
  ADD COLUMN batch_id         BIGINT NULL COMMENT '所属批次(product_batches.batch_id,预留;S2批次管理启用时补FK)';
ALTER TABLE items ADD INDEX idx_items_batch (batch_id);
```

- 防伪字段当前不参与业务逻辑，验真流程为后续路线图（S2/防伪子系统）。
- `batch_id` 本次**一并加列 + 普通索引 + 外键**：因 §14 本次同时建 `product_batches` 表，外键可直接建（`FK→product_batches.batch_id`，`ON DELETE SET NULL`）。当前全部为 NULL，不影响任何逻辑。

### 4.3 系列连号（新增表 + 字段）

```sql
CREATE TABLE product_series (
  series_id   BIGINT NOT NULL AUTO_INCREMENT COMMENT '系列主键',
  series_code VARCHAR(20) NOT NULL COMMENT '可读系列码(全大写,如 SLNB)',
  series_name VARCHAR(100) NOT NULL COMMENT '系列名称',
  next_seq    INT NOT NULL DEFAULT 1 COMMENT '系列内下一个序号(发号用)',
  status      ENUM('active','inactive') NOT NULL DEFAULT 'active',
  created_at  DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (series_id),
  UNIQUE INDEX uk_series_code (series_code)
) COMMENT='产品系列(连号系列号)';

ALTER TABLE exchange_items
  ADD COLUMN series_id  BIGINT NULL COMMENT '所属系列(product_series.series_id,可空)' AFTER item_code,
  ADD COLUMN series_seq INT NULL COMMENT '系列内连续序号' AFTER series_id,
  ADD UNIQUE INDEX uk_series_seq (series_id, series_seq);
```

- 发号在事务内：读 `product_series.next_seq` → 赋给 `series_seq` → `next_seq+1`，用行锁/分布式锁防并发重号。
- 序号不回收，允许空号，保证系列内连续可追溯。
- `product_series` 增 `seq_pad INT NOT NULL DEFAULT 3`：序号展示补零位数（默认 3 → `001`），个别超 999 的系列可单独调大，无需全局改。

### 4.5 供应商主数据 + 商品多对多关联（本次新增，多供应商）

```sql
CREATE TABLE suppliers (
  supplier_id   BIGINT NOT NULL AUTO_INCREMENT COMMENT '供应商主键',
  supplier_name VARCHAR(100) NOT NULL COMMENT '供应商名称',
  contact_name  VARCHAR(50)  NULL COMMENT '联系人',
  contact_phone VARCHAR(20)  NULL COMMENT '联系电话',
  status        ENUM('active','inactive') NOT NULL DEFAULT 'active' COMMENT '状态',
  notes         VARCHAR(500) NULL COMMENT '备注',
  created_at    DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (supplier_id),
  UNIQUE INDEX uk_suppliers_name (supplier_name)
) COMMENT='供货商主数据(区别于核销 merchants)';

CREATE TABLE exchange_item_suppliers (
  id                 BIGINT NOT NULL AUTO_INCREMENT COMMENT '关联主键',
  exchange_item_id   BIGINT NOT NULL COMMENT '商品SPU(exchange_items)',
  supplier_id        BIGINT NOT NULL COMMENT '供应商(suppliers)',
  supplier_item_code VARCHAR(100) NULL COMMENT '该供应商对此SPU的原始货号(可空可重复,仅参考)',
  is_primary         TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否主供货商(展示/默认对账)',
  purchase_price     DECIMAL(10,2) NULL COMMENT '最近进货价(预留,S1采购单启用时维护)',
  quality_score      DECIMAL(3,1)  NULL COMMENT '供货质量评分(预留,0.0~10.0,S1采购评估用)',
  created_at         DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (id),
  UNIQUE INDEX uk_item_supplier (exchange_item_id, supplier_id),
  INDEX idx_eis_supplier (supplier_id),
  INDEX idx_eis_supplier_code (supplier_item_code)
) COMMENT='商品-供应商多对多关联(货号挂关联行)';
```

- `suppliers` 与现网 `merchants`（核销商家）**互不复用**：语义不同（供货 vs 核销结算）；`supplier_name` **加唯一约束**防重复建档。
- 货号 `supplier_item_code` 落在**关联行**上：同一 SPU 多个供应商各有各的货号；**加普通索引**加速 §3.8 辅助查询；仍非唯一、允许重复、可空。
- `purchase_price`/`quality_score` 本次**一并加列**（按用户要求一次性到位），当前留空不参与逻辑；S1 采购单/对账（§13.2）启用时再写入维护。
- `(exchange_item_id, supplier_id)` 唯一：同一 SPU 同一供应商只挂一行。
- SPU 不强制挂供应商（自营/未知留空，无关联行即可），等价"供应商可空"。
- **不在 `exchange_items` 上加 `supplier_id` 列**（多供应商场景下单列表达不了多对多），归属全部走关联表。

> 说明（与早前单列方案的差异）：因拍板「一个商品可多供应商」，原 §3.4/§4.5 设想的 `exchange_items.supplier_id + supplier_item_code` 单列方案作废，改为上面的关联表模型。`exchange_items` 本表**不再新增** `supplier_id`/`supplier_item_code` 两列。

## 5. 生成器 `utils/ProductCodeGenerator.js`

与 `RedemptionCodeGenerator` / `TrackingCodeGenerator` 同范式（静态类 + 唯一性重试），职责单一：

| 方法 | 作用 |
|---|---|
| `generate(prefix)` | 生成 `prefix` + 12 位随机规范形 |
| `format(code)` | 规范形 → 4+4+4 分组展示形 |
| `normalize(input)` | 用户输入 → 转大写 + 去分隔符/空格的规范形 |
| `validate(code, prefix)` | 校验格式 `^PREFIX[CHARS]{12}$` |
| `generateUnique(prefix, checkUnique, maxRetries=5)` | 带唯一性回调重试，5 次冲突抛错（显式失败，不静默兜底） |

要点：
- 使用 `crypto.randomInt`（非 `Math.random`），密码学随机不可预测。
- 唯一性校验在事务内执行 `findOne`，撞码重试；超限抛错，符合"显式失败"原则。

### 5.1 系列号发号 `utils/SeriesSeqAllocator.js`

| 方法 | 作用 |
|---|---|
| `allocate(seriesId, transaction)` | 事务内分配系列内下一个连续序号（行锁读 `next_seq` → 返回 → `next_seq+1`） |
| `format(seriesCode, seq, pad=3)` | 组装展示形系列号，如 `SLNB-001` |

并发安全：发号必须在事务内对 `product_series` 行加锁（`SELECT ... FOR UPDATE`）或配合现有 `UnifiedDistributedLock`，避免并发重号；`(series_id, series_seq)` 唯一索引兜底。

## 6. Service 层改造点

| 位置 | 改造 |
|---|---|
| `ExchangeItemService` 创建 SPU | 事务内调用 `generateUnique('SP', ...)` 回填 `item_code` |
| `_buildCartesianSkuCode`（笛卡尔批量） | 替换为 `generateUnique('SK', ...)`，去除 `P{pid}_{attr}_{opt}` 属性拼接 |
| `_generateUniqueSkuCode`（单 SKU） | 统一收口到 `generateUnique('SK', ...)` |
| 人工传入 `sku_code` 口子 | **移除**，所有 SKU 码统一系统生成，杜绝人工填错 |
| `admin/ItemManagementService.js` 默认 SKU 码 | `default_{id}` 改为 `SK` 随机码 |
| 商品搜索接口 | 按 `item_code` 查询时先 `normalize` 输入再匹配规范形 |
| 系列管理 | 新增 `ProductSeriesService`：系列 CRUD；商品归入系列时事务内分配 `series_seq` |
| SPU 创建/编辑 | 选填 `series_id`；归入系列时分配连续序号，回填 `series_seq` |
| 供应商管理 | 新增 `SupplierService`：`suppliers` CRUD（名称唯一）；SPU 编辑时增删 `exchange_item_suppliers` 关联行（多供应商 + 各自货号 + is_primary） |
| 货号辅助查询 | `QueryService` 增按 `exchange_item_suppliers.supplier_item_code` 模糊 + `supplier_id` 筛选 + 组合查询，返回多条命中（不去重） |

## 7. 影响面与风险

| 项 | 评估 |
|---|---|
| 表结构 | 加 2 列 + 1 唯一索引，SKU 列宽收窄。低风险 |
| 存量数据 | 本次不回填，`item_code` 暂为 NULL，不影响现有查询 |
| 接口 | 不兼容旧接口（已确认），响应体新增 `item_code`/`supplier_item_code` |
| 前端（admin） | 商品列表/详情展示新码列；创建表单移除 sku_code 手填 |
| 旧 `sku_code` 依赖 | 若被用于排序/前缀匹配查询，随机码后失效，需 grep 排查修正 |
| 测试 | 新增 `ProductCodeGenerator` 单测；修正 `ExchangeItemService` 相关断言 |
| 系列号并发 | 发号需事务行锁/分布式锁，否则并发会重号；唯一索引兜底 |
| 系列号暴露规模 | 连号会暴露单系列款数（可接受，主码仍随机保密） |

## 8. 实施步骤

1. 编写 `utils/ProductCodeGenerator.js` + `utils/SeriesSeqAllocator.js` + 单元测试
2. Sequelize migration（item_code/supplier_item_code + sku_code 收窄 + barcode 预留 + product_series 表 + series 字段 + items 防伪预留字段）
3. 改 `models/ExchangeItem.js`、`models/ExchangeItemSku.js`、`models/Item.js`，新增 `models/ProductSeries.js`
4. 改 `ExchangeItemService`（SPU 回填 item_code、SKU 生成收口、移除人工传入口子、系列归属与发号）
5. 新增 `ProductSeriesService`（系列 CRUD + 发号）；改 `admin/ItemManagementService.js` 默认 SKU 码
6. 商品搜索接口接入归一化；grep 全量排查 `sku_code` 引用并修正
7. 跑 jest 相关用例 + lint 确保通过
8. 数据回填（本次在迁移内对现网 2 条 SPU 调生成器回填 `item_code`，详见 §10.6 步骤 9）

### 8.1 本次纳入的增强功能

- **手册导出清单**：管理后台导出"商品名 + SP 码 + 图片"清单（用于印刷手册），输出展示形编码。
- **二维码/条形码**：为商品编码生成二维码，支持扫码跳转商品页，与手工输入并存。

### 8.2 本次范围边界

- **仅处理商品编码**：本次只新增/重构商品 SPU 的 `item_code`、SKU 的 `sku_code` 与供应商货号 `supplier_item_code`。
- **不改动其他编码**：`tracking_code`（物品实例）、`template_code`（物品模板）、`campaign_code`（活动）保持现状——其用途无需用户手册手输，现有"前缀+日期+序号"风格对运营更有意义，且已有线上数据与引用，强行统一风险大、收益低。

## 8.3 Web 管理端配套功能

后端先行，前端依赖后端接口完成后再实现。

### 必备配套（编码方案落地，与前述增强一并实现）

| # | 功能 | 落地位置 | 说明 |
|---|---|---|---|
| 1 | 列表/详情展示 SP/SK 码 | 商品列表表格、商品详情、SKU 管理 | 表格新增编码列，展示形 `SP-XXXX-XXXX-XXXX`；裸自增 ID 不再对运营暴露为主标识 |
| 2 | 编码搜索框 | 商品列表（现缺关键词搜索） | 新增按 `item_code` 搜索，接入容错归一化（大小写/横线/空格均可） |
| 3 | 手册导出清单 | 复用 `export-modal` + 后端 blob 导出 | 导出"商品名 + SP 码 + 图片 + 价格"印刷版清单，扩展 `exchange_item` 预设支持 xlsx |
| 4 | 二维码生成 | 商品详情、导出清单 | 为 SP 码生成二维码，扫码跳转商品页，与手工输入并存 |
| 5 | 供应商货号字段 | 商品编辑表单（多供应商区块） | 可增删多个「供应商 + 该供应商货号 + 主供货商标记」关联行，货号可重复可空，仅对账参考 |
| 9 | 系列管理 + 系列号展示 | 新增系列管理页；商品表单选系列、展示系列号 | 系列 CRUD、商品归入系列、列表/详情/手册展示 `SLNB-001` |
| 10 | 供应商管理页 | 新增 `supplier-management.html`（仿 `merchant-management.html` 结构） | `suppliers` 表 CRUD（名称唯一/联系人/电话/状态/备注），商品表单从这里选供应商挂关联 |
| 11 | 供应商货号辅助查询 | 商品列表新增"按货号/供应商查"入口 | 货号模糊查 + 供应商筛选 + `(供应商+货号)` 组合定位；命中列表标注"共 N 条"，每条带供应商名 + 我方 `item_code` + 商品名 |

### 增强看板（本次纳入）

| # | 功能 | 说明 |
|---|---|---|
| 6 | 扫码/输码快速定位 | 后台输码或扫码直达单品详情，供客服/对账高频使用 |
| 7 | 商品主数据健康看板 | 统计 `item_code` 未回填（NULL）数量、SKU 数、供应商货号缺失率，盯回填进度 |
| 8 | 编码冲突/异常监控 | 记录生成器撞码重试次数与异常告警，问题可追溯 |
| 12 | 供应商货号冲突看板 | 统计「同一供应商下重复货号」清单 + 「无供应商归属」商品数，供采购侧人工核对（非阻断） |
| 13 | 供应商供货分布 | 按供应商统计供货 SPU 数 / 在售数 / 缺货号率，并入健康看板，识别供货集中度 |

### 实现顺序

后端（生成器 + 迁移 + 模型 + Service + 搜索/导出/健康统计 API）完成并自测通过后，再实现 admin 前端（展示列、搜索框、导出清单、二维码、供应商货号表单、健康看板）。

## 9. 关键决策记录

| 决策点 | 结论 | 依据 |
|---|---|---|
| 编码哲学 | 无意义码 + 含义放属性表 | 长期零技术债，契合现有 EAV 架构 |
| 主键策略 | 维持 BIGINT 自增 | 单库无分表，自增安全够用，零新增依赖 |
| 生成方式 | crypto 随机 | 防枚举，不泄露经营数据 |
| 字符集 | 全大写 30 字符去易混淆 | 用户照手册手输，需不区分大小写、无形近字符 |
| 随机位数 | 12 位 | 容量极大、4+4+4 分组工整、配合容错搜索手输负担可接受 |
| 分组展示 | `XX-XXXX-XXXX-XXXX` | 提升手册对照逐段录入准确率 |
| SKU 人工传入 | 移除 | 未上线，统一系统生成彻底防错 |
| 校验位 | 不加 | 唯一索引兜底已足够，避免实现复杂度 |
| 增强功能 | 手册导出清单 + 二维码，本次做 | 支撑线下手册印刷与扫码跳转 |
| 供应商货号 | 本次一起做 | 对接截图"商品端码"，作对账参考字段 |
| 其他编码 | 不动 tracking/template/campaign | 用途不同无需手输，改动风险大收益低 |
| Web 管理端配套 | 必备5项 + 增强看板3项全做 | 编码需配套展示/搜索/导出/二维码方可使用，看板辅助回填与监控 |
| 前后端顺序 | 后端先行，前端后接 | 前端依赖后端接口，避免返工 |
| 系列连号 | 双轨制：随机主码 + 可读系列号 | 连号与随机互斥，分两字段各司其职，主码保密、系列号可读 |
| 系列号实施 | 与基础编码一起设计、一起做 | 改变数据模型，需统一设计避免返工 |
| 国际条码 | SKU 层预留 `barcode` 空字段 | 现在加列成本低，将来对接商超/海外免改迁移 |
| 实物防伪 | items 层预留字段，验真流程列为路线图 | 字段预留成本低；验真是子系统，启用时再设计 |
| 二维码跳转目标 | 小程序商品详情页 path（`item_code` 入参） | 扫码=高级搜索输入，契合"编码只进不出" |
| 手册导出格式 | 先 xlsx（后端 `exceljs`）后 PDF（admin `jspdf` 前端生成） | 两条均零新增依赖，复用现网范式 |
| SKU 码展示程度 | 仅 SKU 管理/详情展示，列表只展示 SPU 的 SP 码 | 避免列表信息过载，对标电商后台 |
| 健康看板位置 | 并入现有 `admin/dashboard-panel.html` 卡片区 | 触达最高、少建一页、降维护成本 |
| 系列码谁定 | 运营手填 + 后端唯一性校验（全大写归一化） | 需印给顾客看，语义需人可读 |
| 系列号补零 | 默认 3 位，建 `product_series.seq_pad` 字段留口 | 实测款数远小于 999，个别超量单独调 |
| 收窄前存量 SKU | 迁移 up 内直接重写现网 2 条为 `SK`+12 位 | 未上线、仅 2 条、无对外引用 |
| C 端 item_code | 保留字段但只读不展示（供搜索回显） | 对标淘宝隐藏 id 可搜，删除则无法回显命中 |
| 供应商货号索引 | 关联表 `exchange_item_suppliers.supplier_item_code` 加普通索引 | §3.8 货号辅助查询本次要做，需索引加速；仍非唯一、允许重复 |
| 供应商维度 | 多供应商：新增 `suppliers` 主数据 + `exchange_item_suppliers` 多对多关联（货号挂关联行） | 拍板一个商品可多源采购；现网 `merchants` 语义不同不可复用 |
| 供应商归属是否必填 | 可空：无关联行=自营/未知，留空不影响流程 | 兼容现网 2 条无供应商存量 |
| 供应商名称唯一 | `suppliers.supplier_name` 加唯一约束 | 防运营重复建档同一供应商 |
| 货号辅助查询 | 本次做：模糊查 + 供应商筛 + 组合定位 + 冲突提示 | 运营需按货号找货；重复多条命中如实展示，不自动合并 |
| 二维码生成方 | 后端出编码 payload，前端渲染图 | 强一致现网消费码范式，后端零新增依赖 |

## 10. 求证结论：对齐真实数据库与现网代码（2026-06-29）

> 求证方式：用项目自带的 Node.js + Sequelize（读 `.env` 的 `DB_HOST=test-db-12-mysql...svc` 内网直连）实连
> `restaurant_points_dev` 库执行 `SHOW FULL COLUMNS / SHOW INDEX` 与抽样查询，未引用任何历史报告/备份文件。
> 后端技术栈以 `package.json`、`config/database.js`、`utils/*Generator.js`、`services/exchange/*`、`migrations/*` 现网代码为准。

### 10.1 真实数据库现状（实测，非备份）

| 表 | 主键 | 与本方案相关的真实列 | 行数 | 结论 |
|---|---|---|---|---|
| `exchange_items` | `exchange_item_id` BIGINT | 无 `item_code`、无 `supplier_item_code`、无 `series_id/series_seq` | 2 | 全部为待新增列 |
| `exchange_item_skus` | `sku_id` BIGINT | `sku_code` VARCHAR(100) NOT NULL UNIQUE；无 `barcode` | 2 | 列宽待收窄、barcode 待加 |
| `exchange_item_attribute_values` | `id` BIGINT | EAV 属性值表，`uk(exchange_item_id,attribute_id)` | 0 | EAV 已就位但暂无数据 |
| `items` | `item_id` BIGINT | `tracking_code` VARCHAR(20) UNIQUE；无 nfc/anti_fake/verify_* | 8 | 防伪列全为待新增 |
| `item_templates` | `item_template_id` BIGINT | `template_code` VARCHAR(100) UNIQUE | 34 | 本方案不动 |

实测样本（`exchange_item_skus.sku_code`）：`P633_202606220629057850800_y5ux`、`P637_202606221301154560800_kt6r`
——印证现行规则为 `P{pid}_{北京时间戳}_{4位随机}`，长度约 30，远超方案目标的 `VARCHAR(14)`，且把 pid 编进了码（耦合）。

### 10.2 现网技术栈（与方案匹配度）

后端完全具备落地本方案的技术基础，**零新增技术栈**：

| 能力 | 现网已有 | 位置 | 复用/扩展 |
|---|---|---|---|
| 随机无意义码 + 去混淆字符集 | `RedemptionCodeGenerator`（30 字符集、`crypto.randomInt`、`generateUnique` 重试） | `utils/RedemptionCodeGenerator.js` | **可直接复用范式**，字符集与方案 3.1 完全一致 |
| 前缀 + 业务码 | `TrackingCodeGenerator`（前缀映射 + 解析/校验/`resolveIdentifier`） | `utils/TrackingCodeGenerator.js` | **可复用** identifier 解析范式做编码搜索 |
| 唯一索引 + 事务重试 | `_generateUniqueSkuCode`（事务内 findOne 重试 5 次显式失败） | `services/exchange/ExchangeItemService.js` | **可复用**，改前缀与字符集即可 |
| 行锁发号 | `UnifiedDistributedLock` + Sequelize 事务 | `utils/` | 系列号 `next_seq` 发号直接复用 |
| 迁移范式 | sequelize-cli + 事务化 up/down + 中文 comment | `migrations/*.js`、`.sequelizerc` | **可复用**，与 §4 SQL 对齐为 queryInterface 写法 |
| Excel 导出 | `exceljs`（已用于商品导出） | `routes/v4/console/exchange/items.js` | **可复用**做手册清单导出 |

> 一处需修正方案描述：方案 §5 写"新增 `utils/ProductCodeGenerator.js`"。现网 `RedemptionCodeGenerator` 已是 30 字符集 + crypto + generateUnique 的同款实现，新生成器应**直接照搬其结构**（静态类、`BASE32_CHARS` 同字符集、`generateUnique(checkUnique, maxRetries)` 同签名），只增 `format/normalize/validate(prefix)` 与前缀参数，避免两套风格分叉。

### 10.3 接口/字段必须以后端技术体系为准（非按前端写法）

现网后端有强约束的 API 规范，方案落地必须遵循，**前端一律适配**：

| 维度 | 后端权威规范（现网） | 落地要求 |
|---|---|---|
| 路径风格 | 扁平 RESTful：C 端 `/api/v4/exchange/items`；管理端 `/api/v4/console/exchange/items` | 编码搜索复用 `GET .../items?keyword=`，**不新开非 v4 路径** |
| 响应包装 | 统一 `res.apiSuccess(data, msg)` / `res.apiError(msg, code, data, status)` | 新增编码字段放进现有 data 结构，不另立响应壳 |
| 字段命名 | 全大写蛇形长名、禁用 12.1 黑名单短名（`check:api-contract` 卡口） | 用 `item_code`/`supplier_item_code`/`series_code`/`series_seq` 全称，**前端直接读同名字段，禁止映射** |
| 主键口径 | `bigNumberStrings:true`，BIGINT/主键以字符串下发（`DataSanitizer` 出口归一） | `item_code` 是字符串天然无溢出问题；前端按字符串处理 |
| C 端脱敏 | `DataSanitizer.sanitizeExchangeMarketItems` 黑名单删字段 + 字段映射出口 | 「小程序不展示编码」由该出口删 `item_code` 落地，而非前端各自隐藏 |
| 搜索归一化 | 现 `keyword` 仅 `item_name LIKE`（`QueryService` 332/400/1519 行） | 在 Service 层加：检测输入命中编码格式→`normalize`→按 `item_code` 精确匹配，**与 keyword 同一入口** |

### 10.4 三端问题归属（项目未上线，不兼容旧接口，一次性改到位）

**A. 后端数据库项目（主改造，权威源）**

1. `exchange_items` 缺 `item_code`/`supplier_item_code`/`series_id`/`series_seq` → 加列 + 唯一索引（§4.1/§4.3）。
2. `exchange_item_skus.sku_code` 现为 `VARCHAR(100)` 且规则把 pid 编进码（`P{pid}_{ts}_{rand}`，实测耦合）→ 收窄 `VARCHAR(14)` + 改 `SK`+12 位随机（§4.2、§6）。
3. `ExchangeItemService` 三处 SKU 码逻辑（`createSku` 人工口子、`_generateUniqueSkuCode`、`_buildCartesianSkuCode`）+ `admin/ItemManagementService.js` 的 `default_{id}` + `BidService` 引用 `sku_code` 仅作展示（657 行，安全）→ 统一收口随机生成、移除人工传入。
4. 搜索：`QueryService` keyword 仅匹配 `item_name` → 增编码归一化匹配。
5. 缺 `product_series` 表与发号器、缺 `items` 防伪预留列、缺 `barcode` → 按 §4 新增。
6. 模型层 `models/ExchangeItem.js`、`ExchangeItemSku.js`、`Item.js` 加字段，新增 `models/ProductSeries.js` 并在 `models/index.js`（447–449 行附近）注册。

**B. Web 管理后台前端（`admin/`，Vite + Alpine.js + Tailwind）**

1. `admin/src/modules/market/composables/prop-shop.js:321` 手填 `sku_code: prop_${id}_default` → **删除该字段**，由后端生成（后端移除人工口子后，前端再传会被忽略/报错）。
2. 商品列表/详情/SKU 管理页未展示编码 → 新增展示形 `SP-/SK-` 列与编码搜索框（§8.3 必备项）。
3. `admin/src/api/exchange-item/index.js` 端点常量已齐全，**路径无需改**；仅需在请求参数/渲染层读后端新字段（同名直读）。

**C. 微信小程序前端（独立仓库，不在本后端工程内）**

1. 本工程内未发现小程序代码（无 `project.config.json`/`app.json`），它是独立仓库，仅通过 `/api/v4/exchange/*` 调用后端。
2. 小程序问题均在其自有仓库侧适配：列表/详情**不渲染** `item_code`（后端 `DataSanitizer` 出口已删则前端零改动）；搜索框把用户输入原样传 `keyword`，归一化由后端做；下单仍用 `default_sku_id`/`sku_id`，**不读 `sku_code`**。
3. 结论：小程序无需为编码做结构性改造，关键约束全部由后端落地，前端只做"不展示 + 搜索透传"。

### 10.5 需要你拍板的事项（已全部定稿，见 §11.6 与 §12）

> 下列 5 项最初待定项**均已拍板**，结论已并入 §9 关键决策记录：
1. `sku_code` 收窄前**直接重写存量 2 条** → 确认。
2. C 端 `item_code` **保留供搜索回显、不作展示字段** → 确认。
3. `series_code` **运营手填 + 后端唯一校验** → 确认。
4. `supplier_item_code` **加普通索引**（落在关联表）→ 确认。
5. 二维码 **后端出 payload、前端渲染** → 确认。
>
> 供应商维度新增的 4 项（多供应商/可空/名称唯一/货号索引）见 §12。

### 10.6 执行步骤（落到现网真实文件路径）

> 顺序遵循现网"后端先行、前端后接"，每步对应真实文件，便于直接施工。

1. **生成器**（复用 `RedemptionCodeGenerator` 范式）
   - 新增 `utils/ProductCodeGenerator.js`：静态类，`BASE32_CHARS='23456789ABCDEFGHJKMNPQRSTUVWXYZ'`，方法 `generate(prefix)`/`format(code)`/`normalize(input)`/`validate(code,prefix)`/`generateUnique(prefix,checkUnique,maxRetries=5)`，内部 `crypto.randomInt`。
   - 新增 `utils/SeriesSeqAllocator.js`：`allocate(seriesId, transaction)` 行锁读 `product_series.next_seq` 自增、`format(seriesCode,seq,pad=3)`。
   - 单测放 `tests/`（jest，`TZ=Asia/Shanghai`）。

2. **迁移**（sequelize-cli 事务化 up/down，参照 `migrations/20260605020159-*.js` 写法）
   - `exchange_items`：加 `item_code`/`series_id`/`series_seq` + `uk_item_code` + `uk_series_seq`。
   - `exchange_item_skus`：`sku_code` 收窄 `VARCHAR(14)`（先重写存量 2 条）+ 加 `barcode`。
   - 新建 `product_series` 表（含 `seq_pad`）+ `suppliers` 表（`supplier_name` 唯一）+ `exchange_item_suppliers` 关联表（货号挂此、加普通索引、`(item,supplier)` 唯一、含预留 `purchase_price`/`quality_score`）+ S1–S5 全部新表（§14，含 `product_batches`）；`items` 加 `nfc_uid`/`anti_fake_code`/`verify_count`/`first_verified_at`/`batch_id`（`batch_id` 含索引，因本次同建 `product_batches` 故**直接建 FK**）。建表顺序严格按 §14.6。
   - `npm run migration:verify` 后 `npx sequelize-cli db:migrate`。

3. **模型**：改 `models/ExchangeItem.js`（加 `item_code`/`series_id`/`series_seq` + `belongsTo(ProductSeries)` + `belongsToMany(Supplier, through ExchangeItemSupplier)`）、`models/ExchangeItemSku.js`（`sku_code` 改 `STRING(14)`、加 `barcode`）、`models/Item.js`（加防伪列 + `batch_id`）；新增 `models/ProductSeries.js`、`models/Supplier.js`、`models/ExchangeItemSupplier.js`（含 `purchase_price`/`quality_score`）并在 `models/index.js` 第 447–449 行附近注册。

4. **Service**：`services/exchange/ExchangeItemService.js`
   - `createExchangeItem` 事务内 `ProductCodeGenerator.generateUnique('SP', ...)` 回填 `item_code`；选填 `series_id` 时调 `SeriesSeqAllocator.allocate`。
   - `_generateUniqueSkuCode`/`_buildCartesianSkuCode` 统一改 `generateUnique('SK', ...)`，去掉 `P{pid}_` 与属性拼接。
   - `createSku` 移除 `data.sku_code` 人工口子（第 297/328 行）。
   - `services/exchange/admin/ItemManagementService.js:269` 的 `default_${id}` 改随机 `SK` 码。

5. **新增** `services/exchange/ProductSeriesService.js`（系列 CRUD + 发号）与 `services/exchange/SupplierService.js`（供应商 CRUD），按现网 Service 风格注册进 ServiceManager。

6. **搜索**：`services/exchange/QueryService.js` keyword 分支（约 400/1519/1821 行）增加：输入命中 `^(SP|SK)` 编码格式时 `normalize` 后按 `item_code` 精确匹配；否则维持 `item_name LIKE`。另增管理端货号辅助查询（`supplier_item_code` 模糊 + `supplier_id` 筛选 + 组合，返回多条命中不去重，带"共 N 条"计数）。

7. **校验/质量**：`npm run check:api-contract`、`npm run lint`、`TZ=Asia/Shanghai jest`（含新生成器单测与受影响的 `ExchangeItemService` 用例）。

8. **管理端配套**（后端通过后）：按 §8.3 在 `admin/` 加展示列/编码搜索框/导出清单/二维码/供应商货号表单/系列管理页/**供应商管理页 `supplier-management.html`**/货号辅助查询/健康看板（含供应商货号冲突 + 供货分布）；同步删除 `prop-shop.js:321` 手填 `sku_code`。

9. **存量回填（本次做）**：拍板对现网 2 条 SPU **本次即生成 `item_code`**——在迁移 up 内（或紧随的一次性脚本）调 `ProductCodeGenerator.generateUnique('SP', ...)` 回填这 2 条，保证数据干净无 NULL。后续新建商品由 `ExchangeItemService` 自动回填。`item_code` 列待回填完成后可在后续迁移收紧为 `NOT NULL`。

## 11. 行业对照与拍板建议（含未答的 4 项 + 系列号 2 项 + 5 项决策）

> 实测前置事实（决定方案选型）：后端 `node_modules` **无任何二维码库**；admin **无二维码库**，但已有 `jspdf` / `html2canvas` / `xlsx`；后端已有 `exceljs`。
> 更关键：项目**已有成熟的二维码范式**——消费/核销二维码全部是「后端只产出**签名字符串 payload**（如 `QRV2_{base64}_{signature}`，见 `routes/v4/user/consumption-qrcode.js`、`utils/QRCodeValidator.js`），前端负责把字符串渲染成图」。本方案的商品二维码**必须沿用同一范式**，不另起炉灶。

### 11.1 各类公司怎么做（横向对照）

| 主体 | 商品/物品标识 | 二维码 | 系列/连号 | 对本项目的借鉴 |
|---|---|---|---|---|
| 阿里/京东（大电商） | 内部雪花/自增 item_id（不对外手输）+ 对外 SKU 数字 | 后端出 payload，端侧渲染；跳转走短链 | 无"系列连号"，靠类目+属性聚合 | 主键内部化思路一致；不照搬雪花（你单库无需） |
| 美团/腾讯（券码/核销） | 一物一码、人类可读券码（你 `tracking_code` 即此范式） | **后端签名串 + 端侧渲染**（你现网就是这套） | 批次号连号印刷 | 二维码范式**直接复用现网** |
| 亚马逊（ASIN） | 前缀 `B0`+随机无意义码，对外展示+可搜 | 条码/二维码贴在商品 | 无连号 | **你 `item_code` 的直接母版**（§2 已采纳） |
| 小公司/传统货号 | 人工含义码 `CB4385` | 多为第三方草料等手动生成 | 货号即连号（语义耦合） | 反面教材：含义码会"说谎"，仅留作 `supplier_item_code` 参考 |
| 游戏公司（装备/道具） | 内部 instance_id + 模板 template_id（你 `items`/`item_templates` 同构） | 少用 | **限量编号 `#42/100`**（你 `serial_number/edition_total` 已有！） | 系列连号 = 游戏"限量编号"思路，落 `series_seq` |
| 活动策划/盲盒 | 系列名 + 套内序号（第几款/共几款） | 包装印二维码扫码验真 | **强连号**（成套收集核心卖点） | 你的 `series_code+series_seq` 正是此场景 |
| 游戏虚拟物品交易/小众二手 | 平台短码 + 防伪/溯源码 | 扫码验真 + 转移记录 | 批次/版次连号 | 对应你预留的 `nfc_uid`/`anti_fake_code`（路线图） |

结论：你的项目是「**半标品手串 + 线下手册查找 + 成套系列 + 交易防枚举**」的复合体，**没有任何单一大厂方案能整套照搬**。最贴合的是「**亚马逊 ASIN 随机主码（防枚举+可搜）＋ 游戏限量编号/盲盒连号（成套印刷）双轨制**」——这正是文档 §3.6 已定的方向，本节只是给出每个细节该取哪种流派。
### 11.2 未答的 4 个配套形态 —— 推荐拍板

| # | 决策点 | 选项与流派 | **推荐（理由：长期低维护、贴合现网技术栈）** |
|---|---|---|---|
| 配1 | **二维码跳转目标** | (a) 小程序商品详情页 path；(b) H5 落地页；(c) 纯编码文本不跳转，仅供输入 | **(a) 小程序商品页 path**：`pages/exchange/detail?item_code=SP...`。手册扫码即达详情，与"编码只进不出"一致（扫码=高级搜索输入）。后端二维码 payload 存编码，**前端拼小程序码/path**，零后端依赖 |
| 配2 | **手册导出格式** | (a) xlsx 清单；(b) PDF 印刷版；(c) 两者 | **(c) 先 xlsx 后 PDF**：xlsx 用后端已装的 `exceljs`（§8.3 已有导出范式，立即可做）；PDF 印刷版用 admin 已装的 `jspdf`（前端生成，零后端依赖）。两条都不新增依赖 |
| 配3 | **SKU 码后台展示程度** | (a) 完全隐藏（SKU 走 sku_id）；(b) 仅 SKU 管理页/详情展示；(c) 列表也展示 | **(b) 仅 SKU 管理与详情展示 `SK-XXXX-XXXX-XXXX`**：商品列表展示 SPU 的 `SP` 码即可，SKU 码下沉到规格管理层，避免列表信息过载（对标电商后台：列表给 SPU，规格抽屉给 SKU） |
| 配4 | **健康看板放哪** | (a) 独立菜单页；(b) 并入现有 `dashboard-panel.html`；(c) 并入 `statistics.html` | **(b) 并入 `admin/dashboard-panel.html` 作一个卡片区**：编码回填率/SKU 数/货号缺失率是运营日常盯的指标，放总览看板触达最高，少建一个页面=少一份维护成本 |

### 11.3 系列号带出的 2 个 —— 推荐拍板

| # | 决策点 | 选项 | **推荐** |
|---|---|---|---|
| 系1 | **系列码 `series_code`（SLNB 这种）谁定** | (a) 运营手填；(b) 系统生成 | **(a) 运营手填 + 后端唯一性校验**。系列码是要**印上手册/包装给顾客看的语义标识**（盲盒/活动策划流派），必须人可读可记（`SLNB`=石榴石手串），系统随机生成会丧失语义。手填后端校验 `uk_series_code` 唯一、全大写归一化即可 |
| 系2 | **序号补零位数** | 默认 3 位（001~999）；或 4 位（0001~9999） | **维持默认 3 位**。单系列商品款数实测远小于 999（你现网全库才 2 个 SPU），3 位足够且手册排版紧凑；做成 `product_series.seq_pad` 字段（默认 3）留口，**极少数超 999 的系列可单独调**，不必全局改 4 位 |

### 11.4 原 5 项决策 —— 推荐拍板

| # | 决策点 | **推荐** | 理由 |
|---|---|---|---|
| 决1 | `sku_code` 收窄前是否**直接重写现网 2 条** | **直接重写**为 `SK`+12 位随机 | 未上线、仅 2 条、旧码无对外引用（`BidService` 仅展示用）；迁移 up 内顺带 UPDATE，零兼容包袱 |
| 决2 | C 端是否保留 `item_code` 供搜索回显 | **保留但只读不展示**：`DataSanitizer` C 端出口**保留** `item_code` 字段，前端搜索结果可回显"已匹配 SP-XXXX"，但商品卡片不渲染该字段 | 对标淘宝隐藏 item_id 但搜索可命中；彻底删除会导致搜索无法回显命中项，体验差 |
| 决3 | `series_code` 运营手填还是系统生成 | **运营手填**（同 系1） | 语义标识需人可读 |
| 决4 | `supplier_item_code` 是否加普通索引 | **加普通索引**（已改）：因 §3.8 货号辅助查询本次要做 | 货号模糊/组合查询需索引加速；仍非唯一、允许重复、可空 |
| 决5 | 二维码前端生成还是后端 | **后端只产出编码 payload，前端（admin `jspdf`/小程序原生）渲染图** | **强一致于现网消费码范式**（后端签名串+端侧渲染），后端零新增依赖，长期维护成本最低 |

### 11.5 一句话总览：哪种最适合你

**「亚马逊式随机主码（`item_code`/`sku_code` 防枚举可搜）＋ 盲盒/游戏式语义连号（`series_code`+`series_seq` 成套印刷）双轨」＋「美团式后端签名码、端侧渲染的二维码」＋「全部复用现网 `crypto` 生成器/`exceljs`/admin `jspdf` 范式，零新增技术栈」**。
这套在「长期低维护、零技术债、贴合你 Node.js+Sequelize+MySQL+Redis 现状」三个维度上同时最优——主码不含义永不说谎、连号满足成套卖点、二维码不引入新库、所有改动落在你已有的代码范式里。

### 11.6 拍板状态（2026-06-29 定稿）

§11.2–11.4 全部 11 项**已采纳上述推荐为最终结论**，并已并入 §9 关键决策记录。施工以此为准，无遗留待定项。如后续需调整，按变更单修订本节再回改 §9。

## 12. 供应商维度拍板记录（2026-06-29 追加）

引入"供应商维度 + 货号辅助查询"后新增 4 项决策，已落入 §3.8 / §4.5 / §9：

| # | 决策点 | 结论 | 影响 |
|---|---|---|---|
| 供1 | 一个商品能否对应多供应商 | **可以（多源采购）** | 用 `exchange_item_suppliers` 多对多关联表，货号挂关联行；`exchange_items` 不加 supplier 列 |
| 供2 | 商品的供应商归属是否必填 | **可空**（无关联行=自营/未知） | 兼容现网 2 条无供应商存量，留空不影响任何流程 |
| 供3 | 供应商货号是否加索引 | **加普通索引**（关联表 `supplier_item_code`） | 加速货号辅助查询；仍非唯一、允许重复、可空 |
| 供4 | 供应商名称是否唯一 | **唯一**（`suppliers.supplier_name`） | 防运营重复建档同一供应商 |
| 范1 | 本次施工范围 | **S1–S5 表结构本次一次性建出**（空表+外键），业务逻辑分阶段 | 见 §14；外键一次建全，免将来补 FK 迁移 |
| 范2 | 现网 2 条 SPU 的 `item_code` | **本次即回填**（迁移内调生成器） | 数据干净无 NULL；见 §10.6 步骤 9 |
| 范3 | 商品是否必须归属系列 | **可选**（`series_id` 可空，不归系列仅有随机主码） | 与 §3.6 设计一致 |

至此全部待定项清零，方案可直接进入施工。

## 13. 前瞻性业务场景与编码体系演进（2026-06-29 追加，已确认会做）

> 用户确认下列场景**均会落地**。本节明确：每个场景与现有编码体系的关系、需要的新表/字段、以及"编码本身是否要改"。核心结论：**编码体系（`item_code`/`sku_code`/`series_code`/`tracking_code`）全部无需改动，扩展只加表/加字段**——这正是"无意义码 + 含义落表"设计的长期回报。

### 13.1 场景全景与编码影响评估

| # | 未来场景 | 是否动编码 | 落地方式（新表/字段） | 与现有结构的挂接点 |
|---|---|---|---|---|
| S1 | 采购单 / 进货管理 | 否 | 新增 `purchase_orders` + `purchase_order_items`；`exchange_item_suppliers.purchase_price`/`quality_score` 本次已预留 | 挂 `supplier_id` + `exchange_item_id` |
| S2 | 批次管理 / 一批一码 | 否 | 新增 `product_batches`；`items.batch_id` 本次已加列（FK 待 S2 补） | 批次↔SKU/系列号/防伪连号 |
| S3 | 二手回流 / 寄卖 / 转赠 | 否 | 复用 `items` + `item_ledger` 流转；新增寄卖/转赠单据表 | `tracking_code` 一物一码做载体 |
| S4 | 组合商品 / 套装 / 赠品 | 否 | 新增 `product_bundles`（BOM 式组合关系表） | SPU↔SPU/SKU 组合，`item_code` 不变 |
| S5 | 第三方平台分销（淘宝/抖音） | 否 | 新增 `external_channel_mappings`（外部平台 ID↔我方 `item_code`） | `item_code` 作对接唯一键 |

### 13.2 S1 采购单 / 进货单（确认会做）

业务：向多个供应商按批进货，记录进货价、批次、到货时间，支撑按供应商结算对账与供货质量评分。

| 表/字段 | 说明 |
|---|---|
| `purchase_orders` | 采购单头：`supplier_id`、单号、下单/到货时间、总额、状态 |
| `purchase_order_items` | 采购单行：`exchange_item_id`/`sku_id`、数量、`purchase_price`（进货价）、`batch_no` |
| `exchange_item_suppliers`（已预留列） | `purchase_price`、`quality_score` 已在 §4.5 本次一并加列，S1 启用时直接写入维护 |

编码影响：**无**。进货价/批次属"采购域"数据，不进商品对外编码，符合"编码无意义"铁律。
### 13.3 S2 批次管理 / 一批一码（确认会做）

业务：半标品天然"这批料 vs 那批料"，需按批次管成本、按批次召回、按批次做防伪连号。

| 表/字段 | 说明 |
|---|---|
| `product_batches` | 批次主数据：`batch_code`（批次码，可读连号或日期+序号风格）、关联 `exchange_item_id`/`sku_id`、进货来源 `supplier_id`、批次成本、生产/入库日期、数量、状态 |
| `items.batch_id`（本次已加列） | 实物实例归属批次，支撑"按批召回/溯源"；§4.4 本次已加列+索引，S2 建 `product_batches` 时补 FK |
| 与防伪连号挂接 | 批次内可发连号防伪码（复用 §3.7 `anti_fake_code` + 批次前缀），实现"一批一码段" |

编码影响：**无主码改动**。批次码是**采购/生产域的独立编码**，与对外 `item_code` 解耦；可借鉴现有 `tracking_code` 的"前缀+日期+序号"风格（运营可读），不纳入随机码体系。

> 设计提醒：`product_batches` 与 §4.3 `product_series`（系列连号）是**两个正交维度**——系列=营销成套，批次=进货生产。一个商品可同时属于某系列、又分多个批次。不要合并两表。

### 13.4 S3 二手回流 / 寄卖 / 转赠（确认会做）

业务：用户把旧物寄卖回平台再售、实物转赠他人；延续现有竞价（`BidService`）与以物易物（`barter`）的资产流转。

| 机制 | 落地方式 |
|---|---|
| 所有权流转 | 复用现有 `items` + `item_ledger`（一物一码 + 流水账），转赠/寄卖即新增 ledger 流转记录，`owner_account_id` 变更 |
| 寄卖单 | 新增 `consignment_orders`：寄卖物 `item_id`、寄卖人、定价、回流后再上架的目标 SPU |
| 转赠 | 新增轻量转赠记录表，或复用现有资产转移链路 + 幂等键 |

编码影响：**无**。`tracking_code`（一物一码）正是二手流转的天然载体；二手物仍指向原 `item_template_id`/`item_code`，流转只改归属不改身份。

> 风控提醒：二手回流涉及"用户→平台"的反向资产流，需复用现有交易合规校验（方向等价/向下、真实销毁等），上线前单独评估，不在本编码方案范围。

### 13.5 S4 组合商品 / 套装 / 赠品（确认会做）

业务："手串 + 礼盒 + 保养油"打包成套售卖。

| 表/字段 | 说明 |
|---|---|
| `product_bundles` | 组合主体：组合自身也是一个 SPU（有自己的 `item_code`），`bundle_type`（套装/赠品搭售） |
| `product_bundle_items` | 组合明细（BOM）：父 `exchange_item_id` ↔ 子 `exchange_item_id`/`sku_id` + 数量 + 是否赠品 |

编码影响：**无**。组合品本身用标准 `item_code`（`SP` 前缀），明细走关系表；子项仍是独立 SPU/SKU，库存/履约按明细拆解。符合电商主流"虚拟组合 SPU + BOM"做法。

### 13.6 S5 第三方平台分销（淘宝/抖音，确认会做）

业务：把商品铺到外部平台，需要把外部平台商品 ID 映射回我方商品。

| 表/字段 | 说明 |
|---|---|
| `external_channel_mappings` | `channel`（taobao/douyin/...）、`external_item_id`、我方 `exchange_item_id`/`item_code`、同步状态、价格/库存同步策略 |

编码影响：**无，且正是 `item_code` 的用武之地**。`item_code` 作为对外稳定唯一键，天然适合当对接锚点；外部平台各自的 ID 只存映射表，不污染主数据。

### 13.7 总结：编码体系的前瞻性结论

- 上述 5 个确认会做的场景，**没有一个需要修改 `item_code`/`sku_code`/`series_code`/`tracking_code` 的生成规则或格式**。
- 扩展全部是"加表 + 加少量外键列 + 填已预留字段"，与"无意义码 + 含义落 EAV/关联表"的设计哲学完全一致，验证了本方案的**长期低维护、零技术债**目标。
- **拍板更新（2026-06-29）**：用户决定 **S1–S5 所有新表的表结构本次一次性建出来**（空表 + 完整外键），业务逻辑/接口/前端仍分阶段实现。表结构 DDL 见 §14。这样 `items.batch_id` 等外键可一次建全，免将来补 FK 迁移。
- **预留字段本次一并加**：`exchange_item_suppliers.purchase_price`/`quality_score`（§4.5）、`items.batch_id`（§4.4，本次因 §14 同时建 `product_batches`，可**直接建 FK**）。
- 落地顺序（业务逻辑层）建议：S1 采购单 → S2 批次 → S4 组合 → S5 分销 → S3 二手（风控最重，最后做）。表结构虽一次性建好，但各场景的 Service/接口/前端按此顺序逐步填充。

## 14. S1–S5 表结构（本次一次性建空表，2026-06-29 拍板）

> 拍板：S1–S5 所有新表**本次随迁移一次性建出**（结构 + 完整外键），仅建表不写业务逻辑；各场景的 Service/接口/前端按 §13.7 顺序后续填充。
> 外键主键口径以实测真实库为准：`accounts.account_id`、`item_ledger.ledger_entry_id`、`exchange_records.exchange_record_id`、`exchange_items.exchange_item_id`、`exchange_item_skus.sku_id`、`suppliers.supplier_id`。
> 所有表统一 `utf8mb4_unicode_ci`、`created_at/updated_at` 北京时间、BIGINT 主键，与现网风格一致。

### 14.1 S2 批次 `product_batches`（先建，供 items.batch_id 外键引用）

```sql
CREATE TABLE product_batches (
  batch_id         BIGINT NOT NULL AUTO_INCREMENT COMMENT '批次主键',
  batch_code       VARCHAR(32) NOT NULL COMMENT '批次码(可读,前缀+日期+序号风格,运营可读)',
  exchange_item_id BIGINT NULL COMMENT '关联SPU(可空,批次可跨SKU)',
  sku_id           BIGINT NULL COMMENT '关联SKU(可空)',
  supplier_id      BIGINT NULL COMMENT '进货来源供应商',
  batch_cost       DECIMAL(10,2) NULL COMMENT '批次成本(进货价)',
  quantity         INT NOT NULL DEFAULT 0 COMMENT '批次数量',
  produced_at      DATETIME NULL COMMENT '生产/入库日期',
  status           ENUM('active','inactive') NOT NULL DEFAULT 'active',
  created_at       DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (batch_id),
  UNIQUE INDEX uk_product_batches_code (batch_code),
  INDEX idx_pb_item (exchange_item_id),
  INDEX idx_pb_supplier (supplier_id),
  CONSTRAINT fk_pb_item    FOREIGN KEY (exchange_item_id) REFERENCES exchange_items(exchange_item_id) ON DELETE SET NULL,
  CONSTRAINT fk_pb_sku     FOREIGN KEY (sku_id)           REFERENCES exchange_item_skus(sku_id)       ON DELETE SET NULL,
  CONSTRAINT fk_pb_supplier FOREIGN KEY (supplier_id)     REFERENCES suppliers(supplier_id)            ON DELETE SET NULL
) COMMENT='产品批次(一批一码;批次与系列号正交)';
```

> 迁移顺序：`suppliers` → `product_batches` → `ALTER items ADD batch_id + FK`，保证外键目标已存在。
### 14.2 S1 采购单 `purchase_orders` + `purchase_order_items`

```sql
CREATE TABLE purchase_orders (
  purchase_order_id BIGINT NOT NULL AUTO_INCREMENT COMMENT '采购单主键',
  order_no          VARCHAR(32) NOT NULL COMMENT '采购单号',
  supplier_id       BIGINT NOT NULL COMMENT '供应商',
  total_amount      DECIMAL(12,2) NULL COMMENT '采购总额',
  status            ENUM('draft','ordered','received','cancelled') NOT NULL DEFAULT 'draft',
  ordered_at        DATETIME NULL COMMENT '下单时间',
  received_at       DATETIME NULL COMMENT '到货时间',
  remark            VARCHAR(500) NULL,
  created_at        DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (purchase_order_id),
  UNIQUE INDEX uk_po_order_no (order_no),
  INDEX idx_po_supplier (supplier_id),
  CONSTRAINT fk_po_supplier FOREIGN KEY (supplier_id) REFERENCES suppliers(supplier_id)
) COMMENT='采购单头(S1进货管理)';

CREATE TABLE purchase_order_items (
  id                BIGINT NOT NULL AUTO_INCREMENT COMMENT '采购单行主键',
  purchase_order_id BIGINT NOT NULL COMMENT '所属采购单',
  exchange_item_id  BIGINT NULL COMMENT '采购的SPU',
  sku_id            BIGINT NULL COMMENT '采购的SKU',
  batch_id          BIGINT NULL COMMENT '入库批次(关联product_batches)',
  quantity          INT NOT NULL DEFAULT 0 COMMENT '采购数量',
  purchase_price    DECIMAL(10,2) NULL COMMENT '进货单价',
  created_at        DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (id),
  INDEX idx_poi_order (purchase_order_id),
  INDEX idx_poi_item (exchange_item_id),
  CONSTRAINT fk_poi_order FOREIGN KEY (purchase_order_id) REFERENCES purchase_orders(purchase_order_id) ON DELETE CASCADE,
  CONSTRAINT fk_poi_item  FOREIGN KEY (exchange_item_id)  REFERENCES exchange_items(exchange_item_id)    ON DELETE SET NULL,
  CONSTRAINT fk_poi_sku   FOREIGN KEY (sku_id)            REFERENCES exchange_item_skus(sku_id)          ON DELETE SET NULL,
  CONSTRAINT fk_poi_batch FOREIGN KEY (batch_id)         REFERENCES product_batches(batch_id)            ON DELETE SET NULL
) COMMENT='采购单明细行(S1)';
```

### 14.3 S4 组合/套装 `product_bundles` + `product_bundle_items`

```sql
CREATE TABLE product_bundles (
  bundle_id        BIGINT NOT NULL AUTO_INCREMENT COMMENT '组合主键',
  exchange_item_id BIGINT NOT NULL COMMENT '组合自身对应的SPU(有自己的item_code)',
  bundle_type      ENUM('suit','gift') NOT NULL DEFAULT 'suit' COMMENT 'suit=套装/gift=赠品搭售',
  status           ENUM('active','inactive') NOT NULL DEFAULT 'active',
  created_at       DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (bundle_id),
  UNIQUE INDEX uk_bundle_spu (exchange_item_id),
  CONSTRAINT fk_bundle_spu FOREIGN KEY (exchange_item_id) REFERENCES exchange_items(exchange_item_id) ON DELETE CASCADE
) COMMENT='组合商品主体(S4,组合本身也是SPU)';

CREATE TABLE product_bundle_items (
  id               BIGINT NOT NULL AUTO_INCREMENT COMMENT '组合明细主键',
  bundle_id        BIGINT NOT NULL COMMENT '所属组合',
  child_item_id    BIGINT NULL COMMENT '子项SPU',
  child_sku_id     BIGINT NULL COMMENT '子项SKU',
  quantity         INT NOT NULL DEFAULT 1 COMMENT '数量',
  is_gift          TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否赠品',
  created_at       DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (id),
  INDEX idx_pbi_bundle (bundle_id),
  CONSTRAINT fk_pbi_bundle FOREIGN KEY (bundle_id)     REFERENCES product_bundles(bundle_id)        ON DELETE CASCADE,
  CONSTRAINT fk_pbi_item   FOREIGN KEY (child_item_id) REFERENCES exchange_items(exchange_item_id)  ON DELETE SET NULL,
  CONSTRAINT fk_pbi_sku    FOREIGN KEY (child_sku_id)  REFERENCES exchange_item_skus(sku_id)        ON DELETE SET NULL
) COMMENT='组合明细BOM(S4)';
```
### 14.4 S5 第三方分销 `external_channel_mappings`

```sql
CREATE TABLE external_channel_mappings (
  id               BIGINT NOT NULL AUTO_INCREMENT COMMENT '映射主键',
  channel          VARCHAR(20) NOT NULL COMMENT '外部渠道:taobao/douyin/...',
  external_item_id VARCHAR(100) NOT NULL COMMENT '外部平台商品ID',
  exchange_item_id BIGINT NOT NULL COMMENT '我方SPU',
  sync_status      ENUM('pending','synced','failed','disabled') NOT NULL DEFAULT 'pending',
  last_synced_at   DATETIME NULL COMMENT '最近同步时间',
  created_at       DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (id),
  UNIQUE INDEX uk_channel_external (channel, external_item_id),
  INDEX idx_ecm_item (exchange_item_id),
  CONSTRAINT fk_ecm_item FOREIGN KEY (exchange_item_id) REFERENCES exchange_items(exchange_item_id) ON DELETE CASCADE
) COMMENT='外部平台商品ID↔我方item_code映射(S5)';
```

### 14.5 S3 二手寄卖 `consignment_orders`

```sql
CREATE TABLE consignment_orders (
  consignment_id    BIGINT NOT NULL AUTO_INCREMENT COMMENT '寄卖单主键',
  order_no          VARCHAR(32) NOT NULL COMMENT '寄卖单号',
  item_id           BIGINT NOT NULL COMMENT '寄卖的实物实例(items.item_id,一物一码)',
  consignor_account_id BIGINT NOT NULL COMMENT '寄卖人账户(accounts.account_id)',
  list_price        BIGINT NULL COMMENT '寄卖定价(计价资产数量)',
  list_asset_code   VARCHAR(50) NULL COMMENT '计价资产码',
  relist_item_id    BIGINT NULL COMMENT '回流后再上架的目标SPU',
  status            ENUM('pending','listed','sold','withdrawn','rejected') NOT NULL DEFAULT 'pending',
  created_at        DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (consignment_id),
  UNIQUE INDEX uk_consign_order_no (order_no),
  INDEX idx_consign_item (item_id),
  INDEX idx_consign_consignor (consignor_account_id),
  CONSTRAINT fk_consign_item     FOREIGN KEY (item_id)              REFERENCES items(item_id),
  CONSTRAINT fk_consign_account  FOREIGN KEY (consignor_account_id) REFERENCES accounts(account_id),
  CONSTRAINT fk_consign_relist   FOREIGN KEY (relist_item_id)       REFERENCES exchange_items(exchange_item_id) ON DELETE SET NULL
) COMMENT='二手寄卖单(S3;所有权流转复用item_ledger,转赠不单独建表)';
```

- **转赠**不单独建表：复用现有 `item_ledger`（流水账 + `owner_account_id` 变更 + `event_type='transfer'`），与现网资产流转链路一致，无需新表。
- S3 涉及"用户→平台"反向资产流，业务逻辑上线前需单独过合规/风控评审（§13.4 已注明），本次仅建寄卖单空表。

### 14.6 建表顺序与施工边界

外键依赖顺序（迁移内严格按此建，避免引用未建表）：
1. `suppliers`（§4.5）
2. `product_batches`（§14.1）→ 随后 `ALTER items ADD batch_id + FK`
3. `purchase_orders` → `purchase_order_items`（§14.2，依赖 suppliers/batches）
4. `product_bundles` → `product_bundle_items`（§14.3）
5. `external_channel_mappings`（§14.4）
6. `consignment_orders`（§14.5，依赖 items/accounts）

施工边界（重要）：
- 本次**只建表结构 + 外键 + 索引**，不写任何业务逻辑、不加 Service/路由/前端。
- 对应模型文件可一并新增（空模型 + 关联声明），注册进 `models/index.js`，但**不接入业务流**。
- 各场景的业务逻辑按 §13.7 顺序后续独立迭代，每个 S 场景单独评审上线。

## 15. 求证复核（2026-07-05，实连真实库 + 现网代码二次核对）

> 复核方式：后端无 mysql 客户端，改用项目自带 Node.js + Sequelize（`config/database.js` 读 `.env`
> 的 `DB_HOST=test-db-12-mysql.ns-br0za7uc.svc / restaurant_points_dev`）**内网直连真实库**执行
> `SHOW FULL COLUMNS / SHOW INDEX / COUNT / 抽样`；**未读任何 `.env.bak`/备份、未引用任何历史报告或其它文档**。
> 后端/前端技术栈以 `package.json`、`admin/package.json`、`config/`、`utils/*Generator.js`、
> `services/exchange/*`、`routes/v4/*`、`models/*`、`migrations/*` **现网源码**为准。本节结论若与 §5/§10 早前描述冲突，**以本节为准**。

### 15.1 真实库实测（2026-07-05，非备份）

| 表 | 实测存在 | 主键/类型 | 行数 | 与本方案相关列现状 |
|---|---|---|---|---|
| `exchange_items` | ✅ | `exchange_item_id` BIGINT | 2 | 37 列；**无** `item_code`/`series_id`/`series_seq`；已有 `merchant_id`(int)、`fulfillment_type`、`applicable_scope` 等 |
| `exchange_item_skus` | ✅ | `sku_id` BIGINT | 2 | `sku_code` VARCHAR(100) NOT NULL UNIQUE；**无** `barcode` |
| `exchange_item_attribute_values` | ✅ | `id` BIGINT | 0 | EAV 已建、暂无数据 |
| `items` | ✅ | `item_id` BIGINT | 8 | `tracking_code` VARCHAR(20) UNIQUE；已有 `serial_number`/`edition_total`；**无** nfc/anti_fake/verify_*/batch_id |
| `item_templates` | ✅ | `item_template_id` BIGINT | 34 | `template_code` VARCHAR(100) UNIQUE，本方案不动 |
| `merchants` | ✅ | `merchant_id` **INT** | 2 | 核销商家（`commission_rate`/`settlement_account_id`），语义≠供货商 |
| `item_ledger` | ✅ | `ledger_entry_id` BIGINT | 0 | 一物一码流水账，S3 复用 |
| `accounts` | ✅ | `account_id` BIGINT | 19 | S3 寄卖人外键目标 |
| `exchange_records` | ✅ | `exchange_record_id` BIGINT | 28 | 删除 SPU 前引用校验已用 |
| `suppliers` / `product_series` / `exchange_item_suppliers` / `product_batches` / `purchase_orders` / `purchase_order_items` / `product_bundles` / `product_bundle_items` / `external_channel_mappings` / `consignment_orders` | ❌ 全部不存在 | —— | —— | 均为本次待新建 |

实测 `sku_code` 样本：`P633_202606220629057850800_y5ux`、`P637_202606221301154560800_kt6r`（长度约 30，把 pid 编进码，印证耦合，远超目标 `VARCHAR(14)`）。
实测 `tracking_code` 样本：`TS782085269006`（`items` 8 条均为 TS 测试前缀），本方案不动。

### 15.2 对早前方案文本的关键修正（以现网代码为准）

复核发现下列几处早前描述与现网真实代码/数据不符，**施工以本节修正为准**：

| # | 早前描述位置 | 早前说法 | 实测真相 | 修正落地 |
|---|---|---|---|---|
| 修1 | §5 / §10.6 | 新增 `utils/ProductCodeGenerator.js`，`generateUnique(prefix, checkUnique, maxRetries=5)` | 现网 `RedemptionCodeGenerator.generateUnique(checkUnique, maxRetries=3)` **无 prefix 入参**、默认 **3 次**且 `generate()` 无前缀 | 新生成器**照搬 `BASE32_CHARS` + `crypto.randomInt` 结构**，但**签名不照抄**：本方案定 `generate(prefix)`、`generateUnique(prefix, checkUnique, maxRetries=5)`（见 §5），属"扩展"非"复用同签名"，避免误以为能直接调旧方法 |
| 修2 | §10.6 步骤3 | "在 `models/index.js` 第 447–449 行附近注册" | 实测 447–449 行是 `AttributeOption`/`CategoryAttribute`/`ExchangeItem`；`ExchangeItemSku` 在 449 行、`ExchangeChannelPrice` 451 行 | 新模型 `ProductSeries`/`Supplier`/`ExchangeItemSupplier`/`ProductBatch` 等**紧跟 `ExchangeChannelPrice`（约 451 行）之后注册**，行号以现网为准，勿硬编码旧行号 |
| 修3 | §4.5 / §14 外键 | 关联表外键指向各主表 | 实测 `merchants.merchant_id` 是 **INT**，其余 `exchange_items`/`exchange_item_skus`/`items`/`accounts`/`item_ledger`/`exchange_records` 主键均 **BIGINT** | 新表所有 FK 列类型**必须与目标主键逐一对齐**：指向 BIGINT 主键的 FK 用 `BIGINT`；本方案新表不引用 `merchants`（供货商独立），无 INT/BIGINT 混用风险；`suppliers.supplier_id` 自身定 `BIGINT` |
| 修4 | §3.5 / §10.4-C | "小程序不展示编码由 `DataSanitizer` C 端出口删 `item_code`" | 实测 `DataSanitizer.sanitizeExchangeMarketItems` **是黑名单式**（`delete` 指定字段 + 派生字段透传），当前**不会自动带出新加的 `item_code`**——除非 QueryService 查询显式 select 且此处不 delete | C 端策略明确为：**QueryService C 端查询 select 出 `item_code`，`DataSanitizer` C 端出口保留该字段**（供搜索回显），商品卡片前端不渲染。落点在 `services/DataSanitizer.js` 的 `sanitizeExchangeMarketItems`（约 1466–1480 行黑名单区**不加** `item_code`），而非"删字段" |
| 修5 | §6 / §10.4-A③ | `admin/ItemManagementService.js` 默认 SKU 码 `default_{id}` | 实测在 `services/exchange/admin/ItemManagementService.js:269` `const defaultSkuCode = \`default_${item.exchange_item_id}\``，269/273/299 三处引用 | 改为 `ProductCodeGenerator.generateUnique('SK', ...)`，三处同步（269 生成、273/299 使用同一变量即可） |
| 修6 | §10.4-B① | `prop-shop.js:321` 手填 `sku_code: prop_${id}_default` | 实测在 `admin/src/modules/market/composables/prop-shop.js:321` `sku_code: \`prop_${exchangeItemId}_default\``（建默认 SKU 时）；另 143–154 行 `openEditForm` 读 `min_cost_amount`/`sku_id` 不涉编码 | 删除 321 行 `sku_code` 传参（后端移除人工口子后自动生成）；`openEditForm` 无需改 |

### 15.3 后端技术栈匹配度（实测，零新增依赖）

后端 `package.json` 实测依赖：`sequelize@^6.35`、`mysql2@^3.6`、`exceljs@^4.4`、`ioredis@^5.7`、`joi@^17`、`express@^4.18`；Node `>=20.18`（实测 v20.18.0）。**无任何二维码库**。落地本方案**无需新增任何后端依赖**：

| 本方案所需能力 | 现网可复用资产（实测路径） | 复用 or 扩展 |
|---|---|---|
| 随机无意义码 + 30 去混淆字符集 + crypto | `utils/RedemptionCodeGenerator.js`（`BASE32_CHARS='23456789ABCDEFGHJKMNPQRSTUVWXYZ'`、`crypto.randomInt`、`generateUnique`） | **可复用结构**，新建 `ProductCodeGenerator` 照搬字符集与 crypto，扩展加前缀参数（见修1） |
| 前缀 + 标识符解析（编码搜索归一化） | `utils/TrackingCodeGenerator.js`（`parse`/`validate`/`resolveIdentifier`） | **可复用范式**做 `^(SP|SK)` 命中判断与 normalize |
| 事务内唯一性重试（撞码显式失败） | `ExchangeItemService._generateUniqueSkuCode`（事务内 `findOne` 重试 5 次抛 `BusinessError`） | **可复用**，改前缀/字符集即可 |
| 行锁发号（系列号 next_seq） | Sequelize 事务 + `SELECT ... FOR UPDATE`；`UnifiedDistributedLock`（`utils/`） | **可复用**做 `SeriesSeqAllocator` |
| 事务化迁移 up/down + 中文 comment | `migrations/*.js`（`queryInterface.changeColumn/createTable`，`.sequelizerc`）；`npm run migration:verify` | **可复用**，§4/§14 SQL 转 `queryInterface` 写法 |
| Excel 导出（手册清单 xlsx） | `routes/v4/console/exchange/items.js:18` `require('exceljs')` + `/export`（`ITEM_EXPORT` 端点已存在） | **可扩展**导出预设，加 SP 码/图片列 |
| 统一响应 / 主键字符串口径 | `res.apiSuccess/apiError`；`config/database.js` `bigNumberStrings:true`；`DataSanitizer` 出口 | **强约束遵循**：新字段进现有 data 结构、`item_code` 天然字符串 |
| 服务注册 | `services/index.js:336/502`（`exchange_query`/`exchange_item_service` 已注册） | **可扩展**：新增 `product_series_service`/`supplier_service` 按同范式注册 |

### 15.4 Web 管理后台前端技术栈匹配度（admin/，实测）

`admin/package.json` 实测：**Vite@^6 + Alpine.js@^3.15 + Tailwind@^3.4**；已装 `exceljs` 无、但有 `xlsx@^0.18`、`jspdf@^4.2`、`html2canvas@^1.4`、`echarts@^6`、`konva`、`sortablejs`、`@wangeditor`。**无二维码库**。

| §8.3 配套 | admin 现网可复用资产 | 复用/扩展/需拍板 |
|---|---|---|
| 手册导出（前端 PDF/xlsx） | `xlsx@^0.18`（前端表格导出）、`jspdf@^4.2`+`html2canvas`（PDF）、`export-modal`（`admin/src/alpine/components/export-modal.js` + `.ejs` 模板） | **可复用**：xlsx 走后端 `exceljs` blob（§15.3），PDF 印刷版用前端 `jspdf` |
| 健康看板图表 | `echarts@^6`（已用于 dashboard） | **可复用** |
| 供应商管理页 | `admin/merchant-management.html` + `admin/src/modules/merchant/pages/merchant-management.js` 现成结构 | **可复用为母版**新建 `supplier-management.html` |
| 商品 API 客户端 | `admin/src/api/exchange-item/index.js`（`EXCHANGE_ITEM_ENDPOINTS` 端点常量齐全，含 `ITEM_LIST`/`SKU_CREATE`/`ITEM_EXPORT`） | **路径无需改**（后端沿用 `/api/v4/console/exchange/items`）；仅渲染层读新字段（同名直读，禁映射） |
| 二维码渲染 | **admin 无二维码库** | **需拍板**（见 §15.6 拍1）：装轻量前端库 or 后端出图 |
| 字段映射禁令 | `admin` 有 `lint:mappings`（`scripts/check-frontend-mappings.cjs`）卡口 | **强约束**：前端直读 `item_code`/`series_code` 等后端同名字段，禁止建映射表（否则 lint 拦截） |

### 15.5 三端问题归属（实测复核，未上线 / 不兼容旧接口 / 一次到位）

**A. 后端数据库项目（`/home/devbox/project`，主改造·权威源）**

1. 表结构：`exchange_items` 加 `item_code`(VARCHAR14 UNIQUE)/`series_id`/`series_seq`；`exchange_item_skus` 收窄 `sku_code`→VARCHAR(14) + 加 `barcode`；`items` 加防伪 5 列 + `batch_id`；新建 §4/§14 全部空表（严格按 §14.6 建表序）。
2. 生成器：新建 `utils/ProductCodeGenerator.js`（照搬 `RedemptionCodeGenerator` 字符集+crypto，扩展前缀，见修1）、`utils/SeriesSeqAllocator.js`。
3. SKU 码收口：`ExchangeItemService` 三处——`createSku`(328–330 人工口子**移除**)、`_generateUniqueSkuCode`(896)、`_buildCartesianSkuCode`(878)统一改 `generateUnique('SK',...)`；`admin/ItemManagementService.js:269` `default_{id}` 改随机 SK 码（修5）。
4. 搜索：`QueryService.getMarketItems` keyword 分支（402 行 ORM）与 §1519 原生 SQL 分支，增 `^(SP|SK)` 命中→normalize→按 `item_code` 精确匹配，与 `keyword` 同入口。
5. C 端展示：`DataSanitizer.sanitizeExchangeMarketItems`（1466–1480 黑名单区**不 delete `item_code`**）保留供搜索回显（修4）。
6. 模型：改 `ExchangeItem`/`ExchangeItemSku`/`Item`，新增 `ProductSeries`/`Supplier`/`ExchangeItemSupplier`/`ProductBatch` 等，注册进 `models/index.js`（紧跟 `ExchangeChannelPrice` 约 451 行后，修2）；新增服务按 `services/index.js` 范式注册。
7. `BidService.js:659` 仅把 `sku_code` 用于报错文案展示——**安全，无需改**。

**B. Web 管理后台前端（`admin/`，Vite+Alpine+Tailwind）**

1. `prop-shop.js:321` 删 `sku_code` 传参（修6）。
2. 商品列表/详情/SKU 管理未展示编码 → 加展示形 `SP-/SK-` 列 + 编码搜索框（`exchange-item/index.js` 端点无需改，仅渲染层读新字段）。
3. 供应商管理页 → 仿 `merchant-management.html` 新建 `supplier-management.html`；系列管理页新建；健康看板并入 `dashboard-panel.html`（echarts 复用）。
4. 二维码渲染库缺失 → 见 §15.6 拍1。

**C. 微信小程序前端（独立仓库，不在本工程内）**

1. 复核确认：本工程内**无** `project.config.json`/`app.json`/`pages.json`/`manifest.json`（Glob 全库 0 命中），小程序确为独立仓库，仅经 `/api/v4/exchange/*` 调后端。
2. 小程序侧改动：列表/详情**不渲染** `item_code`（后端保留字段但前端不展示）；搜索框用户输入原样传 `keyword`，归一化后端做；下单继续用 `default_sku_id`/`sku_id`，**不读 `sku_code`**。
3. 结论：小程序**无结构性改造**，关键约束全落后端；仅"不展示 + 搜索透传"两点前端配合。

### 15.6 复核后新增 4 项拍板记录（2026-07-05 定稿，采纳推荐）

> §9–§12 原有决策**均仍成立**。下列为本次实连复核后新浮现的 4 项，**已全部采纳 §15.9 推荐结论，施工以此为准**。行业对照与理由详见 §15.9。

| # | 拍板点 | **最终结论（已拍板）** | 依据 |
|---|---|---|---|
| 拍1 | admin 二维码渲染库 | **方案 A：后端只出编码 payload，admin 装纯 JS `qrcode` 库渲染、小程序用原生 canvas、印刷手册用 `jspdf` 嵌图** | 与现网消费码"后端签名串+端侧渲染"范式 100% 一致；后端零新增依赖；`qrcode` 纯 JS 无原生编译、体积极小，Vite 直接打包（§15.9 拍1 方案A） |
| 拍2 | `item_code` 收紧 NOT NULL 时机 | **本次迁移内一步到位：加列→回填 2 条→同迁移末尾 `changeColumn` 收紧 NOT NULL** | 现网仅 2 条、未上线、无并发写入，一次到位不留可空历史包袱（分步=技术债）（§15.9 拍2） |
| 拍3 | 迁移是否拆分多文件 | **拆 3 个迁移文件**：①编码核心（`exchange_items`/`exchange_item_skus` 改列+`item_code`/`barcode`/系列字段+回填）②供应商+系列（`suppliers`/`product_series`/`exchange_item_suppliers`）③S1–S5 扩展（`product_batches`+`items` 防伪列+采购/组合/分销/寄卖表）；各自独立 up/down，按 §14.6 依赖序执行 | Sequelize/Flyway/Liquibase 社区一致惯例：一个迁移=一个内聚变更单元，便于单独回滚与失败定位（§15.9 拍3） |
| 拍4 | 供货商与核销 merchants 是否分表 | **确认彻底分表**：供货商走独立 `suppliers`（BIGINT 主键），不复用 `merchants` | merchants 是核销商家（抽佣/结算），供货商是进货来源，两个业务实体；分表符合主数据建模惯例，且供货商 BIGINT 主键与其余业务表口径统一，避开 merchants 的 INT 主键（§15.9 拍4） |

### 15.7 修正后的执行步骤（落到现网真实文件与行号）

> 顺序遵循"后端先行、前端后接"。本列表在 §10.6 基础上按 §15.2 修正校准。

1. **生成器**：新增 `utils/ProductCodeGenerator.js`（`BASE32_CHARS` 照搬 `RedemptionCodeGenerator`，`crypto.randomInt`；方法 `generate(prefix)`/`format(code)`/`normalize(input)`/`validate(code,prefix)`/`generateUnique(prefix,checkUnique,maxRetries=5)`）+ `utils/SeriesSeqAllocator.js`（`allocate(seriesId,transaction)` 行锁、`format(code,seq,pad=3)`）；单测入 `tests/`（`TZ=Asia/Shanghai jest`）。
2. **迁移**（`queryInterface` 事务化 up/down，参照 `migrations/20260609000000-*.js` 写法；**按拍3 拆 3 个文件**，各自独立 up/down）：
   - **文件①编码核心**：`exchange_items` 加 `item_code`/`series_id`/`series_seq` + `uk_item_code` + `uk_series_seq`；`exchange_item_skus` `sku_code` `changeColumn`→`STRING(14)`（先 UPDATE 重写现网 2 条为 `SK`+12 位）+ 加 `barcode`；**up 内回填 2 条 SPU 的 `item_code`，同文件末尾 `changeColumn` 收紧 `item_code` 为 NOT NULL（拍2 一步到位）**。
   - **文件②供应商+系列**：建 `suppliers`（`supplier_name` 唯一，BIGINT 主键）→ `product_series`（含 `seq_pad`）→ `exchange_item_suppliers`（货号普通索引、`(item,supplier)` 唯一、含预留 `purchase_price`/`quality_score`）。
   - **文件③S1–S5 扩展**：按 §14.6 序建 `product_batches`→`ALTER items` 加防伪 5 列+`batch_id`(直接建 FK)→`purchase_orders`/`_items`→`product_bundles`/`_items`→`external_channel_mappings`→`consignment_orders`。所有 FK 类型对齐目标 BIGINT 主键（§15.2 修3）。
   - 每个文件均 `npm run migration:verify` → `npx sequelize-cli db:migrate`。
3. **模型**：改 `models/ExchangeItem.js`（加 3 列 + `belongsTo(ProductSeries)` + `belongsToMany(Supplier)`）、`models/ExchangeItemSku.js`（`sku_code`→`STRING(14)`、加 `barcode`）、`models/Item.js`（加防伪列+`batch_id`）；新增 `models/ProductSeries.js`/`Supplier.js`/`ExchangeItemSupplier.js`/`ProductBatch.js` 等，**紧跟 `ExchangeChannelPrice`（约 451 行）后注册**（§15.2 修2）。
4. **Service**：`ExchangeItemService.createExchangeItem`(208) 事务内回填 `item_code`、选填 `series_id` 时调 `SeriesSeqAllocator`；`createSku`(328–330) 移除人工口子；`_generateUniqueSkuCode`(896)/`_buildCartesianSkuCode`(878) 改 `generateUnique('SK',...)`；`admin/ItemManagementService.js:269` 改随机 SK 码。
5. **新增服务**：`services/exchange/ProductSeriesService.js`、`services/exchange/SupplierService.js`，按 `services/index.js`（336/502 范式）注册为 `product_series_service`/`supplier_service`。
6. **搜索**：`QueryService.getMarketItems` keyword 分支（402 行 ORM + 1519 行原生 SQL）增编码归一化匹配；管理端货号辅助查询（`exchange_item_suppliers.supplier_item_code` 模糊 + `supplier_id` 筛 + 组合，返回多条"共 N 条"不去重）。
7. **C 端保留**：`DataSanitizer.sanitizeExchangeMarketItems`（1466–1480 黑名单区**不加** `item_code`）。
8. **校验**：`npm run check:api-contract`、`npm run lint`、`TZ=Asia/Shanghai jest`（新生成器单测 + 受影响 `ExchangeItemService` 用例全绿）。
9. **管理端配套**（后端通过后）：admin 展示列/编码搜索框/手册导出（`exceljs` 后端 + `jspdf` 前端）/**二维码（拍1 方案A：`admin/` 装纯 JS `qrcode` 库，后端出 payload、前端渲染）**/供应商管理页（仿 `merchant-management.html`）/系列管理页/健康看板（`dashboard-panel.html` + echarts）；删 `prop-shop.js:321`。

### 15.8 复核总结（一句话）

**真实库实测证实：编码相关列/表全部待建，现网技术栈（Sequelize+mysql2+exceljs+crypto 生成器范式 / admin Vite+Alpine+Tailwind+jspdf+xlsx+echarts）足以零后端新增依赖落地本方案；三端归属清晰——后端主改造、admin 配套改造、小程序仅"不展示+搜索透传"。复核后新增 4 项（§15.6）已全部拍板采纳推荐：①二维码走方案A（后端出 payload、admin `qrcode` 纯 JS 渲染）②`item_code` 本次迁移一步到位 NOT NULL ③迁移拆 3 个文件 ④供货商确认独立分表。至此全部待定项清零，方案可直接进入施工。**

### 15.9 四项待拍板的行业对照与推荐（回应"大厂/小厂/游戏/活动/二手怎么做"）

> 说明：4 项里**只有拍1（二维码）存在真正的行业设计流派分野**；拍2/拍3/拍4 本质是"数据迁移与建模的工程规范"，大厂/开源社区做法高度一致、无流派之争，故只给工程惯例与最优解。

#### 拍1 · 二维码渲染库放哪（唯一有行业流派的点）

核心分歧只有一个：**"编码 payload 在后端生成、二维码图片在哪渲染"**。

| 主体 | 典型做法 | 图在哪生成 | 借鉴 |
|---|---|---|---|
| 美团/微信支付（券码核销） | 后端出**签名字符串 payload**，端侧 SDK/JS 渲染图 | 端侧 | **你现网消费码就是这套**（`QRV2_{base64}_{sig}`，见 `utils/QRCodeValidator.js`）——直接对齐 |
| 阿里/京东（商品/营销物料） | 后端出短链/ID，前端 JS 库渲染；批量印刷走内部物料工具 | 端侧为主 | 前端库渲染是主流 |
| 小公司/传统零售 | 草料二维码等**第三方 SaaS 手动生成静态图**贴上去 | 第三方 | 零开发但不可批量/不可追溯，反面 |
| 游戏公司 | 商品二维码少用；防伪/激活码走后端签名 + 端渲染 | 端侧 | 同美团范式 |
| 活动策划/盲盒 | 包装印刷二维码，设计工具/第三方**批量导出静态图** | 第三方/工具 | 手册印刷可借鉴（导出时一次性生成） |
| 得物/闲鱼（二手/鉴别） | 鉴别码、防伪码后端签名，展示端渲染 | 端侧 | 同美团范式 |

**三种落地方案对比（结合你的实测栈）**：

| 方案 | 做法 | 新增依赖 | 长期维护 / 技术债 | 适配你现网 |
|---|---|---|---|---|
| A（推荐）**前端库渲染** | 后端出编码 payload；admin 装 `qrcode`（纯JS）渲染、小程序用原生 `wx.canvas`；印刷手册用 `jspdf` 嵌图 | 仅 admin **+1 个纯前端库**（零后端依赖） | **最低**：强一致现网"后端串+端侧渲染"消费码范式，后端零改动面 | ★★★★★ 完美贴合 |
| B 后端出图 | 后端装 `qrcode`/`sharp` 生成 PNG 返回 | 后端 +1 依赖（`sharp` 已有但用于缩略图，职责混淆） | 中：后端多一条图片生成链路、占 CPU、与现网"码只出串"哲学冲突 | ★★☆ 违背现网范式 |
| C 第三方 SaaS | 草料等手动/API 生成 | 外部服务依赖 | 高：不可批量、数据出域、断网即挂，不可控 | ★☆ 不考虑 |

> **推荐 A**。理由：①你现网所有二维码（消费/核销）已是"后端签名串 + 端侧渲染",A 与之 100% 一致,不引入第二种范式;②后端零新增依赖,`sharp` 不被拉去做与缩略图无关的事,职责单一;③admin 那一个 `qrcode` 纯 JS 库体积极小、无原生编译、Vite 直接打包,长期维护成本几乎为零。**这就是"长期低维护、零技术债、贴合现有技术栈"三维度同时最优的选择。**

#### 拍2 · `item_code` 何时收紧 NOT NULL（工程规范,无流派）

大厂标准做法(在线加非空列)是**三步走**:加可空列 → 回填 → 收紧非空,避免长表锁与回填期写入空值。**但那是为"亿级存量、不能停机"设计的**。

| 场景 | 行业做法 | 你的情况 |
|---|---|---|
| 大厂亿级存量、7×24 不停机 | 分批回填、影子列、最后灰度收紧 NOT NULL | 不适用——你现网**仅 2 条 SPU、未上线** |
| 中小项目、可短暂维护窗 | 同一迁移内"加列→回填→收紧" | **正是你的情况** |

> **推荐:本次迁移内一步到位 `NOT NULL`**。2 条数据回填零风险、未上线无并发写入,分步反而留下"可空的历史包袱"(将来还得再来一次收紧迁移)=技术债。一次性投入、不留尾巴,正合你的诉求。

#### 拍3 · 迁移是否拆分多文件（工程规范,无流派）

Rails/Sequelize/Flyway/Liquibase 社区**一致惯例**:一个迁移文件 = 一个内聚变更单元,便于**单独回滚、定位失败**。10 张新表 + 3 表改列塞进一个文件,一旦第 8 张表建失败,回滚与排查都痛苦。

> **推荐:拆 3 个迁移文件**,按 §14.6 依赖序执行:
> 1. `*-product-code-core`:`exchange_items`/`exchange_item_skus` 改列 + `item_code`/`barcode`/系列字段 + 回填。
> 2. `*-supplier-and-series`:`suppliers` + `product_series` + `exchange_item_suppliers`。
> 3. `*-future-scenarios-s1-s5`:`product_batches` + `items` 防伪列 + 采购/组合/分销/寄卖表。
>
> 每个文件独立 up/down,失败只回滚该单元。这是"降低技术债"在迁移层的直接体现。

#### 拍4 · 确认供货商与核销 merchants 彻底分表(建模规范,无流派)

实测 `merchants` 是**核销商家**(`commission_rate` 抽佣、`settlement_account_id` 结算账户、`merchant_type` 餐厅/店铺),语义是"谁来核销券、跟谁分账";而供货商是"从谁进货"。**这是两个不同的业务实体**,大厂(如 SAP/用友的供应商主数据 vs 门店主数据)一律分表。强行合并会导致一张表背两套字段与状态机,是典型技术债。

> **推荐:确认分表**(方案已按此设计,§3.8/§4.5)。你只需认可"供货商走独立 `suppliers`(BIGINT 主键)、不复用 merchants"即可,无额外动作。附带好处:供货商主键 BIGINT 与其余业务表口径统一,避开 merchants 的 INT 主键。

#### 一句话汇总

**真正要你做流派选择的只有二维码(拍1)→ 推荐方案 A「后端出 payload、admin 用纯 JS `qrcode` 渲染」,与你现网消费码范式一致、后端零新增依赖;其余 3 项是工程规范,最优解已明确:`item_code` 本次直接 NOT NULL、迁移拆 3 个文件、供货商确认独立分表。四项全部指向同一原则——一次投入到位、不留可空列/不塞巨型迁移/不混用实体,即"长期低维护、零技术债、贴合 Node.js+Sequelize+MySQL 现状"。**

## 16. 施工完成状态（2026-07-06，实连真实库验证通过）

> 本节记录 §15.7 全部执行步骤的落地状态。所有验证均实连真实库 `restaurant_points_dev` + 真实 API（登录 13612227910 / 验证码 123456）完成，**非预设数据**。

### 16.1 后端数据库项目（已全量落地）

| # | 项 | 落点（真实文件） | 验证结果 |
|---|---|---|---|
| 1 | 生成器 | `utils/ProductCodeGenerator.js`（generate/format/normalize/validate/detect/generateUnique）、`utils/SeriesSeqAllocator.js`（allocate 行锁发号/format） | 单测 24 个全绿（`tests/unit/utils/`） |
| 2 | 迁移（拍3 拆3文件） | `migrations/20260706040000-alter-table-product-code-core.js`（item_code NOT NULL 一步到位·拍2）、`…040100-create-table-supplier-and-series.js`、`…040200-create-table-future-scenarios-s1-s5.js` | 已执行；实测 `exchange_items.item_code` 已回填（`SPAFBF8URGS4CY`/`SPK7ZAPJHJQTEJ`），`sku_code` 已重写为 SK+12 位并收窄 VARCHAR(14)，10 张新表全部建出 |
| 3 | 模型 | `models/ExchangeItem.js`（item_code/series_id/series_seq + series/suppliers 关联）、`ExchangeItemSku.js`（sku_code STRING(14) + barcode）、`Item.js`（防伪5列+batch_id）；新增 `ProductSeries`/`Supplier`/`ExchangeItemSupplier`/`ProductBatch`/`PurchaseOrder(Item)`/`ProductBundle(Item)`/`ExternalChannelMapping`/`ConsignmentOrder`，均已注册 `models/index.js` | 模型加载 136 个正常 |
| 4 | SKU/SPU 码收口 | `ExchangeItemService`（createExchangeItem 回填 SP 码 + 系列发号；createSku 移除人工口子；`_generateUniqueSkuCode`/`_buildCartesianSkuCode` 统一 `generateUnique('SK',…)`）；`admin/ItemManagementService.js` 默认 SKU 码改随机 SK 码 | 通过 |
| 5 | 新增服务 | `services/exchange/ProductSeriesService.js`（系列 CRUD）、`services/exchange/SupplierService.js`（供应商 CRUD + 商品关联全量替换 + 货号辅助查询 + 冲突提示 + 健康统计）；注册键 `product_series_service`/`supplier_service`（`services/index.js`） | ServiceManager 获取正常 |
| 6 | 编码搜索 | C 端 `QueryService.getMarketItems` keyword 分支（ORM + `_buildFiltersCount` 原生 SQL 分支）与管理端 `ExchangeItemService.listExchangeItems`：`detect` 命中 SP→item_code 精确匹配、SK→反查所属 SPU、未命中→item_name 模糊（原行为） | 实测 `keyword=sp afbf 8urg s4cy`（含空格容错）C端/管理端均精确命中 |
| 7 | C 端只进不出 | `QueryService` marketItemView/marketItemDetailView 增 `item_code` 下发；`DataSanitizer.sanitizeExchangeMarketItems` 黑名单**不删** item_code（供搜索回显） | C 端响应含 item_code |
| 8 | 新路由 | `routes/v4/console/exchange/suppliers.js`（CRUD + `/code-search` + `/health-stats`）、`product-series.js`（CRUD）、`items.js` 增 `GET/PUT /:id/supplier-links`；均经 ServiceManager + TransactionManager（路由管事务边界） | 全部实测通过（含删除保护 409 语义） |
| 9 | 幂等白名单 | `CANONICAL_OPERATION_MAP` 新增 `ADMIN_SUPPLIER_CREATE/UPDATE`、`ADMIN_PRODUCT_SERIES_CREATE/UPDATE`、`ADMIN_EXCHANGE_ITEM_SUPPLIER_LINKS_SET` | 写操作实测未被白名单拒绝 |
| 10 | 手册导出 | `items.js` GET /export 增列：商品编码（SP 展示形）/系列号/展示价/计价资产/图片URL | exceljs 后端 blob，展示形统一带连字符 |

### 16.2 Web 管理后台（admin/，已全量落地并重新构建）

| # | 项 | 落点 |
|---|---|---|
| 1 | 删手填口子（修6） | `prop-shop.js` 建默认 SKU 不再传 sku_code（后端自动生成） |
| 2 | 编码展示 | 商品列表表格/宫格卡片增「商品编码」「系列号」列（展示形）；SKU 规格表增「SKU编码」列；编辑弹窗只读展示 SP 码 + 系列号 |
| 3 | 编码搜索框 | 兑换市场商品列表：输 SP/SK 编码（容错大小写/横线/空格）精确定位、输文字名称模糊搜（归一化在后端） |
| 4 | 商品表单 | 「所属系列」下拉（归入系列后端自动发连号）+「供应商关联」多行区块（供应商+货号+主供标记，全量替换保存） |
| 5 | 二维码（拍1 方案A） | admin 安装纯 JS `qrcode` 库（懒加载），编辑弹窗渲染 item_code 二维码（内容=编码规范形，扫码结果可作搜索输入） |
| 5b | 手册导出 PDF（§8.3 项3/4，2026-07-06 补齐） | `exchange-items.js` 新增 `exportManualPdf`：拉全量商品→每款用 `qrcode` 生成 item_code 二维码→HTML 卡片（图片+名称+SP码+系列号+价格+二维码）→`html2canvas` 截图→`jspdf` 逐页嵌图（中文用截图方案，非 jsPDF 内置字体）；工具栏「导出手册 PDF」按钮 |
| 5c | 导出清单二维码（§8.3 项4，2026-07-06 补齐） | 上条 PDF 每款商品卡片内嵌 item_code 二维码，随手册一并印刷 |
| 5d | 扫码/输码快速定位（§8.3 增强项6，2026-07-06 补齐） | `exchange-items.js` 新增 `quickLocate`：输码/扫码枪录入回车→调编码搜索 API（后端归一化）→唯一命中直达单品编辑弹窗、多条命中回退列表筛选；商品列表工具栏「🎯 定位」入口（区别于列表过滤搜索框） |
| 5e | 修复导出下载（2026-07-06） | `exportItems` 原以 json 解析 xlsx 二进制流（导出实际失效），改 `responseType:'blob'` + createObjectURL 下载；按钮文案「导出 CSV」正名为「导出 xlsx」（后端产出即 xlsx） |
| 6 | 供应商管理页 | 新建 `supplier-management.html` + `src/modules/market/pages/supplier-management.js`（Tab1 供应商 CRUD；Tab2 按货号找货：模糊+供应商筛+组合定位+"共N条"+⚠️重复货号提示） |
| 7 | 系列管理页 | 新建 `product-series-management.html` + `product-series-management.js`（系列 CRUD，next_seq 只读展示） |
| 8 | 健康看板 | `dashboard-panel.html` 数据驾驶舱新增「商品主数据」Tab：编码回填数/无供应商归属/货号缺失/重复货号清单/供货分布/系列归属/条码预留统计（数据源 `/console/exchange/suppliers/health-stats` 真实库聚合） |
| 9 | API 客户端 | `src/api/exchange-item/index.js` 增供应商/系列/关联/货号查询/健康统计端点常量与方法（直接反映后端路由，零映射） |
| 10 | 工具函数 | `src/utils/index.js` 增 `formatProductCode`/`formatSeriesNo`（与后端 format 完全同规则） |
| 11 | 注册 | `vite.config.js` pageConfigs、`sidebar-nav.js` 资产交易组增「🏭 供应商管理」「📚 产品系列管理」入口；`npm run build` 已重新构建，`lint`/`lint:mappings` 通过 |

### 16.3 质量验证记录（2026-07-06 北京时间）

- ESLint（后端改动文件 + admin `src/` 全量）：0 error；Prettier：改动文件全部通过。
- Jest：`ProductCodeGenerator`/`SeriesSeqAllocator` 24 个单测全绿。
- 实连 API 验证链（PM2 重启 + `--update-env` 后）：创建供应商 → 创建系列（`slnb` 自动大写归一化为 `SLNB`）→ 商品633 挂关联（货号 `CB4385` + 主供）→ 货号模糊查询命中 1 条（带我方 item_code + 商品名）→ C 端/管理端编码容错搜索精确命中 → 解除关联 → 删除供应商/系列成功；删除保护（被引用 409）语义验证通过。验证用测试数据已经业务接口清理，真实库无残留。
- 健康检查：`/health` 正常，PM2 4 实例 online，Redis online。

### 16.4 微信小程序前端对接说明（拿本节给小程序开发，独立仓库）

> 适用：微信小程序前端（独立仓库，不在后端 devbox 工程内）。本节即完整对接说明，无需另开文档。
> 本节所有结论均已实连真实库 `restaurant_points_dev` + 真实 API（管理员 13612227910 / 验证码 123456）复核通过，非预设数据。

#### 16.4.0 一句话总览

小程序**无结构性改造**，只需两点配合，其余全部由后端落地：
1. **编码只进不出**：商品接口会返回 `item_code`，但前端**不要把它当商品信息字段展示**（对标淘宝隐藏 item_id），仅可用于搜索命中后的回显。
2. **搜索透传**：搜索框把用户输入**原样**传 `keyword`，不要在前端做任何大小写/横线/空格处理，归一化与「编码精确匹配 or 名称模糊匹配」的分流全部由后端完成。

#### 16.4.1 前端要做的两件事

**（1）编码只进不出（不展示 item_code）**
- 商品列表 `GET /api/v4/exchange/items` 与详情 `GET /api/v4/exchange/items/:id` 的响应中**会包含 `item_code` 字段**（字符串，规范形如 `SPAFBF8URGS4CY`）。
- **前端不要把 `item_code` 渲染为商品信息字段**展示在商品卡片/详情页上（防止暴露内部标识、防枚举）。
- 唯一用途：用户按手册输码搜索后，前端可在搜索结果处回显"已匹配 SP-AFBF-8URG-S4CY"作为命中确认（可选）；要展示回显请用带连字符的展示形（规范形每 4 位加 `-`，前缀 `SP`/`SK` 后加 `-`）。

**（2）搜索透传（不做前端归一化）**
- 搜索框把用户输入**原样**传 `keyword`：`GET /api/v4/exchange/items?keyword=<用户原始输入>`，**不要**在前端做大小写/去横线/去空格处理。
- 后端自动分流：命中 `SP`/`SK` 编码格式 → 归一化后按 `item_code` 精确匹配；否则按 `item_name` 模糊匹配。
- 实测容错：`sp-afbf-8urg-s4cy`、`SP AFBF 8URG S4CY`、`spafbf8urgs4cy`（大小写/横线/空格任意组合）**均精确命中同一商品**。

#### 16.4.2 接口契约（对接要点）

| 维度 | 约定 | 说明 |
|---|---|---|
| 列表接口 | `GET /api/v4/exchange/items?keyword=&page=&page_size=` | C 端商品列表，`keyword` 为搜索/编码输入统一入口 |
| 详情接口 | `GET /api/v4/exchange/items/:id` | `:id` 为商品自增主键（数字，字符串下发） |
| 响应包装 | `{ success, code, message, data, timestamp }` | 统一壳，读 `data` |
| 字段命名 | snake_case（`item_code`/`item_name`/`min_cost_amount`） | 前端直读同名字段，**禁止字段映射** |
| 主键口径 | BIGINT/主键以**字符串**下发（防溢出） | `exchange_item_id`、`sku_id` 均按字符串处理 |
| 时间字段 | UTC ISO8601 带 `Z`（如 `2026-07-05T19:14:09.000Z`） | 展示转北京时间由前端统一处理 |
| 下单标识 | 继续用 `default_sku_id` / `sku_id`（数字ID字符串） | **不读 `sku_code`**，编码不参与下单链路 |

商品展示相关字段（照常展示，非编码字段）：`item_name`、`min_cost_amount`（展示价）、`min_cost_asset_code`（计价资产）、`primary_image`（主图对象，取 `.url`/`.thumbnail_url`）、`series`（系列对象，可空）。

#### 16.4.3 明确不用改的地方

- **下单流程**：继续用 `default_sku_id` / `sku_id`，不读 `sku_code`，编码体系与下单解耦。
- **响应包装、字段命名（snake_case）、BIGINT 字符串口径、时间 UTC 下发**：与现网其它接口一致，无特殊处理。
- **系列号 `series_code`+`series_seq`**：如需展示系列可读展示形（`SLNB-001`），从 `series` 对象拼装；不需要则忽略。

#### 16.4.4 二维码（后续增强，非本期必须）

- 手册/管理后台会为 `item_code` 生成二维码（后端只出编码 payload，端侧渲染图）。
- 小程序若要支持"扫码找商品"：**扫码得到的内容就是编码规范形字符串**（如 `SPAFBF8URGS4CY`），直接填入搜索框同一入口（`keyword`）即可跳转商品，**无需新接口**；归一化仍由后端做，前端扫码结果原样透传。

#### 16.4.5 联调自测清单（前端可自行验证）

1. 列表/详情响应里能看到 `item_code`，但商品卡片/详情页**没有把它渲染出来**。
2. 搜索框输入带横线展示形（`sp-afbf-8urg-s4cy`）能精确命中对应商品。
3. 搜索框输入商品名称片段能模糊命中。
4. 下单请求体里用的是 `sku_id`/`default_sku_id`，不含 `sku_code`。
5. 时间字段前端按北京时间展示正确（后端下发 UTC 带 `Z`）。

#### 16.4.6 遇到问题找谁

- 某编码搜不到、或响应缺 `item_code`：属**后端问题**，反馈给后端；前端不要加兜底/降级或前端归一化绕过。
- 需要新增编码相关能力（如按 `sku_code` 直接下单、编码批量查询）：先与后端确认是否已有端点，未有则由后端评估开发，前端不自造映射。

## 17. S1–S5 业务逻辑实施方案（2026-07-06 拍板：5 个全做，按 §13.7 顺序逐个迭代）

> 背景：§14/§16 已把 S1–S5 的 **10 张表 + 外键 + 索引 + 模型（含 associate 关联）** 全部建好并实连真实库验证（`purchase_orders`/`purchase_order_items`/`product_batches`/`product_bundles`/`product_bundle_items`/`external_channel_mappings`/`consignment_orders` 等，均为空表）。本章补齐"表之上的业务逻辑"——Service / 路由 / 幂等映射 / admin 前端。
>
> 拍板（2026-07-06）：**5 个场景全做**，但**按 §13.7 顺序分期逐个上线**：S1 采购单 → S2 批次 → S4 组合 → S5 分销 → S3 二手（S3 涉反向资产流+合规，最后做、单独过风控评审）。每期独立评审、独立发布，避免一次性堆砌无人使用的功能（YAGNI）。
>
> **零新增技术栈铁律**：全部复用现网范式——Service 实例类 + `assertAndGetTransaction`（写操作强制外部传入事务）、路由经 `ServiceManager` 取服务 + `TransactionManager.execute()` 管事务边界、`res.apiSuccess/apiError`、`BusinessError`、`CANONICAL_OPERATION_MAP` 幂等白名单、时间 UTC 存储展示转北京、snake_case 字段直读禁映射。**不新建基类、不新增依赖、不动编码体系（`item_code`/`sku_code` 等全不变）。**

### 17.0 现状盘点（实测，施工起点）

| 层 | S1–S5 现状 | 待补 |
|---|---|---|
| 数据库表 + 外键 + 索引 | ✅ 已建（空表，实连真实库确认） | 无 |
| 模型 + associate 关联 | ✅ 已注册 `models/index.js`（`PurchaseOrder`/`PurchaseOrderItem`/`ProductBatch`/`ProductBundle`/`ProductBundleItem`/`ExternalChannelMapping`/`ConsignmentOrder`） | 无 |
| Service（业务逻辑） | ❌ 无 | 每场景新增 1 个 Service 类，注册进 `services/index.js` |
| 路由 API | ❌ 无 | 每场景新增 1 个路由文件，挂 `routes/v4/console/exchange/index.js` |
| 幂等白名单 | ❌ 无 | 写路径登记进 `CANONICAL_OPERATION_MAP` |
| admin 前端 | ❌ 无 | 每场景新增管理页（仿现有页面范式） |
| 预留字段读写 | ❌ 仅健康看板统计 barcode | 各场景填 `purchase_price`/`quality_score`/`batch_id`/`anti_fake_code` 等 |

### 17.1 统一施工范式（每个 S 场景都照此，不重复造轮子）

**后端（复用 SupplierService / suppliers.js 现成范式）**
1. **Service**：`services/exchange/XxxService.js`，`class XxxService { constructor(models){this.models=models;this.sequelize=models.sequelize} }`；读方法直接查询、写方法首行 `const transaction = assertAndGetTransaction(options, 'XxxService.method')`；错误一律 `throw new BusinessError(msg, CODE, httpStatus)`；单号/批次码复用现网 `TrackingCodeGenerator`「前缀+日期+序号」范式（**不纳入随机码体系**，§13.3 已定）。
2. **注册**：`services/index.js` 顶部 `require` + `this._services.set('xxx_service', new XxxService(this.models))`（键用业务域 snake_case）。
3. **路由**：`routes/v4/console/exchange/xxx.js`，`getXxxService(req){return req.app.locals.services.getService('xxx_service')}`；读接口直接 `await service.xxx()`；写接口包 `await TransactionManager.execute(async transaction => service.xxx(body,{transaction}))`；统一 `authenticateToken + requireRoleLevel(100)`；具体静态路径（如 `/stats`）必须定义在 `/:id` 之前。
4. **挂载**：`routes/v4/console/exchange/index.js` 加 `router.use('/xxx', require('./xxx'))`。
5. **幂等**：写路径登记 `CANONICAL_OPERATION_MAP`（动态参数统一 `:id`），如 `'/api/v4/console/exchange/purchase-orders': 'ADMIN_PURCHASE_ORDER_CREATE'`。

**前端（复用 admin 现有页面范式，先查后建）**
6. 先 `grep`/`list_dir` 找可复用页面（如 supplier-management/exchange-market），能扩展就扩展；新页仿 `merchant-management.html` 结构：EJS + Alpine.js + Tailwind + `*_ENDPOINTS` + `request()` 封装 + `data-table` 组件；改完 `npm run build`。字段 snake_case 直读后端、禁映射。

**质量门禁（每期收尾）**：`npm run check:api-contract` + `npm run lint`（后端）/ admin `lint` + `lint:mappings` + `npm run build`；`TZ=Asia/Shanghai jest` 新增单测；`/health` 健康；实连真实库 + 真实 API（13612227910/123456）验证数据真实变化。

### 17.2 第 1 期 · S1 采购单 / 进货管理

业务：向供应商按批进货，记录进货价、数量、到货时间，支撑按供应商结算对账与供货质量评分。

| 项 | 落点 | 说明 |
|---|---|---|
| Service | 新增 `services/exchange/PurchaseOrderService.js` | 采购单头+明细的 CRUD、状态机（draft→ordered→received→cancelled）、收货入库 |
| 表 | `purchase_orders`（已建）+ `purchase_order_items`（已建） | 头挂 `supplier_id`；行挂 `exchange_item_id`/`sku_id`/`batch_id`/`purchase_price` |
| 单号 | `order_no` 复用 `TrackingCodeGenerator` 前缀+日期+序号（如 `PO20260706001`） | 唯一索引兜底；非随机码体系 |
| 预留字段回写 | 收货时把行 `purchase_price` 汇总回写 `exchange_item_suppliers.purchase_price`（§4.5 已预留列） | S1 启用即激活该预留列 |
| 路由 | 新增 `routes/v4/console/exchange/purchase-orders.js` | 列表/详情/创建/更新/收货/取消；写操作 TransactionManager |
| 幂等 | `POST /purchase-orders`→`ADMIN_PURCHASE_ORDER_CREATE`；`/:id`→`ADMIN_PURCHASE_ORDER_UPDATE`；`/:id/receive`→`ADMIN_PURCHASE_ORDER_RECEIVE` | 动态参数统一 `:id` |
| 前端 | 新增 `admin/purchase-order-management.html` + `src/modules/market/pages/purchase-order-management.js`（仿供应商页）；侧栏加入口 | 选供应商→加采购行（选商品/SKU、数量、进货价）→保存→收货 |

状态机约束：`received` 后不可改行；`cancelled` 为终态。收货是唯一会写库存/回写进货价的动作，必须在事务内完成。编码影响：**无**（进货价/批次属采购域，不进对外编码，§13.2 铁律）。

### 17.3 第 2 期 · S2 批次管理 / 一批一码

业务：半标品"这批料 vs 那批料"，按批次管成本、按批次召回、按批次做防伪连号。

| 项 | 落点 | 说明 |
|---|---|---|
| Service | 新增 `services/exchange/ProductBatchService.js` | 批次 CRUD、批次码生成、按批次查实物、按批次召回 |
| 表 | `product_batches`（已建）；`items.batch_id`（已建 FK） | 批次挂 `exchange_item_id`/`sku_id`/`supplier_id`；实物 `items.batch_id` 归属批次 |
| 批次码 | `batch_code` 复用「前缀+日期+序号」（如 `BC20260706001`），运营可读，非随机码 | §13.3 已定，与 `item_code` 正交 |
| 与 S1 挂接 | S1 收货可选生成/关联批次，把 `purchase_order_items.batch_id` 填上 | 采购→批次→实物三段打通 |
| 防伪连号（预留） | 批次内可发连号 `items.anti_fake_code`（§3.7 预留字段，本期可选启用） | 一批一码段；验真接口仍为后续 |
| 路由 | 新增 `routes/v4/console/exchange/product-batches.js` | 列表/详情/创建/更新/按批召回 |
| 幂等 | `POST /product-batches`→`ADMIN_PRODUCT_BATCH_CREATE`；`/:id`→`ADMIN_PRODUCT_BATCH_UPDATE` | |
| 前端 | 新增 `admin/product-batch-management.html` + JS（仿供应商页）；侧栏加入口 | |

设计提醒：批次（进货生产维度）与 §4.3 系列（营销成套维度）**正交，不合并**。一个商品可同属某系列、又分多批次。编码影响：**无**。

### 17.4 第 3 期 · S4 组合商品 / 套装 / 赠品

业务："手串 + 礼盒 + 保养油"打包成套售卖。

| 项 | 落点 | 说明 |
|---|---|---|
| Service | 新增 `services/exchange/ProductBundleService.js` | 组合 CRUD、明细 BOM 管理、库存/履约按明细拆解 |
| 表 | `product_bundles`（已建）+ `product_bundle_items`（已建） | 组合本身是一个 SPU（有自己的 `item_code`，`SP` 前缀，复用现有生成器）；明细挂子 `exchange_item_id`/`sku_id` + 数量 + 是否赠品 |
| 与商品创建挂接 | 建组合时先走 `ExchangeItemService.createExchangeItem` 产出组合 SPU（自动回填 `item_code`），再建 BOM 明细 | 复用现有 SPU 创建链路，零新编码逻辑 |
| 路由 | 新增 `routes/v4/console/exchange/product-bundles.js` | 列表/详情/创建/更新/删除 + 明细增删 |
| 幂等 | `POST /product-bundles`→`ADMIN_PRODUCT_BUNDLE_CREATE`；`/:id`→`ADMIN_PRODUCT_BUNDLE_UPDATE` | |
| 前端 | 兑换市场页新增「组合商品」Tab 或独立页（仿商品管理，先查 exchange-market 能否扩展 Tab 再决定） | 选父 SPU→加子项（商品/SKU、数量、是否赠品） |

编码影响：**无**，组合品用标准 `item_code`，明细走关系表，符合电商"虚拟组合 SPU + BOM"主流做法。

### 17.5 第 4 期 · S5 第三方平台分销（淘宝/抖音）

业务：把商品铺到外部平台，需把外部平台商品 ID 映射回我方商品。

| 项 | 落点 | 说明 |
|---|---|---|
| Service | 新增 `services/exchange/ExternalChannelMappingService.js` | 映射 CRUD、同步状态管理（pending/synced/failed/disabled） |
| 表 | `external_channel_mappings`（已建） | `channel`+`external_item_id` 唯一；我方 `exchange_item_id`/`item_code` 作对接锚点 |
| 关键点 | `item_code` 作对外稳定唯一键，天然适合当对接锚点；外部平台 ID 只存映射表，不污染主数据 | §13.6 已定 |
| 路由 | 新增 `routes/v4/console/exchange/channel-mappings.js` | 列表/详情/创建/更新/删除 + 同步状态更新 |
| 幂等 | `POST /channel-mappings`→`ADMIN_CHANNEL_MAPPING_CREATE`；`/:id`→`ADMIN_CHANNEL_MAPPING_UPDATE` | |
| 前端 | 新增 `admin/channel-mapping-management.html` + JS（仿供应商页）；侧栏加入口 | 选我方商品→填渠道+外部ID→管理同步状态 |

注：实际与淘宝/抖音的 API 对接（拉单/推库存）是**独立子系统**，本期只做"映射主数据管理"，对接联调另立项。编码影响：**无，且正是 `item_code` 的用武之地**。

### 17.6 第 5 期 · S3 二手回流 / 寄卖 / 转赠（最后做，需过风控评审）

业务：用户把旧物寄卖回平台再售、实物转赠他人。

| 项 | 落点 | 说明 |
|---|---|---|
| Service | 新增 `services/exchange/ConsignmentService.js` | 寄卖单 CRUD + 状态机（pending→listed→sold→withdrawn/rejected） |
| 表 | `consignment_orders`（已建） | 寄卖物 `item_id`（一物一码）、寄卖人 `consignor_account_id`、回流后再上架目标 `relist_item_id` |
| 所有权流转 | **复用现有** `items` + `item_ledger`（流水账 + `owner_account_id` 变更），转赠 `event_type='transfer'` 不单独建表 | §14.5 已定 |
| ⚠️ 风控前置 | S3 涉"用户→平台"反向资产流，**上线前必须单独过合规/风控评审**（方向等价/向下、真实销毁校验），复用现有交易合规校验链路 | §13.4 已注明 |
| 路由 | 新增 `routes/v4/console/exchange/consignments.js` | 列表/详情/创建/上架/售出/撤回/驳回 |
| 幂等 | `POST /consignments`→`ADMIN_CONSIGNMENT_CREATE`；`/:id/xxx`→对应操作码 | |
| 前端 | 新增 `admin/consignment-management.html` + JS；侧栏加入口 | |

编码影响：**无**。`tracking_code`（一物一码）是二手流转天然载体，二手物仍指向原 `item_template_id`/`item_code`，流转只改归属不改身份。

### 17.7 分期交付里程碑

| 期 | 场景 | 依赖 | 交付物 | 风险 |
|---|---|---|---|---|
| 1 | S1 采购单 | 供应商（已就位） | PurchaseOrderService + 路由 + 采购管理页 + 回写 `exchange_item_suppliers.purchase_price` | 低 |
| 2 | S2 批次 | S1（收货关联批次） | ProductBatchService + 路由 + 批次管理页 + `items.batch_id` 回写 | 低 |
| 3 | S4 组合 | 商品创建链路（已就位） | ProductBundleService + 路由 + 组合管理页/Tab | 低 |
| 4 | S5 分销 | 商品（已就位） | ExternalChannelMappingService + 路由 + 映射管理页 | 中（外部对接另立项） |
| 5 | S3 二手 | items/item_ledger（已就位）+ **风控评审** | ConsignmentService + 路由 + 寄卖管理页 | 高（反向资产流，须先过合规） |

每期均为独立可发布单元，做完一期验收上线再启动下一期；任一期未启动前，其空表放置不影响任何现有流程（零成本）。

