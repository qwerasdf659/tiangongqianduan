# 水晶奖品倍率活动设计方案

> 版本：v1.6　|　创建日期：2026-07-05（北京时间）　|　状态：后端 + Web 管理后台已实施落地（见 §23），微信小程序待对接
> 适用项目：天工商户营销平台（Node.js + Express + Sequelize + MySQL 8 + Redis）
> 求证方式：本方案表结构/字段/代码落点均以 Node.js(mysql2/Sequelize) 实连 `restaurant_points_dev` 真实库
> 执行 `SHOW FULL COLUMNS`/真实数据抽样核对，并逐行对齐现网 `services/UnifiedLotteryEngine` 抽奖管线代码，未引用历史报告/备份。
>
> **v1.3 求证更新（2026-07-05，实连真实库 + 通读现网代码）**：以后端数据库项目为唯一权威，
> 新增 §14 求证结论、§15 三方问题划分、§16 后端权威 API 契约、§17 技术栈对齐(可复用/可扩展)、
> §18 执行步骤与真实代码落点、§19 拍板项。凡与 §1–§13 早期表述冲突处，**以 §14 起的求证结论为准**。
> 本文档同时作为**微信小程序前端求证文档**：小程序为独立仓库，其字段/路径/响应一律以 §16 后端契约为准，前端直接改用后端字段名，不做映射层。
>
> **v1.4 决策更新（2026-07-05）**：拍板 **D-1~D-8 全部执行、一次性全量建设、不分一二期**，原"不做/二期"项（`event_points` 活动专属货币、预算归集重定向、到期清零 job、tag 定向、双层货币、全部看板/查询/运维）**全部纳入本次实施**。§20 重写为"全量建设范围"，是唯一施工依据。
>
> **v1.5 二次求证更新（2026-07-05，重新实连真实库 `restaurant_points_dev` + 重新通读现网代码）**：本轮以 `mysql2/promise` 直连 `.env`(`DB_HOST=test-db-12-mysql.ns-br0za7uc.svc`) 真实库逐表 `SHOW FULL COLUMNS`/`information_schema` 核对，并重读 `SettleStage.js`/`CoreService.js`/`segment_rules.js`/`segment_field_registry.js`/`ApiResponse.js`/`maintenanceMode.js`/`platformDetector.js`/console 与 C 端路由/admin 前端。新增 **§22 二次求证校准**：修正 §1.1/A12 关于 `lottery_draws.material_amount` 的错误断言、校准 §16 API 契约以对齐现网 console 路由真实命名与鉴权风格、新增拍板项 D-9~D-13。**凡与前文冲突处，以 §22 为准。**
>
> **v1.6 实施落地回写（2026-07-06，后端 + Web 管理后台已全量实施并真实验证）**：本方案后端与 admin 前端已按 §20 全量建设完毕（迁移已执行、服务/路由已上线、单测 50/50 通过、真实 API 冒烟通过、PM2 重启健康）。新增 **§23 实施落地状态与最终契约（微信小程序对接权威）**，回写与设计不同的最终结果（归集规则独立表、扁平路由命名、C 端参数名 `:code`、新增 `event-points` 查询接口、抽奖响应 `prizes[]` 直带倍率字段、D-2 白名单最终补充字段等）。**凡涉及微信小程序对接的字段/路径/响应，一律以 §23 为最终权威（优先级：§23 > §22 > §14–§21 > §1–§13）。**
>
> **v1.7 第二轮补齐（2026-07-06，后端 + admin 补齐 §23.5 遗留 1~5 项，真实验证）**：新增 **§23.6**——落地 `lottery_campaigns.entry_asset_code` 入场资产（event_points 消耗链路真正闭环，双层货币可见层不再是纸面功能）、§13.2/§13.3/§13.4 监控看板/客服解释/运维视图（后端聚合 API + admin 监控看板 Tab）、§21 维护中间件 Jest 用例、并修复标签生产 `AdTagAggregationService` 两处 schema bug（真实产出 130 标签）。**凡涉及微信小程序对接的入场资产/扣费口径，一律以 §23.6 为最终权威（优先级：§23.6 > §23.3 > §23 > 其余）。**

## 0. 本文档解决什么问题（一句话导读）

**核心目标**：让运营能对"特定活动 + 特定人群"配置"抽中水晶类奖品数量翻倍"（如新用户/高等级 ×1.5~×2.5），且**倍率按活动隔离、成本可控、用户感知清晰**。

**本文档依次解决 4 个问题**：

| # | 问题 | 解决方案 | 章节 |
|---|---|---|---|
| P1 | 怎么给"某活动的某些人"翻倍水晶奖品，且运营可自助调控 | 独立倍率规则实体，绑定抽奖活动 + 复用现网分群引擎圈人群 | §1–§6 |
| P2 | 多个活动/多条规则都能翻倍时，倍率怎么算才不打架、用户不困惑 | 活动隔离 + 同活动内取最高(Max) + 用户只见单一倍率 | §2、§5 |
| P3 | 翻倍放大真实成本，怎么防止成本失控与薅羊毛 | 倍率硬上限 + 每规则额外成本封顶(`extra_cost_limit`) + 兜底不翻 | §7、§10 |
| P4 | 用户平时囤积分、专等翻倍日套现（囤积套利）怎么根治 | 活动专属预算(隐藏层) + 活动专属可见积分/代币(可见层) + 到期清零 | §12 |

**一句话方案**：翻倍规则绑定到具体抽奖活动、只作用"抽中水晶的发放数量"；短期翻倍活动用**独立的活动专属货币**（可见层入场 + 隐藏层成本双桶）承接，到期清零，从根上杜绝"平时囤、翻倍日套现"。全部复用现网技术栈（`segment_rule_configs`/`lottery_campaigns.allowed_campaign_ids`/`material_asset_types`/`SettleStage`），零新增技术栈。

**版本变更**：
- v1.1：人群定向改为复用现网 `segment_rule_configs`/`user_ad_tags`/`user_growth_levels`（§1.3、§3）。
- v1.2：新增 §12 防囤积套利（活动专属预算 + 双层货币模型 + 到期清零）。

## 1. 背景与业务目标

### 1.1 现有业务链路（实测确认）

平台现有资产与抽奖链路：

```
用户消费 → 审核通过 → 获得可见积分(points) + 0.08% 计入预算账户(预算积分/水晶碎片)
→ 预算积分抽奖 → 抽中材料类奖品(material，如红源晶碎片) → 按 material_amount 发放入账
```

关键实测事实：

| 环节 | 现网真实载体 |
|---|---|
| 预算账户 | `account_asset_balances`（`lottery_campaign_id` 区分预算积分归属活动） |
| 抽奖活动 | `lottery_campaigns`（`budget_mode`=user/pool/none） |
| 活动奖品 | `lottery_campaign_prizes`（`win_weight`/`stock_quantity`/`reward_tier`） |
| 奖品定义 | `prize_definitions`（`prize_type`=material/item/coupon/points） |
| 水晶类资产 | `material_asset_types`（如 `red_core_shard` 红源晶碎片、`red_core_gem` 红源晶，`group_code`=red/orange/...，`form`=shard/gem） |
| 发奖入账 | `SettleStage.js` material 分支 → `BalanceService.changeBalance(delta_amount = material_amount)` |
| 抽奖流水 | `lottery_draws`（含 `result_metadata` JSON；**注意：无 `material_amount` 列**，材料发放量展示走 `result_metadata` 快照，见 §22 修正 C1） |
| 资产流水 | `asset_transactions`（双录记账 + `idempotency_key` 幂等 + `meta` JSON） |

### 1.2 业务目标

> **对特定用户 / 特定用户群体 / 特定活动，抽中"水晶类奖品"时，发放数量可 ×2 / ×3（运营后台可调控、可开关）。**

核心诉求：

| 目标 | 说明 |
|---|---|
| 运营可调控 | 后台配置倍率、作用人群、作用奖品范围、时间窗、开关，无需改代码 |
| 多活动不打架 | 用户同时命中多个倍率活动时，结果唯一、成本可控、用户无困惑 |
| 感知清晰 | 用户任何时候只看到"一个确定的倍率"，抽中后能看懂 `基础×倍率=最终` |
| 成本封顶 | 倍率直接放大真实价值奖品，必须防止成本失控与薅羊毛 |
| 与现有系统解耦 | 只作用于"抽中水晶类 → 发放数量"这一步，不动抽奖概率、不动预算发放 |

### 1.3 方案选型：两个轴 + 复用现网人群基础设施

需求"某些人的水晶奖品翻倍"其实涉及两个正交的轴，早期容易混淆，先厘清：

| 轴 | 选项 A | 选项 B | 本方案选择 |
|---|---|---|---|
| **作用目标（翻什么）** | 预算积分（抽奖的"钱"，input） | 水晶奖品数量（抽到的"货"，output） | **B 水晶奖品数量** |
| **作用对象（给谁）** | 绑用户属性 / 绑抽奖活动 | 独立规则实体，引用人群 | **独立规则 + 引用人群** |

**为什么作用目标选"水晶奖品"而非"预算积分"**：

- 翻预算积分 = 放大抽奖次数 → 连非水晶奖品（item/coupon/points）一起变多，成本不可控，且不符合"只翻水晶"的目标；还会牵动 `account_asset_balances` 整个预算体系。
- 翻水晶奖品 = 只在"抽中水晶"这一小概率事件放大产出，精准、成本可预测、改动面最小（只碰 `SettleStage` 发奖最后一步）。

**为什么作用对象用"独立规则实体 + 引用现网人群"，不绑用户属性、也不绑抽奖活动**：

| 建模流派 | 缺点 | 是否采用 |
|---|---|---|
| 倍率写死在用户/人群属性上 | 无法时间盒、无法多规则共存、无法审计、规则塞进主数据=技术债 | ❌ |
| 倍率绑在抽奖活动 `lottery_campaigns` 上 | 与"参加活动"耦合；抽奖活动管"能不能抽/概率"，塞进"发奖翻倍"职责混乱 | ❌ |
| **独立倍率规则实体 + 引用现网人群引擎** | 可时间盒/开关/审计、多规则 Max 合并、人群零重复造轮子、与抽奖/预算/主数据解耦 | ✅ |

**关键：人群定向复用现网已有基础设施（实测确认存在，无需新造人群表）**：

| 现网已有 | 用途 | 实测状态 |
|---|---|---|
| `segment_rule_configs` | 条件构建器**分群引擎**（field/operator/value → `segment_key`），已用于抽奖档位概率的"新老用户分层""VIP 分层" | 5 条规则，`is_system` 内置，活跃使用中 |
| `user_ad_tags` | 用户**标签**（`tag_key`/`tag_value`，凌晨 3 点定时计算） | 表已就位 |
| `user_growth_levels` | 用户**成长等级**（青铜/白银/黄金/钻石） | 已有阶梯 |
| `lottery_campaign_user_quota` + `quota_grants` | 按"用户×活动"定向发配额 | 已有定向发放机制 |

倍率规则的 target 直接引用上述 `segment_key` / `tag_key` / `growth_level_key`，"某些人"由现成引擎圈定，本方案不再自造人群表。

**对"活动 1/2/3"场景的落地示例**：

```text
活动1（全体可参加）        → 不配倍率规则，水晶正常 ×1
活动2（某些人可参加）      → 不改活动2本身；另配一条倍率规则，
                            target 引用 segment_key=vip_premium（或某标签/等级）→ 命中者抽中水晶 ×2
活动3（另一批人）          → 再配一条规则，引用另一 segment_key → ×3
用户同时命中活动2、活动3规则 → 取 max = ×3（不叠加）
```

倍率与抽奖活动彻底解耦：以后加活动、换人群，只增改倍率规则，**不动抽奖活动、不逐个调商品兑换条件**。

## 2. 核心设计原则

### 2.1 倍率作用位置（只作用一层）

倍率**只作用于**：抽中 `prize_type='material'` 且属于"水晶类"的奖品后，其 `material_amount` 发放数量的放大。

**不影响**：

- ❌ 不影响抽奖概率 / 选奖权重（`win_weight`），避免与抽奖引擎耦合。
- ❌ 不影响预算积分本身（`account_asset_balances` 预算余额）。
- ❌ 不影响非水晶类奖品（item/coupon/points，及非水晶 material）。

作用点唯一：`services/UnifiedLotteryEngine/pipeline/stages/SettleStage.js` 的 `material` 发放分支（现约 710–716 行），在 `delta_amount: prize.material_amount` 之前乘以倍率。

### 2.2 同活动内多规则合并策略：取最高（Max），不叠加、不叠乘

> **同一抽奖活动内，某用户同时命中多条倍率规则时，取"最高倍率"，不叠乘、不叠加。**
> （不同抽奖活动之间由 §2.4 的活动隔离天然互不影响，此处合并只针对"同一活动内命中的多条规则"。）

| 策略 | 示例（命中 ×2/×3/×2） | 结果 | 是否采用 |
|---|---|---|---|
| **取最大 Max** | max(2,3,2) | ×3 | ✅ 采用 |
| 叠乘 | 2×3×2 | ×12 | ❌ 成本爆炸 |
| 叠加 | 1+1+2+1 | ×5 | ❌ 易被薅 |
| 优先级取一 | 按 priority 取某个 | 视配置 | ❌ 用户难理解 |

选择 Max 的理由：

1. **成本封顶**：同活动内无论命中多少规则，最多是当前最高倍率，预算不失控。
2. **用户易懂**：用户永远只看到"本次最高 ×N"，不用自己算。
3. **运营易解释**：一句"倍率不叠加，取最高"即可公示（同大厂优惠券"不可叠加，取最优"）。
4. **防薅羊毛**：杜绝在同一活动内故意凑齐多条规则把倍率叠到天上。

### 2.3 "水晶类"如何界定

复用现网 `material_asset_types` 已有维度，不新造分类：

- 按 `group_code` 命中（如 red/orange/yellow/green/blue/purple 等水晶色系），或
- 按 `form in ('shard','gem')`（碎片/宝石形态），或
- 按显式资产码白名单（倍率活动可配 `reward_scope` 精确到某资产码）。

**拍板默认**：**`form in ('shard','gem')` 且非货币类（排除 points/star_stone/budget_points）即视为水晶类，碎片(shard)与宝石(gem)均参与翻倍**；运营可用 `reward_scope` 收窄到指定色系或资产码。新增水晶形态资产自动纳入，免维护清单。

### 2.4 活动隔离：倍率规则绑定到具体抽奖活动（目标效果）

> 倍率规则通过 `lottery_campaign_id` **绑定到具体抽奖活动**，只在该活动内生效；不同活动的倍率**天然互不影响**。

目标效果（与用户诉求对齐）：

| 场景 | 行为 |
|---|---|
| 只有一个活动、未配倍率规则 | 任何人参加都是**初始中奖数量（×1）** |
| 运营给活动内某人/某人群配倍率 | 满足条件者（新用户/高等级/指定人）享 1.5/1.75/2/2.5 等运营设定倍率，不满足者仍 ×1 |
| 多个活动 | 每条规则绑定各自活动，**互不影响**；用户在活动1 是 ×2、在活动2 可能是 ×1.5 或 ×1，各算各的 |
| 同一活动内某用户命中多条规则 | 取最高（Max），不叠加 |

举例（抽中 10 个红源晶碎片）：

```text
活动1：配"高等级会员 ×2"规则
  - 高等级会员小王抽中 10 个 → 发 20 个
  - 普通用户小李抽中 10 个 → 发 10 个（不满足条件，×1）
活动2：配"新用户 ×1.5"规则（与活动1 无关）
  - 新用户小王在活动2 抽中 10 个 → 发 15 个
  - 小王在活动1 的 ×2 不会带到活动2
活动1 同时又配了"新用户 ×1.75"：
  - 既是高等级又是新用户的小王在活动1 → max(2, 1.75)=×2 → 发 20 个
```

> 说明：`multiplier` 支持小数（1.50/1.75/2.50…），非整数结果按 `rounding_mode`（**已拍板默认向上 ceil**）取整，如 10×1.75=17.5→18、10×1.55=15.5→16。

## 3. 数据模型设计

新增两张表（主表 + 作用对象表），全部遵循现网规范：`utf8mb4_unicode_ci`、BIGINT 主键、`created_at/updated_at` 北京时间、Sequelize 迁移事务化。
作用对象表**不存人群定义**，只存"引用哪个现网人群标识"（segment_key / tag / growth_level），人群本身由 `segment_rule_configs` / `user_ad_tags` / `user_growth_levels` 负责。

### 3.1 倍率活动主表 `reward_multiplier_campaigns`

```sql
CREATE TABLE reward_multiplier_campaigns (
  multiplier_campaign_id BIGINT NOT NULL AUTO_INCREMENT COMMENT '倍率规则主键',
  lottery_campaign_id INT NOT NULL COMMENT '绑定的抽奖活动(FK→lottery_campaigns.lottery_campaign_id);该规则只在此活动内生效;强制必填,禁止全局规则(拍板:活动隔离)',
  campaign_name   VARCHAR(100) NOT NULL COMMENT '规则名(对内运营识别)',
  display_name    VARCHAR(100) NOT NULL COMMENT '对用户展示名(如"新春水晶翻倍")',
  multiplier      DECIMAL(4,2) NOT NULL COMMENT '倍率(支持小数,如1.50/1.75/2.00/2.50),>=1',
  reward_scope    ENUM('crystal_all','group','asset_codes') NOT NULL DEFAULT 'crystal_all'
                  COMMENT '作用奖品范围:crystal_all=全部水晶/group=按group_code/asset_codes=指定资产码',
  scope_values    JSON NULL COMMENT 'reward_scope=group时存group_code数组;=asset_codes时存asset_code数组;=crystal_all时NULL',
  target_type     ENUM('all','segment','tag','growth_level','user') NOT NULL DEFAULT 'all'
                  COMMENT '作用人群:all=全体/segment=分群(segment_rule_configs)/tag=标签(user_ad_tags)/growth_level=等级(user_growth_levels)/user=指定用户',
  rounding_mode   ENUM('round','floor','ceil') NOT NULL DEFAULT 'ceil'
                  COMMENT '小数倍率取整方式(拍板默认ceil向上,偏用户体感):ceil=向上(如10×1.75=17.5→18,10×1.55=15.5→16)/round=四舍五入/floor=向下',
  stack_strategy  ENUM('max') NOT NULL DEFAULT 'max' COMMENT '同活动内多规则命中合并策略(当前全局仅max,预留枚举)',
  priority        INT NOT NULL DEFAULT 0 COMMENT '优先级(并列同倍率时决胜与展示排序,越大越优先)',
  max_multiplier_cap DECIMAL(4,2) NOT NULL DEFAULT 3.00 COMMENT '倍率硬上限(拍板默认3.00,覆盖2.5需求且留余量;防误填,发放时二次夹紧)',
  extra_cost_limit   BIGINT NOT NULL COMMENT '因翻倍额外送出水晶的成本上限(按material_asset_types.budget_value_points折算累计;非用户账户,是本规则的成本刹车;拍板强制必填,达上限自动停翻回落×1)',
  extra_cost_used    BIGINT NOT NULL DEFAULT 0 COMMENT '本规则已累计的额外翻倍成本(实时累加,达 extra_cost_limit 自动停翻)',
  per_user_daily_limit  INT NULL COMMENT 'per-user 每日最多享受翻倍次数(拍板:运营可调,NULL=不限);防单人长期薅,复用 lottery_campaign_user_quota 计次范式',
  eligibility_days      INT NULL COMMENT '资格时间盒:仅在"进入命中人群后N天内"享翻倍(如新用户注册后30天),NULL=不限;过期实时判定自动失效',
  per_user_extra_cap    INT NULL COMMENT 'per-user 累计翻倍额外发放数量上限(单人最多多拿N个水晶),NULL=不限',
  start_at        DATETIME NULL COMMENT '生效开始(北京时间),NULL=不限',
  end_at          DATETIME NULL COMMENT '生效结束(北京时间),NULL=不限',
  status          ENUM('active','inactive') NOT NULL DEFAULT 'inactive' COMMENT '开关:active生效/inactive停用',
  remark          VARCHAR(500) NULL COMMENT '备注',
  created_at      DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (multiplier_campaign_id),
  INDEX idx_rmc_scope_status (lottery_campaign_id, status, start_at, end_at),
  INDEX idx_rmc_target (target_type),
  CONSTRAINT fk_rmc_lottery_campaign FOREIGN KEY (lottery_campaign_id)
    REFERENCES lottery_campaigns(lottery_campaign_id) ON DELETE CASCADE
) COMMENT='水晶奖品倍率规则(可绑定到具体抽奖活动,活动间隔离)';
```

- **活动隔离（关键）**：`lottery_campaign_id` 把规则绑定到某个抽奖活动，该规则**只在此活动内生效**。用户 A 在活动1 配 ×2，只影响活动1 抽中的水晶，活动2/活动3 完全不受影响。**强制必填（已拍板），禁止全局规则**，杜绝"一条规则到处翻倍"的误伤。
- **倍率支持小数**：`1.50/1.75/2.00/2.50` 等运营任意设置；小数结果按 `rounding_mode` 取整（**已拍板默认向上 ceil，偏体感**）。
- **`lottery_campaign_id` 为 INT**：对齐现网 `lottery_campaigns.lottery_campaign_id`（实测为 INT，非 BIGINT）。
- **per-user 三重护栏（拍板·常驻翻倍防长期薅，均运营可调、NULL=不限）**：`per_user_daily_limit`（每日翻倍次数）、`eligibility_days`（资格时间盒，如新用户注册后 N 天）、`per_user_extra_cap`（单人累计额外数量上限）。配合"人群实时判定（掉出人群即失效）"与规则级 `extra_cost_limit`，形成"实时资格 + 单人次数/时长/数量 + 规则总成本"多层限制。

### 3.2 倍率活动作用对象表 `reward_multiplier_targets`（引用现网人群，不自造人群）

本表只登记"引用哪个现网人群标识"，人群定义由 `segment_rule_configs` / `user_ad_tags` / `user_growth_levels` 负责，本方案零重复造轮子。

```sql
CREATE TABLE reward_multiplier_targets (
  id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  multiplier_campaign_id BIGINT NOT NULL COMMENT '所属倍率活动',
  target_type ENUM('segment','tag','growth_level','user') NOT NULL COMMENT '对象类型',
  target_ref  VARCHAR(100) NOT NULL
    COMMENT '引用现网人群标识:segment→segment_rule_configs.rules[].segment_key / tag→user_ad_tags.tag_key / growth_level→user_growth_levels.level_key / user→users.user_id',
  target_value VARCHAR(100) NULL
    COMMENT '可选精确值:target_type=tag时匹配 user_ad_tags.tag_value(如 true);其它类型可空',
  created_at  DATETIME NOT NULL, updated_at DATETIME NOT NULL,
  PRIMARY KEY (id),
  UNIQUE INDEX uk_rmt (multiplier_campaign_id, target_type, target_ref),
  INDEX idx_rmt_ref (target_type, target_ref),
  CONSTRAINT fk_rmt_campaign FOREIGN KEY (multiplier_campaign_id)
    REFERENCES reward_multiplier_campaigns(multiplier_campaign_id) ON DELETE CASCADE
) COMMENT='倍率活动作用对象(仅引用现网人群标识);target_type=all时本表无记录';
```

- `target_type='all'`（全体生效）时本表无记录，主表 `target_type=all` 即可。
- 其它类型在本表登记引用，支持一个倍率活动绑定多个人群标识：
  - `segment`：`target_ref` = `segment_rule_configs` 规则里的 `segment_key`（如 `vip_premium`、`new_user`）。命中判定复用现网分群引擎对该用户求值。
  - `tag`：`target_ref` = `user_ad_tags.tag_key`（如 `lottery_active_7d`），`target_value` 可选精确匹配 `tag_value`。
  - `growth_level`：`target_ref` = `user_growth_levels.level_key`（如 `gold`/`diamond`）。
  - `user`：`target_ref` = 具体 `user_id`（用于"某个人"精确定向）。
- **不建外键到人群表**（`target_ref` 是弱引用软标识，人群配置可独立增删版本），仅在读取时由服务层解析命中；引用失效（如 segment_key 被删）视为"无人命中"，安全降级为 ×1。

### 3.3 发放倍率快照（复用现有 `lottery_draws.result_metadata`）

**不新增列**，把倍率来源快照写入现网已有的 `lottery_draws.result_metadata`（JSON），防纠纷、可审计：

```json
{
  "crystal_multiplier": {
    "base_quantity": 10,
    "applied_multiplier": 3.00,
    "final_quantity": 30,
    "multiplier_campaign_id": 12,
    "reason": "新春水晶翻倍 ×3",
    "candidates": [
      { "multiplier_campaign_id": 12, "multiplier": 3.00 },
      { "multiplier_campaign_id": 8, "multiplier": 2.00 }
    ]
  }
}
```

同时 `asset_transactions.meta`（材料入账流水）追加 `crystal_multiplier` 摘要，保证资产侧也可对账追溯。

## 4. 抽奖发放计算逻辑

### 4.1 计算流程（伪代码）

```text
入口：SettleStage 处理 prize_type='material' 且命中水晶类的奖品
1. base_quantity = prize.material_amount
2. 判断 prize 是否水晶类（form in shard/gem 且非货币，或命中 reward_scope）
   - 非水晶类 → multiplier = 1，正常发放，不写快照
   - 是水晶类但 prize.is_fallback=1（兜底/保底奖）→ multiplier = 1，兜底不翻倍（拍板）
3. 查询「当前对该用户、本次抽奖活动生效」的倍率规则：
     status = 'active'
     AND lottery_campaign_id = 本次 draw.lottery_campaign_id  // ★活动隔离(强制绑定,无全局规则)
     AND (start_at IS NULL OR now >= start_at)
     AND (end_at   IS NULL OR now <= end_at)
     AND reward_scope 覆盖该奖品
     AND 命中人群（复用现网人群引擎解析）：
         target_type='all'
         OR target_type='segment'      且该用户经 segment_rule_configs 求值命中 target_ref(segment_key)
         OR target_type='tag'          且 user_ad_tags 存在 tag_key=target_ref (且 tag_value 命中 target_value)
         OR target_type='growth_level' 且该用户当前成长等级(user_growth_levels 派生) = target_ref
         OR target_type='user'         且 target_ref = 本人 user_id
     AND extra_cost_used < extra_cost_limit   // 该规则额外成本未击穿(强制必填,无NULL)
4. 收集所有命中规则的 multiplier（因已按活动过滤，这些都是"本活动内"的规则）
5. 合并（同活动内取 max）：applied = max(candidates.multiplier, 1)
6. 二次夹紧：applied = min(applied, 命中规则的 max_multiplier_cap 最小值)   // 防误填
7. 取整（按 rounding_mode，已拍板默认 ceil 向上）：final_quantity = ceil(base_quantity × applied)
      // 例：10×2=20；10×1.5=15；10×1.75=17.5→18(ceil)；10×1.55=15.5→16；10×2.5=25
8. extra_cost = (final_quantity - base_quantity) × 该资产 budget_value_points
9. 事务内：
     - 累加中选规则 extra_cost_used += extra_cost（行锁，达上限则该规则本次不参与→回退到次高倍率）
     - 用 final_quantity 入账（BalanceService.changeBalance）
     - 写 lottery_draws.result_metadata.crystal_multiplier 快照
     - asset_transactions.meta 追加倍率摘要
```

> **活动隔离说明**：一次抽奖必然属于且仅属于一个抽奖活动（`draw.lottery_campaign_id`），第 3 步已按此过滤，因此**不同抽奖活动的倍率规则天然互不影响**；`max` 合并只发生在"同一活动内、同一用户命中的多条规则"之间（如该活动同时配了"新用户 ×1.5"和"高等级 ×2"，用户两者都命中则取 ×2）。

### 4.2 代码落点（现网真实文件）

| 步骤 | 落点 |
|---|---|
| 人群命中判定 | **复用现网分群/标签/等级**：分群走 `segment_rule_configs` 现有求值逻辑（与抽奖档位概率同源）、标签查 `user_ad_tags`、等级查 `user_growth_levels` 派生；不新写人群引擎 |
| 倍率查询与合并 | 新增 `services/lottery/CrystalMultiplierService.js`（读倍率规则 + 调人群判定 + max 合并 + cap 夹紧，纯计算，可单测） |
| 发放时应用倍率 | `services/UnifiedLotteryEngine/pipeline/stages/SettleStage.js` material 分支（约 710–716 行）：`delta_amount` 由 `prize.material_amount` 改为 `finalQuantity` |
| 快照写入 | 同 SettleStage，写 `lottery_draws.result_metadata` + `asset_transactions.meta` |
| 额外成本累加/击穿 | 事务内对 `reward_multiplier_campaigns.extra_cost_used` 行锁自增（复用现有 `TransactionManager`/分布式锁范式） |

> 关键：全程在**现有结算事务内**完成，复用现网幂等键（`${idempotency_key}:material`），杜绝重复发放翻倍奖品。

### 4.3 击穿回退（预算保护）

若最高倍率规则的额外成本已击穿（`extra_cost_used >= extra_cost_limit`），该规则本次不参与合并，自动回退到**次高**仍有额度的倍率；全部击穿则 ×1 正常发放。保证成本永不超上限。

## 5. 用户感知设计（避免多活动困惑）

这是本方案的核心，多个活动都能翻倍时用户最易困惑，靠"规则透明 + 展示唯一 + 结果可解释"解决。

### 5.1 全局公示一句话

抽奖页/活动页固定文案：

> **水晶奖品倍率活动不叠加，同一次抽奖按最高倍率计算。**

### 5.2 抽奖前：只显示一个"合并后倍率"

顶部只显示合并后的最终倍率，**不罗列多个活动让用户自己算**：

```
🎉 当前水晶奖品 ×3 倍（新春水晶翻倍）  有效期至 2 月 15 日
```

只给结果、不给过程。日常态（×1）不显示倍率，避免"×1"让用户觉得亏；仅在有加成时高亮。

### 5.3 抽中后：写清 `基础 × 倍率 = 最终`

```
恭喜获得 红源晶碎片 ×30
（基础 10 × 新春翻倍 3 倍）
```

数据来源即 `result_metadata.crystal_multiplier` 快照，用户一看就懂，客服也可据此答疑。

### 5.4 定向人群不做公开攀比

A 用户 ×3、B 用户 ×2（因人群不同）时，**各自只看到自己的倍率**：

- 定向活动 `display_name` 用中性词（如"专属加成"），不强调"因为你是 XX"。
- 用户只在自己的抽奖页看到自己的倍率，看不到他人倍率，杜绝攀比纠纷。

### 5.5 倍率回落有预告

活动结束前提示"活动即将结束"，避免用户从 ×3 突然回到 ×1 的落差感。

## 6. 运营可调控能力（后台配置，零改码）

| 运营需求 | 配置方式 |
|---|---|
| 规则只在某活动生效 | `lottery_campaign_id` = 该抽奖活动 ID（活动间隔离；留 NULL 才跨全部活动） |
| 某个人翻倍 | `target_type=user` + `reward_multiplier_targets` 绑定 user_id |
| 某个分群翻倍 | `target_type=segment` + 绑定 `segment_rule_configs` 的 `segment_key`（如 vip_premium/new_user，复用现网分群） |
| 某类标签人群翻倍 | `target_type=tag` + 绑定 `user_ad_tags.tag_key`（可选精确匹配 tag_value） |
| 某等级人群翻倍 | `target_type=growth_level` + 绑定 `user_growth_levels.level_key`（如 gold/diamond） |
| 该活动全体翻倍 | `target_type=all`（配合 `lottery_campaign_id` 限定活动） |
| 调整倍率(含小数) | 改 `multiplier`（1.50/1.75/2.00/2.50…） |
| 小数取整方式 | 改 `rounding_mode`（round/floor/ceil） |
| 限定奖品范围 | `reward_scope` + `scope_values`（全部水晶/某色系/某资产码） |
| 开关活动 | `status` active/inactive（应急可秒级下线） |
| 时间窗 | `start_at` / `end_at` |
| 成本封顶 | `extra_cost_limit`（额外成本达上限自动停翻） |
| 防误填 | `max_multiplier_cap`（发放时二次夹紧） |

## 7. 成本与风控

| 风险 | 对策 |
|---|---|
| 倍率放大真实价值奖品 → 成本失控 | 同活动内 Max 不叠乘 + `max_multiplier_cap` 硬上限 + `extra_cost_limit` 成本封顶 + 击穿回退 |
| 多活动被薅 | Max 策略天然防叠加；定向人群精确控制 |
| 重复发放 | 复用结算事务 + 幂等键 `${idempotency_key}:material` |
| 并发重复累加成本 | `extra_cost_used` 行锁自增（`SELECT ... FOR UPDATE`）或分布式锁 |
| 成品奖品不可翻倍 | 倍率只作用"可计数材料数量"(shard/gem)；成品(item)不参与，奖品定义层区分 |
| 审计追溯 | `lottery_draws.result_metadata` + `asset_transactions.meta` 双端快照 |

## 8. 实施步骤

1. **迁移**：新增 `reward_multiplier_campaigns` + `reward_multiplier_targets`（sequelize-cli 事务化 up/down，参照现网 migrations 风格）。
2. **模型**：新增 `models/RewardMultiplierCampaign.js` + `models/RewardMultiplierTarget.js`，在 `models/index.js` 注册。
3. **服务**：新增 `services/lottery/CrystalMultiplierService.js`：
   - `resolveMultiplier(userId, prize, now, transaction)` → 返回 `{ applied, campaignId, candidates }`（纯匹配 + max 合并 + cap 夹紧）。
   - 人群命中**复用现网**：分群调 `segment_rule_configs` 现有求值（与抽奖档位概率同源，避免两套分群逻辑）、标签查 `user_ad_tags`、等级用 `user_growth_levels` 派生；**不新写人群引擎**。
   - 单测覆盖：无命中→×1、单命中、多命中取 max、cap 夹紧、预算击穿回退、非水晶类跳过、segment/tag/growth_level/user 各命中分支。
4. **结算接入**：改 `SettleStage.js` material 分支，发放前调用 `CrystalMultiplierService`，用 `finalQuantity` 入账 + 累加预算 + 写快照。
5. **展示接口**：抽奖详情/活动页返回"当前生效合并倍率"（单值）；中奖结果返回 base/倍率/final。
6. **管理端**：新增倍率活动 CRUD 页（倍率、范围、人群、时间、开关、预算上限、成本看板）。
7. **校验/质量**：`npm run lint`、`TZ=Asia/Shanghai jest`（含 `CrystalMultiplierService` 单测与 SettleStage 受影响用例）、`npm run check:api-contract`。

## 9. 范围边界

- 本方案**只做"水晶类材料奖品发放数量倍率"**，不改抽奖概率、不改预算发放、不改非材料奖品。
- 倍率与抽奖引擎、预算系统**解耦**：仅在 SettleStage 发放材料的最后一步介入，改动面最小、最安全。
- 与既有 `lottery_campaigns.participation_conditions`（参与条件）互不冲突：参与条件决定"能不能抽"，本方案决定"抽中水晶发多少"，两层正交。

## 10. 关键决策记录

| 决策点 | 结论 | 依据 |
|---|---|---|
| 倍率作用层 | 只作用抽中水晶材料的发放数量 | 与抽奖概率/预算解耦，改动最小最安全 |
| 活动隔离 | 倍率规则 `lottery_campaign_id` **强制必填**，只在该活动生效，活动间互不影响 | 精确匹配"用户A在活动1的倍率不带到活动2"；禁止全局规则防误伤 |
| 同活动多规则合并 | 同一活动内取最高(Max)，不叠乘不叠加 | 成本封顶、用户易懂、防薅羊毛、大厂通行 |
| 小数倍率取整 | 支持小数倍率(1.5/1.75/2.5)，**默认向上取整(ceil，偏体感)** | 满足运营任意倍率设定；有小数一律进位，用户体感更好 |
| 水晶界定 | 复用 `material_asset_types`（form=shard/gem 或 reward_scope） | 不新造分类，贴合现网资产体系 |
| 倍率快照 | 复用 `lottery_draws.result_metadata` + `asset_transactions.meta` | 零新增列，双端可对账追溯 |
| 成本保护 | Max + 倍率硬上限 + 预算上限 + 击穿回退 | 翻倍直接放大真实成本，必须封顶 |
| 用户展示 | 抽前只显示合并后单一倍率，抽后显示 base×倍率=final | 杜绝多活动困惑与攀比纠纷 |
| 作用目标（翻什么） | 水晶奖品数量(output)，非预算积分(input) | 预算翻倍会放大全部产出且成本不可控，不符"只翻水晶"目标 |
| 作用对象建模 | 独立倍率规则实体，不绑用户属性、不绑抽奖活动 | 可时间盒/开关/审计/多规则 Max 合并，与抽奖/主数据解耦 |
| 人群定向来源 | **复用现网** `segment_rule_configs`(主)/`user_ad_tags`/`user_growth_levels`/指定人(辅)，不自造人群表 | 现网已有成熟分群引擎(抽奖分层在用)，零重复造轮子、维护成本最低 |
| 水晶界定 | `form in ('shard','gem')` 自动判定 + `reward_scope` 可收窄；**碎片与宝石均翻** | 免维护资产清单，新增水晶自动纳入 |
| 倍率硬上限 | `max_multiplier_cap` 默认 **3.00** | 覆盖 2.5 需求且留余量，防误填击穿 |
| 成本封顶 | `extra_cost_limit` **强制必填**，达上限自动停翻回落 ×1 | 翻倍放大真实成本，每条规则必须有天花板 |
| 兜底翻倍 | 保底/兜底(is_fallback)**不翻倍** | 保底是补偿非福利，游戏通行做法，省成本 |
| 幂等并发 | 复用结算事务 + 幂等键 + 行锁扣预算 | 防重复发放与并发扣预算 |

## 11. 拍板记录（2026-07-05 全部定稿）

全部 9 项已拍板，无遗留待定项，可直接进入 §8 施工：

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 作用目标 | **翻水晶奖品数量(output)**，非预算积分 |
| 2 | 人群来源 | **以 `segment_rule_configs` 分群为主**，标签/等级/指定人为辅 |
| 3 | 水晶界定 | **按 `form in ('shard','gem')` 自动判定** + `reward_scope` 可收窄 |
| 4 | 碎片/宝石 | **碎片与宝石均翻倍**（不排除 gem） |
| 5 | 倍率硬上限 | `max_multiplier_cap` **默认 3.00** |
| 6 | 成本封顶 | `extra_cost_limit` **强制必填(NOT NULL)**，每条规则必设额外成本天花板 |
| 7 | 兜底翻倍 | **兜底/保底(is_fallback)不翻倍** |
| 8 | 活动绑定 | `lottery_campaign_id` **强制必填**，禁止全局规则（活动隔离） |
| 9 | 小数取整 | **向上取整(ceil，偏体感)** |

## 12. 防囤积套利：活动专属预算 + 到期清零/折算（2026-07-05 追加）

### 12.1 问题：翻倍活动的"囤积套利"

用户平时消费攒预算积分不抽，专等翻倍活动一次性抽走，等于用平时低成本预算套翻倍高价值奖品，平台资损。

**根因**：预算积分"赚"与"用"之间无活动隔离——翻倍活动若接受平时囤的全局预算，即被套利。

### 12.2 现网已有的钩子（实测确认，无需大改）

| 现网结构 | 现状 | 用途 |
|---|---|---|
| `services/consumption/CoreService.js` 预算发放 | 消费审核通过发 `BUDGET_POINTS`，打标 `lottery_campaign_id`（当前硬编码 `'CONSUMPTION_DEFAULT'` 全局桶） | 预算积分本就按活动分桶存储 |
| `account_asset_balances.lottery_campaign_id` / `lottery_campaign_key` | 预算积分按活动隔离存储 | 天然支持活动专属预算 |
| `lottery_campaigns.allowed_campaign_ids` (JSON) | 抽奖活动可指定"接受哪些来源活动的预算" | 决定翻倍活动吃不吃全局囤积预算 |
| `lottery_campaign_user_quota.expires_at` + status `expired` | 配额已支持到期 | 到期清零/折算范式可复用 |
| `lottery_campaign_user_quota` / `max_draws_per_user_*` | 每用户配额/次数上限 | 兜底护栏 |

### 12.3 方案：三层防护

**第 1 层（根治）活动专属预算，翻倍活动不吃全局囤积预算**

- 翻倍活动期间，消费审核发的预算积分**额外打标该活动的 `lottery_campaign_id`**（改 `CoreService` 那行硬编码 `CONSUMPTION_DEFAULT` 为"命中活动专属累积配置时用活动桶"）。
- 翻倍活动 `allowed_campaign_ids` **只允许本活动专属预算桶**，排除 `CONSUMPTION_DEFAULT`。
- 效果：享受翻倍必须活动期间消费赚活动专属预算；平时囤的全局预算进不了翻倍场 → 套利失效（游戏"活动限定代币"模型）。

**第 2 层（防长期囤积）活动专属预算到期清零/折算**

- 活动专属预算带 `expires_at`（复用 `lottery_campaign_user_quota.expires_at` 范式）。
- 活动结束由定时任务处理：**直接清零（已拍板 防2，不折算）**，与游戏活动代币一致。

**第 3 层（兜底护栏，已在本方案内）**

- 翻倍活动 per-user 抽奖次数/额度上限（`lottery_campaign_user_quota` / `max_draws_per_user_*`）。
- 翻倍规则 `extra_cost_limit` 成本封顶（§10 已定稿）——即便钻空子，平台总成本封死。

### 12.4 行业对照

| 主体 | 做法 | 归类 |
|---|---|---|
| 游戏公司(米哈游/腾讯/网易) | 活动限定代币，活动内赚活动内用、结束清零 | 活动专属货币(最强) |
| 大电商(阿里/京东) | 积分年底清零 + 膨胀券时效 + 抵扣上限 | 到期清零 + 时效 + 封顶 |
| 航空里程 | 里程有效期过期作废 | 到期清零 |
| 美团/本地生活 | 活动券短有效期 + 活动积分活动内有效 | 时效 + 活动隔离 |
| 活动策划公司 | 活动积分周期内有效，结束清零/折算，预算锁周期 | 活动专属 + 到期折算 |
| 小公司 | 无隔离无到期 → 被薅穿 | 反面教材 |
| 虚拟物品/二手平台 | 赛季/活动货币季末重置 | 活动专属 |

**共识**：成熟玩家均为「活动专属货币 + 时间盒 + 到期清零/折算」。本方案第 1+2 层与之一致，第 3 层为额外兜底。

### 12.5 落地点（现网真实文件）

| 步骤 | 落点 |
|---|---|
| 活动专属预算发放 | `services/consumption/CoreService.js` 预算分配处：命中"活动专属累积配置"时，`lottery_campaign_id` 用活动桶 + 写 `expires_at`（否则维持 `CONSUMPTION_DEFAULT`） |
| 翻倍活动只吃专属预算 | 配置 `lottery_campaigns.allowed_campaign_ids` = [活动专属桶]，排除 `CONSUMPTION_DEFAULT` |
| 到期清零 | 新增定时任务（参照现网 `jobs/` 范式），活动 `end_time` 到期时清零该活动的专属预算 + `event_points`（已拍板直接清零，不折算） |
| 兜底护栏 | 复用 `lottery_campaign_user_quota` / `max_draws_per_user_*` + 本方案 `extra_cost_limit` |

### 12.6 两类翻倍活动的区分（常驻翻倍 vs 限时翻倍）

现实会同时存在两类翻倍活动，二者**是否清零由"翻倍是否稀缺限时"决定**，不是拍脑袋：

- **全局长期翻倍（如活动1）**：翻倍长期有效 → 用户随时抽都翻倍、囤积无额外收益 → **无囤积套利动机** → 用全局永久预算 `CONSUMPTION_DEFAULT`，**不清零**。典型：新用户/高等级常驻加成。
- **短期稀缺翻倍（如活动2）**：翻倍限时 → 有"平时囤、翻倍日套现"动机 → **必须活动专属预算 + 到期清零**（§12.3 第 1、2 层）。

用同一套机制、靠配置区分，无需两套代码：

| 维度 | 活动1 全局长期翻倍 | 活动2 短期稀缺翻倍 |
|---|---|---|
| `lottery_campaign_id` | 活动1 | 活动2 |
| 预算桶 | `CONSUMPTION_DEFAULT`（全局永久） | 活动2 专属桶 |
| 预算来源 | 平时消费常规归集 | 活动2 期间消费归集 / 活动任务 |
| `allowed_campaign_ids` | `[CONSUMPTION_DEFAULT]` | `[活动2专属桶]`（排除全局） |
| 到期 | 永不清零 | 活动 `end_time` 触发直接清零 |
| 倍率规则 | 绑活动1，target=新用户/高等级 | 绑活动2，target=符合条件用户 |
| 套利风险 | 无（常驻翻倍无囤积动机） | 有 → 专属预算+清零根治 |

两个关键设计点（降低耦合与技术债）：

1. **"到期清零"是预算桶级属性，不是倍率级属性**：倍率逻辑单一职责（只管发奖翻倍），预算生命周期由"桶 + 归集 + 清理任务"管理，两块解耦。
2. **到期用活动 `lottery_campaigns.end_time` 驱动，不给每条预算加 `expires_at`**：活动2 结束时清理任务扫其专属桶余额直接清零；活动1 无 end_time（常驻）则桶永不清理。"短期清零、长期不清零"是桶级配置的自然结果。

### 12.7 双层货币模型：可见积分也要分（防套利要"看得见"）

**实测确认的关键机制**：抽一次奖，扣的是 **`AssetCode.POINTS`（可见积分）**（`SettleStage.js` 第 206 行 `delta_amount: -draw_cost`）；**预算积分是后台的"奖品价值门槛 + 成本控制"，用户看不见**。

因此：若防套利只在"看不见的预算积分"层做，会出现**用户看到自己有一堆可见积分、却在活动2 抽不了/抽不到好东西**的困惑（后台预算桶不匹配，用户无法理解）。所以**可见积分层也要区分**，且要让用户看得懂。

**双层货币模型**：

| 层 | 货币 | 用户可见 | 作用 | 分桶策略 |
|---|---|---|---|---|
| **可见层（入场）** | 可见积分 `points` | ✅ 看得见 | 决定"能在哪个活动抽" | 全局可见积分（常驻）+ 活动专属可见积分/代币（限时，到期清零） |
| **隐藏层（成本）** | 预算积分 `budget_points` | ❌ 看不见 | 决定"能抽出多大价值 + 平台成本封顶" | 已分桶（§12.3），按活动隔离 |

可见层怎么分：
- **全局可见积分**：平时消费获得，供常驻活动（活动1），不清零。
- **活动专属可见积分/代币**：短期翻倍活动2 期间获得，**只能在活动2 抽**，活动结束清零 —— 等价游戏"活动限定代币"（可见、限时、到期清空），用户天然理解，无"有积分却抽不了"的困惑。

**拍板实现（方式 A：新增独立"活动代币"资产类型）**：

- **新增一个通用资产类型** `event_points`（`material_asset_types` 加一条），与全局 `points` 是两种不同的钱；全局 `points` 语义/代码**完全不动**。
- **按 `lottery_campaign_id` 分桶**：一种 `event_points` 服务所有活动，每个活动的余额独立（同预算积分分桶范式），不为每个活动新造货币类型，避免货币泛滥。
- **展示名运营每活动可配**：系统名 `event_points`，展示名如"新春活动积分""周年庆代币"（同倍率规则 display_name 套路），默认"活动积分"。
- **仅在活动场景露出**：主钱包用户只见"积分"；进入该活动才显示"本活动积分：N，可在本活动抽奖 + 到期倒计时"。等价游戏活动代币（活动外不可见），把用户认知负担降到最低。
- **到期清零**：活动 `end_time` 触发清理任务清零该活动的 `event_points`（与隐藏层预算桶同一任务处理）。

> 为何不选"给全局 `points` 分桶"（方式 B）：`points` 用途广、改动影响面大；且"同名货币不同可用范围"会让用户困惑（"我有 120 积分为何活动 2 只能用 20"）。新增独立 `event_points` 让用户从第一天就建立"活动积分分活动、限时、会清零"的认知，不违背既有习惯。

设计原则：**可见层（`event_points`）管"能不能进这个活动抽"，隐藏层（`budget_points` 分桶）管"抽出多大价值 + 平台成本"**，两层各司其职，共同根治套利。

### 12.8 拍板记录（2026-07-05 全部定稿）

| # | 决策点 | 结论 |
|---|---|---|
| 防1 | 限时翻倍活动预算来源 | **严格只吃活动专属预算/代币**，排除全局 `CONSUMPTION_DEFAULT` |
| 防2 | 限时活动到期处理 | **直接清零**（不折算），同游戏活动代币 |
| 防3 | 全局 `CONSUMPTION_DEFAULT` 是否定期清零 | **永不清零**，仅靠活动隔离防套利（重用户信任） |
| 防4 | 常驻翻倍（活动1）预算 | 用全局永久预算**不清零**，但加 per-user 三重护栏（见 防5） |
| 防5 | 常驻翻倍防长期薅 | **三重护栏全做且运营可调**：`per_user_daily_limit`（每日次数）+ `eligibility_days`（资格时间盒）+ `per_user_extra_cap`（单人累计额外量），配合实时人群判定 + `extra_cost_limit` |
| 防6 | 可见活动代币实现 | **方式 A：新增独立 `event_points` 资产类型**，按 `lottery_campaign_id` 分桶、每活动可配展示名、仅活动内露出、到期清零 |
| 防7 | 活动期间预算归集 | **全额重定向**进"个人活动预算账户"，不再进全局 `CONSUMPTION_DEFAULT`（模型 B 全量重定向） |
| 防8 | 活动内双账户 + 抽取顺序 | **个人账户 + 活动池账户**双轨，`preset_budget_policy=pool_first`（先扣池、池空回退个人） |
| 防9 | 预算入口是否加人工选择口 | **不加**，由后端"活动预算归集规则"自动判定；运营在 Web 后台配规则，小程序/商家端不做选择（见 §12.10） |

至此 §12 防囤积套利全部定稿，与 §11（倍率规则 9 项）共同构成完整方案，可进入施工。

### 12.9 活动预算账户模型（个人账户 + 池账户，复用现网预算架构）

用户诉求"个人活动账户 vs 全局(池)活动账户区分、池空回退个人、按活动隔离"——**实测确认 1:1 命中现网 `lottery_campaigns` 已有预算字段，零新增账户体系**。

| 概念 | 现网真实载体 | 说明 |
|---|---|---|
| 个人活动预算账户 | `account_asset_balances`（`asset=BUDGET_POINTS` + `lottery_campaign_id=活动ID`） | 每"用户×活动"一行，个人预算，按活动隔离 |
| 全局(池)活动预算账户 | `lottery_campaigns.pool_budget_total` / `pool_budget_remaining` | 运营注资的活动池，实时扣减 |
| 预算模式 | `lottery_campaigns.budget_mode`（user/pool/none） | 个人扣 / 池扣 / 不限 |
| 抽取顺序（池空回退个人） | `lottery_campaigns.preset_budget_policy = 'pool_first'` | 先扣池（运营补贴）→ 池空自动用个人账户 |
| 只吃本活动个人预算 | `lottery_campaigns.allowed_campaign_ids = [本活动]` | 排除其他来源，实现活动隔离 |
| 活动间隔离 | 全部按 `lottery_campaign_id` 分桶 | 活动1/活动2/常驻 天然互不干扰，与常驻活动同机制 |

归集与抽取流程：

```
活动期间消费审核通过：
  预算积分全额 → 该用户在该活动的【个人活动预算账户】(BUDGET_POINTS, lottery_campaign_id=活动ID)
  (不再进全局 CONSUMPTION_DEFAULT —— 防7 全量重定向)

活动内抽奖扣预算(pool_first)：
  先扣【活动池账户】pool_budget_remaining (运营补贴)
  池 = 0 → 回退扣用户【个人活动预算账户】(自己攒的)
  两账户均按 lottery_campaign_id 隔离，与常驻活动1、其他活动互不影响
```

设计要点：

- **与常驻活动一致的隔离**：常驻活动1 用全局永久桶（`CONSUMPTION_DEFAULT` + 其自身池），限时活动2 用活动专属桶（个人 + 池），全部 `lottery_campaign_id` 隔离，一套机制、配置区分。
- **池账户 = 运营补贴，个人账户 = 用户自攒**：`pool_first` 让活动初期用运营池给足体感，池耗尽后回落到用户自有预算，成本与体感平衡。
- **到期清零**（防2）作用于活动的个人账户 + `event_points`；活动池余额到期一并处理（清零或回收，运营配）。

### 12.10 预算归集入口设计（不加人工选择口，后端规则自动判定）

现网消费提交由**商家人员/管理员扫用户码提交审核**（非用户操作）。预算进"全局还是活动"**不做人工选择口**，改由后端归集规则自动判定：

**为何不加人工选择口**：一线手动选 = 舞弊/选错风险 + 培训与客诉成本 + 泄露内部机制 + 违背"规则化配置"设计哲学。

**归集规则（运营在 Web 后台配置）**声明：关联活动 `lottery_campaign_id`、生效时间窗（对齐活动 `end_time`）、命中条件（门店/商家/消费类型/用户人群，复用现网分群与门店维度）、归集去向（防7：全额进活动个人账户）。

**各端职责**：

| 端 | 是否加选择口 | 职责 |
|---|---|---|
| 用户微信小程序 | ❌ 不加 | 仅出示二维码；可选展示"本次获得 X 活动积分" |
| 商家端（提交消费） | ❌ 不加 | 照常提交消费；可选展示"当前有 XX 活动进行中" |
| Web 管理后台 | ✅ 在此设计 | 配置"活动预算归集规则"（时间/条件/去向），真正的入口设计在这里 |

**落地点**：`services/consumption/CoreService.js` 预算分配处，现硬编码 `lottery_campaign_id: 'CONSUMPTION_DEFAULT'` → 改为"先查命中的活动归集规则，命中则全额投该活动个人账户（`BUDGET_POINTS` + 活动ID）+ 发 `event_points`；未命中维持全局 `CONSUMPTION_DEFAULT`"。全自动，一线与用户零选择。

## 13. Web 管理端配套功能（Vite + Alpine.js + Tailwind + ECharts）

> 实测：admin 技术栈为 Vite + Alpine.js + Tailwind + **ECharts 6.0**（图表现成），已有 `lottery` 模块与 `lottery-management/lottery-alerts/dashboard-panel/statistics/finance-management/reconciliation/risk-alerts/user-data-query` 等页。下述功能全部复用现网页面/模块与图表库，零新增前端技术栈。后端先行、前端后接。

### 13.1 核心管理（必备 CRUD）

| 功能 | 落地位置 | 说明 |
|---|---|---|
| 倍率规则管理（CRUD） | `admin/src/modules/lottery` 新增页（仿 `lottery-management`） | 绑活动、倍率、人群、`reward_scope`、`rounding_mode`、`max_multiplier_cap`、`extra_cost_limit`(必填)、per-user 三护栏、时间窗、开关 |
| 人群选择器 | 规则表单内组件 | 下拉复用 `segment_rule_configs`(segment_key)/`user_ad_tags`(tag)/`user_growth_levels`(level)/指定 user_id |
| 活动预算归集规则配置 | `lottery-management` 扩展或新页 | 配活动、时间窗、命中条件（门店/商家/人群）、去向（全额进个人活动账户） |
| 活动代币(`event_points`)配置 | 规则/活动表单内 | 每活动展示名、到期清零策略 |
| 活动预算账户配置 | `lottery-management` 扩展 | `budget_mode`、活动池注资 `pool_budget`、`preset_budget_policy=pool_first` |

### 13.2 监控看板（增强，用 ECharts）

| 看板 | 落地位置 | 关键指标 |
|---|---|---|
| 倍率成本监控 | `lottery-alerts` / `dashboard-panel` | 每规则 `extra_cost_used/limit` 水位、击穿告警、翻倍触发次数、受益人数、多发水晶总量、按日趋势 |
| 活动预算池看板 | `dashboard-panel` / `finance-management` | 池 `pool_budget_remaining/total` 水位、耗尽预警、`pool_first` 抽取来源分布（池 vs 个人） |
| 活动代币看板 | `statistics` | `event_points` 发放/消耗/在途余额/到期清零量 |
| 防套利监控 | `risk-alerts` / `lottery-alerts` | 个人活动账户余额分布（识别异常高余额囤积）、到期清零执行记录、per-user 护栏触达统计 |

### 13.3 客服/查询（必备）

| 功能 | 落地位置 | 说明 |
|---|---|---|
| 用户维度查询 | `user-data-query` | 查某用户各活动的倍率、活动预算账户、活动代币余额、翻倍历史；**"为什么翻/没翻"解释**（读 `lottery_draws.result_metadata` 快照） |

### 13.4 运维/对账

| 功能 | 落地位置 | 说明 |
|---|---|---|
| 到期清零 job 运维视图 | `feature-flags` / 系统运维页 | 执行状态、下次执行、历史清零量 |
| 翻倍成本 + 代币对账 | `reconciliation` | 翻倍额外成本、代币发放/消耗纳入对账（`asset_transactions` 流水已具备） |

### 13.5 实现顺序

后端（倍率规则表 + `event_points` + 归集路由 + `SettleStage` 接入 + 到期 job + 查询/统计 API）完成并自测通过后，再实现 admin 前端（规则管理页 → 监控看板 → 客服查询 → 运维视图）。前端依赖后端接口，避免返工。

---

## 14. 求证结论（2026-07-05 实连 `restaurant_points_dev` 真实库 + 通读现网代码）

> 本节是以后端数据库项目为唯一权威的实测校准。求证方法：`mysql2/promise` 直连 `.env` 中 `DB_HOST=test-db-12-mysql.ns-br0za7uc.svc / DB_NAME=restaurant_points_dev`，逐表 `SHOW FULL COLUMNS` + 真实数据抽样；逐文件通读 `services/UnifiedLotteryEngine/pipeline/stages/SettleStage.js`、`TierPickStage.js`、`config/segment_rules.js`、`config/segment_field_registry.js`、`services/SegmentRuleService.js`、`services/user/UserSegmentService.js`、`services/lottery/UserGrowthLevelService.js`、`services/consumption/CoreService.js`、`utils/ApiResponse.js`。

### 14.1 方案假设 vs 真实库/代码（逐条核对）

| # | 方案原假设 | 真实情况（实测） | 结论 |
|---|---|---|---|
| A1 | 抽奖扣 `AssetCode.POINTS`（可见积分） | ✅ `SettleStage.js:206` `asset_code: AssetCode.POINTS, delta_amount: -draw_cost` | 成立 |
| A2 | 材料发奖在 `SettleStage` material 分支，约 710–716 行 | ✅ 实测 `SettleStage.js:709` `case 'material':`，`712` `BalanceService.changeBalance({ asset_code: prize.material_asset_code, delta_amount: prize.material_amount, idempotency_key: '${idempotency_key}:material' })` | 成立，行号精确 |
| A3 | `material_asset_types` 有 `form(shard/gem/currency/quota)`、`group_code`、`budget_value_points` | ✅ 全部存在，16 条数据（6 shard + 6 gem + 2 currency + 2 quota） | 成立 |
| A4 | 水晶用 `form in ('shard','gem')` 自动判定 | ⚠️ 可行；原发现 `red_core_gem=0`（漏填），**已按 D-9 订正为 10**（§22.8），订正后全色系 gem=shard×10，成本折算按真实 `budget_value_points` 累加即统一有效 | 成立（成本折算见 §22.8） |
| A5 | 资产码用 `red_core_shard`/`red_core_gem` | ✅ 与 `constants/AssetCode.js` 完全一致（`RED_CORE_SHARD='red_core_shard'` 等） | 成立 |
| A6 | 预算/积分资产码是 `BUDGET_POINTS`/`POINTS`（大写） | ❌ **真实 asset_code 是小写 `budget_points`/`points`**；大写只是 `AssetCode` 常量的键名。`account_asset_balances` 实测 `asset_code='budget_points'`/`'points'` | **需修正**：文档/前端一律用小写 `points`/`budget_points` |
| A7 | 人群 `segment_rule_configs` 有 `segment_key` 列，直接引用 | ❌ **`segment_key` 不是列**，它藏在 `rules`(JSON) 数组的每个规则对象里；表主键是 `version_key`(如 default/v1/v2)。运行时解析走 `config/segment_rules.js` 的 `SegmentResolver.resolveSegmentAsync(version, user)` | **需重构人群模型**（见 §14.2） |
| A8 | 存在"运行时按 user_id 求 segment_key"的通用引擎可复用 | ⚠️ 部分。`SegmentResolver` 能算，但**入参是 user 对象且只支持 5 个白名单字段**（`created_at`/`last_active_at`/`history_total_points`/`user_level`/`consecutive_fail_count`，见 `segment_field_registry.js`）；且**分群版本是"每活动一个 resolver_version"**（`lottery_strategy_config` 实测 `config_group='segment',config_key='resolver_version',config_value='v1',lottery_campaign_id=1`），并非"一个活动挂多个 segment_key" | 可复用但**语义要改**（见 §14.2） |
| A9 | `user_ad_tags` 表就位、可按 tag 圈人 | ⚠️ 表在（`tag_key`/`tag_value`/`calculated_at`），但**实测 0 行数据**（`user_ad_tags` 无任何标签），且方案里"凌晨 3 点计算"的任务需确认是否真正产出。作为 target 来源属"可用但当前空跑" | 谨慎：上线前需先有标签生产 |
| A10 | `user_growth_levels` 有青铜/白银/黄金/钻石阶梯 | ✅ 实测 4 档：bronze(0)/silver(100000)/gold(500000)/diamond(2000000)；`level_key` 唯一。运行时派生走 `UserGrowthLevelService.resolveUserLevel(user_id)`→`UserGrowthLevel.resolveLevelKey(history_total_points)` | 成立，直接可用 |
| A11 | `lottery_campaigns.lottery_campaign_id` 是 INT | ✅ 实测 INT 主键；`lottery_campaign_prizes.lottery_campaign_id` 也是 INT。**倍率表外键必须 INT** | 成立 |
| A12 | `lottery_draws.result_metadata` 是 JSON、可写倍率快照 | ⚠️ **部分修正**：`result_metadata`(JSON) 存在✅、可写快照；但**实测 `lottery_draws` 无 `material_amount` 列**（列清单实测：…`prize_value`/`prize_value_points`/`budget_points_before/after`/`points_deducted`/`result_metadata`… 无 `material_amount`）。原 §1.1 "含 material_amount 展示字段" **不成立** | 快照可写✅；材料发放量只能落 `result_metadata`，不能写不存在的列（见 §22 C1） |
| A13 | `asset_transactions.meta` 是 JSON、可追加倍率摘要 | ✅ 存在 JSON `meta` 列 + `idempotency_key` UNIQUE + 双录(`counterpart_account_id`) | 成立 |
| A14 | `account_asset_balances.lottery_campaign_id` 分桶存预算 | ✅ 实测 `varchar(50)`，真实数据全是字符串桶 `'CONSUMPTION_DEFAULT'`。**注意它是 VARCHAR 不是 INT**，与 `lottery_campaigns.lottery_campaign_id`(INT) 类型不同，靠 `lottery_campaign_key` 统一 | 成立但**桶标识是字符串**，活动专属桶要用字符串键 |
| A15 | `lottery_campaigns.allowed_campaign_ids`(JSON) 决定吃哪个预算桶 | ✅ 存在；`UserBudgetProvider.js` 实测按 `allowed_campaign_ids` 数组从多桶汇总/顺序扣减。§12 防套利机制**技术上成立** | 成立 |
| A16 | `lottery_campaigns` 有 `budget_mode`/`pool_budget_*`/`preset_budget_policy=pool_first` | ✅ 全部存在（`budget_mode enum(user/pool/none)`、`pool_budget_total/remaining bigint`、`preset_budget_policy enum(follow_campaign/pool_first/user_first)`） | 成立，§12.9 双账户零新增 |
| A17 | `CoreService` 预算发放硬编码 `CONSUMPTION_DEFAULT` | ✅ `services/consumption/CoreService.js:403` `lottery_campaign_id: 'CONSUMPTION_DEFAULT'`。§12 归集重定向落点精确 | 成立 |
| A18 | `event_points` 资产类型可新增 | ⚠️ 实测**当前不存在**（`material_asset_types` 无 `event_points`）；需新增一条 + `asset_group_defs` 可能需配套 group。技术可行但是**从 0 新建** | 需新建（见 §18） |
| A19 | 倍率表 `reward_multiplier_campaigns`/`_targets` 已有 | ❌ 实测**两表均不存在**，全新建 | 需迁移新建 |
| A20 | `lottery_campaign_quota_grant`（方案 §1.3 写法） | ❌ 表名实际是 **`lottery_campaign_quota_grants`（复数）**；另有 `lottery_draw_quota_rules`/`lottery_user_daily_draw_quota` | 引用需改表名 |

### 14.2 最关键的一处返工：人群定向模型必须改（A7/A8）

方案 §3.2 设计的 `reward_multiplier_targets.target_ref` 直接存 `segment_key`，并假设"一个活动可绑多个 segment_key"。但真实后端语义是：

- **`segment_key` 是"某个 resolver_version 内、按优先级求值命中的结果标签"**，不是可自由引用的独立实体。一个活动通过 `lottery_strategy_config.segment.resolver_version` **只绑定一个版本**（如 v1），该版本内的规则数组决定用户落到哪个 `segment_key`（如 new_user / regular_user）。
- 运行时求值 `SegmentResolver.resolveSegmentAsync(version, user)` **一次只返回一个 segment_key**，且只认 `segment_field_registry.js` 的 5 个白名单字段。

**因此后端权威的正确建模**（见 §16.2）：倍率规则的人群命中判定，应复用"活动已绑定的 `resolver_version` + `SegmentResolver` 求出的 `segment_key`"，规则里存"该活动 resolver 版本下、命中哪个 `segment_key` 才翻倍"。即 `target_type='segment'` 时，`target_ref` 存 **segment_key**（如 `new_user`），由 `CrystalMultiplierService` 调 `SegmentResolver.resolveSegmentAsync(该活动的resolver_version, user)` 得到用户实际 segment_key，与 `target_ref` 比较是否命中——**这与 TierPickStage 现网分群同源，零新增分群引擎**，但不能像方案原文那样"跨版本自由引用任意 segment_key"。

---

## 15. 三方问题划分（哪些是后端 / Web 管理后台前端 / 微信小程序前端的问题）

> 项目未上线、不兼容旧接口、以后端数据库项目为准、修改前端适配后端、前端直接改用后端字段名不做映射。以下按此原则划分。

### 15.1 后端数据库项目的问题（唯一权威，先行）

| 编号 | 问题 | 处理 |
|---|---|---|
| B-1 | 倍率两表 `reward_multiplier_campaigns` / `reward_multiplier_targets` 不存在 | 新建迁移 + 模型（§18.1） |
| B-2 | 人群 target 模型错位（A7/A8/§14.2）：不能自由引用 segment_key | 按 §16.2 改为"活动 resolver_version + segment_key 命中"语义 |
| B-3 | `event_points` 资产类型不存在（A18） | 新增 `material_asset_types` 一条 + 视需要加 `asset_group_defs` 分组（§18.2） |
| B-4 | `SettleStage` material 分支未接倍率（现 `delta_amount: prize.material_amount`） | 发放前调 `CrystalMultiplierService`，改用 `finalQuantity`（§18.3） |
| B-5 | 无 `CrystalMultiplierService`（倍率查询/合并/夹紧/成本刹车） | 新建纯计算服务 + 单测（§18.3） |
| B-6 | `CoreService` 预算硬编码 `CONSUMPTION_DEFAULT`（A17），限时活动无法归集专属桶 | 改为查归集规则，命中则用活动桶字符串键 + 发 `event_points`（§18.4） |
| B-7 | 无活动到期清零 job | 参照 `jobs/daily-*.js` 范式新增（§18.5） |
| B-8 | 无倍率规则/成本/代币的查询与统计 API | 新增 console 域只读 API（§16、§18.6），收口到 Service |
| B-9 | 成本折算按各资产真实 `budget_value_points` 累加；原 `red_core_gem=0` 已按 D-9 订正为 10（A4/§22.8） | `extra_cost` 严格按各资产真实 `budget_value_points` 累加；订正后含 red 全色系口径有效 |
| B-10 | 资产码大小写：真实是小写 `points`/`budget_points`（A6） | 后端一切以 `constants/AssetCode.js` 常量为准（值即小写），API 只出小写 |

### 15.2 Web 管理后台前端项目的问题（admin/，Vite + Alpine.js + Tailwind + ECharts 6.0）

| 编号 | 问题 | 处理 |
|---|---|---|
| W-1 | 无倍率规则 CRUD 页 | 新增页，仿 `admin/lottery-management.html` 结构（§18.7）；字段名直接用后端契约（§16）字段，不做前端映射 |
| W-2 | 人群选择器需按"后端真实语义"做：先选活动的 `resolver_version`，再从该版本 `rules[].segment_key` 列出可选项；growth_level 从 `user_growth_levels.level_key` 列；tag 从 `user_ad_tags.tag_key`（当前空，需提示"暂无标签数据"） | 选择器数据源改为后端返回，不硬编码人群清单 |
| W-3 | 成本/预算/代币看板 | 复用 ECharts 6.0，落点 `lottery-alerts`/`dashboard-panel`/`finance-management`/`statistics`/`risk-alerts`/`reconciliation`（§13 已列，字段对齐 §16） |
| W-4 | 归集规则配置入口在 Web（§12.10 已定：小程序/商家端不加选择口） | 在 `lottery-management` 扩展或新页 |
| W-5 | 现网前端字段映射层（若有）需删除，直接用后端 snake_case 字段 | 全局排查 admin 是否有 camelCase 映射，删除改直连 |

### 15.3 微信小程序前端项目的问题（独立仓库，不在本后端 repo 内）

> 实测：本仓库（`/home/devbox/project`）**不包含**微信小程序源码（无 `app.json`/`project.config.json`），小程序是独立前端工程。本文档作为小程序求证文档，给出它必须遵守的后端契约。

| 编号 | 问题 | 处理（一律以后端为准，前端改字段名，不做映射） |
|---|---|---|
| M-1 | 抽奖结果展示"基础×倍率=最终"所需字段 | 读 `POST /api/v4/lottery/draw` 响应里 `result_metadata.crystal_multiplier`（§16.4），字段名照抄后端 |
| M-2 | 抽奖前"当前生效合并倍率"展示 | 调 §16.1 的活动倍率查询接口，取单一 `applied_multiplier` |
| M-3 | 资产/余额字段大小写：必须用小写 `points`/`budget_points`/`event_points`/`red_core_shard`… | 小程序若有大写 `POINTS` 等，全部改小写后端 asset_code |
| M-4 | 活动代币 `event_points` 展示（仅活动内露出 + 到期倒计时） | 字段以 §16.3 为准；主钱包不展示 |
| M-5 | 商家端/用户端**不加预算归集选择口**（§12.10 防9） | 小程序仅出示二维码，可选展示"本次获得 X 活动积分" |
| M-6 | 响应结构统一 `{ success, code, message, data, timestamp, request_id }`（§16.5） | 小程序统一按此解析，读 `data` |

---

## 16. 后端权威 API 契约（以后端技术体系为准，前端适配）

> 设计原则（实测对齐现网）：
> - 路径风格：扁平 RESTful `/api/v4/{resource}`；C 端抽奖域 `/api/v4/lottery/*`；管理域 `/api/v4/console/*`（`requireRoleLevel(100)`）。
> - 路由层不直连 models，全部走 `req.app.locals.services.getService('xxx')`（ServiceManager，snake_case key）。
> - 响应统一 `res.apiSuccess(data, message, code)` / `res.apiError(...)`（`utils/ApiResponse.js`）。
> - 写接口强制 Header `Idempotency-Key`（缺失 400），复用现有幂等范式。
> - 时间字段：存储 UTC、序列化 ISO8601(Z)、展示层转北京时间（现网 B-2 口径）。
> - 字段命名：**全 snake_case**，asset_code 全小写。**前端直接使用这些字段名，不做映射。**

### 16.1 管理端 · 倍率规则 CRUD（console 域，新增 `routes/v4/console/lottery/crystal-multiplier.js`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v4/console/lottery/multiplier-rules` | 列表（分页，`res.apiPaginated`），支持 `?lottery_campaign_id=&status=` |
| GET | `/api/v4/console/lottery/multiplier-rules/:id` | 详情（含 targets） |
| POST | `/api/v4/console/lottery/multiplier-rules` | 创建（Header Idempotency-Key） |
| PUT | `/api/v4/console/lottery/multiplier-rules/:id` | 更新 |
| PATCH | `/api/v4/console/lottery/multiplier-rules/:id/status` | 开关 active/inactive |
| DELETE | `/api/v4/console/lottery/multiplier-rules/:id` | 删除（级联 targets） |
| GET | `/api/v4/console/lottery/multiplier-rules/:id/cost` | 成本水位（`extra_cost_used/limit`、受益人数、多发总量） |

创建/更新请求体（snake_case，字段名 = 数据库列名）：

```json
{
  "lottery_campaign_id": 1,
  "campaign_name": "新春高等级翻倍",
  "display_name": "新春水晶翻倍",
  "multiplier": 2.00,
  "reward_scope": "crystal_all",
  "scope_values": null,
  "rounding_mode": "ceil",
  "max_multiplier_cap": 3.00,
  "extra_cost_limit": 1000000,
  "per_user_daily_limit": 5,
  "eligibility_days": 30,
  "per_user_extra_cap": 200,
  "start_at": "2026-02-01 00:00:00",
  "end_at": "2026-02-15 23:59:59",
  "status": "inactive",
  "targets": [
    { "target_type": "segment", "target_ref": "new_user", "target_value": null },
    { "target_type": "growth_level", "target_ref": "gold", "target_value": null }
  ]
}
```

### 16.2 人群选择器数据源（后端提供，前端不硬编码）

| 方法 | 路径 | 返回 |
|---|---|---|
| GET | `/api/v4/console/lottery/campaigns/:id/segment-options` | 该活动 `lottery_strategy_config.segment.resolver_version` 对应版本的可选 `segment_key` 列表（含 label）；复用 `SegmentResolver.getSegmentKeys(version)` |
| GET | `/api/v4/console/lottery/growth-levels` | `user_growth_levels` 启用等级（`level_key`/`level_name`），复用 `UserGrowthLevelService.listLevels()` |
| GET | `/api/v4/console/lottery/ad-tags` | `user_ad_tags` 去重 `tag_key`（当前实测空，需返回空数组 + 提示） |

> **权威语义（§14.2）**：`target_type='segment'` 时 `target_ref` 必须是"该活动 resolver_version 版本内存在的 segment_key"，后端创建时校验；命中判定 = `SegmentResolver.resolveSegmentAsync(resolver_version, user) === target_ref`。

### 16.3 C 端 · 当前生效合并倍率（小程序抽奖前展示）

> ⚠️ **本节为早期设计，最终上线契约以 §23.3 为准**（真实路径参数是 `:code` 不是 `:campaign_code`；另有新增的 `event-points` 查询接口）。小程序对接直接看 §23.3。

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v4/lottery/campaigns/:code/multiplier` | 返回当前登录用户在该活动的**合并后单一倍率**（Max + cap 夹紧后）。★ 参数名 `:code`（见 §23.3） |

响应 `data`：

```json
{
  "lottery_campaign_id": 1,
  "applied_multiplier": 2.00,
  "display_name": "新春水晶翻倍",
  "end_at": "2026-02-15 23:59:59",
  "active": true
}
```

> 日常态（无加成）`applied_multiplier=1` 且 `active=false`，前端不展示倍率角标（§5.2）。活动代币 `event_points` 余额走现有资产查询接口，asset_code 用小写 `event_points`。

### 16.4 C 端 · 抽奖结果倍率快照（写入现网 `lottery_draws.result_metadata`）

> ⚠️ **最终上线：`POST /api/v4/lottery/draw` 响应的 `data.prizes[]` 每个奖品对象已直接带 `crystal_multiplier` 和 `material_amount` 字段**，小程序无需从 `result_metadata` 挖取。完整字段见 §23.3 接口 2。下方 JSON 为落库快照结构（含 `multiplier_campaign_id`/`candidates`，用于审计对账），C 端展示字段以 §23.3 为准。

`POST /api/v4/lottery/draw` 响应中，每次抽中水晶的记录其 `result_metadata.crystal_multiplier`：

```json
{
  "crystal_multiplier": {
    "base_quantity": 10,
    "applied_multiplier": 2.00,
    "final_quantity": 20,
    "multiplier_campaign_id": 12,
    "reason": "新春水晶翻倍 ×2",
    "candidates": [
      { "multiplier_campaign_id": 12, "multiplier": 2.00 },
      { "multiplier_campaign_id": 8, "multiplier": 1.50 }
    ]
  }
}
```

> 小程序展示"红源晶碎片 ×20（基础 10 × 新春翻倍 2 倍）"直接读此快照，字段名照抄。非水晶/未翻倍时无 `crystal_multiplier` 键。

### 16.5 统一响应结构（实测 `utils/ApiResponse.js`，前端按此解析）

```json
{
  "success": true,
  "code": "SUCCESS",
  "message": "操作成功",
  "data": { },
  "timestamp": "2026-07-05 08:12:00",
  "request_id": "req_xxx"
}
```

---

## 17. 技术栈对齐：可复用 / 可扩展（后端 + Web 前端均已核对）

### 17.1 后端数据库项目（Node.js 20 + Express 4 + Sequelize 6 + MySQL 8 + Redis/ioredis）

**可直接复用（零新增技术栈，实测存在）**：

| 能力 | 现网载体（实测） | 复用点 |
|---|---|---|
| 发奖入账 | `SettleStage.js` material 分支 + `BalanceService.changeBalance`（双录 + 幂等键 `${idempotency_key}:material`） | 倍率只改 `delta_amount` 计算，不改入账机制 |
| 事务边界 | `utils/TransactionManager.execute()`（draw.js 已用） | 倍率全程在现有结算事务内 |
| 人群分层 | `config/segment_rules.js`(`SegmentResolver`) + `config/segment_field_registry.js`（5 字段白名单） + `segment_rule_configs`(JSON rules) | segment 命中判定直接调 `resolveSegmentAsync`，与 TierPickStage 同源 |
| 成长等级 | `services/lottery/UserGrowthLevelService.js`（`resolveUserLevel`/`getUserLevelMultiplier`） + `user_growth_levels`(4档) | growth_level 命中直接调 |
| 预算分桶/来源限制 | `account_asset_balances.lottery_campaign_id`(VARCHAR桶) + `lottery_campaigns.allowed_campaign_ids`(JSON) + `UserBudgetProvider` | §12 活动专属预算/防套利零新增账户体系 |
| 池账户/抽取顺序 | `lottery_campaigns.pool_budget_total/remaining` + `preset_budget_policy=pool_first` | §12.9 双账户直接用 |
| 配额到期范式 | `lottery_campaign_user_quota.expires_at` + status `expired` | 到期清零范式可参照 |
| 资产流水/对账 | `asset_transactions`(meta JSON + 双录 + idempotency UNIQUE) | 倍率摘要写 meta，对账复用 |
| 快照 | `lottery_draws.result_metadata`(JSON) | 倍率快照零新增列 |
| 定时任务 | `jobs/daily-*.js`（node-cron，`ecosystem.config.js` 调度） | 到期清零 job 参照 |
| 统一响应/幂等/鉴权 | `utils/ApiResponse.js`、Header Idempotency-Key、`middleware/auth.js`(`requireRoleLevel`) | 新 API 直接套用 |
| 模型注册 | `models/index.js`（`require('./X')(sequelize, DataTypes)` + `associate`） | 新模型按此注册 |
| 迁移 | sequelize-cli，`migrations/` 事务化 up/down，命名 `YYYYMMDDHHMMSS-verb-object.js` | 新表迁移按此 |

**需扩展/新建（后端）**：`reward_multiplier_campaigns`/`reward_multiplier_targets` 两表 + 两模型、`CrystalMultiplierService`、`event_points` 资产 + 归集规则、到期 job、console 域倍率 API。均为**在现有架构内新增文件**，不引入任何新框架/新中间件。

### 17.2 Web 管理后台前端（admin/：Vite 6 + Alpine.js 3 + Tailwind 3 + ECharts 6.0）

**可复用**：多页 HTML 结构（`lottery-management.html` 等 60+ 页现成）、ECharts 6.0 图表、Alpine 组件范式、`socket.io-client` 实时。倍率规则页/看板全部复用现有页面骨架与图表库，**零新增前端技术栈**。

**是否符合 Web 前端现有技术栈**：✅ 完全符合。倍率管理、成本看板、归集规则配置、客服查询、运维视图均可在现有 Alpine + ECharts 体系内实现，无需引入新库。唯一要求：**前端字段名直接用后端 snake_case 契约（§16），删除任何 camelCase 映射层**。

### 17.3 微信小程序前端（独立仓库）

不在本 repo，无法核其技术栈；本文档只约束其**必须遵守 §16 后端契约**（字段名、路径、响应结构、asset_code 小写）。前端改字段适配后端，不做映射。

---

## 18. 执行步骤与真实代码落点（后端先行，前端后接）

### 18.1 新增倍率两表迁移 + 模型（B-1/B-2）

- 迁移：`migrations/<ts>-create-reward-multiplier-tables.js`，事务化 up/down。字段严格按 §3.1/§3.2，但**修正**：
  - `reward_multiplier_targets.target_ref` 语义按 §14.2/§16.2（segment 存 segment_key，需活动 resolver_version 内存在）。
  - `lottery_campaign_id INT`（对齐真实库，A11），外键 `→ lottery_campaigns.lottery_campaign_id ON DELETE CASCADE`。
  - `utf8mb4_unicode_ci`、`created_at/updated_at DATETIME`、BIGINT 主键。
- 模型：`models/RewardMultiplierCampaign.js` + `models/RewardMultiplierTarget.js`，`models/index.js` 注册（`require(...)(sequelize, DataTypes)` + `associate` hasMany/belongsTo）。

### 18.2 新增 `event_points` 资产（B-3）

- 迁移插入 `material_asset_types` 一条：`asset_code='event_points'`, `form='quota'`（受限、不可自由交易）, `group_code`（复用 `points` 或新增 `asset_group_defs` 的 event 组）, `is_enabled=1`, `is_tradable=0`。
- 按 `lottery_campaign_id` 分桶复用 `account_asset_balances`（同 budget_points 范式），不为每活动造新币种。

### 18.3 `CrystalMultiplierService` + 接入 `SettleStage`（B-4/B-5）

- 新建 `services/lottery/CrystalMultiplierService.js`：
  - `resolveMultiplier(user_id, prize, campaign, now, transaction)` → `{ applied, campaign_id, candidates }`。
  - 水晶判定：`prize.material_asset_code` 对应 `material_asset_types.form in ('shard','gem')` 且非货币；`is_fallback` 不翻（§11-7）。
  - 人群命中：segment 调 `SegmentResolver.resolveSegmentAsync(该活动resolver_version, user)`；growth_level 调 `UserGrowthLevelService.resolveUserLevel`；tag 查 `user_ad_tags`；user 比对 user_id。
  - Max 合并 + `max_multiplier_cap` 夹紧 + `extra_cost_limit` 行锁刹车（`SELECT ... FOR UPDATE`）。
  - `extra_cost = (final-base) × 真实 budget_value_points`（B-9，逐资产取值）。
  - 单测：无命中→×1、单/多命中 max、cap 夹紧、成本击穿回退、非水晶跳过、segment/growth_level/tag/user 各分支、ceil 取整。
- 接入 `services/UnifiedLotteryEngine/pipeline/stages/SettleStage.js` **第 709–736 行 material 分支**：`changeBalance` 前调 service，`delta_amount` 由 `prize.material_amount` 改 `finalQuantity`；写 `result_metadata.crystal_multiplier`（§16.4）+ `asset_transactions.meta` 摘要。全程用现有事务与幂等键。

### 18.4 预算归集重定向（B-6，§12）

- 改 `services/consumption/CoreService.js` **第 403 行** `lottery_campaign_id: 'CONSUMPTION_DEFAULT'`：先查命中的活动归集规则，命中则用活动专属桶字符串键 + 写 `expires_at` + 发 `event_points`；未命中维持 `CONSUMPTION_DEFAULT`。
- 限时翻倍活动 `lottery_campaigns.allowed_campaign_ids` 只填活动专属桶（排除 `CONSUMPTION_DEFAULT`）。

### 18.5 到期清零 job（B-7）

- 新增 `jobs/daily-event-budget-expiry.js`（参照 `jobs/daily-*.js` + node-cron），活动 `end_time` 到期清零该活动专属 `budget_points` 桶 + `event_points`；`ecosystem.config.js`/环境开关接入。

### 18.6 console 域倍率/成本/代币 API（B-8）

- 新增 `routes/v4/console/lottery/crystal-multiplier.js`（§16.1/16.2），`requireRoleLevel(100)`，收口到 `CrystalMultiplierService` + 查询服务；C 端 `routes/v4/lottery/campaigns.js` 扩展 §16.3 合并倍率接口。

### 18.7 质量校验（每步后）

`npm run lint`、`TZ=Asia/Shanghai jest`（含 CrystalMultiplierService 单测与 SettleStage 受影响用例）、`npm run check:api-contract`、`npm run migration:verify`。

### 18.8 前端接入（后端自测通过后）

- admin：新增倍率规则 CRUD 页（W-1）+ 人群选择器接后端（W-2）+ 看板（W-3）+ 归集规则页（W-4）+ 删映射层（W-5），字段全用 §16 契约。
- 小程序：按 §15.3 逐条改字段适配后端。

---

## 19. 拍板事项（求证后新增，均因真实库/代码与原方案有出入）

> §11、§12.8 已定稿的 9+9 项不再重复。**2026-07-05 决策：本文档提及的全部内容一次性建设，不分一二期，原"不做/二期"项全部纳入本次实施。** 下表 D-1~D-8 全部拍板执行（见 §20）。

| # | 事项 | 背景（实测） | 结论（全部一次性执行） |
|---|---|---|---|
| D-1 | **人群定向语义改为"活动 resolver_version + segment_key 命中"** | §14.2：后端无"自由引用任意 segment_key"能力，segment_key 依附于活动绑定的单一 resolver 版本 | ✅ **执行**：采纳 §16.2 语义，复用 `SegmentResolver.resolveSegmentAsync(该活动 resolver_version, user)`，零新增分群引擎 |
| D-2 | **扩展 segment 定向字段白名单** | `segment_field_registry.js` 仅 5 字段（created_at/last_active_at/history_total_points/user_level/consecutive_fail_count） | ✅ **执行**：按倍率活动定向需求评估并扩充白名单，后端在 `segment_field_registry.js` 补齐；不足处一并补 |
| D-3 | **tag 定向 + 标签生产** | `user_ad_tags` 当前 0 行 | ✅ **执行**：tag 定向一并上线；同时确认/接通标签生产任务（凌晨 3 点计算），无标签时该 target 安全降级为不命中（×1），不阻塞 |
| D-4 | **`red_core_gem` 的 budget_value_points 原=0** | 原判"红色系成本折算≈0、`extra_cost_limit` 对红几乎不设限"，拟默认排除红 | ⚠️ **已被 D-9 取代（2026-07-05，以 §22.8 为准）**：red_gem=0 定性为**数据漏填**，订正为 10（gem=shard×10 规律）。修正后全部水晶成本口径非零有效，**取消"默认排除红 + 二次确认"**，red 与其它色系一视同仁（`crystal_all` 默认含 red）；`reward_scope` 收窄保留为运营可选项 |
| D-5 | **活动专属预算桶用字符串键** | `account_asset_balances.lottery_campaign_id` 是 VARCHAR(50)，真实数据是字符串桶 | ✅ **执行**：桶命名规范 `EVENT_<活动code>`，用于限时活动专属预算隔离 |
| D-6 | **`event_points` 的 group/form 归类** | 需新增到 `material_asset_types` | ✅ **执行**：新增 `event_points`，`form=quota, is_tradable=0`，`group_code` 复用 points 组（或新增 event 组，见 §18.2） |
| D-7 | **到期清零 vs 折算** | §12.8 防2 已定"直接清零" | ✅ **执行**：到期清零个人活动桶 + `event_points`；活动池 `pool_budget_remaining` 余额到期由运营手动回收 |
| D-8 | **建设范围** | 原拟分期 | ✅ **执行**：**不分期，一次性全量建设**——倍率规则 + SettleStage 接入 + 常驻翻倍 + 限时稀缺翻倍（活动专属 `event_points` + 预算归集重定向 + 到期清零 job）+ 双层货币 + 全部看板/查询/运维 |

---

## 20. 全量建设范围、执行清单与代码落点（2026-07-05 拍板：一次性全做，不分期）

> 本节是**唯一施工依据**，涵盖本文档 §1–§18 提及的全部功能。凡与 §18 表述冲突处以本节为准；§18 各条（含 event_points/归集重定向/到期 job）**全部纳入本次实施**。

### 20.1 全量建设清单（全部要做，无"不做"项）

| 类别 | 内容 | 落点 |
|---|---|---|
| 倍率规则表/模型 | `reward_multiplier_campaigns` + `reward_multiplier_targets` | §18.1 |
| 活动专属货币 | 新增 `event_points` 资产（D-6） | §18.2 |
| 倍率计算服务 | `CrystalMultiplierService`（判定/合并/夹紧/成本刹车） | §18.3 |
| 发奖接入 | `SettleStage` material 分支接倍率 + 快照 | §18.3 |
| 人群定向 | segment（D-1）+ growth_level + user + **tag（D-3）** | §16.2、§18.3 |
| 定向字段白名单 | 按需扩 `segment_field_registry.js`（D-2） | §18.3 |
| 水晶范围 | **D-9 取代 D-4**：red_gem 订正为 10（§22.8），red 与其它色系一视同仁，`crystal_all` 默认含 red；`reward_scope` 收窄为运营可选 | §22.8、§18.1/18.3 |
| 常驻翻倍 | 绑活动、走全局 `CONSUMPTION_DEFAULT`、不清零 | §12.6 |
| 限时稀缺翻倍 | 活动专属预算桶 `EVENT_<code>`（D-5）+ 归集重定向 + 到期清零 | §12、§18.4/18.5 |
| 预算归集重定向 | 改 `CoreService` 硬编码 `CONSUMPTION_DEFAULT` → 命中活动归集规则用专属桶 + 发 `event_points` | §18.4 |
| 到期清零 job | `jobs/daily-event-budget-expiry.js` 清专属桶 + `event_points`（D-7） | §18.5 |
| 双层货币 | 可见层 `event_points`（入场）+ 隐藏层 `budget_points` 分桶（成本） | §12.7 |
| 成本封顶 | `extra_cost_limit` + `max_multiplier_cap` + 击穿回退（按真实 `budget_value_points`，B-9） | §18.3 |
| console API | 倍率 CRUD + 人群选择器 + 成本/预算/代币看板 + 归集规则配置 + 客服查询 + 运维视图 | §16、§13 |
| C 端 API | 合并倍率查询 + 结果快照 + 活动代币余额展示 | §16.3/16.4 |
| Web 前端 | 倍率 CRUD 页 + 人群选择器 + 全部看板 + 归集规则页 + 代币配置 + 删映射层 | §13、§18.7 |
| 小程序前端 | 合并倍率/结果展示 + 活动代币展示 + asset_code 小写 + 无归集选择口 | §15.3、§16 |

### 20.2 执行顺序（后端先行，逐步自测；一次性全部完成）

0. **数据订正迁移（D-9 前置，§22.8）**：`migrations/<ts>-fix-red-core-gem-budget-value.js`，事务化订正 `material_asset_types` 中 `red_core_gem.budget_value_points 0 → 10`（可回滚）；`migration:verify` + 只读校验 6 色系 gem=shard×10。**此步先行**，确保成本口径对全部水晶有效。
1. **建表迁移**：`migrations/<ts>-create-reward-multiplier-tables.js`（§18.1）。水晶范围按 **D-9**（red 与其它色系一视同仁）：
   - `reward_scope` 默认 `crystal_all`（含 red）；`reward_scope`/`scope_values` 允许运营按需收窄到指定色系/资产码，无 DB 硬禁止、无默认排除红。
   - **不新增** `per_draw_extra_cap` 列（D-9 取消双口径）。
2. **新增 `event_points` 资产迁移**（D-6/§18.2）：`material_asset_types` 插一条 `asset_code='event_points', form='quota', is_tradable=0, group_code`（复用 points 组或新增 event 组）；按 `lottery_campaign_id` 分桶复用 `account_asset_balances`。
3. **模型**：`models/RewardMultiplierCampaign.js` + `models/RewardMultiplierTarget.js` + `models/index.js` 注册（§18.1）。
4. **扩定向字段白名单**（D-2）：按倍率活动定向需求评估 `config/segment_field_registry.js`，需要则补字段/运算符。
5. **CrystalMultiplierService**（§18.3）：
   - segment 命中按 **D-1**：`SegmentResolver.resolveSegmentAsync(该活动 resolver_version, user) === target_ref`。
   - **tag 命中（D-3）**：查 `user_ad_tags` `tag_key=target_ref`（可选 `tag_value=target_value`）；无标签数据时降级为不命中。
   - growth_level 命中：`UserGrowthLevelService.resolveUserLevel(user_id) === target_ref`；user：比对 user_id。
   - 水晶判定（**D-9**，单一预算口径，无双口径）：`form in ('shard','gem')`（`form` 仅 shard/gem/currency/quota，货币天然排除，见 C3），再按规则 `reward_scope`/`scope_values` 收窄。
   - `extra_cost` 逐资产按真实 `material_asset_types.budget_value_points` 累加（B-9）；订正后全部水晶（含 red）成本非零，`extra_cost_limit` 统一有效。
   - 单测：D-1 segment 命中/不命中、D-3 tag 命中/无标签降级、水晶范围 `crystal_all`/收窄到指定色系、含 red 正常翻且成本按 red_gem=10 折算、growth_level/user、max 合并、cap 夹紧、成本击穿回退、ceil。
6. **接入 SettleStage**：`SettleStage.js:709` material 分支，`delta_amount` 改 `finalQuantity` + 写 `result_metadata.crystal_multiplier`（§16.4）+ `asset_transactions.meta`。
7. **预算归集重定向**（D-5/§18.4）：改 `services/consumption/CoreService.js:403` 硬编码 `CONSUMPTION_DEFAULT` → 查命中活动归集规则则用专属桶 `EVENT_<code>` + 写 `expires_at` + 发 `event_points`；未命中维持 `CONSUMPTION_DEFAULT`。限时活动 `allowed_campaign_ids` 只填专属桶。
8. **到期清零 job**（D-7/§18.5）：`jobs/daily-event-budget-expiry.js`（node-cron），活动 `end_time` 到期清零专属 `budget_points` 桶 + `event_points`，池余额运营手动回收；接 `ecosystem.config.js`/环境开关。
9. **console API**：`routes/v4/console/lottery/crystal-multiplier.js`（§16.1）+ 人群选择器（§16.2，segment/growth_level/tag）+ 归集规则配置 API + 成本/预算/代币看板 API + 客服查询 + 运维视图（§13）；C 端 `/api/v4/lottery/campaigns/:code/multiplier`（§16.3）+ 代币余额查询。
10. **质量校验**（§18.7）：`npm run lint`、`TZ=Asia/Shanghai jest`、`npm run check:api-contract`、`npm run migration:verify`。
11. **前端**：
    - admin：倍率 CRUD 页 + 人群选择器（segment/growth_level/tag，接后端）+ 全部看板（§13.2）+ 归集规则页（§13.1）+ 代币配置 + 客服查询（§13.3）+ 运维视图（§13.4）。**W-5 修正**：admin 无 camelCase 映射层可删，用 snake_case + 跑 `npm run lint:mappings` 黑名单扫描即可。**D-9**：范围选择器 `crystal_all` 默认含 red，无二次确认弹窗（red 成本口径已订正有效）；运营可选收窄色系。
    - 小程序：合并倍率展示（§16.3）+ 结果"基础×倍率=最终"（§16.4）+ 活动代币 `event_points` 展示（仅活动内露出 + 到期倒计时）+ asset_code 全小写 + 不加归集选择口（§15.3）。

### 20.3 全量建设下仍须遵守的边界（安全护栏，不因全做而放松）

- 倍率**只作用** `SettleStage` material 发放数量一层，抽奖概率/选奖权重/非水晶奖品零影响（§9 边界不变）。
- 全程复用现有结算事务 + 幂等键 `${idempotency_key}:material`，防重复发放。
- `event_points`/活动专属桶到期清零走定时任务，与倍率逻辑解耦（§12.6 单一职责）。
- 前端字段一律用 §16 后端 snake_case 契约、asset_code 小写，**不做映射层**。

---

### 求证总结（一句话）

本次已**实连真实库 `restaurant_points_dev` + 通读现网抽奖/预算/分群/资产代码**校准方案：核心机制（SettleStage 发奖、material_asset_types 水晶判定、预算分桶、allowed_campaign_ids、result_metadata 快照、成长等级、事务/幂等/响应）**在后端现有技术栈内均可复用、零新增框架**；D-1~D-8 全部拍板、**一次性全量建设不分期**（倍率规则 + SettleStage 接入 + 常驻翻倍 + 限时专属货币 `event_points` + 归集重定向 + 到期清零 + 双层货币 + 全部看板/查询/运维），segment 按活动 resolver 语义；red_gem=0 定性数据缺陷并按 **D-9 订正为 10**（§22.8），red 与其它色系一视同仁、单一成本口径覆盖全部水晶。前端一律以 §16 后端契约为准、直接改字段不做映射。

---

## 21. 附：微信小程序前端维护模式开关（独立功能，2026-07-05 追加）

> 与水晶倍率无直接耦合的独立功能，因同属本平台后端 + 三端（后端/Web 后台/小程序）改造范畴，并入本文档统一管理。
> 拍板：**分端独立开关——只让微信小程序进维护模式，Web 管理后台/商家端/其他端不受影响**；运营可在 Web 后台秒级开关。
> 求证：实连真实库 + 通读现网 `middleware/maintenanceMode.js`、`utils/platformDetector.js`、`services/AdminSystemService.js`、`config/system-settings-whitelist.js`、`admin/src/modules/system` 后校准，**零新增表、零新增依赖**。

### 21.1 现网求证结论（实测可复用）

| 现网能力 | 载体（实测） | 复用点 |
|---|---|---|
| 全局维护模式 | `middleware/maintenanceMode.js`（读 `system_settings.basic.maintenance_mode`，true 则用户端 `/api/*` 返 503，放行 `/admin`/`/api/v4/console`/`/api/v4/auth`/`/health`/`app-version`） | 分端开关在此中间件内叠加 |
| 平台识别 | `utils/platformDetector.js` `detectLoginPlatform(req)`：`X-Platform` 头/body → `Referer: servicewechat.com/{appid}`（微信框架强制注入、不可伪造）→ UA → 兜底 `web` | 直接判"是否小程序请求" |
| 平台枚举 | `web`/`wechat_mp`/`douyin_mp`/`alipay_mp`/`app`/`unknown`（`AuthenticationSession` ENUM + `VALID_PLATFORMS`） | `wechat_mp`=微信小程序 |
| 设置存储/更新 | `AdminSystemService.getSettingValue` / `updateSettings`（改后自动失效缓存，秒级生效） | 新开关走同一套 |
| 设置白名单 | `config/system-settings-whitelist.js`（已有 `basic/maintenance_mode`/`_message`/`_end_time`） | 新开关按同结构加白名单 |
| 后台设置 UI | `admin/config-tools.html` + `admin/src/modules/system/composables/config.js`（`BOOLEAN_KEYS` 含 `maintenance_mode`，布尔项自动渲染开关） | 新 key 加进 `BOOLEAN_KEYS` 即自动出开关 |

**关键结论**：`detectLoginPlatform(req)` 靠 `servicewechat.com` Referer 已能可靠识别小程序请求，"只维护小程序"技术上完全成立、无需新增基础设施。

### 21.2 设计原则

- **分端隔离**：小程序开关与全局 `maintenance_mode` **并存正交**。全局=全站维护；新开关只作用 `wechat_mp`。
- **管理端永不自锁**：`/api/v4/console`、`/admin`、`/api/v4/auth` 始终白名单，运营任何时候都能进后台开关。
- **失败放行**：维护状态检查异常时放行（沿用现网 fail-open），避免检查故障导致小程序全挂。
- **契约优先**：小程序按后端 503 契约展示，字段用后端 snake_case，不做映射。

### 21.3 配置项（复用 `system_settings` basic 分类，零新增表）

| 配置 key | 类型 | 默认 | 说明 |
|---|---|---|---|
| `basic/maintenance_mode_wechat_mp` | boolean | false | 微信小程序维护开关（仅 `wechat_mp` 端用户 API 返 503） |
| `basic/maintenance_message_wechat_mp` | string | "小程序正在升级维护中，预计30分钟后恢复，敬请谅解。" | 小程序维护公告 |
| `basic/maintenance_end_time_wechat_mp` | string | "" | 预计结束时间（留空=未确定，格式 `2026-07-05 12:00`） |

- 与现有全局 `basic/maintenance_mode` 互不干扰；全部走 `updateSettings('basic', {...}, userId, {transaction})`，自动失效缓存。

### 21.4 后端改造落点（真实文件）

**（1）`config/system-settings-whitelist.js`** —— 在 `basic/maintenance_end_time` 之后按同结构新增 3 条：

```js
'basic/maintenance_mode_wechat_mp': {
  type: 'boolean', default: false, readonly: false,
  description: '微信小程序维护模式开关（开启后仅微信小程序端 API 返回 503，其他端不受影响）',
  changeRequiresRestart: false, businessImpact: 'CRITICAL', auditRequired: true
},
'basic/maintenance_message_wechat_mp': {
  type: 'string', minLength: 0, maxLength: 500,
  default: '小程序正在升级维护中，预计30分钟后恢复，敬请谅解。',
  readonly: false, description: '微信小程序维护公告内容',
  changeRequiresRestart: false, businessImpact: 'MEDIUM', auditRequired: true
},
'basic/maintenance_end_time_wechat_mp': {
  type: 'string', minLength: 0, maxLength: 30, default: '',
  readonly: false, description: '微信小程序维护预计结束时间（留空=未确定）',
  changeRequiresRestart: false, businessImpact: 'MEDIUM', auditRequired: true
}
```

**（2）`middleware/maintenanceMode.js`** —— 在现有全局判断之后、`next()` 之前，叠加分端分支。按 Q3 用"平台→配置 key"映射实现，本次只登记 `wechat_mp`，其它端留空位可扩展：

```js
const { detectLoginPlatform } = require('../utils/platformDetector')
// 可扩展：未来加 app/douyin_mp/alipay_mp 只需在此登记，中间件逻辑不变
const PLATFORM_MAINTENANCE_KEYS = {
  wechat_mp: {
    mode: 'maintenance_mode_wechat_mp',
    message: 'maintenance_message_wechat_mp',
    end_time: 'maintenance_end_time_wechat_mp'
  }
}
const platform = detectLoginPlatform(req) // Q1：优先取 X-Platform 头，再 Referer
const keys = PLATFORM_MAINTENANCE_KEYS[platform]
if (keys) {
  const on = await AdminSystemService.getSettingValue('basic', keys.mode, false)
  if (on === true || on === 'true') {
    const [msg, endTime] = await Promise.all([
      AdminSystemService.getSettingValue('basic', keys.message, '小程序正在维护中，请稍后再试。'),
      AdminSystemService.getSettingValue('basic', keys.end_time, '')
    ])
    const data = { maintenance: true, platform, message: msg, end_time: endTime || null }
    return res.apiError(msg, 'SYSTEM_MAINTENANCE', data, 503) // Q2：复用全站维护码
  }
}
```

- 白名单 `BYPASS_PREFIXES` 不变（`/api/v4/console`/`/admin`/`auth`/`health`/`app-version` 始终放行），保证运营能进后台关开关、小程序能读版本闸门。
- **Q2**：复用错误码 `SYSTEM_MAINTENANCE`，前端靠响应 `data.platform` 区分"全站维护"（`data.platform` 缺省/无）与"仅小程序维护"（`data.platform='wechat_mp'`）；fail-open 沿用现网 catch 放行。
- **Q3**：`PLATFORM_MAINTENANCE_KEYS` 是唯一扩展点，未来加端只加映射 + 对应 3 个白名单配置，中间件主体零改动。

**（3）显示名映射（可选）** —— `services/AdminSystemService.js` 的 `SETTING_DISPLAY_NAMES` 与 `services/SystemConfigService.js` 加：`maintenance_mode_wechat_mp:'微信小程序维护模式'`、`maintenance_message_wechat_mp:'小程序维护公告'`、`maintenance_end_time_wechat_mp:'小程序维护结束时间'`。

### 21.5 Web 管理后台前端（admin/）

- `admin/src/modules/system/composables/config.js`：把 `maintenance_mode_wechat_mp` 加入 `BOOLEAN_KEYS`，布尔项自动渲染为开关（与现有 `maintenance_mode` 同款）。
- `admin/config-tools.html`：basic 分类下自动多出"微信小程序维护模式"开关 + 公告 + 结束时间输入，无需单独写页。保存走现有设置 API，字段用后端 snake_case，不做映射。
- 开关旁加说明："仅影响微信小程序，Web 后台与商家端不受影响。"

### 21.6 微信小程序前端（独立仓库，求证契约）

- **所有请求统一带 `X-Platform: wechat_mp` 头**（Q1，双保险）。
- 全局请求拦截器：HTTP `503` 且 `code==='SYSTEM_MAINTENANCE'`（Q2 复用码）→ 展示维护页；可用 `data.platform==='wechat_mp'` 判断是"仅小程序维护"。
- 维护页读响应 `data`：`data.message`（公告）、`data.end_time`（预计恢复，可空）。
- 维护期间仍可调 `GET /api/v4/system/app-version`（白名单放行）引导旧版更新。

**503 响应契约（后端权威）**：

```json
{
  "success": false,
  "code": "SYSTEM_MAINTENANCE",
  "message": "小程序正在升级维护中，预计30分钟后恢复，敬请谅解。",
  "data": {
    "maintenance": true,
    "platform": "wechat_mp",
    "message": "小程序正在升级维护中，预计30分钟后恢复，敬请谅解。",
    "end_time": "2026-07-05 12:00"
  },
  "timestamp": "2026-07-05 08:49:00",
  "request_id": "req_xxx"
}
```

> 区分口径：全站维护响应 `data.platform` 无值（或非 `wechat_mp`）；仅小程序维护时 `data.platform='wechat_mp'`。

### 21.7 三方问题划分

| 端 | 改动 |
|---|---|
| 后端数据库项目 | ① 白名单加 3 项（21.4-1）；② `maintenanceMode.js` 叠加 `wechat_mp` 判断（21.4-2）；③ 显示名映射（21.4-3，可选）。**无新增表/依赖** |
| Web 管理后台前端 | `config.js` 的 `BOOLEAN_KEYS` 加一项，config-tools 页自动出开关（21.5）。字段用后端 key，不做映射 |
| 微信小程序前端（独立仓库） | 统一带 `X-Platform: wechat_mp` 头；拦截器识别 `503 + SYSTEM_MAINTENANCE` 且 `data.platform='wechat_mp'` → 维护页；读 `data.message`/`data.end_time`（21.6） |

### 21.8 执行步骤（后端先行）

1. `config/system-settings-whitelist.js` 加 3 条白名单（21.4-1）。
2. `middleware/maintenanceMode.js` 叠加小程序分端判断（21.4-2）。
3. `AdminSystemService`/`SystemConfigService` 加显示名（21.4-3）。
4. 自测：带 `X-Platform: wechat_mp`（或 `Referer: servicewechat.com/xxx`）的用户端请求 → 开关开时返 503/`SYSTEM_MAINTENANCE`（`data.platform='wechat_mp'`）；`web`/商家端请求正常放行；`/api/v4/console`/`/admin`/`auth` 始终放行。
5. 质量校验：`npm run lint`、`TZ=Asia/Shanghai jest`（补中间件用例：wechat_mp 拦截 / web 放行 / 白名单放行）。
6. 前端：admin 加开关（21.5）→ 小程序加拦截（21.6）。

### 21.9 边界与风险

- **只作用 `wechat_mp`**：其他端零影响。
- **平台识别可靠性**：小程序 `wx.request` 的 `Referer` 由微信框架强制注入 `servicewechat.com`、不可伪造；建议小程序统一带 `X-Platform: wechat_mp` 头作双保险。
- **fail-open**：设置读取异常时放行，避免检查故障导致小程序全挂（代价：极端情况开关可能短暂不生效，可接受）。
- **与全站维护叠加**：全站 `maintenance_mode=true` 时所有端（含小程序）都维护；小程序专属开关只在全站未维护时对 `wechat_mp` 生效。
- **审计**：`auditRequired:true`，开关变更进审计日志（沿用 `updateSettings` 审计）。

### 21.10 拍板记录（2026-07-05 定稿）

| # | 点 | 结论 |
|---|---|---|
| Q1 | 小程序维护识别 | ✅ **要求小程序前端统一带 `X-Platform: wechat_mp` 请求头**，配合 `Referer: servicewechat.com` 双保险；后端 `detectLoginPlatform` 优先取 `X-Platform` 头 |
| Q2 | 错误码 | ✅ **复用现有 `SYSTEM_MAINTENANCE`**（与全站维护同码，不新增码）；前端靠响应 `data.platform='wechat_mp'` 区分"仅小程序维护" |
| Q3 | 建设范围 | ✅ **本次只做微信小程序 `wechat_mp`**；中间件按 `platform → 配置 key` 映射实现，`app`/`douyin_mp`/`alipay_mp` 留可扩展点，暂不建对应开关 |

---

## 22. 二次求证校准（v1.5，2026-07-05 重新实连真实库 + 重读现网代码）

> 本节是**最新一轮**以后端数据库项目为唯一权威的实测校准，覆盖 §1–§21 的所有关键断言。求证手段与证据同 v1.5 头注。**结论优先级：§22 > §14–§21 > §1–§13。**

### 22.1 逐条实测复核结果（真实库/代码为准）

| 复核项 | 真实结果（本轮实测） | 对文档的影响 |
|---|---|---|
| `reward_multiplier_campaigns` / `reward_multiplier_targets` | ❌ 两表均不存在；代码库无任何 `reward_multiplier`/`CrystalMultiplier`/`crystal_multiplier` 引用 | 确认全新建，§18.1/§20 成立 |
| `material_asset_types`（16 条） | ✅ 6 shard + 6 gem + `points`(currency) + `budget_points`(quota,is_enabled=0) + `star_stone`(currency) + `star_stone_quota`(quota)。`form enum('shard','gem','currency','quota')` | 见 22.2 水晶判定需精确排除 currency/quota |
| `budget_value_points` 真实值 | ✅ 实测原值：red_shard=1、**red_gem=0（漏填）**、orange 10/100、yellow 20/200、green 40/400、blue 80/800、purple 160/1600；`star_stone`/`points`=0；`budget_points`=1；`star_stone_quota`=NULL | 定性为数据缺陷，**D-9 拍板订正 red_gem=0→10**（§22.8）；订正后全色系 gem=shard×10，成本封顶统一有效 |
| `account_asset_balances.asset_code` | ✅ 实测值全小写：`points`/`budget_points`/`red_core_shard`/`star_stone`/`star_stone_quota` | A6 成立，前端一律小写 |
| `account_asset_balances.lottery_campaign_id` | ✅ `varchar(50)`，实测值 `NULL` 与 `'CONSUMPTION_DEFAULT'`；另有 `lottery_campaign_key varchar(50) NOT NULL`(COALESCE 生成) | A14 成立，专属桶用字符串键（D-5） |
| `segment_rule_configs` | ✅ 主键 `version_key`(default/v1/v2/v3/v4)，`rules`(JSON) 内含 `segment_key`；5 条 `is_system=1 active`。`segment_key` 确非独立列 | A7 成立，人群模型按 §16.2/D-1 |
| `lottery_strategy_config` | ✅ 实测 `lottery_campaign_id=1, config_group='segment', config_key='resolver_version', config_value='v1'`（JSON）；一活动绑一个 resolver 版本 | A8 成立，D-1 语义正确 |
| `segment_field_registry.js` | ✅ 恰 5 字段：`created_at`/`last_active_at`/`history_total_points`/`user_level`/`consecutive_fail_count`；运算符 days_within/days_exceed/gte/lte/eq/neq/in/between | A8 成立，D-2 扩白名单确有必要 |
| `user_ad_tags` | ✅ 表在(`tag_key varchar(50)`/`tag_value varchar(100)`/`calculated_at`)，**实测 0 行** | A9 成立，D-3 需先接通标签生产，空则降级不命中 |
| `user_growth_levels` | ✅ 4 档 active：bronze(0)/silver(100000)/gold(500000)/diamond(2000000)，`level_key` 唯一 | A10 成立，直接可用 |
| `lottery_campaigns` | ✅ `lottery_campaign_id INT PK`；`budget_mode enum(user/pool/none)`、`preset_budget_policy enum(follow_campaign/pool_first/user_first)`、`pool_budget_total/remaining bigint`、`allowed_campaign_ids json`。实测活动 1 `allowed_campaign_ids=["CONSUMPTION_DEFAULT"]` | A11/A15/A16 成立，倍率表外键 INT |
| `lottery_draws` | ⚠️ `result_metadata`(json) 存在✅；**无 `material_amount` 列**（见 22.2 C1）；有 `prize_value_points`/`budget_points_before/after`/`points_deducted` | 修正 §1.1、A12 |
| `asset_transactions` | ✅ `meta`(json) + `idempotency_key varchar(100) UNIQUE` + `counterpart_account_id`(双录) | A13 成立 |
| `SettleStage.js` material 分支 | ✅ 实测 `case 'material':` 在**第 709 行**，`712` 起 `BalanceService.changeBalance({ asset_code: prize.material_asset_code, delta_amount: prize.material_amount, idempotency_key: '${idempotency_key}:material', counterpart_account_id: mintAccount.account_id, meta:{lottery_campaign_prize_id, prize_name} })`；star_stone 额外扣 quota | A2 成立，行号精确到 709 |
| `SettleStage.js` 扣积分 | ✅ 第 **206** 行 `asset_code: AssetCode.POINTS, delta_amount: -draw_cost` | A1 成立 |
| `CoreService.js` 预算硬编码 | ✅ 第 **403** 行 `lottery_campaign_id: 'CONSUMPTION_DEFAULT'`（`AssetCode.BUDGET_POINTS` 发放，`budget_allocation_ratio` 折算） | A17/B-6 成立，落点精确 |
| `ApiResponse.js` | ✅ `success(data,message,code='SUCCESS')` → `{success,code,message,data,timestamp,request_id}`；中间件注入 `res.apiSuccess/apiError/apiPaginated/apiCreated`。**`apiError(message, errorCode, details, httpStatus)`** | 见 22.3：§16.5 契约成立，但 §21.4 `res.apiError` 参数顺序需按真实签名 |
| `maintenanceMode.js` | ✅ 读 `basic/maintenance_mode`，`BYPASS_PREFIXES=[/health,/admin,/api/v4/auth,/api/v4/console,/api/v4/debug-control,/api/v4/system/app-version]`，fail-open，用 `res.apiError(msg,'SYSTEM_MAINTENANCE',data,503)` | §21 落点成立 |
| `platformDetector.js` | ✅ `detectLoginPlatform`：优先 `body.platform`/`headers['x-platform']`(白名单校验) → `Referer servicewechat.com` → UA。`VALID_PLATFORMS={web,wechat_mp,douyin_mp,alipay_mp,app}` | §21 Q1/Q3 成立 |
| `SegmentResolver` | ✅ `resolveSegmentAsync(version,user)`（先查 DB `SegmentRuleConfig` active 再回退内置）、`getSegmentKeys(version)`（**读内置 `SEGMENT_RULE_VERSIONS`**）；`_evaluateConditions` 按 priority 降序取首个命中 | 见 22.3 §16.2 选项来源修正 |
| admin 技术栈 | ✅ `alpinejs ^3.15.4` + `echarts ^6.0.0` + `tailwindcss ^3.4.19` + `vite ^6.4.1`；有 `wangeditor/konva/socket.io-client/xlsx/jspdf`。页面 `lottery-management/lottery-alerts/dashboard-panel/finance-management/reconciliation/risk-alerts/statistics/user-data-query/config-tools.html` 均在 | §13/§17.2 成立 |
| admin 是否有 camelCase 映射层 | ✅ 实测 `admin/src` **无** `toCamelCase/camelize/snakeToCamel/mapFields` 映射；已有 `admin/scripts/check-frontend-mappings.cjs`(黑名单扫描) + `npm run lint:mappings` | 修正 W-5：无映射层可删，只需继续用 snake_case + 跑黑名单校验 |
| system 端 §21 复用 | ✅ `system_settings` 有 `basic/maintenance_mode|message|end_time`(id 45/46/47)；`config.js` `BOOLEAN_KEYS` 含 `maintenance_mode`；但 `config-tools.js` 维护模式是**独立 `maintenanceForm` 手写表单**（saveMaintenanceMode 显式提交 3 个 key），非纯 BOOLEAN_KEYS 自动渲染 | 见 22.4：§21.5 需在 `maintenanceForm` 表单同款扩 3 字段，不能只加 BOOLEAN_KEYS |

### 22.2 关键修正项（前文有误，以此为准）

**C1（事实性错误）`lottery_draws` 无 `material_amount` 列**
- §1.1 表格"抽奖流水 … `material_amount` 展示字段"、A12"`material_amount` 展示列也在"**均为误述**。实测 `lottery_draws` 列中无 `material_amount`。
- 影响：§16.4 抽中水晶展示"红源晶碎片 ×20（基础 10 × 倍率）"的数据**只能来自 `result_metadata.crystal_multiplier`**（`base_quantity`/`applied_multiplier`/`final_quantity`），不得读/写不存在的 `lottery_draws.material_amount`。发放真实入账量仍走 `SettleStage` 的 `BalanceService.changeBalance(delta_amount=finalQuantity)`，`lottery_draws` 侧只落 `result_metadata` 快照。**方案其余逻辑不受影响**（本就规划写 result_metadata，无需新增列）。

**C2（已定性为数据缺陷，2026-07-05 拍板修数据）`red_core_gem` 原 `budget_value_points=0` 属漏填，订正为 10**
- 实测原值 `red_core_gem=0`、`red_core_shard=1`；全局规律为**每色系 `gem = shard × 10`**（orange 10/100、yellow 20/200、green 40/400、blue 80/800、purple 160/1600），唯 red 破例（`gem=0 < shard=1`，形态更高反而更便宜，业务不成立）。
- **拍板结论（D-9 定稿）**：`red_core_gem` 是**数据漏填**，按规律订正为 `red_shard(1) × 10 = 10`。修正后**全部 16 条水晶 `budget_value_points` 均非零且符合 ×10 规律**，`extra_cost = (final-base) × budget_value_points` 对**所有色系统一有效**。
- **连带简化（降技术债）**：因所有水晶成本口径都非零有效，**取消原拟的"双口径切换 / `per_draw_extra_cap` 数量兜底"复杂机制**——`extra_cost_limit` 单一成本封顶即可覆盖全部水晶。倍率表**不新增** `per_draw_extra_cap` 列（若未来出现真·0成本引流品再议）。
- **D-4 相应调整**：red 不再需要"默认排除 + 二次确认"，可与其它色系一视同仁（`crystal_all` 默认即含 red）；`reward_scope` 收窄能力保留给运营按需使用（非成本安全所必需）。

**C3（水晶判定表达式收敛）**
- 实测 `form` 仅 4 值 `shard/gem/currency/quota`。"水晶类"= `form IN ('shard','gem')`；`points`(currency)/`budget_points`(quota)/`star_stone`(currency)/`star_stone_quota`(quota) 自动排除。判定应显式 `form IN ('shard','gem')`，**无需再额外写"排除货币"**（货币本就不是 shard/gem），文档 §2.3/§4.1 措辞据此简化。

**C4（`event_points` 归类落地）**
- 实测 `material_asset_types` 无 `event_points`；`asset_group_defs` 有 `points`/`currency`/`system` 系统组。新增 `event_points` 建议 `form='quota'`、`is_tradable=0`、`is_enabled=1`、`group_code` 复用 `points` 组或新增 `event` 组（D-6 成立）。注意 `budget_points` 现 `is_enabled=0`，`event_points` 需 `is_enabled=1` 才能正常参与余额/展示。

**C5（`getSegmentKeys` 数据源一致性）**
- 实测 `SegmentResolver.getSegmentKeys(version)` 读的是**内置常量 `SEGMENT_RULE_VERSIONS`**，而 `resolveSegmentAsync` 运行时优先读**数据库 `segment_rule_configs`**。二者版本内容当前一致，但**属两个数据源**。
- 处置：§16.2 人群选择器的 segment 选项**应以数据库 `segment_rule_configs.rules[].segment_key` 为准**（与运行时命中同源），不要直接用 `getSegmentKeys`（内置），避免"选项列表与实际命中版本漂移"。后端选项 API 从 DB 读取该活动 resolver_version 对应 `version_key` 的 rules 提取 segment_key。

**C6（幂等 Header 现网范围）**
- 实测强制 `Idempotency-Key` Header（缺失 400）的是 **C 端抽奖 `routes/v4/lottery/draw.js`**；console 域各资源**未统一强制**该 Header（仅少数如 assets/transactions、asset-adjustment 使用）。
- 处置：§16.1 倍率规则写接口"强制 Idempotency-Key"是**新增要求**而非现网通例——落地时需在该 console 路由**自行接入幂等**（可复用现有幂等范式），不要默认 console 已全局强制。

### 22.3 §16 API 契约与现网 console 真实风格对齐（修正路径命名）

实测 console 路由的真实组织与 §16 原写法有出入，需按现网风格校准（**否则不符合后端现有技术体系**）：

**现网事实**：
- `routes/v4/console/index.js` 各域以 `router.use('/', require('./lottery'))` 挂载；`lottery/index.js` 内子资源用**扁平短横线命名**：`/lottery-campaigns`、`/lottery-configs`、`/lottery-quota`、`/lottery-tier-rules`、`/prize-definitions`、`/lottery-presets` 等（不是 `/lottery/xxx` 两级嵌套）。仅活动列表个别用 `/lottery/campaigns`。
- 鉴权：写/敏感操作用 `authenticateToken + requireRoleLevel(100)`（即 `adminAuthMiddleware`，`shared/middleware.js` 导出）；只读运营可 `requireRoleLevel(PERMISSION_LEVELS.OPS=30)`。**不是**每路由散写。
- 路由层通过 `req.app.locals.services.getService('xxx')`（snake_case key）拿 Service，**禁止直连 models**。
- 响应统一 `res.apiSuccess(data,message,code)` / `res.apiError(message,errorCode,details,httpStatus)` / `res.apiPaginated(data,pagination,message)`。

**据此修正 §16.1 路径**（改为与现网 `lottery-*` 扁平风格一致，新建文件 `routes/v4/console/lottery/multiplier-rules.js`，在 `lottery/index.js` 加 `router.use('/multiplier-rules', require('./multiplier-rules'))`）：

| 方法 | 修正后路径（现网风格） | 说明 |
|---|---|---|
| GET | `/api/v4/console/multiplier-rules` | 列表（`res.apiPaginated`），`?lottery_campaign_id=&status=&page=&page_size=` |
| GET | `/api/v4/console/multiplier-rules/:id` | 详情（含 targets） |
| POST | `/api/v4/console/multiplier-rules` | 创建（本路由自接幂等，见 C6） |
| PUT | `/api/v4/console/multiplier-rules/:id` | 更新 |
| PATCH | `/api/v4/console/multiplier-rules/:id/status` | 开关 active/inactive |
| DELETE | `/api/v4/console/multiplier-rules/:id` | 删除（级联 targets） |
| GET | `/api/v4/console/multiplier-rules/:id/cost` | 成本水位 |

**§16.2 人群选项**（同样扁平化 + 数据源按 C5）：
| 方法 | 修正后路径 | 返回 |
|---|---|---|
| GET | `/api/v4/console/lottery-campaigns/:id/segment-options` | 从 DB `segment_rule_configs`（该活动 resolver_version 对应 `version_key`）的 `rules[].segment_key` 提取（**非** `getSegmentKeys` 内置） |
| GET | `/api/v4/console/growth-levels` | `UserGrowthLevelService.listLevels()`（level_key/level_name/min_history_points） |
| GET | `/api/v4/console/ad-tags` | `user_ad_tags` 去重 `tag_key`（实测空 → 返回 `[]` + 提示） |

**§16.3 C 端合并倍率**：C 端抽奖域挂在 `routes/v4/lottery/`（`campaigns` 子路由 `router.use('/campaigns', ...)`）。路径 `GET /api/v4/lottery/campaigns/:code/multiplier` 与现网 `campaigns/:code/prizes`、`campaigns/:code/config` 风格一致，成立。

**§16.5 响应结构**：与实测 `ApiResponse` 完全一致（`success/code/message/data/timestamp/request_id`），成立。前端读 `data`。

### 22.4 §21（小程序维护开关）校准

- §21.4-2 中间件伪代码调用 `res.apiError(msg,'SYSTEM_MAINTENANCE',data,503)` 与真实签名 `apiError(message,errorCode,details,httpStatus)` **一致**，成立。
- §21.5 **需修正**：admin 维护模式**不是**靠 `BOOLEAN_KEYS` 自动渲染，而是 `config-tools.js` 里独立的 `maintenanceForm`（`loadMaintenanceMode`/`saveMaintenanceMode` 显式读写 `maintenance_mode`/`maintenance_message`/`maintenance_end_time` 三个 key）。因此"仅小程序维护"开关要在**同一个 `maintenanceForm` 表单**里扩出 `maintenance_mode_wechat_mp` 等 3 字段并在 `saveMaintenanceMode` 一并提交，而非仅把 key 加进 `BOOLEAN_KEYS`。
- §21.4-1 白名单新增 3 条配置：`system_settings` 表按现有 `basic/maintenance_*`(id 45-47) 同结构 `INSERT` 即可；但 `config/system-settings-whitelist.js` 是否为写入必经校验需在落地时确认（现网维护三项走的是 basic 分类 `updateSettings`）。

### 22.5 新增拍板项（本轮求证产生，需你确认）

| # | 事项 | 背景（实测） | 建议结论 |
|---|---|---|---|
| D-9 | **red_gem 成本锚点** | red_gem `budget_value_points` 原=0，破坏"gem=shard×10"全局规律（C2） | ✅ **已拍板（2026-07-05）：定性为数据缺陷，订正 `red_core_gem.budget_value_points=0 → 10`**。连带**取消双口径 / `per_draw_extra_cap`**，`extra_cost_limit` 单一成本封顶覆盖全部水晶；D-4 red 不再需默认排除，与其它色系一视同仁。**执行动作见 §22.8**（数据订正迁移） |
| D-10 | **倍率快照不落 `lottery_draws.material_amount`** | 该列不存在（C1） | 建议**执行**：发放量入账走 `BalanceService`，展示量只落 `result_metadata.crystal_multiplier`；不新增 `material_amount` 列（避免为纯展示动主流水表结构） |
| D-11 | **console 倍率写接口自接幂等** | console 未统一强制 Idempotency-Key（C6） | 建议**执行**：倍率 CRUD 写接口在路由内复用现有幂等范式接入 `Idempotency-Key`，与 C 端一致；不改造 console 全局 |
| D-12 | **segment 选项数据源统一为 DB** | `getSegmentKeys` 读内置、命中读 DB（C5） | 建议**执行**：选项 API 与命中判定同源读 `segment_rule_configs`，防漂移 |
| D-13 | **console 倍率路由命名采用现网 `multiplier-rules` 扁平风格** | console 现网扁平短横线命名（22.3） | 建议**执行**：用 `/api/v4/console/multiplier-rules`，不用 `/console/lottery/multiplier-rules` 两级嵌套，与 `lottery-campaigns` 等对齐 |

### 22.6 三方问题划分补充（本轮新增/修正条目）

| 端 | 编号 | 修正/新增 |
|---|---|---|
| 后端 | B-11 | `lottery_draws` 无 `material_amount`（C1/D-10）：展示量落 `result_metadata`，代码勿引用该列 |
| 后端 | B-12 | red_gem 成本=0 系数据漏填（C2/D-9）：**数据订正迁移** `red_core_gem.budget_value_points → 10`（§22.8）；成本封顶 `extra_cost_limit` 即覆盖全部水晶，无需数量兜底 |
| 后端 | B-13 | console 倍率写接口自接幂等（C6/D-11） |
| 后端 | B-14 | segment 选项 API 从 DB `segment_rule_configs` 取（C5/D-12） |
| 后端 | B-15 | 倍率 console 路由命名走 `/console/multiplier-rules` 扁平风格（22.3/D-13） |
| Web 前端 | W-5（修正） | admin **无 camelCase 映射层**可删；已用 snake_case，只需继续跑 `npm run lint:mappings`（黑名单扫描）保证不引入废弃短字段 |
| Web 前端 | W-6 | 小程序维护开关在 `config-tools.js` 的 `maintenanceForm` 扩 3 字段（22.4），非 BOOLEAN_KEYS 自动渲染 |
| 小程序 | M-1（修正） | 抽中展示读 `result_metadata.crystal_multiplier`（base/applied/final），**不存在** `material_amount` 字段可读 |

### 22.7 本轮求证总结（一句话）

重新实连真实库 + 重读现网代码后，方案**主体机制全部成立**（SettleStage:709 material 发放、CoreService:403 预算硬编码、material_asset_types 16 条水晶判定、segment resolver_version 语义、预算分桶/allowed_campaign_ids/pool_first、result_metadata 快照、ApiResponse/幂等/rbac、admin 技术栈与页面、维护中间件与平台识别）；**唯一事实性错误是 `lottery_draws.material_amount` 不存在（C1）**，另有 red_gem 成本=0（C2，已定性数据缺陷并拍板订正为 10）、console 路由命名与幂等范围（22.3/C6）、segment 选项数据源（C5）、admin 维护表单形态（22.4）等需按 §22 校准。D-9 已拍板（修数据 + 取消双口径，降技术债），D-10~D-13 为"现有技术栈内收敛"直接执行，**零新增框架**。

### 22.8 执行动作：red_gem 成本锚点数据订正（D-9 落地）

> 拍板 `red_core_gem.budget_value_points = 10`。此为**数据订正**，非结构变更，走事务化迁移，可回滚。

**迁移**（`migrations/<ts>-fix-red-core-gem-budget-value.js`，事务化 up/down，参照现网迁移风格）：

```sql
-- up：订正 red_core_gem 预算价值锚点（漏填 0 → 按 gem=shard×10 规律修正为 10）
UPDATE material_asset_types
   SET budget_value_points = 10, updated_at = NOW()
 WHERE asset_code = 'red_core_gem' AND budget_value_points = 0;

-- down：回滚为原值 0
UPDATE material_asset_types
   SET budget_value_points = 0, updated_at = NOW()
 WHERE asset_code = 'red_core_gem';
```

**订正后全量校验**（应满足每色系 gem=shard×10）：

| 色系 | shard | gem | 校验 |
|---|---|---|---|
| red | 1 | **10**（订正后） | 1×10 ✅ |
| orange | 10 | 100 | ✅ |
| yellow | 20 | 200 | ✅ |
| green | 40 | 400 | ✅ |
| blue | 80 | 800 | ✅ |
| purple | 160 | 1600 | ✅ |

**连带从施工清单移除的项**（因不再需要）：
- ~~`reward_multiplier_campaigns.per_draw_extra_cap` 列~~（取消）
- ~~`CrystalMultiplierService` 双口径（预算/数量）自动切换分支~~（取消，只保留 `extra_cost = (final-base) × budget_value_points` 单一预算口径）
- ~~D-4 "默认排除 red + 二次确认" UI~~（取消，red 与其它色系一视同仁；`reward_scope` 收窄仍保留为运营可选项）

**质量校验**：迁移后跑 `npm run migration:verify`，并以只读查询确认 6 色系 gem/shard 比值均为 10。

---

## 23. 实施落地状态与最终契约（2026-07-06 回写，微信小程序对接权威）

> 本节回写"设计 → 真实实施"的最终结果。后端数据库项目与 Web 管理后台前端均已实施完毕并真实验证：
> 3 个新迁移已在 `restaurant_points_dev` 执行、服务/路由已上线、`CrystalMultiplierService`/`RewardMultiplierService`/`EventBudgetService` 单测 50/50 通过、真实 API 冒烟通过、`npm run lint` 0 错误、PM2 重启后 `/health` 健康。
> **凡与 §1–§22 冲突处，以本节为最终权威。微信小程序前端只需读本节即可对接。**

### 23.1 后端数据库项目：实际落地清单（与设计的差异已标注）

| 类别 | 实际落地 | 与设计差异 |
|---|---|---|
| 迁移 | `20260706010001`(red_gem→10)、`20260706010002`(倍率两表)、`20260706010003`(event_points 资产)、`20260706060000`(归集规则表)、`20260706060100`(小程序维护 3 项 seed)，均已执行 | 归集规则用**独立表**（见 23.2），非设计中"仅改 CoreService" |
| 模型 | `RewardMultiplierCampaign`/`RewardMultiplierTarget`/`EventBudgetCollectionRule`，`models/index.js` 已注册 | 倍率子表主键实际为 `reward_multiplier_target_id`（非设计的 `id`） |
| 服务 | `CrystalMultiplierService`(运行时计算)、`RewardMultiplierService`(管理 CRUD)、`EventBudgetService`(归集规则 CRUD + 归集判定 + 到期清零)，均 ServiceManager 注册（键 `crystal_multiplier`/`reward_multiplier`/`event_budget`） | 新增 `EventBudgetService` 承载归集与清零，设计未拆此服务 |
| 结算接入 | `SettleStage.js` material 分支已接 `CrystalMultiplierService.resolveMultiplier`，写 `result_metadata.crystal_multiplier` + `asset_transactions.meta` | 一致 |
| 归集重定向 | `CoreService.js` 预算分配处已接 `EventBudgetService.resolveCollectionTarget`：命中→`EVENT_<code>` 桶 + 发 `event_points`；未命中→`CONSUMPTION_DEFAULT` | 一致（落点从"403 行"改为逻辑块，行号已变） |
| 到期清零 job | `jobs/daily-event-budget-expiry.js`（每日 01:00，`ENABLE_EVENT_BUDGET_EXPIRY=true`），`app.js` cron worker 已接入 | 一致 |
| 分桶资产 | `BalanceService` 分桶判断扩为 `budget_points` + `event_points`（`isCampaignBucketedAsset`），`AssetCode.EVENT_POINTS='event_points'` | 设计只提 budget_points 分桶，实施统一收口 |
| 幂等白名单 | `CANONICAL_OPERATION_MAP` 注册 `/api/v4/console/multiplier-rules → ADMIN_MULTIPLIER_RULE_CREATE`（D-11 落地） | 设计未提此注册（写接口白名单强制，必须注册否则 500） |
| D-2 白名单 | `segment_field_registry.js` 最终补 **`login_count`（次数）+ `last_login`（最后登录时间）** 两字段；`TierPickStage`/`CrystalMultiplierService` 取数已补齐全白名单字段 | 设计只说"按需评估"，最终确定为这 2 个字段 |

### 23.2 归集规则独立表 `event_budget_collection_rules`（设计新增落地）

设计 §12.10/§18.4 只说"改 CoreService 查归集规则"，未给规则存储载体。实施时新建独立配置表承载（运营在 Web 后台配置）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `collection_rule_id` | BIGINT PK | 归集规则主键 |
| `lottery_campaign_id` | INT FK | 归集去向活动（消费预算全额进 `EVENT_<该活动campaign_code>` 专属桶） |
| `rule_name` | VARCHAR(100) | 规则名（对内） |
| `store_ids` | JSON | 命中门店ID数组（NULL=不限） |
| `merchant_ids` | JSON | 命中商家ID数组（NULL=不限） |
| `event_points_ratio` | DECIMAL(10,4) | 活动积分发放比率（`event_points = round(消费金额 × 比率)`，0=只归集预算不发代币） |
| `start_at`/`end_at` | DATETIME | 时间窗（NULL=对齐活动 start_time/end_time） |
| `priority`/`status`/`remark` | - | 优先级/开关/备注 |

- 命中规则：`status=active` + 时间窗覆盖 + 活动进行中 + 门店/商家匹配（NULL=不限），多命中取最高 `priority`。
- 专属桶键运行时派生 `EVENT_<campaign_code>`（D-5），不冗余存储。

### 23.3 微信小程序对接最终契约（★★★ 前端只看这一节，覆盖 §16.3/§16.4）

> 以下路径/参数名/字段位置为**真实上线接口实测结果**，与 §16 早期设计有出入处一律以此为准。响应统一结构见 §16.5（`{success,code,message,data,timestamp,request_id}`），前端读 `data`。asset_code 全小写。

**接口 1：抽奖前查"当前合并倍率"（展示 ×N 角标）**

- `GET /api/v4/lottery/campaigns/:code/multiplier`（需登录）
- ⚠️ 路径参数是 **`:code`（活动业务码 campaign_code）**，不是 §16.3 写的 `:campaign_code`。
- 响应 `data`：

```json
{
  "lottery_campaign_id": 1,
  "applied_multiplier": 2.0,
  "display_name": "新春水晶翻倍",
  "end_at": "2026-02-15 23:59:59",
  "active": true
}
```

- `active=false` 且 `applied_multiplier=1` 时前端**不展示**倍率角标（§5.2 日常态）。

**接口 2：抽奖结果读倍率（展示"基础×倍率=最终"）**

- `POST /api/v4/lottery/draw`（需登录，Header `Idempotency-Key` 必填）
- ⚠️ 倍率数据在**每个奖品对象上直接暴露**（不必只从 `result_metadata` 里挖）。`data.prizes[]` 每个元素含：

```json
{
  "reward_tier": "high",
  "prize_name": "红源晶碎片",
  "prize_type": "material",
  "material_asset_code": "red_core_shard",
  "material_amount": 20,
  "crystal_multiplier": {
    "base_quantity": 10,
    "applied_multiplier": 2.0,
    "final_quantity": 20,
    "reason": "新春水晶翻倍 ×2"
  },
  "image": { "url": "..." }
}
```

- `material_amount` 已是**翻倍后最终数量**；`crystal_multiplier` 命中翻倍时非空、未翻倍/非水晶时为 `null`（前端据此决定是否展示"（基础 10 × 新春翻倍 2 倍）"）。

**接口 3：查"活动积分"余额（活动内露出 + 到期倒计时，§16 原缺此接口）**

- `GET /api/v4/lottery/campaigns/:code/event-points`（需登录）
- 活动积分 `event_points` 是 `form=quota` 资产，**主钱包 `/assets/balances` 不返回**（仅活动内露出，§12.7 拍板）；本接口是 C 端查询该余额的**唯一入口**。
- 响应 `data`：

```json
{
  "lottery_campaign_id": 1,
  "asset_code": "event_points",
  "available_amount": 120,
  "campaign_end_time": "2026-12-08 23:59:00"
}
```

- `available_amount` 为该活动专属桶余额（从未获得=0）；`campaign_end_time` 供前端显示到期倒计时（活动结束后每日任务清零，防2）。

**接口 4：小程序维护开关（§21，与倍率无关但同期上线）**

- 小程序所有请求带 Header `X-Platform: wechat_mp`。
- 命中维护时返回 HTTP 503 + `code=SYSTEM_MAINTENANCE`，`data.platform='wechat_mp'` 区分"仅小程序维护"；`data.message`/`data.end_time` 用于维护页展示。已实测：开关开→小程序 503、Web 端正常；关→恢复 200。

### 23.4 Web 管理后台前端：实际落地（admin/）

- 新页 `admin/multiplier-management.html` + `src/modules/lottery/pages/multiplier-management.js` + `src/api/lottery/multiplier.js`（Tab：倍率规则 / 预算归集规则；含人群选择器 segment/tag/growth_level/user、成本水位进度条 + 成本详情弹窗）。
- 侧边栏「回馈运营」加「✨ 水晶倍率活动」入口；`permission-rules.js` 页面权限 minLevel 100；`vite.config.js` 已注册页面。已 `npm run build` 重新构建，`dist/multiplier-management.html` 已产出。
- §21 小程序维护开关：`config-tools.html` + `config-tools.js` 的 `maintenanceForm` 扩了 3 个 `*_wechat_mp` 字段 + `saveWechatMpMaintenance()`。
- 管理端 console 路由（扁平命名，与 §22.3/D-13 一致）：`/api/v4/console/multiplier-rules`(CRUD + `:id/cost`)、`/api/v4/console/ad-tags`、`/api/v4/console/lottery-campaigns/:id/segment-options`、`/api/v4/console/event-budget-collection-rules`(CRUD)。

### 23.5 已完成 vs 仍未完成清单（施工收尾对照）

**已完成（后端 + admin，真实验证通过）**：倍率两表 + 归集规则表 + `event_points` 资产、三个服务、SettleStage 接入、归集重定向 + 发 event_points、到期清零 job、console 全部 CRUD/成本/人群选项 API、C 端 3 个接口（multiplier/draw 倍率字段/event-points）、§21 小程序维护开关、D-2 白名单、admin 倍率管理页 + 归集配置 + 成本水位、单测 50/50。

**仍未完成（需你后续拍板或另派）**：§23.5 原列 1~5 项**已于 2026-07-06 第二轮全部落地并真实验证**（见 §23.6），仅剩：
- 微信小程序端 M-1~M-6 由小程序仓库按 §23.3 + §23.6 对接（后端契约已就绪）。

## 23.6 第二轮实施落地（2026-07-06，后端 + admin 补齐 §23.5 遗留 1~5 项，真实验证）

> 本轮补齐 §23.5 遗留的 5 项后端/admin 缺口，均已连真实库 `restaurant_points_dev` 验证、`npm run lint` 0 错误、
> 服务单测 62/62 + 引擎相关 93/93 通过、真实抽奖冒烟通过（points 55900→55870）、`/health` 健康。
> **凡涉及微信小程序对接的入场资产/扣费口径，一律以本节为最终权威（优先级：§23.6 > §23.3 > §23）。**

### 23.6.1 遗留项①落地：event_points 消耗链路（双层货币可见层真正闭环）

- **新增字段** `lottery_campaigns.entry_asset_code`（VARCHAR(50) NOT NULL DEFAULT `'points'`，迁移 `20260706070000`）：
  - `points`（默认，常驻活动）：抽奖扣全局可见积分 `points`，语义与历史完全一致。
  - `event_points`（限时稀缺翻倍活动）：抽奖扣活动专属可见代币 `event_points`，按 `EVENT_<campaign_code>` 桶隔离、到期清零。
- **扣费链路改造**（真实落点）：`services/UnifiedLotteryEngine/UnifiedLotteryEngine.js`（连抽外层统一扣费 + 入场资产充足性预检查）
  与 `services/UnifiedLotteryEngine/pipeline/stages/SettleStage.js`（单抽直扣路径）均改为按 `entry_asset_code` 扣对应资产；
  扣费资产码与分桶键统一由 `EventBudgetService.resolveEntryAsset(campaign)` 收口（不再硬编码 `AssetCode.POINTS`）。
- **运营配置**：admin 活动新建/编辑表单新增「入场资产」下拉（`admin/lottery-management.html` + `campaigns.js`）；
  后端 `CRUDService.createCampaign/updateCampaign` 校验 `entry_asset_code ∈ {points, event_points}`。
- **小程序影响**：抽奖扣费资产由活动配置决定；限时活动余额不足时报错码 `ENGINE_INSUFFICIENT`，
  文案含「活动积分不足」。小程序展示活动积分余额仍走 §23.3 接口 3（`GET /api/v4/lottery/campaigns/:code/event-points`）。

### 23.6.2 遗留项②③④落地：监控看板 + 客服解释 + 运维视图（后端聚合 API + admin）

新增 console 只读聚合 API（`requireRoleLevel(100)`，路由 `routes/v4/console/lottery/multiplier-rules.js` 与 `event-budget-collection-rules.js`）：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v4/console/multiplier-analytics/event-points-trend?days=` | 活动积分发放/消耗按日趋势（§13.2 折线图） |
| GET | `/api/v4/console/multiplier-analytics/event-points-overview` | 活动积分概览（发放/消耗/在途/到期清零量/持有人数） |
| GET | `/api/v4/console/multiplier-analytics/budget-distribution?asset_code=` | 个人活动账户余额分布（§13.2 防套利监控，柱状图分档） |
| GET | `/api/v4/console/multiplier-user-explain?user_id=&lottery_campaign_id=&limit=` | 客服"为什么翻/没翻"（§13.3，读 `result_metadata.crystal_multiplier` 快照） |
| GET | `/api/v4/console/event-budget-collection-rules/expiry-clear-history?days=` | 到期清零执行历史（§13.4 运维：`enabled`/日清零笔数与量） |

- admin `multiplier-management.html` 新增「监控看板」Tab：ECharts 趋势图 + 余额分布图 + 概览卡片 + 到期清零运维表 + 客服查询。
- 聚合数据源：`asset_transactions`（event_points 收支）、`account_asset_balances`（在途/分布）、`lottery_draws.result_metadata`（客服解释）——均真实数据，空数据直接暴露空白不隐藏。

### 23.6.3 遗留项⑤落地 + 标签生产修复

- **§21 维护中间件 Jest**：新增 `tests/middleware/maintenance_mode_middleware.test.js`（9 用例：白名单放行/全站维护/仅小程序维护 `data.platform=wechat_mp`/web 放行/fail-open），全通过。
- **标签生产（tag 定向数据源）根因修复**：`services/AdTagAggregationService.js` 原有两处 schema 不符导致标签聚合 100% 失败、`user_ad_tags` 恒空：
  - `AccountAssetBalance` 按 `user_id`+`balance` 直查（真实表按 `account_id` 存、字段 `available_amount`）→ 改走 `BalanceService.getBalance({user_id,asset_code})`。
  - `LotteryDraw.draw_time` 列不存在（真实为 `created_at`）→ 修正。
  - 修复后真实执行：13/13 活跃用户成功、产出 130 条真实标签（凌晨 3 点 `jobs/ad-cron-jobs.js` 已调度、`ENABLE_AD_CRON_JOBS=true`）。tag 定向自此有真实数据源；无标签时倍率 tag 命中仍安全降级为不命中（×1）。


