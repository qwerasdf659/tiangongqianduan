# 微信小程序前端 - 多活动抽奖系统实施清单

> **项目**: 天工餐厅积分抽奖微信小程序
> **技术栈**: TypeScript + WXML + SCSS + 微信小程序API + MobX
> **依据文档**: 《多活动抽奖系统-三端协作方案》+《抽奖UI设计方案-行业对比与技术可行性分析》
> **创建日期**: 2026-02-11

---

## 目录

1. [文档关系与总体思路](#1-文档关系与总体思路)（含 [1.3 架构决策：渲染器选择](#13-架构决策渲染器选择)、[1.4 架构决策：分包隔离方案](#14-架构决策16个子组件的加载策略)、[1.5 架构决策：多实例状态隔离](#15-架构决策多实例状态隔离)、[1.6 架构决策：Mock数据打包策略](#16-架构决策mock数据打包策略)、[1.7 架构决策：特殊玩法API模型](#17-架构决策特殊玩法api模型)、[1.8 架构决策：模板路由方案](#18-架构决策模板路由方案)）
2. [现有代码现状分析](#2-现有代码现状分析)
3. [第一期：组件化重构 + 增强现有九宫格](#3-第一期组件化重构--增强现有九宫格)
4. [第二期：中奖动画增强](#4-第二期中奖动画增强)
5. [第三期：新增玩法组件](#5-第三期新增玩法组件)
6. [第四期：多活动多页面](#6-第四期多活动多页面)
7. [明确不做的事项](#7-明确不做的事项)
8. [目标文件结构](#8-目标文件结构)
9. [实施顺序总览](#9-实施顺序总览)
10. [前端模拟数据方案](#10-前端模拟数据方案)

---

## 1. 文档关系与总体思路

### 1.1 两份源文档的定位

| 文档 | 定位 | 核心结论 |
|------|------|---------|
| 《抽奖UI设计方案》 | 分析调研 | 九宫格保持固定8格（行业标准），重点做**稀有度光效**和**卡牌翻转动画** |
| 《三端协作方案》 | 架构实施 | 把抽奖拆成**万能组件**，支持多种玩法、多活动、多页面投放 |

两份文档互补：UI设计方案的结论（保持8格、加光效、加卡牌翻转）是三端协作方案中九宫格组件的设计依据。

### 1.2 总体思路

```
当前状态：
  pages/lottery/lottery.ts（约2600+行，所有逻辑混在一起）
  lottery.wxml 九宫格8个奖品位写死（prizes[0]-prizes[7]）
  campaign_code 写死为 'BASIC_LOTTERY'
  无稀有度视觉区分、无主题色、无多活动支持

目标状态：
  lottery-activity 万能组件（任意页面一行代码嵌入）
  多种抽奖子组件（九宫格/转盘/卡牌/金蛋/刮刮卡/盲盒/扭蛋/福袋/摇一摇/翻牌/拆红包/老虎机/打地鼠/弹珠机/集卡/限时秒杀）
  稀有度光效 + 主题色系统 + 多种中奖动画
  多活动同时运行，分布在不同页面
```

### 1.3 架构决策：渲染器选择

> **决策日期**: 2026-02-11
> **决策结论**: 选择方案A — 全局 WebView，清理 Skyline 残留配置

**问题背景**：`app.json` 中原有 `rendererOptions.skyline` 配置残留（已清理），项目实际运行在 WebView 上。此决策明确渲染器策略为全局 WebView，不采用 Skyline。

**Skyline 是什么**：微信自研的原生渲染引擎，绕过浏览器 WebView，用原生组件直接绘制 UI。卖点是长列表滚动更流畅（worklet 动画独立线程）、原生瀑布流组件 `masonry-layout`、共享元素过渡动画、理论上更快的首屏渲染。

**备选方案**：

| 方案 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| A：全局 WebView | 清理 `rendererOptions.skyline` 配置，保留 `glass-easel` | CSS 兼容性完整、Canvas 可在开发者工具调试、零迁移成本 | 无法使用 Skyline 原生瀑布流和 worklet 动画 |
| B：全局 Skyline | 添加 `"renderer": "skyline"` 全面启用 | 长列表性能理论更优、原生瀑布流组件 | CSS 限制多、Canvas 无法开发者工具调试、3D transform 未文档化 |
| C：混合模式 | 部分页面 Skyline、部分 WebView | 按需使用各自优势 | 两套渲染器维护成本翻倍、页面切换有额外开销 |

**选A的理由**：

1. **16种抽奖玩法全部与 Skyline 不兼容**：
   - Canvas 类玩法（转盘、刮刮卡、老虎机、弹珠机、砸金蛋等）：Skyline 下 Canvas 2D 能运行但无法在开发者工具调试，只能连真机。16种玩法的开发调试量很大，全程真机调试严重拖慢开发效率
   - CSS 动画类玩法（翻牌、盲盒开箱、红包雨、扭蛋机）：需要 `rotateY`、`preserve-3d`、`backface-visibility` 等 CSS 3D transform，Skyline 对此没有明确文档支持，属于灰色地带，随时可能在某个基础库版本出问题
   - 视觉效果类（霓虹灯光效、发光按钮、卡片层叠阴影）：Skyline 只支持单个 `box-shadow`，多重阴影效果会被削弱
2. **商品瀑布流不需要 Skyline**：exchange 和 market 页面的瀑布流已用 JS 计算绝对定位实现，运行在 WebView 上无性能问题。要用 Skyline 的原生 `masonry-layout` 反而需要重写布局逻辑
3. **数据量有分页控制**：exchange 分页加载、market 无限加载均有分页，不会出现需要 Skyline 优化的超长列表场景
4. **行业实践**：美团/阿里/游戏公司/活动策划公司 95%+ 使用 WebView，只有腾讯自家应用激进采用 Skyline。Skyline 社区踩坑资料少，出问题排查困难
5. `glass-easel` 组件框架与渲染器无关，保留它不影响 WebView 运行，且是微信官方推荐的组件框架

**实施要点**：

- ~~删除 `app.json` 中的 `rendererOptions.skyline` 配置块~~ ✅ 已完成
- 保留 `componentFramework: "glass-easel"`（这是组件框架，不是渲染器）
- 不需要任何页面级别的渲染器声明（WebView 是默认值）

### 1.4 架构决策：16个子组件的加载策略

> **决策日期**: 2026-02-11
> **决策结论**: 选择方案A — 分包隔离（`packageLottery` 独立子包）

**问题背景**：`lottery-activity` 万能组件在 `lottery-activity.json` 的 `usingComponents` 中注册了16个子组件（grid/wheel/card/egg/scratch/blindbox/gashapon/luckybag/shake/flipmulti/redpacket/slotmachine/whackmole/pinball/cardcollect/flashsale）。小程序 `usingComponents` 声明的组件无论是否使用都会加载，用户只看九宫格也会加载全部16个子组件的代码。

**备选方案**：

| 方案 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| A：分包隔离 | 把 `lottery-activity` 整体放进 `packageLottery` 独立子包，利用分包 + 分包预下载隔离体积 | 简单可靠、全行业验证、兼容所有基础库版本 | 整个抽奖模块变成子包 |
| B：动态组件 | 用 `<component is="{{name}}" />` 按需加载子组件 | 更精细的按需加载 | 需基础库 2.23.4+、调试困难、行业采用率 <5% |

**选A的理由**：

1. 全行业（美团/腾讯/阿里/游戏公司/活动策划公司）95%+ 的项目使用分包方案，踩坑资料多，出问题好排查
2. 分包预下载配合后，用户从首页进抽奖模块体感几乎无延迟
3. 不依赖高版本基础库，兼容性最好
4. 用户进了抽奖页大概率会用到多个组件，方案B的包内按需加载收益很小
5. 活动模块天然适合独立分包，与主包业务边界清晰

**实施要点**：

- `lottery-activity` 组件及全部子组件（sub/ + shared/ + themes/ + mock/）整体放入 `packageLottery/` 子包
- `app.json` 中配置 `subpackages` 和 `preloadRule`（从首页/抽奖入口预下载）
- 主包 `pages/lottery/lottery.json` 通过分包引用组件路径

### 1.5 架构决策：多实例状态隔离

> **决策日期**: 2026-02-11
> **决策结论**: 选择方案A — 组件内部 data 管理运行时状态，lotteryStore 降级为缓存/预加载层

**问题背景**：现有 `store/lottery.ts` 是单例 MobX Store，同一页面放两个 `lottery-activity` 组件会共享状态（prizes、isDrawing 互相打架）。

**备选方案**：

| 方案 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| A：组件内部 data | 万能组件用自身 `data` 管理运行时状态，lotteryStore 降级为缓存/预加载 | 天然隔离、组件销毁自动清理、行业标准做法 | 需要把 store 逻辑搬到组件，改动较大 |
| B：Store 改 Map | lotteryStore 改成 `Map<campaignCode, state>` 按 key 隔离 | 组件层改动小 | key 管理复杂、需手动清理防内存泄漏、同 key 跨页面仍会冲突 |

**选A的理由**：

1. 全行业（美团/腾讯/阿里/游戏公司/活动策划公司）90%+ 采用组件内部 data 管理运行时状态，Store 只用于跨页面共享数据（用户信息、积分余额）
2. 天然隔离，不可能状态串台——游戏行业对此极其敏感，两个抽卡池状态串了是致命 bug
3. 组件销毁时 data 自动清理，不用操心内存泄漏；方案B需要手动 `store.delete(key)`
4. 调试体验好，DevTools 直接看组件 data 即可，不用展开 Map 找 key
5. 现有 `lotteryStore` 里真正需要跨页面共享的只有用户积分余额（已在 `pointsStore` 里），抽奖运行时状态无跨页面需求

**不删除 lotteryStore 的理由**：

项目未上线，理论上可以直接删除 `store/lottery.ts`。但选择保留降级而非删除：

1. `lotteryStore` 缓存活动配置列表可避免多个组件实例重复请求同一活动配置，降级后仍有实际价值
2. 保留 store 文件为未来可能的跨页面共享需求（如"上次抽奖结果"通知栏）留出扩展点，成本仅为维护一个轻量文件
3. 与 `userStore` / `pointsStore` / `exchangeStore` / `tradeStore` 保持一致的 5 store 架构，认知模型统一

**实施要点**：

- `lottery-activity.ts` 的 `data` 管理全部运行时状态：prizes、isDrawing、highlightIndex、drawResult、displayMode、displayConfig 等
- `lotteryStore` 降级职责：缓存活动配置列表、预加载奖品数据（可选优化）。不删除，保留为轻量缓存层
- `pointsStore` / `userStore` 保持不变，组件通过 MobX observer 读取积分余额和用户信息
- 子组件（sub/grid、sub/wheel 等）通过 properties 接收父组件传入的数据，不直接访问任何 store

### 1.6 架构决策：Mock数据打包策略

> **决策日期**: 2026-02-11
> **决策结论**: 选择方案A — 运行时 `USE_MOCK` 开关，mock 文件随代码打包，上线前改为 `false`

**问题背景**：文档方案是运行时 `if (USE_MOCK)` 判断数据来源，mock 文件会进入生产包。是否需要构建时物理剔除？

**备选方案**：

| 方案 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| A：运行时开关 | `USE_MOCK = false` 即可，mock 文件留在包里 | 零成本、已实现、不引入构建复杂度 | mock 数据约 6KB 留在生产包 |
| B：构建时剔除 | 引入 gulp/自定义脚本在打包时物理删除 mock 目录 | 包体积完全干净 | 需要搭建构建管线，增加维护成本和出错风险 |

**选A的理由**：

1. 项目是原生微信小程序，没有现成构建管线，为 6KB 数据引入 gulp/构建脚本投入产出比太低
2. mock 数据总计约 6KB（压缩后），占 `packageLottery` 分包 2MB 上限的 0.3%，完全可忽略
3. mock 文件在 `packageLottery` 分包内，不占主包体积
4. `USE_MOCK = false` 加进上线 checklist，比维护构建脚本更可靠
5. 未来项目规模膨胀需要构建管线时，再升级到方案B也很容易

**实施要点**：

- `mock-switch.ts` 加醒目注释提醒上线前修改：
  ```typescript
  // ⚠️ 上线前必须改为 false ⚠️
  export const USE_MOCK = true
  ```
- 上线 checklist 中增加一项：确认 `USE_MOCK = false`

### 1.7 架构决策：特殊玩法API模型

> **决策日期**: 2026-02-11
> **决策结论**: 选择方案A — 万能组件统一处理标准流程，3个特殊玩法子组件自己调额外API

**问题背景**：16种玩法中，13种使用标准 `performLottery(code, count)` 即可，但集卡/秒杀/打地鼠需要额外API：

| 特殊玩法 | 额外需要的API | 与标准流程的差异 |
|---------|-------------|----------------|
| 集卡 | `getCollectionProgress()` 查进度、`redeemCollection()` 兑换大奖 | 抽卡用标准API，但有额外的进度查询和兑换流程 |
| 秒杀 | `getRealTimeStock()` 实时库存、`rushBuy()` 抢购 | 完全不用 `performLottery`，是先到先得模型 |
| 打地鼠 | `submitGameScore(score)` 提交得分换奖品 | 完全不用 `performLottery`，是游戏计分模型 |

**备选方案**：

| 方案 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| A：子组件自己调特殊API | 万能组件只管标准流程，3个特殊子组件自己 import API 调用 | 简单直接、万能组件保持简洁、调试容易 | 3个子组件不纯粹，会直接调API |
| B：父组件提供扩展钩子 | 万能组件设计 onBeforeDraw/onCustomAction 等钩子协议 | 所有API调用统一经过父组件 | 钩子设计复杂、3种特殊交互差异大难以统一覆盖、过度设计 |

**选A的理由**：

1. 16种玩法只有3种（18.75%）需要额外API，为此设计通用钩子系统是过度设计
2. 这3种玩法的额外API差异极大（查进度/实时库存/提交得分），无法用一套钩子优雅覆盖
3. 与决策1.5（组件内部data管理状态）天然配合——子组件既然已经自治管理状态，自己调API也顺理成章
4. 全行业（美团/腾讯/游戏公司/活动策划公司）90%+采用此模式：父组件管通用流程，特殊交互子组件自治

**实施要点**：

万能组件 `lottery-activity.ts` 统一处理（所有玩法通用）：
- `loadConfig(campaignCode)` — 加载活动配置
- `loadPrizes(campaignCode)` — 加载奖品列表
- `performDraw(campaignCode, count)` — 13种标准玩法的抽奖调用
- 通过 properties 把配置和奖品传给子组件

特殊子组件自己额外处理：
- `sub/cardcollect/cardcollect.ts` — 自己调 `getCollectionProgress()` 和 `redeemCollection()`
- `sub/flashsale/flashsale.ts` — 自己调 `getRealTimeStock()`（轮询）和 `rushBuy()`（替代 performDraw）
- `sub/whackmole/whackmole.ts` — 自己调 `submitGameScore(score)`（替代 performDraw）

### 1.8 架构决策：模板路由方案

> **决策日期**: 2026-02-11
> **决策结论**: 接受 wx:elif 条件渲染方案，不加 JS 映射表，wxml 末尾用 `wx:else` 兜底默认九宫格

**问题背景**：`lottery-activity.wxml` 中 16 个 `wx:elif` 根据 `display.mode` 条件渲染子组件，每新增一种玩法需要改 wxml 加一行。是否需要用动态组件替代？是否需要额外的 JS 映射表？

**结论**：elif 不是硬编码问题。运营动态调整的能力由后端 `display.mode` 字段保证——运营在后台改 mode 值，前端自动渲染对应组件。elif 只是"前端支持哪些 mode"的声明。新增全新玩法无论用什么方案都需要前端写新组件代码并发版，elif 不是瓶颈。

**不加映射表的理由**：
- wxml 的 elif 链本身就是 mode 的完整声明，映射表是重复信息
- wxml 末尾 `wx:else` 兜底默认九宫格，已能处理后端返回不认识的 mode，效果与映射表校验一致
- 不加映射表少维护一处，新增玩法改动集中在 wxml + json 两处即可

**新增玩法 checklist**：① 开发子组件 ② wxml 加一行 elif ③ json 注册组件

**不选动态组件的理由**：
- 决策 1.3 已选分包隔离方案，与动态组件互斥
- 动态组件需基础库 2.23.4+，分包方案兼容所有版本
- 行业采用率 <5%，elif 方案 85%+

---

## 2. 现有代码现状分析

### 2.1 文件结构

```
pages/lottery/
├── lottery.ts            ← 主逻辑（约2600+行）
├── lottery.wxml          ← 页面模板
├── lottery.scss          ← 页面样式
├── lottery.json          ← 页面配置
└── lottery-config.ts     ← 转盘技术参数（wheelSize:8, canvasSize:600等）
```

**尚无** `components/lottery-activity/` 或任何抽奖子组件目录。

### 2.2 写死的位置（需要改造的3处）

| 位置 | 文件 | 写死内容 | 行号参考 |
|------|------|---------|---------|
| WXML模板 | lottery.wxml | `prizes[0]` 到 `prizes[7]` 共8个重复的模板块 | 第122-284行 |
| JS动画 | lottery.ts | `(currentIndex + 1) % 8` 高亮循环写死8格 | 第1659行 |
| 常量 | constants.ts | `LOTTERY.GRID_SIZE = 9` | 第52行 |
| 技术配置 | lottery-config.ts | `wheelSize: 8`, `anglePerSlice: 45` | 第10-12行 |
| API调用 | lottery.ts | `performLottery('BASIC_LOTTERY', count)` 写死campaign_code | 第1565行 |

### 2.3 已有的可复用基础

| 内容 | 说明 |
|------|------|
| `rarity` 字段已读取 | lottery.ts:726 已有 `rarity: prize.rarity \|\| 'common'`，但无视觉区分 |
| 奖品100%后端获取 | `loadLotteryData()` 从API动态加载，无硬编码奖品 |
| 连抽+逐个揭晓 | `revealNextPrize()` 已实现多连抽逐个揭晓逻辑 |
| MobX状态管理 | 已接入 userStore / pointsStore / lotteryStore |
| 转盘技术参数 | lottery-config.ts 已有Canvas绘制参数（但尚未实现转盘组件） |

### 2.4 100%可复用不改一行的部分

| 内容 | 文件 | 说明 |
|------|------|------|
| API调用层 | `utils/api.ts` | getLotteryConfig、performLottery等方法不变 |
| 认证检查 | `utils/auth-helper` | checkAuth逻辑不变 |
| 工具函数入口 | `utils/index.ts` | 导出方式不变 |
| 环境配置 | `config/env.ts` | API地址不变 |
| 常量定义 | `config/constants.ts` | UI常量不变（LOTTERY.ANIMATION_DURATION等） |
| 二维码功能 | 二维码相关代码 | 与抽奖方式无关 |
| 管理员功能条 | 扫码/审核按钮 | 与抽奖方式无关 |
| 弹窗横幅 | popup-banner组件 | 与抽奖方式无关 |
| 积分显示 | 积分余额/冻结积分 | 与抽奖方式无关 |
| MobX Store | store/lottery等 | 状态管理层不变 |

---

## 3. 第一期：组件化重构 + 增强现有九宫格

> 投入产出比最高的阶段，两份文档共同强调的优先事项。

### 3.1 任务1：创建万能组件骨架

**目标**：创建 `components/lottery-activity/` 作为对外唯一入口，接收 `campaign_code` 和 `size` 属性。

**具体工作**：

- 创建 `lottery-activity.ts`：主逻辑，接收 `campaign_code` 属性，自动调用 API 加载配置和奖品
- 创建 `lottery-activity.wxml`：根据 `display.mode` 条件渲染不同子组件
- 创建 `lottery-activity.scss`：公共样式 + 主题 CSS 变量挂载点
- 创建 `lottery-activity.json`：注册所有子组件

**组件接口定义**：

```
Properties（外部传入）：
├── campaignCode   String   必填，活动标识（如 'SPRING_2026'）
└── size           String   可选，展示尺寸（full/medium/small/mini，默认 full）

内部自动处理：
├── 调用 API.getLotteryConfig(campaignCode) → 拿到 display 配置
├── 调用 API.getLotteryPrizes(campaignCode) → 拿到奖品列表
├── 根据 display.mode 选择子组件渲染
├── 根据 display.effect_theme 设置主题色
└── 用户抽奖时调用 API.performLottery(campaignCode, count)
```

**使用方式**（目标）：

```xml
<!-- 任何页面只需一行代码 -->
<lottery-activity campaign_code="SPRING_2026" size="full" />
```

### 3.2 任务2：提取九宫格为独立子组件

**目标**：从 `lottery.wxml` 第122-284行提取九宫格 HTML + CSS + 动画 JS 到 `sub/grid/`。

**具体工作**：

| 提取内容 | 来源 | 目标 |
|---------|------|------|
| 九宫格 HTML（8个奖品位 + 中心按钮） | lottery.wxml 第122-284行 | `sub/grid/grid.wxml` |
| 九宫格样式（prize-grid、prize-area、highlighted等） | lottery.scss 相关样式 | `sub/grid/grid.scss` |
| 高亮动画 `startHighlightAnimation()` | lottery.ts 第1617行起 | `sub/grid/grid.ts` |
| 中奖索引提取 `extractWinningIndex()` | lottery.ts 第992行起 | 提取为公共方法 |
| 动画常量 `% 8` 循环逻辑 | lottery.ts 第1659行 | `sub/grid/grid.ts` |

**子组件统一接口**：

```
Properties（父组件传入）：
├── prizes          Array    奖品列表（后端数据）
├── costPoints      Number   单次消耗积分
├── pointsBalance   Number   用户当前积分
├── isInProgress    Boolean  是否正在抽奖
├── effectTheme     String   特效主题
├── rarityEffects   Boolean  是否启用稀有度光效
└── winAnimation    String   中奖动画类型

Events（子组件触发）：
├── bind:draw       用户点击抽奖（父组件调API）
├── bind:result     显示抽奖结果
└── bind:close      关闭结果弹窗
```

**验收标准**：提取后效果与当前完全一致，无视觉差异。

### 3.3 任务3：提取结果弹窗为共享组件

**目标**：从 `lottery.wxml` 提取结果弹窗到 `shared/result-modal/`，所有抽奖模式共用。

**具体工作**：

| 提取内容 | 来源 | 目标 |
|---------|------|------|
| 结果弹窗 HTML | lottery.wxml 结果弹窗部分 | `shared/result-modal/result-modal.wxml` |
| 结果弹窗样式 | lottery.scss 相关样式 | `shared/result-modal/result-modal.scss` |
| 逐个揭晓逻辑 `revealNextPrize()` | lottery.ts 第2104行起 | `shared/result-modal/result-modal.ts` |
| 多连抽揭晓状态管理 | lottery.ts multiDrawCurrentIndex等 | `shared/result-modal/result-modal.ts` |

### 3.4 任务4：提取连抽按钮为共享组件

**目标**：三连抽/五连抽/十连抽按钮提取到 `shared/draw-buttons/`。

**具体工作**：

- 从 lottery.wxml 第289行起的多连抽按钮组提取
- 从 lottery.ts 提取 `onTripleDraw`、`onFiveDraw`、`onTenDraw` 触发逻辑
- 按钮积分显示从 `drawPricing` 读取（已有后端对接）

### 3.5 任务5：稀有度光效

**目标**：根据奖品 `rarity` 字段在九宫格格子和结果弹窗上显示不同视觉效果。

**来源依据**：《UI设计方案》第6.2节 + 《三端协作方案》第2.7节

**现状**：lottery.ts:726 已读取 `rarity: prize.rarity || 'common'`，但前端无任何视觉区分。

**具体工作**：

创建 `shared/rarity-effects/` 组件，实现4级光效：

| rarity值 | 含义 | CSS效果 | 颜色 |
|----------|------|---------|------|
| `common` | 普通 | 蓝色边框，无特效 | `#3498db` |
| `rare` | 稀有 | 紫色呼吸光，CSS pulse动画，轻微脉冲 | `#9b59b6` |
| `epic` | 史诗 | 橙色光环，CSS box-shadow持续闪烁 | `#e67e22` |
| `legendary` | 传说 | 金色旋转光环，CSS conic-gradient + rotate + 星星飘落 | `#f1c40f` |

**技术方案**：

```
纯CSS实现，不依赖Canvas或JS动画：
├── common   → border: 2rpx solid #3498db
├── rare     → box-shadow: 0 0 15rpx #9b59b6 + @keyframes pulse
├── epic     → box-shadow: 0 0 25rpx #e67e22 + @keyframes flicker
└── legendary → conic-gradient旋转背景 + @keyframes rotate + 星星伪元素
```

**应用位置**：
- 九宫格每个奖品格子（根据 rarity 加 class）
- 结果弹窗中奖品展示卡片
- 连抽逐个揭晓时的卡片边框

### 3.6 任务6：主题色 CSS 变量系统

**目标**：根据后端返回的 `display.effect_theme` 动态切换全局主题色。

**来源依据**：《三端协作方案》第4.4节 + 第2.5节

**具体工作**：

创建 `themes/themes.ts`，定义6套主题的 CSS 变量映射：

| theme值 | 主色 | 辅色 | 背景色 | 适用场景 |
|---------|------|------|--------|---------|
| `default` | `#e67e22`（橙） | `#ffffff`（白） | `#fff8f0` | 日常活动 |
| `gold_luxury` | `#f1c40f`（金） | `#2c3e50`（黑） | `#1a1a2e` | 高端活动 |
| `purple_mystery` | `#9b59b6`（紫） | `#2c3e50`（深蓝） | `#1a1a3e` | 神秘活动 |
| `spring_festival` | `#e74c3c`（红） | `#f1c40f`（金） | `#fff5f5` | 春节活动 |
| `christmas` | `#27ae60`（绿） | `#e74c3c`（红） | `#f0fff0` | 圣诞活动 |
| `summer` | `#3498db`（蓝） | `#ffffff`（白） | `#f0f8ff` | 夏季活动 |

**实现方式**：

```
lottery-activity.ts 中：
  1. 从 API 响应读取 display.effect_theme
  2. 查 themes.ts 映射表获取 CSS 变量值
  3. 通过 setData 设置内联 style 变量
  4. 所有子组件通过 var(--theme-primary) 读取颜色
```

### 3.7 任务7：读取后端 display 配置字段

**目标**：`loadLotteryData()` 中新增读取 `display` 对象，为组件路由做准备。

**具体工作**：

- 解析 API 响应中的 `display` 字段
- 后端暂未开发时使用默认值兜底：

```typescript
const defaultDisplay = {
  mode: 'grid_3x3',
  grid_cols: 3,
  effect_theme: 'default',
  rarity_effects_enabled: false,
  win_animation: 'simple',
  background_image_url: null
}
```

- 主页面 WXML 根据 `display.mode` 做条件渲染（第一期只有 grid，但架构先搭好）：

```xml
<grid wx:if="{{displayMode === 'grid_3x3' || displayMode === 'grid_4x4'}}" ... />
<wheel wx:elif="{{displayMode === 'wheel'}}" ... />
<card wx:elif="{{displayMode === 'card_flip'}}" ... />
<egg wx:elif="{{displayMode === 'golden_egg'}}" ... />
<scratch wx:elif="{{displayMode === 'scratch_card'}}" ... />
<blindbox wx:elif="{{displayMode === 'blind_box'}}" ... />
<gashapon wx:elif="{{displayMode === 'gashapon'}}" ... />
<luckybag wx:elif="{{displayMode === 'lucky_bag'}}" ... />
<shake wx:elif="{{displayMode === 'shake'}}" ... />
<flipmulti wx:elif="{{displayMode === 'flip_card_multi'}}" ... />
<redpacket wx:elif="{{displayMode === 'red_packet'}}" ... />
<slotmachine wx:elif="{{displayMode === 'slot_machine'}}" ... />
<whackmole wx:elif="{{displayMode === 'whack_mole'}}" ... />
<pinball wx:elif="{{displayMode === 'pinball'}}" ... />
<cardcollect wx:elif="{{displayMode === 'card_collect'}}" ... />
<flashsale wx:elif="{{displayMode === 'flash_sale'}}" ... />
```

### 3.8 任务8：lottery.ts 瘦身

**目标**：将抽奖核心逻辑迁移到万能组件后，`lottery.ts` 变成薄壳页面。

**迁移后 lottery.ts 只保留**：

| 保留内容 | 说明 |
|---------|------|
| 页面生命周期 | onLoad / onShow / onHide / onUnload |
| 二维码功能 | 生成、刷新、倒计时、放大 |
| 管理员功能条 | 扫码、审核详情跳转 |
| 审核记录弹窗 | viewRecentAudits 等 |
| 弹窗横幅 | popup-banner 数据加载 |
| 积分显示 | 格式化、响应式字体 |
| MobX Store 绑定 | userStore / pointsStore 绑定 |

**迁移到万能组件的内容**：

| 迁移内容 | 原位置 | 目标 |
|---------|--------|------|
| `loadLotteryData()` | lottery.ts:606 | lottery-activity.ts |
| `startHighlightAnimation()` | lottery.ts:1617 | sub/grid/grid.ts |
| `extractWinningIndex()` | lottery.ts:992 | 公共工具方法 |
| `performDraw()` / `onSingleDraw()` | lottery.ts | lottery-activity.ts |
| `revealNextPrize()` | lottery.ts:2104 | shared/result-modal/ |
| 九宫格 WXML | lottery.wxml:122-284 | sub/grid/grid.wxml |
| 结果弹窗 WXML | lottery.wxml 弹窗部分 | shared/result-modal/ |
| 连抽按钮 WXML | lottery.wxml:289起 | shared/draw-buttons/ |

---

## 4. 第二期：中奖动画增强

### 4.1 任务9：卡牌翻转中奖动画

**目标**：九宫格停下后，结果弹窗改为卡牌翻转方式揭晓奖品。

**来源依据**：《UI设计方案》第6.2节①、第6.3节

**对应配置**：`display.win_animation = 'card_flip'`

**技术方案**：纯 CSS 3D 翻转，约20行核心 CSS

```
单抽流程：
  第1步：点击"开始抽奖" → 九宫格高亮动画照常播放
  第2步：动画停止后，弹出卡牌（scale 0→1 + opacity 0→1）
  第3步：卡牌背面展示统一花纹 + ❓
  第4步：卡牌3D翻转（CSS rotateY 0°→180°，约0.6秒）
  第5步：翻转到正面，显示奖品名称+图标
  第6步：根据 rarity 播放对应光效（金光/紫光/蓝光）

连抽流程：
  第1步：点击连抽 → 九宫格高亮动画播放
  第2步：弹出N张卡牌，排列为2行×5列（十连抽）
  第3步：用户选择"逐个揭晓"或"全部翻开"
  第4步：卡牌逐个翻转，中奖的亮对应稀有度光效，未中奖的变灰
```

**CSS 核心实现**：

```css
.card-flip-container {
  perspective: 1000rpx;
}
.card {
  transform-style: preserve-3d;
  transition: transform 0.6s;
}
.card.flipped {
  transform: rotateY(180deg);
}
.card-front, .card-back {
  backface-visibility: hidden;
  position: absolute;
}
.card-front {
  transform: rotateY(180deg);
}
```

### 4.2 任务10：烟花特效（可选）

**目标**：中奖时播放简化版烟花效果。

**对应配置**：`display.win_animation = 'fireworks'`

**来源依据**：《UI设计方案》第6.1节标注"勉强能做，中低端机可能卡"

**技术方案**：

- 使用简化版 CSS 粒子动画（不用 Canvas），控制粒子数量在20个以内
- 多个小圆点从中心向四周扩散 + 渐隐
- 仅在 `rarity` 为 `epic` 或 `legendary` 时触发，避免频繁播放

**注意事项**：
- 需要在低端机（如iPhone 7、红米系列）上测试性能
- 如果卡顿明显，降级为简单的 scale + opacity 扩散效果

---

## 5. 第三期：新增玩法组件

### 5.1 任务11：转盘组件 `sub/wheel/`

**开发量**：大

**对应配置**：`display.mode = 'wheel'`

**技术方案**：

| 项目 | 方案 |
|------|------|
| 绘制 | Canvas 2D 绘制圆形转盘（lottery-config.ts 已有基础参数） |
| 奖品数量 | 固定6-8个（行业标准） |
| 动画 | CSS rotate 旋转 + 缓动函数（ease-out）减速停止 |
| 指针 | 固定指针在顶部，转盘旋转 |
| 停止位置 | 根据后端返回的 sort_order 计算目标角度 |

**现有基础**：`lottery-config.ts` 已定义：
- `wheelSize: 8`
- `anglePerSlice: 45`
- `canvasSize: 600`
- `centerSize: 100`
- `sliceColors: [8种颜色]`

**需要新增**：
- Canvas 绘制逻辑（扇形、文字、图标）
- 旋转动画控制（起始速度、减速曲线、目标角度）
- 触发抽奖和结果回调

### 5.2 任务12：卡牌翻转玩法组件 `sub/card/`

**开发量**：中

**对应配置**：`display.mode = 'card_flip'`

**说明**：这是独立的抽奖玩法（整个交互就是翻卡牌），不同于任务9的"中奖动画"（九宫格停下后的结果展示方式）。

**技术方案**：

```
页面展示N张卡牌背面
  → 用户点击某张卡牌
    → 调用后端抽奖API
      → 卡牌3D翻转揭晓
        → 根据 rarity 播放光效
```

- CSS rotateY + backface-visibility（与任务9共用翻转CSS）
- 卡牌排列：单抽1张居中，连抽排列为网格
- 每张卡牌独立翻转，支持逐个揭晓

### 5.3 任务13：砸金蛋组件 `sub/egg/`

**开发量**：中

**对应配置**：`display.mode = 'golden_egg'`

**技术方案**：

| 项目 | 方案 |
|------|------|
| 金蛋展示 | 图片或CSS绘制的金蛋造型 |
| 砸蛋动画 | CSS帧动画（@keyframes），蛋壳裂纹扩散序列 |
| 碎裂效果 | 蛋壳碎片CSS位移 + 旋转 + 渐隐 |
| 奖品揭晓 | 碎裂后奖品从中心 scale 放大出现 |

**交互流程**：

```
展示3个金蛋（或后端配置数量）
  → 用户点击某个金蛋
    → 调用后端抽奖API
      → 播放砸蛋动画（裂纹→碎裂→奖品飞出）
        → 根据 rarity 播放光效
```

**资源需求**：需要金蛋完整态和裂纹态的图片素材（或纯CSS绘制）

### 5.4 任务14：刮刮卡组件 `sub/scratch/`

**开发量**：中

**对应配置**：`display.mode = 'scratch_card'`

**技术方案**：

| 项目 | 方案 |
|------|------|
| 覆盖层 | Canvas 2D 绘制灰色涂层 |
| 刮除效果 | touchmove 事件 + globalCompositeOperation = 'destination-out' |
| 揭晓判定 | 刮除面积超过50%时自动揭晓全部 |
| 底层奖品 | 普通 WXML 元素，Canvas 覆盖在上方 |

**交互流程**：

```
展示一张刮刮卡（灰色覆盖层 + "刮开有奖"文字）
  → 调用后端抽奖API获取结果（预先获取，刮开只是展示）
    → 用户手指滑动擦除覆盖层
      → 逐渐露出下方奖品
        → 刮除面积>50%时自动清除剩余覆盖
          → 显示完整奖品 + rarity光效
```

### 5.5 任务15：虚拟盲盒组件 `sub/blindbox/`

**开发量**：中

**对应配置**：`display.mode = 'blind_box'`

**玩法说明**：模拟实体盲盒拆盒体验。展示一个封闭的盲盒，用户点击后播放开盒动画，揭晓内部奖品。重点在"未知感"和开盒的仪式感。

**技术方案**：

| 项目 | 方案 |
|------|------|
| 盲盒展示 | CSS绘制立体盒子（transform: rotateX/rotateY 模拟3D透视）或图片素材 |
| 开盒动画 | CSS @keyframes：盒盖上翻 → 光效溢出 → 奖品从盒内升起 |
| 光效溢出 | CSS box-shadow + scale 从盒口向外扩散 |
| 奖品揭晓 | 奖品卡片从盒内 translateY 上移 + scale放大 + rarity光效 |
| 晃动提示 | 开盒前盲盒轻微CSS晃动动画（@keyframes wobble），暗示用户点击 |

**交互流程**：

```
展示一个封闭的盲盒（带"？"标识 + 轻微晃动动画）
  → 用户点击盲盒
    → 调用后端抽奖API
      → 播放开盒动画（盒盖上翻 → 光效溢出）
        → 奖品从盒内升起
          → 根据 rarity 播放对应光效
            → legendary: 金光爆发 + 星星
            → epic: 橙色光柱
            → rare: 紫色光晕
            → common: 蓝色微光
```

**连抽模式**：展示多个盲盒排列（如十连抽展示2行×5列），支持逐个开启或一键全开。

**资源需求**：盲盒封闭态和开启态的图片素材（或纯CSS 3D绘制）

### 5.6 任务16：扭蛋组件 `sub/gashapon/`

**开发量**：中

**对应配置**：`display.mode = 'gashapon'`

**玩法说明**：模拟日本扭蛋机体验。展示一台扭蛋机，用户点击旋钮/投币后，扭蛋从出口滚出，打开扭蛋壳揭晓奖品。

**技术方案**：

| 项目 | 方案 |
|------|------|
| 扭蛋机展示 | 图片素材（透明玻璃罩内可见彩色扭蛋球）或CSS绘制 |
| 投币/旋钮动画 | CSS rotate旋转旋钮 + translateY投币下落 |
| 扭蛋滚出 | CSS translateY + bounce缓动（从机器出口弹出） |
| 扭蛋打开 | CSS scale + 蛋壳左右分离（translateX 正负方向） |
| 奖品揭晓 | 蛋壳分开后奖品 scale 0→1 出现 + rarity光效 |
| 扭蛋球颜色 | 根据 rarity 预设球壳颜色（金/紫/蓝/灰），给用户颜色暗示 |

**交互流程**：

```
展示扭蛋机（玻璃罩内可见多个彩色扭蛋球）
  → 用户点击旋钮 / "投币"按钮
    → 调用后端抽奖API
      → 旋钮旋转动画
        → 一个扭蛋球从出口滚出（球壳颜色暗示稀有度）
          → 扭蛋球弹跳落地
            → 用户点击扭蛋球（或自动打开）
              → 蛋壳左右分离 → 奖品出现 + rarity光效
```

**扭蛋球颜色与稀有度对应**：

| rarity | 球壳颜色 | 视觉暗示 |
|--------|---------|---------|
| `legendary` | 金色球壳 | 用户看到金球就知道是大奖 |
| `epic` | 橙色球壳 | 高级奖品 |
| `rare` | 紫色球壳 | 中级奖品 |
| `common` | 蓝色/灰色球壳 | 普通奖品 |

**资源需求**：扭蛋机整体图片素材、不同颜色的扭蛋球素材

### 5.7 任务17：福袋组件 `sub/luckybag/`

**开发量**：中

**对应配置**：`display.mode = 'lucky_bag'`

**玩法说明**：模拟传统福袋/红包体验。展示一个扎口的福袋，用户点击后福袋打开，奖品从袋口飞出。适合节日活动（春节福袋、新年红包等）。

**技术方案**：

| 项目 | 方案 |
|------|------|
| 福袋展示 | 图片素材（扎口的布袋/红包造型）或CSS绘制 |
| 打开动画 | CSS @keyframes：袋口绳结松开 → 袋口张开 → 光效溢出 |
| 奖品飞出 | 多个奖品图标从袋口依次 translateY 上飞 + 散开（类似撒金币效果） |
| 主奖品揭晓 | 最终主奖品居中放大展示 + rarity光效 |
| 摇一摇交互（可选） | 监听 `wx.onAccelerometerChange`，摇手机触发开袋 |

**交互流程**：

```
展示一个扎口的福袋（带节日装饰 + 轻微浮动动画）
  → 用户点击福袋（或摇一摇手机）
    → 调用后端抽奖API
      → 播放开袋动画（绳结松开 → 袋口张开）
        → 光效从袋口溢出
          → 奖品图标从袋口飞出散开
            → 主奖品居中放大 + rarity光效

连抽模式：
  → 多个福袋排列展示
    → 逐个打开或一键全开
      → 每个福袋飞出各自的奖品
```

**与主题色的配合**：

| effect_theme | 福袋造型 | 适用场景 |
|-------------|---------|---------|
| `spring_festival` | 红色布袋 + 金色绳结 + "福"字 | 春节福袋 |
| `christmas` | 绿色/红色圣诞袜造型 | 圣诞礼袋 |
| `gold_luxury` | 金色丝绒袋 + 黑色绳结 | 高端活动 |
| `default` | 橙色布袋 + 白色绳结 | 日常活动 |
| `purple_mystery` | 紫色神秘袋 + 星星装饰 | 神秘福袋 |
| `summer` | 蓝色清凉袋 + 贝壳装饰 | 夏日活动 |

**资源需求**：不同主题的福袋图片素材（至少需要默认款和春节款）

### 5.8 任务18：摇一摇组件 `sub/shake/`

**开发量**：小

**对应配置**：`display.mode = 'shake'`

**玩法说明**：利用手机加速度传感器，用户摇晃手机触发抽奖。摇动过程中屏幕显示动态效果（如金币飞舞、能量聚集），摇动达到阈值后自动触发开奖。

**技术方案**：

| 项目 | 方案 |
|------|------|
| 传感器监听 | `wx.startAccelerometer()` + `wx.onAccelerometerChange` 监听加速度 |
| 摇动检测 | 计算三轴加速度合力 `Math.sqrt(x² + y² + z²)`，超过阈值（如15）计为一次有效摇动 |
| 摇动进度 | 进度条/能量条 CSS 动画，随摇动次数递增 |
| 摇动中效果 | 屏幕轻微震动（`wx.vibrateShort`）+ 金币/星星粒子飘散动画 |
| 触发开奖 | 累计摇动次数达标 → 调用后端API → 结果弹窗 |
| 备用触发 | 提供"点击抽奖"按钮，兼容不支持传感器的设备 |

**交互流程**：

```
展示摇一摇引导界面（手机图标 + "摇一摇抽奖"文字 + 能量条）
  → 用户开始摇动手机
    → 实时更新能量条进度 + 粒子动画 + 短震动反馈
      → 能量条满（摇动次数达标）
        → 调用后端抽奖API
          → 播放开奖动画 + rarity光效
            → 结果弹窗展示
```

**资源需求**：摇一摇引导图标素材、能量条UI素材

### 5.9 任务19：翻牌组件 `sub/flipmulti/`

**开发量**：中

**对应配置**：`display.mode = 'flip_card_multi'`

**玩法说明**：多张牌面朝下排列（如6张或9张），用户从中选择翻开N张（如选3张）。区别于卡牌翻转（card_flip是单张揭晓动画），翻牌是"多选N"的策略玩法，用户有选择权。

**技术方案**：

| 项目 | 方案 |
|------|------|
| 牌面布局 | CSS Grid 排列（2×3 或 3×3），每张牌使用 CSS 3D 翻转 |
| 翻转动画 | `transform: rotateY(180deg)` + `backface-visibility: hidden` |
| 选择限制 | 后端配置可翻张数（如3张），前端计数，达到上限后剩余牌不可点击 |
| 已翻标记 | 翻开的牌添加 `flipped` class，显示奖品内容 + rarity光效 |
| 全部揭晓 | 用户翻完可选张数后，可选择"揭晓全部"查看未翻的牌 |
| 连抽模式 | 每次连抽生成一组新的牌面 |

**交互流程**：

```
展示N张背面朝上的牌（带轻微浮动动画）
  → 用户点击选择第1张牌
    → 调用后端API获取该位置结果
      → 播放翻转动画，显示奖品 + rarity光效
  → 用户继续选择第2、3张...
    → 达到可翻上限
      → 显示"揭晓全部"按钮（可选）
        → 剩余牌依次自动翻开
          → 结果汇总弹窗
```

**资源需求**：牌面背面图案素材（可按主题切换）

### 5.10 任务20：拆红包组件 `sub/redpacket/`

**开发量**：小

**对应配置**：`display.mode = 'red_packet'`

**玩法说明**：模拟微信红包拆开体验。展示一个红包封面，用户点击"开"按钮后红包从中间裂开，露出奖品内容。适合春节、节日等喜庆场景。

**技术方案**：

| 项目 | 方案 |
|------|------|
| 红包展示 | 图片素材或CSS绘制（红色底 + 金色"开"字圆形按钮） |
| 拆开动画 | CSS @keyframes：红包上下两半分离（translateY）+ 中间光效扩散 |
| 金币飘落 | 多个金币元素从顶部随机位置 translateY 下落 + 旋转 |
| 奖品揭晓 | 红包打开后中间显示奖品内容 + 金额/名称放大动画 |
| 连抽模式 | 多个红包排列，逐个拆开或一键全拆 |

**交互流程**：

```
展示红包封面（带金色"开"字按钮 + 轻微呼吸动画）
  → 用户点击"开"按钮
    → 调用后端抽奖API
      → 播放拆开动画（上下分离 + 光效）
        → 金币飘落特效
          → 奖品内容居中展示 + rarity光效

连抽模式：
  → 多个红包排列展示
    → 逐个拆开或一键全拆
      → 每个红包独立播放拆开动画
```

**与主题色的配合**：

| effect_theme | 红包造型 | 适用场景 |
|-------------|---------|---------|
| `spring_festival` | 经典红金配色 + "福"字 | 春节红包 |
| `gold_luxury` | 金色封面 + 黑色"开"字 | 高端活动 |
| `default` | 标准红色 + 金色"开"字 | 日常活动 |
| `purple_mystery` | 紫色封面 + 银色"开"字 | 神秘红包 |

**资源需求**：红包封面图片素材（至少需要默认款和春节款）

### 5.11 任务21：老虎机组件 `sub/slotmachine/`

**开发量**：中

**对应配置**：`display.mode = 'slot_machine'`

**玩法说明**：经典三列（或多列）滚动老虎机。用户拉下拉杆（或点击按钮），三列图标高速滚动后依次停止，最终对齐的图标组合决定奖品。

**技术方案**：

| 项目 | 方案 |
|------|------|
| 滚轮布局 | 3列垂直滚轮，每列包含所有奖品图标的循环列表 |
| 滚动动画 | CSS `transform: translateY` + `transition` 缓动，模拟减速停止 |
| 依次停止 | 第1列先停 → 延迟后第2列停 → 再延迟第3列停，制造悬念 |
| 中奖线 | 中间横线高亮，三列对齐时闪烁 + 光效 |
| 拉杆交互 | 右侧拉杆图片，支持拖拽下拉手势（`touchstart/touchmove/touchend`） |
| 备用按钮 | "SPIN"按钮，点击触发（兼容无拉杆交互） |

**交互流程**：

```
展示老虎机界面（3列静止 + 右侧拉杆 + 底部SPIN按钮）
  → 用户拉下拉杆或点击SPIN
    → 调用后端抽奖API
      → 三列同时开始高速滚动
        → 第1列减速停止（约1秒）
          → 第2列减速停止（约1.5秒）
            → 第3列减速停止（约2秒）
              → 判断中奖线对齐 → 闪烁 + 光效 + rarity动画
                → 结果弹窗
```

**资源需求**：老虎机外框素材、拉杆素材、各奖品图标素材

### 5.12 任务22：打地鼠组件 `sub/whackmole/`

**开发量**：中

**对应配置**：`display.mode = 'whack_mole'`

**玩法说明**：限时小游戏玩法。多个洞口中随机冒出目标（地鼠/奖品图标），用户在限定时间内点击冒出的目标，点中即获得对应奖品。偏游戏化，趣味性强。

**技术方案**：

| 项目 | 方案 |
|------|------|
| 洞口布局 | 3×3 CSS Grid 布局，每个洞口为一个圆形区域 |
| 冒出动画 | 目标从洞口底部 `translateY` 上升 → 停留 → 下降回洞口 |
| 随机出现 | `setInterval` 控制出现频率，`Math.random()` 选择洞口位置 |
| 点击判定 | `bindtap` 事件，判断目标是否在可点击状态（已冒出且未缩回） |
| 倒计时 | 顶部显示剩余时间（如10秒），`setInterval` 每秒递减 |
| 计分/结算 | 记录点中次数，时间结束后调用后端API，按点中数量返回奖品 |
| 难度递增 | 随时间推移，目标出现频率加快、停留时间缩短 |

**交互流程**：

```
展示打地鼠界面（3×3洞口 + 顶部倒计时 + 底部得分）
  → 倒计时开始（如10秒）
    → 目标随机从洞口冒出（停留约0.8-1.2秒）
      → 用户点击冒出的目标
        → 命中：+1分 + 击中动画 + 短震动
        → 未命中：目标缩回
  → 倒计时结束
    → 调用后端API（传入得分/点中次数）
      → 返回对应奖品 + rarity光效
        → 结果弹窗
```

**资源需求**：洞口素材、地鼠/目标角色素材、锤子点击效果素材

### 5.13 任务23：弹珠机组件 `sub/pinball/`

**开发量**：大

**对应配置**：`display.mode = 'pinball'`

**玩法说明**：模拟弹珠机/柏青哥。弹珠从顶部发射，碰撞钉子后随机下落，最终进入底部的某个奖品槽。视觉效果丰富，观赏性强。

**技术方案**：

| 项目 | 方案 |
|------|------|
| 整体渲染 | Canvas 2D 绘制（钉子阵列 + 弹珠 + 奖品槽） |
| 物理模拟 | 简化物理：预设多条路径轨迹，根据后端结果选择对应路径播放 |
| 钉子碰撞 | 弹珠经过钉子时播放偏转动画（左/右随机偏转，但最终路径确定） |
| 弹珠动画 | Canvas `requestAnimationFrame` 逐帧绘制弹珠位置 |
| 奖品槽 | 底部N个槽位，每个对应一个奖品，弹珠落入后高亮 |
| 发射交互 | 右侧弹簧拉杆，拖拽释放发射（或点击"发射"按钮） |

**交互流程**：

```
展示弹珠机界面（钉子阵列 + 底部奖品槽 + 右侧发射弹簧）
  → 用户拉动弹簧释放（或点击发射按钮）
    → 调用后端抽奖API，获取中奖结果
      → 根据结果选择预设路径
        → Canvas动画：弹珠从顶部出发，碰钉偏转，逐级下落
          → 弹珠落入对应奖品槽
            → 奖品槽高亮 + rarity光效
              → 结果弹窗
```

**简化方案**（降低开发难度）：
- 不做真实物理模拟，预设8-10条固定路径动画
- 后端返回结果后，选择通往对应奖品槽的路径播放
- 路径用贝塞尔曲线定义，Canvas逐帧绘制弹珠沿路径移动

**资源需求**：弹珠机背景素材、弹珠素材、弹簧拉杆素材

### 5.14 任务24：集卡组件 `sub/cardcollect/`

**开发量**：中

**对应配置**：`display.mode = 'card_collect'`

**玩法说明**：长线收集玩法。每次抽奖获得一张卡片/碎片，集齐一套（如5张不同卡片）可兑换大奖。展示卡册界面，已收集的卡片点亮，未收集的显示剪影。

**技术方案**：

| 项目 | 方案 |
|------|------|
| 卡册布局 | CSS Grid 展示全套卡片位置（如2×3或3×3） |
| 已收集卡片 | 正常显示卡片图片 + 名称，带轻微光效 |
| 未收集卡片 | 灰色剪影/问号占位 + "未收集"标签 |
| 抽卡动画 | 点击抽卡 → 卡片从卡包飞出 → 翻转揭晓 → 飞入卡册对应位置 |
| 重复卡片 | 已有的卡片再次抽到时，显示"已拥有"+ 可转换为积分/碎片 |
| 集齐提示 | 集齐全套时，卡册整体发光 + 庆祝动画 + "兑换大奖"按钮 |
| 进度显示 | 顶部显示"已收集 3/5" 进度条 |

**交互流程**：

```
展示卡册界面（全套卡片位 + 已收集进度 + "抽卡"按钮）
  → 用户点击"抽卡"
    → 调用后端抽奖API
      → 播放抽卡动画（卡片飞出 + 翻转揭晓）
        → 新卡片：飞入卡册对应位置 + 点亮动画
        → 重复卡片：显示"已拥有" + 转换提示
  → 集齐全套
    → 卡册整体发光 + 庆祝动画
      → 显示"兑换大奖"按钮
        → 点击兑换 → 调用后端兑换API → 大奖结果弹窗
```

**资源需求**：各卡片正面图片素材、卡片背面/剪影素材、卡册背景素材

### 5.15 任务25：限时秒杀组件 `sub/flashsale/`

**开发量**：小

**对应配置**：`display.mode = 'flash_sale'`

**玩法说明**：限时抢购玩法，不是随机抽奖而是先到先得。展示倒计时 + 剩余库存，用户在开抢时间点击"立即抢"按钮，抢到即获得奖品。适合限量高价值奖品。

**技术方案**：

| 项目 | 方案 |
|------|------|
| 倒计时 | `setInterval` 每秒更新，显示"距开抢 HH:MM:SS"或"抢购中 剩余XX件" |
| 库存显示 | 进度条展示剩余库存占比，实时更新（轮询或WebSocket） |
| 抢购按钮 | 未开始：灰色不可点击；进行中：红色高亮+脉冲动画；已结束：灰色"已抢光" |
| 抢购动画 | 点击后按钮变为loading → 成功：爆炸光效+奖品飞入；失败："手慢了"提示 |
| 库存预警 | 剩余<10%时，库存数字变红+闪烁，营造紧迫感 |

**交互流程**：

```
展示秒杀界面（奖品大图 + 倒计时 + 库存进度条 + 抢购按钮）
  → 倒计时阶段：
    → 按钮灰色不可点击，显示"距开抢 XX:XX:XX"
  → 开抢时刻：
    → 按钮变红+脉冲动画，显示"立即抢"
      → 用户点击"立即抢"
        → 调用后端抢购API
          → 成功：爆炸光效 + 奖品飞入动画 + 结果弹窗
          → 失败：显示"手慢了，已被抢光"
  → 库存为0：
    → 按钮变灰，显示"已抢光"
```

**资源需求**：秒杀标签素材、倒计时UI素材

---

## 6. 第四期：多活动多页面

### 6.1 任务26：万能组件 size 属性适配

**目标**：同一个 `lottery-activity` 组件通过 `size` 属性适应不同页面位置。

**来源依据**：《三端协作方案》第8.6节

| size值 | 用途 | 展示方式 |
|--------|------|---------|
| `full` | 占满整个页面（抽奖页主活动） | 完整九宫格/转盘 + 连抽按钮 + 积分显示 |
| `medium` | 页面中间区域（我的页嵌入活动） | 完整九宫格/转盘，无连抽按钮 |
| `small` | 页面角落入口（发现页小活动） | 只显示活动封面图 + "点击参与"按钮 |
| `mini` | 悬浮按钮（任意页面角落） | 圆形图标，点击弹出全屏抽奖弹窗 |

**small/mini 模式交互流程**：

```
small模式：
  显示封面图 + 活动名称 + "点击参与"
    → 用户点击
      → 弹出全屏弹窗（内部渲染完整抽奖组件）

mini模式：
  角落浮动圆形图标
    → 用户点击
      → 弹出全屏弹窗（内部渲染完整抽奖组件）
```

### 6.2 任务27：各页面接入活动列表

**目标**：各页面 `onShow` 中获取活动列表，按 `placement` 配置渲染对应活动。

**来源依据**：《三端协作方案》第8.7-8.9节

**需要后端提供的新API**：

```
GET /api/v4/lottery/campaigns/active

响应：
{
  "success": true,
  "data": [
    {
      "campaign_code": "SPRING_2026",
      "campaign_name": "春节大抽奖",
      "display": { "mode": "grid_3x3", ... },
      "placement": {
        "page": "lottery",
        "position": "main",
        "size": "full"
      },
      "cover_image": "https://...",
      "status": "active"
    },
    ...
  ]
}
```

**各页面加载逻辑**：

```
页面 onShow
  → 调用 API.getActiveCampaigns()
  → 过滤 placement.page === 当前页面名 的活动
  → 按 placement.position 放到对应位置
  → 渲染 <lottery-activity campaign_code="xxx" size="xxx" />
```

**抽奖页（lottery）**：

```xml
<lottery-activity
  wx:if="{{mainCampaign}}"
  campaign_code="{{mainCampaign.campaign_code}}"
  size="full"
/>
```

**其他页面（如发现页）**：

```xml
<block wx:for="{{pageCampaigns}}" wx:key="campaign_code">
  <view class="activity-slot {{item.placement.position}}">
    <lottery-activity
      campaign_code="{{item.campaign_code}}"
      size="{{item.placement.size}}"
    />
  </view>
</block>
```

### 6.3 任务28：三端联调测试

**测试要点**：

| 测试项 | 验证内容 |
|--------|---------|
| 多活动独立性 | 同一页面两个活动实例互不干扰（各自加载、各自动画、各自主题色） |
| display.mode 路由 | 后端返回不同 mode 时，前端正确加载对应子组件 |
| effect_theme 主题 | 后端切换主题后，前端颜色正确变化 |
| rarity 光效 | 不同稀有度奖品显示对应光效 |
| win_animation 动画 | simple/card_flip/fireworks 三种动画正确播放 |
| size 适配 | full/medium/small/mini 四种尺寸正确展示 |
| placement 投放 | 活动按配置出现在正确的页面和位置 |
| 降级兜底 | 后端未返回 display 字段时，使用默认值正常运行 |
| 奖品超过8个 | 格子只展示 sort_order 1-8 的奖品，其余在结果弹窗展示 |

---

## 7. 明确不做的事项

以下事项经《UI设计方案》行业调研后明确排除：

| 不做的事 | 原因 |
|---------|------|
| 动态网格数量（3×3/4×4/5×5自适应） | 全行业无公司这样做，格子数量固定是行业共识 |
| 前端根据奖品数量重新布局 | 奖品超过8个时由后端选8个展示，其余在奖池中 |
| 动态计算顺时针路径 | 固定8格路径即可，无需动态计算 |
| 骨骼动画 / Live2D | 微信小程序不支持 WebGL |
| 视频过场CG | video组件可用但包体积爆炸，不推荐 |
| 全屏烟花（Canvas粒子版） | 中低端机卡顿，改用简化CSS版本 |
| 12格/15格九宫格 | 格子小文字挤、动画节奏差、无行业先例 |

**奖品超过8个的处理方式**（行业标准做法）：

```
后端奖池（例如12个奖品）
├── 展示层（8个）→ sort_order 1-8 → 放在九宫格上
│   └── 运营选择最能吸引用户的8个奖品
│
└── 隐藏层（4个）→ 不在格子上显示
    └── 抽中时在结果弹窗展示
```

---

## 8. 目标文件结构

> **架构决策**：采用分包隔离方案（见 [1.4 架构决策](#14-架构决策16个子组件的加载策略)），`lottery-activity` 及全部子组件放入 `packageLottery/` 独立子包。

```
packageLottery/                       ← 独立子包（app.json subpackages 配置）
└── components/
    └── lottery-activity/              ← 万能抽奖组件（对外唯一入口）
    ├── lottery-activity.ts        ← 主逻辑：加载配置、路由分发、API调用
    ├── lottery-activity.wxml      ← 根据 display.mode 条件渲染子组件
    ├── lottery-activity.scss      ← 公共样式 + 主题CSS变量挂载
    ├── lottery-activity.json      ← 注册所有子组件
    │
    ├── sub/                       ← 子组件（按抽奖方式分）
    │   ├── grid/                  ← 九宫格（从现有代码提取）
    │   │   ├── grid.ts            ← startHighlightAnimation 等
    │   │   ├── grid.wxml          ← 3×3网格 + 中心按钮
    │   │   ├── grid.scss
    │   │   └── grid.json
    │   │
    │   ├── wheel/                 ← 转盘（新增）
    │   │   ├── wheel.ts           ← Canvas绘制 + 旋转动画
    │   │   ├── wheel.wxml
    │   │   ├── wheel.scss
    │   │   └── wheel.json
    │   │
    │   ├── card/                  ← 卡牌翻转（新增）
    │   │   ├── card.ts
    │   │   ├── card.wxml
    │   │   ├── card.scss
    │   │   └── card.json
    │   │
    │   ├── egg/                   ← 砸金蛋（新增）
    │   │   ├── egg.ts
    │   │   ├── egg.wxml
    │   │   ├── egg.scss
    │   │   └── egg.json
    │   │
    │   └── scratch/               ← 刮刮卡（新增）
    │   │   ├── scratch.ts
    │   │   ├── scratch.wxml
    │   │   ├── scratch.scss
    │   │   └── scratch.json
    │   │
    │   ├── blindbox/              ← 虚拟盲盒（新增）
    │   │   ├── blindbox.ts
    │   │   ├── blindbox.wxml
    │   │   ├── blindbox.scss
    │   │   └── blindbox.json
    │   │
    │   ├── gashapon/              ← 扭蛋（新增）
    │   │   ├── gashapon.ts
    │   │   ├── gashapon.wxml
    │   │   ├── gashapon.scss
    │   │   └── gashapon.json
    │   │
    │   └── luckybag/              ← 福袋（新增）
    │       ├── luckybag.ts
    │       ├── luckybag.wxml
    │       ├── luckybag.scss
    │       └── luckybag.json
    │
    │   ├── shake/                 ← 摇一摇（新增）
    │   │   ├── shake.ts
    │   │   ├── shake.wxml
    │   │   ├── shake.scss
    │   │   └── shake.json
    │   │
    │   ├── flipmulti/             ← 翻牌多选（新增）
    │   │   ├── flipmulti.ts
    │   │   ├── flipmulti.wxml
    │   │   ├── flipmulti.scss
    │   │   └── flipmulti.json
    │   │
    │   ├── redpacket/             ← 拆红包（新增）
    │   │   ├── redpacket.ts
    │   │   ├── redpacket.wxml
    │   │   ├── redpacket.scss
    │   │   └── redpacket.json
    │   │
    │   ├── slotmachine/           ← 老虎机（新增）
    │   │   ├── slotmachine.ts
    │   │   ├── slotmachine.wxml
    │   │   ├── slotmachine.scss
    │   │   └── slotmachine.json
    │   │
    │   ├── whackmole/             ← 打地鼠（新增）
    │   │   ├── whackmole.ts
    │   │   ├── whackmole.wxml
    │   │   ├── whackmole.scss
    │   │   └── whackmole.json
    │   │
    │   ├── pinball/               ← 弹珠机（新增）
    │   │   ├── pinball.ts
    │   │   ├── pinball.wxml
    │   │   ├── pinball.scss
    │   │   └── pinball.json
    │   │
    │   ├── cardcollect/           ← 集卡（新增）
    │   │   ├── cardcollect.ts
    │   │   ├── cardcollect.wxml
    │   │   ├── cardcollect.scss
    │   │   └── cardcollect.json
    │   │
    │   └── flashsale/             ← 限时秒杀（新增）
    │       ├── flashsale.ts
    │       ├── flashsale.wxml
    │       ├── flashsale.scss
    │       └── flashsale.json
    │
    ├── shared/                    ← 共享组件（所有玩法通用）
    │   ├── result-modal/          ← 结果弹窗（从现有代码提取）
    │   │   ├── result-modal.ts    ← revealNextPrize 等
    │   │   ├── result-modal.wxml
    │   │   ├── result-modal.scss
    │   │   └── result-modal.json
    │   │
    │   ├── rarity-effects/        ← 稀有度光效（新增）
    │   │   ├── rarity-effects.ts
    │   │   ├── rarity-effects.wxml
    │   │   ├── rarity-effects.scss  ← common/rare/epic/legendary 四级CSS
    │   │   └── rarity-effects.json
    │   │
    │   └── draw-buttons/          ← 连抽按钮组（从现有代码提取）
    │       ├── draw-buttons.ts
    │       ├── draw-buttons.wxml
    │       ├── draw-buttons.scss
    │       └── draw-buttons.json
    │
    ├── themes/                    ← 主题色配置
    │   └── themes.ts              ← 6套主题的CSS变量定义
    │
    └── mock/                      ← 模拟数据（前端独立开发用）
        ├── mock-config.ts         ← 活动配置模拟数据（含16种display.mode）
        ├── mock-prizes.ts         ← 奖品列表模拟数据（含4种rarity）
        ├── mock-draw-result.ts    ← 抽奖结果模拟数据（模拟中奖/未中奖）
        ├── mock-campaigns.ts      ← 多活动列表模拟数据（含placement）
        └── mock-switch.ts         ← Mock开关（一行代码切换真实API/模拟数据）

pages/lottery/                         ← 主包页面（瘦身后的薄壳）
├── lottery.ts                     ← 瘦身后：只保留页面生命周期+二维码+管理员功能
├── lottery.wxml                   ← 引用 <lottery-activity> 组件
├── lottery.scss
├── lottery.json                   ← usingComponents 通过分包路径引用 lottery-activity
└── lottery-config.ts              ← 转盘技术参数（保留）
```

**app.json 分包配置**：

```json
{
  "subpackages": [
    {
      "root": "packageLottery",
      "name": "lottery",
      "pages": []
    }
  ],
  "preloadRule": {
    "pages/lottery/lottery": {
      "network": "all",
      "packages": ["lottery"]
    },
    "pages/index/index": {
      "network": "wifi",
      "packages": ["lottery"]
    }
  }
}
```

**lottery.json 组件引用**：

```json
{
  "usingComponents": {
    "lottery-activity": "/packageLottery/components/lottery-activity/lottery-activity"
  }
}
```

---

## 9. 实施顺序总览

### 9.1 全部任务清单

| 期次 | 编号 | 任务 | 类型 | 开发量 |
|------|------|------|------|--------|
| **一期** | 1 | 创建万能组件骨架 lottery-activity | 新增 | 中 |
| | 2 | 提取九宫格为 sub/grid 子组件 | 提取 | 中 |
| | 3 | 提取结果弹窗为 shared/result-modal | 提取 | 小 |
| | 4 | 提取连抽按钮为 shared/draw-buttons | 提取 | 小 |
| | 5 | 稀有度光效 rarity-effects | 新增 | 小 |
| | 6 | 主题色CSS变量系统 themes | 新增 | 小 |
| | 7 | 读取后端 display 配置字段 | 修改 | 小 |
| | 8 | lottery.ts 瘦身 | 重构 | 中 |
| **二期** | 9 | 卡牌翻转中奖动画 | 新增 | 中 |
| | 10 | 烟花特效（可选） | 新增 | 小 |
| **三期** | 11 | 转盘组件 sub/wheel | 新增 | 大 |
| | 12 | 卡牌翻转玩法组件 sub/card | 新增 | 中 |
| | 13 | 砸金蛋组件 sub/egg | 新增 | 中 |
| | 14 | 刮刮卡组件 sub/scratch | 新增 | 中 |
| | 15 | 虚拟盲盒组件 sub/blindbox | 新增 | 中 |
| | 16 | 扭蛋组件 sub/gashapon | 新增 | 中 |
| | 17 | 福袋组件 sub/luckybag | 新增 | 中 |
| | 18 | 摇一摇组件 sub/shake | 新增 | 小 |
| | 19 | 翻牌组件 sub/flipmulti | 新增 | 中 |
| | 20 | 拆红包组件 sub/redpacket | 新增 | 小 |
| | 21 | 老虎机组件 sub/slotmachine | 新增 | 中 |
| | 22 | 打地鼠组件 sub/whackmole | 新增 | 中 |
| | 23 | 弹珠机组件 sub/pinball | 新增 | 大 |
| | 24 | 集卡组件 sub/cardcollect | 新增 | 中 |
| | 25 | 限时秒杀组件 sub/flashsale | 新增 | 小 |
| **四期** | 26 | 万能组件 size 属性适配 | 新增 | 中 |
| | 27 | 各页面接入活动列表 | 新增 | 中 |
| | 28 | 三端联调测试 | 测试 | 中 |

### 9.2 依赖关系

```
任务1（万能组件骨架）
  ├── 任务2（提取九宫格）  ← 依赖任务1的组件结构
  ├── 任务3（提取结果弹窗）
  └── 任务4（提取连抽按钮）
        │
        ├── 任务5（稀有度光效）  ← 可与2/3/4并行
        ├── 任务6（主题色系统）  ← 可与2/3/4并行
        └── 任务7（读取display字段）
              │
              └── 任务8（lottery.ts瘦身）  ← 依赖2/3/4/7全部完成
                    │
                    ├── 任务9（卡牌翻转动画）  ← 依赖任务3的结果弹窗组件
                    ├── 任务10（烟花特效）
                    │
                    ├── 任务11-25（新玩法组件）  ← 依赖任务1的组件路由架构
                    │
                    └── 任务26-28（多活动多页面）  ← 依赖任务8完成瘦身
```

### 9.3 前端与后端的协作节点

> **核心策略：前端全程使用模拟数据独立开发，先看到效果，后端接口预留即可。**
> 详见 [第10章：前端模拟数据方案](#10-前端模拟数据方案)。

| 节点 | 前端状态 | 后端接口 | 前端数据来源 |
|------|---------|---------|------------|
| 一期~三期全程 | 独立开发，可在真机预览全部效果 | 预留接口定义，暂不实现 | `mock/` 模拟数据 |
| 四期联调前 | 全部UI和动画开发完成 | 实现全部API | `mock-switch.ts` 一行切换到真实API |
| 四期联调 | 切换到真实API，验证数据对接 | 全部就绪 | 真实后端API |

**预留的后端接口清单**（前端先用mock模拟，后端按此格式实现即可）：

| API | 方法 | 说明 | 前端mock文件 |
|-----|------|------|-------------|
| `/api/v4/lottery/campaigns/:code/config` | GET | 活动配置（含display字段） | `mock-config.ts` |
| `/api/v4/lottery/campaigns/:code/prizes` | GET | 奖品列表（含rarity字段） | `mock-prizes.ts` |
| `/api/v4/lottery/draw` | POST | 执行抽奖 | `mock-draw-result.ts` |
| `/api/v4/lottery/campaigns/active` | GET | 进行中的活动列表（含placement） | `mock-campaigns.ts` |

---

## 10. 前端模拟数据方案

> **目标**：前端全程不依赖后端，使用模拟数据独立开发，在真机上可预览全部效果（16种玩法、4种稀有度光效、6套主题色、3种中奖动画）。后端接口就绪后，一行代码切换到真实API。

### 10.1 Mock开关机制

创建 `mock/mock-switch.ts`，通过一个常量控制全局数据来源：

```typescript
/**
 * Mock数据开关
 * true  = 使用模拟数据（前端独立开发阶段）
 * false = 使用真实后端API（联调阶段）
 */
export const USE_MOCK = true
```

万能组件 `lottery-activity.ts` 中根据开关选择数据来源：

```typescript
const { USE_MOCK } = require('./mock/mock-switch')
const MockConfig = require('./mock/mock-config')
const MockPrizes = require('./mock/mock-prizes')
const MockDrawResult = require('./mock/mock-draw-result')

// 加载活动配置
async loadConfig(campaignCode: string) {
  if (USE_MOCK) {
    return MockConfig.getConfig(campaignCode)
  }
  return API.getLotteryConfig(campaignCode)
}

// 加载奖品列表
async loadPrizes(campaignCode: string) {
  if (USE_MOCK) {
    return MockPrizes.getPrizes(campaignCode)
  }
  return API.getLotteryPrizes(campaignCode)
}

// 执行抽奖
async performDraw(campaignCode: string, count: number) {
  if (USE_MOCK) {
    return MockDrawResult.draw(campaignCode, count)
  }
  return API.performLottery(campaignCode, count)
}
```

**联调时只需改一行**：`USE_MOCK = true` → `USE_MOCK = false`

### 10.2 mock-config.ts — 活动配置模拟数据

模拟16种不同 `display.mode` 的活动配置，用于验证组件路由和主题切换：

```typescript
/** 模拟活动配置 - 覆盖全部16种玩法 + 6套主题 */
const MOCK_CONFIGS = {
  // 九宫格 + 春节主题
  'GRID_DEMO': {
    campaign_code: 'GRID_DEMO',
    campaign_name: '九宫格演示',
    status: 'active',
    cost_per_draw: 100,
    max_draws_per_user_daily: 50,
    draw_buttons: [
      { count: 1, label: '单抽', total_cost: 100 },
      { count: 3, label: '3连抽', total_cost: 300 },
      { count: 5, label: '5连抽', total_cost: 500 },
      { count: 10, label: '10连抽', total_cost: 900 }
    ],
    display: {
      mode: 'grid_3x3',
      grid_cols: 3,
      effect_theme: 'spring_festival',
      rarity_effects_enabled: true,
      win_animation: 'card_flip',
      background_image_url: null
    }
  },

  // 转盘 + 金色主题
  'WHEEL_DEMO': {
    campaign_code: 'WHEEL_DEMO',
    campaign_name: '转盘演示',
    status: 'active',
    cost_per_draw: 50,
    max_draws_per_user_daily: 30,
    draw_buttons: [
      { count: 1, label: '单抽', total_cost: 50 }
    ],
    display: {
      mode: 'wheel',
      grid_cols: null,
      effect_theme: 'gold_luxury',
      rarity_effects_enabled: true,
      win_animation: 'simple',
      background_image_url: null
    }
  },

  // 卡牌翻转 + 紫色主题
  'CARD_DEMO': {
    campaign_code: 'CARD_DEMO',
    campaign_name: '卡牌翻转演示',
    status: 'active',
    cost_per_draw: 100,
    max_draws_per_user_daily: 20,
    draw_buttons: [
      { count: 1, label: '单抽', total_cost: 100 },
      { count: 10, label: '10连抽', total_cost: 900 }
    ],
    display: {
      mode: 'card_flip',
      grid_cols: null,
      effect_theme: 'purple_mystery',
      rarity_effects_enabled: true,
      win_animation: 'card_flip',
      background_image_url: null
    }
  },

  // 砸金蛋 + 默认主题
  'EGG_DEMO': {
    campaign_code: 'EGG_DEMO',
    campaign_name: '砸金蛋演示',
    status: 'active',
    cost_per_draw: 80,
    max_draws_per_user_daily: 10,
    draw_buttons: [
      { count: 1, label: '砸一个', total_cost: 80 }
    ],
    display: {
      mode: 'golden_egg',
      grid_cols: null,
      effect_theme: 'default',
      rarity_effects_enabled: true,
      win_animation: 'fireworks',
      background_image_url: null
    }
  },

  // 刮刮卡 + 夏日主题
  'SCRATCH_DEMO': {
    campaign_code: 'SCRATCH_DEMO',
    campaign_name: '刮刮卡演示',
    status: 'active',
    cost_per_draw: 60,
    max_draws_per_user_daily: 15,
    draw_buttons: [
      { count: 1, label: '刮一张', total_cost: 60 }
    ],
    display: {
      mode: 'scratch_card',
      grid_cols: null,
      effect_theme: 'summer',
      rarity_effects_enabled: true,
      win_animation: 'simple',
      background_image_url: null
    }
  },

  // 虚拟盲盒 + 赛博主题
  'BLINDBOX_DEMO': {
    campaign_code: 'BLINDBOX_DEMO',
    campaign_name: '虚拟盲盒演示',
    status: 'active',
    cost_per_draw: 120,
    max_draws_per_user_daily: 20,
    draw_buttons: [
      { count: 1, label: '开一个', total_cost: 120 },
      { count: 3, label: '开3个', total_cost: 360 },
      { count: 5, label: '开5个', total_cost: 540 }
    ],
    display: {
      mode: 'blind_box',
      grid_cols: null,
      effect_theme: 'default',
      rarity_effects_enabled: true,
      win_animation: 'card_flip',
      background_image_url: null,
      // 盲盒专属配置
      box_style: 'cube',           // 盒子外观: cube/cylinder/gift
      shake_before_open: true,     // 开启前摇晃动画
      series_name: '美食盲盒第1弹'  // 系列名称
    }
  },

  // 扭蛋 + 糖果主题
  'GASHAPON_DEMO': {
    campaign_code: 'GASHAPON_DEMO',
    campaign_name: '扭蛋机演示',
    status: 'active',
    cost_per_draw: 80,
    max_draws_per_user_daily: 30,
    draw_buttons: [
      { count: 1, label: '扭一次', total_cost: 80 },
      { count: 3, label: '扭3次', total_cost: 240 }
    ],
    display: {
      mode: 'gashapon',
      grid_cols: null,
      effect_theme: 'spring_festival',
      rarity_effects_enabled: true,
      win_animation: 'simple',
      background_image_url: null,
      // 扭蛋专属配置
      machine_style: 'classic',    // 机器外观: classic/modern/mini
      capsule_colors: ['#FF6B6B', '#4ECDC4', '#FFD93D', '#9775FA'], // 蛋壳颜色池
      show_remaining: true         // 显示剩余蛋数
    }
  },

  // 福袋 + 金色主题
  'LUCKYBAG_DEMO': {
    campaign_code: 'LUCKYBAG_DEMO',
    campaign_name: '福袋演示',
    status: 'active',
    cost_per_draw: 150,
    max_draws_per_user_daily: 10,
    draw_buttons: [
      { count: 1, label: '抢一个', total_cost: 150 }
    ],
    display: {
      mode: 'lucky_bag',
      grid_cols: null,
      effect_theme: 'gold_luxury',
      rarity_effects_enabled: true,
      win_animation: 'fireworks',
      background_image_url: null,
      // 福袋专属配置
      bag_style: 'traditional',    // 福袋外观: traditional/modern/envelope
      countdown_enabled: true,     // 限时抢购倒计时
      stock_total: 100,            // 总库存（前端展示用）
      stock_remaining: 37          // 剩余库存（前端展示用）
    }
  },

  // 摇一摇 + 默认主题
  'SHAKE_DEMO': {
    campaign_code: 'SHAKE_DEMO',
    campaign_name: '摇一摇演示',
    status: 'active',
    cost_per_draw: 50,
    max_draws_per_user_daily: 30,
    draw_buttons: [
      { count: 1, label: '摇一次', total_cost: 50 }
    ],
    display: {
      mode: 'shake',
      grid_cols: null,
      effect_theme: 'default',
      rarity_effects_enabled: true,
      win_animation: 'simple',
      background_image_url: null,
      shake_threshold: 15,         // 摇动触发阈值
      shake_count_required: 5      // 需要摇动次数
    }
  },

  // 翻牌（多选一） + 紫色主题
  'FLIPMULTI_DEMO': {
    campaign_code: 'FLIPMULTI_DEMO',
    campaign_name: '翻牌演示',
    status: 'active',
    cost_per_draw: 100,
    max_draws_per_user_daily: 20,
    draw_buttons: [
      { count: 1, label: '翻一局', total_cost: 100 }
    ],
    display: {
      mode: 'flip_card_multi',
      grid_cols: 3,
      effect_theme: 'purple_mystery',
      rarity_effects_enabled: true,
      win_animation: 'card_flip',
      background_image_url: null,
      total_cards: 9,              // 总牌数（3×3）
      pick_count: 3                // 可翻张数
    }
  },

  // 拆红包 + 春节主题
  'REDPACKET_DEMO': {
    campaign_code: 'REDPACKET_DEMO',
    campaign_name: '拆红包演示',
    status: 'active',
    cost_per_draw: 60,
    max_draws_per_user_daily: 30,
    draw_buttons: [
      { count: 1, label: '拆一个', total_cost: 60 },
      { count: 3, label: '拆3个', total_cost: 180 }
    ],
    display: {
      mode: 'red_packet',
      grid_cols: null,
      effect_theme: 'spring_festival',
      rarity_effects_enabled: true,
      win_animation: 'fireworks',
      background_image_url: null
    }
  },

  // 老虎机 + 金色主题
  'SLOT_DEMO': {
    campaign_code: 'SLOT_DEMO',
    campaign_name: '老虎机演示',
    status: 'active',
    cost_per_draw: 80,
    max_draws_per_user_daily: 30,
    draw_buttons: [
      { count: 1, label: 'SPIN', total_cost: 80 }
    ],
    display: {
      mode: 'slot_machine',
      grid_cols: null,
      effect_theme: 'gold_luxury',
      rarity_effects_enabled: true,
      win_animation: 'fireworks',
      background_image_url: null,
      reels: 3,                    // 滚轮列数
      stop_delay: 500              // 每列停止间隔（毫秒）
    }
  },

  // 打地鼠 + 夏日主题
  'WHACKMOLE_DEMO': {
    campaign_code: 'WHACKMOLE_DEMO',
    campaign_name: '打地鼠演示',
    status: 'active',
    cost_per_draw: 100,
    max_draws_per_user_daily: 10,
    draw_buttons: [
      { count: 1, label: '开始', total_cost: 100 }
    ],
    display: {
      mode: 'whack_mole',
      grid_cols: 3,
      effect_theme: 'summer',
      rarity_effects_enabled: true,
      win_animation: 'simple',
      background_image_url: null,
      game_duration: 10000,        // 游戏时长（毫秒）
      mole_interval: 800           // 地鼠出现间隔（毫秒）
    }
  },

  // 弹珠机 + 默认主题
  'PINBALL_DEMO': {
    campaign_code: 'PINBALL_DEMO',
    campaign_name: '弹珠机演示',
    status: 'active',
    cost_per_draw: 120,
    max_draws_per_user_daily: 15,
    draw_buttons: [
      { count: 1, label: '发射', total_cost: 120 }
    ],
    display: {
      mode: 'pinball',
      grid_cols: null,
      effect_theme: 'default',
      rarity_effects_enabled: true,
      win_animation: 'fireworks',
      background_image_url: null,
      path_count: 8                // 预设路径数量
    }
  },

  // 集卡 + 紫色主题
  'CARDCOLLECT_DEMO': {
    campaign_code: 'CARDCOLLECT_DEMO',
    campaign_name: '集卡演示',
    status: 'active',
    cost_per_draw: 50,
    max_draws_per_user_daily: 50,
    draw_buttons: [
      { count: 1, label: '抽一张', total_cost: 50 },
      { count: 5, label: '抽5张', total_cost: 250 }
    ],
    display: {
      mode: 'card_collect',
      grid_cols: 3,
      effect_theme: 'purple_mystery',
      rarity_effects_enabled: true,
      win_animation: 'card_flip',
      background_image_url: null,
      total_cards_in_set: 6,       // 全套卡片数量
      duplicate_convert: 'points'  // 重复卡片转换方式
    }
  },

  // 限时秒杀 + 春节主题
  'FLASHSALE_DEMO': {
    campaign_code: 'FLASHSALE_DEMO',
    campaign_name: '限时秒杀演示',
    status: 'active',
    cost_per_draw: 200,
    max_draws_per_user_daily: 1,
    draw_buttons: [
      { count: 1, label: '立即抢', total_cost: 200 }
    ],
    display: {
      mode: 'flash_sale',
      grid_cols: null,
      effect_theme: 'spring_festival',
      rarity_effects_enabled: false,
      win_animation: 'fireworks',
      background_image_url: null,
      start_time: '2026-02-14T10:00:00',  // 开抢时间
      stock_total: 50,                     // 总库存
      stock_remaining: 23                  // 剩余库存
    }
  },

  // 兼容现有活动（默认九宫格）
  'BASIC_LOTTERY': {
    campaign_code: 'BASIC_LOTTERY',
    campaign_name: '日常抽奖',
    status: 'active',
    cost_per_draw: 100,
    max_draws_per_user_daily: 50,
    draw_buttons: [
      { count: 1, label: '单抽', total_cost: 100 },
      { count: 3, label: '3连抽', total_cost: 300 },
      { count: 5, label: '5连抽', total_cost: 500 },
      { count: 10, label: '10连抽', total_cost: 900 }
    ],
    display: {
      mode: 'grid_3x3',
      grid_cols: 3,
      effect_theme: 'default',
      rarity_effects_enabled: true,
      win_animation: 'simple',
      background_image_url: null
    }
  }
}

function getConfig(campaignCode: string) {
  const config = MOCK_CONFIGS[campaignCode] || MOCK_CONFIGS['BASIC_LOTTERY']
  return { success: true, data: config }
}

module.exports = { getConfig, MOCK_CONFIGS }
```

### 10.3 mock-prizes.ts — 奖品列表模拟数据

覆盖4种稀有度，用于验证光效：

```typescript
/** 模拟奖品列表 - 覆盖4种稀有度 */
const MOCK_PRIZES = {
  // 九宫格用（8个奖品，sort_order 1-8）
  'GRID_PRIZES': [
    { id: 1, name: '精品首饰',   icon: '💎', rarity: 'legendary', sort_order: 1, probability: 1 },
    { id: 2, name: '八八折券',   icon: '🎫', rarity: 'epic',      sort_order: 2, probability: 5 },
    { id: 3, name: '生腌拼盘',   icon: '🦐', rarity: 'epic',      sort_order: 3, probability: 5 },
    { id: 4, name: '甜品1份',    icon: '🍰', rarity: 'rare',      sort_order: 4, probability: 10 },
    { id: 5, name: '2000积分',   icon: '💰', rarity: 'rare',      sort_order: 5, probability: 10 },
    { id: 6, name: '青菜1份',    icon: '🥬', rarity: 'common',    sort_order: 6, probability: 20 },
    { id: 7, name: '500积分',    icon: '🪙', rarity: 'common',    sort_order: 7, probability: 20 },
    { id: 8, name: '100积分',    icon: '🎁', rarity: 'common',    sort_order: 8, probability: 29 }
  ],

  // 转盘用（8个奖品）
  'WHEEL_PRIZES': [
    { id: 1, name: '精品首饰',   icon: '💎', rarity: 'legendary', sort_order: 1, probability: 1 },
    { id: 2, name: '八八折券',   icon: '🎫', rarity: 'epic',      sort_order: 2, probability: 5 },
    { id: 3, name: '甜品1份',    icon: '🍰', rarity: 'rare',      sort_order: 3, probability: 10 },
    { id: 4, name: '生腌拼盘',   icon: '🦐', rarity: 'rare',      sort_order: 4, probability: 10 },
    { id: 5, name: '2000积分',   icon: '💰', rarity: 'common',    sort_order: 5, probability: 15 },
    { id: 6, name: '500积分',    icon: '🪙', rarity: 'common',    sort_order: 6, probability: 20 },
    { id: 7, name: '100积分',    icon: '🎁', rarity: 'common',    sort_order: 7, probability: 20 },
    { id: 8, name: '青菜1份',    icon: '🥬', rarity: 'common',    sort_order: 8, probability: 19 }
  ],

  // 卡牌/金蛋/刮刮卡通用（不依赖sort_order）
  'GENERAL_PRIZES': [
    { id: 1, name: '精品首饰',   icon: '💎', rarity: 'legendary', sort_order: null, probability: 1 },
    { id: 2, name: '八八折券',   icon: '🎫', rarity: 'epic',      sort_order: null, probability: 5 },
    { id: 3, name: '九八折券',   icon: '🏷️', rarity: 'epic',      sort_order: null, probability: 5 },
    { id: 4, name: '甜品1份',    icon: '🍰', rarity: 'rare',      sort_order: null, probability: 10 },
    { id: 5, name: '生腌拼盘',   icon: '🦐', rarity: 'rare',      sort_order: null, probability: 10 },
    { id: 6, name: '2000积分',   icon: '💰', rarity: 'common',    sort_order: null, probability: 15 },
    { id: 7, name: '500积分',    icon: '🪙', rarity: 'common',    sort_order: null, probability: 20 },
    { id: 8, name: '100积分',    icon: '🎁', rarity: 'common',    sort_order: null, probability: 34 }
  ]
}

/** 根据活动mode返回对应奖品列表 */
function getPrizes(campaignCode: string) {
  const MockConfig = require('./mock-config')
  const config = MockConfig.MOCK_CONFIGS[campaignCode]
  const mode = config?.display?.mode || 'grid_3x3'

  let prizes
  if (mode.startsWith('grid') || mode === 'wheel') {
    prizes = mode === 'wheel' ? MOCK_PRIZES['WHEEL_PRIZES'] : MOCK_PRIZES['GRID_PRIZES']
  } else {
    prizes = MOCK_PRIZES['GENERAL_PRIZES']
  }
  return { success: true, data: prizes }
}

module.exports = { getPrizes, MOCK_PRIZES }
```

### 10.4 mock-draw-result.ts — 抽奖结果模拟数据

模拟中奖/未中奖结果，按概率随机返回：

```typescript
/**
 * 模拟抽奖结果
 * 按奖品概率加权随机，模拟真实后端行为
 */
function draw(campaignCode: string, count: number) {
  const MockPrizes = require('./mock-prizes')
  const { data: prizes } = MockPrizes.getPrizes(campaignCode)

  const results = []
  for (let i = 0; i < count; i++) {
    const prize = weightedRandom(prizes)
    results.push({
      is_winner: true,
      prize_id: prize.id,
      name: prize.name,
      icon: prize.icon,
      rarity: prize.rarity,
      sort_order: prize.sort_order,
      type: 'prize'
    })
  }

  // 单抽返回单个对象，连抽返回数组
  return {
    success: true,
    data: count === 1 ? results[0] : { items: results, count: count }
  }
}

/** 按概率加权随机选择奖品 */
function weightedRandom(prizes: any[]) {
  const totalWeight = prizes.reduce((sum, p) => sum + p.probability, 0)
  let random = Math.random() * totalWeight
  for (const prize of prizes) {
    random -= prize.probability
    if (random <= 0) return prize
  }
  return prizes[prizes.length - 1]
}

module.exports = { draw }
```

### 10.5 mock-campaigns.ts — 多活动列表模拟数据

模拟多个活动投放在不同页面，用于验证第四期多活动多页面功能：

```typescript
/** 模拟进行中的活动列表（含投放位置） */
const MOCK_ACTIVE_CAMPAIGNS = [
  {
    campaign_code: 'GRID_DEMO',
    campaign_name: '九宫格演示',
    display: { mode: 'grid_3x3', effect_theme: 'spring_festival' },
    placement: { page: 'lottery', position: 'main', size: 'full' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'SCRATCH_DEMO',
    campaign_name: '刮刮卡演示',
    display: { mode: 'scratch_card', effect_theme: 'summer' },
    placement: { page: 'discover', position: 'top_left', size: 'small' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'EGG_DEMO',
    campaign_name: '砸金蛋演示',
    display: { mode: 'golden_egg', effect_theme: 'default' },
    placement: { page: 'discover', position: 'bottom_right', size: 'small' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'CARD_DEMO',
    campaign_name: '卡牌翻转演示',
    display: { mode: 'card_flip', effect_theme: 'purple_mystery' },
    placement: { page: 'user', position: 'main', size: 'medium' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'BLINDBOX_DEMO',
    campaign_name: '虚拟盲盒演示',
    display: { mode: 'blind_box', effect_theme: 'default' },
    placement: { page: 'lottery', position: 'secondary', size: 'medium' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'GASHAPON_DEMO',
    campaign_name: '扭蛋机演示',
    display: { mode: 'gashapon', effect_theme: 'spring_festival' },
    placement: { page: 'discover', position: 'center', size: 'medium' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'LUCKYBAG_DEMO',
    campaign_name: '福袋演示',
    display: { mode: 'lucky_bag', effect_theme: 'gold_luxury' },
    placement: { page: 'user', position: 'top', size: 'small' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'SHAKE_DEMO',
    campaign_name: '摇一摇演示',
    display: { mode: 'shake', effect_theme: 'default' },
    placement: { page: 'lottery', position: 'floating', size: 'mini' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'FLIPMULTI_DEMO',
    campaign_name: '翻牌演示',
    display: { mode: 'flip_card_multi', effect_theme: 'purple_mystery' },
    placement: { page: 'discover', position: 'main', size: 'full' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'REDPACKET_DEMO',
    campaign_name: '拆红包演示',
    display: { mode: 'red_packet', effect_theme: 'spring_festival' },
    placement: { page: 'lottery', position: 'top_banner', size: 'medium' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'SLOT_DEMO',
    campaign_name: '老虎机演示',
    display: { mode: 'slot_machine', effect_theme: 'gold_luxury' },
    placement: { page: 'discover', position: 'bottom', size: 'medium' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'WHACKMOLE_DEMO',
    campaign_name: '打地鼠演示',
    display: { mode: 'whack_mole', effect_theme: 'summer' },
    placement: { page: 'user', position: 'center', size: 'medium' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'PINBALL_DEMO',
    campaign_name: '弹珠机演示',
    display: { mode: 'pinball', effect_theme: 'default' },
    placement: { page: 'discover', position: 'top_right', size: 'small' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'CARDCOLLECT_DEMO',
    campaign_name: '集卡演示',
    display: { mode: 'card_collect', effect_theme: 'purple_mystery' },
    placement: { page: 'user', position: 'bottom', size: 'medium' },
    cover_image: '',
    status: 'active'
  },
  {
    campaign_code: 'FLASHSALE_DEMO',
    campaign_name: '限时秒杀演示',
    display: { mode: 'flash_sale', effect_theme: 'spring_festival' },
    placement: { page: 'lottery', position: 'top', size: 'small' },
    cover_image: '',
    status: 'active'
  }
]

/** 获取全部活动 */
function getActiveCampaigns() {
  return { success: true, data: MOCK_ACTIVE_CAMPAIGNS }
}

/** 按页面过滤活动 */
function getCampaignsByPage(pageName: string) {
  const filtered = MOCK_ACTIVE_CAMPAIGNS.filter(c =>
    c.placement.page === pageName || c.placement.page === 'all'
  )
  return { success: true, data: filtered }
}

module.exports = { getActiveCampaigns, getCampaignsByPage, MOCK_ACTIVE_CAMPAIGNS }
```

### 10.6 开发阶段快速切换玩法预览

开发过程中，可以通过修改 `mock-config.ts` 中 `BASIC_LOTTERY` 的 `display.mode` 来快速切换当前抽奖页的玩法预览：

```typescript
// 想看九宫格效果 → mode: 'grid_3x3'
// 想看转盘效果   → mode: 'wheel'
// 想看卡牌效果   → mode: 'card_flip'
// 想看金蛋效果   → mode: 'golden_egg'
// 想看刮刮卡效果 → mode: 'scratch_card'
// 想看盲盒效果   → mode: 'blind_box'
// 想看扭蛋效果   → mode: 'gashapon'
// 想看福袋效果   → mode: 'lucky_bag'
// 想看摇一摇效果 → mode: 'shake'
// 想看翻牌效果   → mode: 'flip_card_multi'
// 想看红包效果   → mode: 'red_packet'
// 想看老虎机效果 → mode: 'slot_machine'
// 想看打地鼠效果 → mode: 'whack_mole'
// 想看弹珠机效果 → mode: 'pinball'
// 想看集卡效果   → mode: 'card_collect'
// 想看秒杀效果   → mode: 'flash_sale'

// 想看不同主题色 → effect_theme: 'spring_festival' / 'gold_luxury' / 'purple_mystery' 等
// 想看不同中奖动画 → win_animation: 'simple' / 'card_flip' / 'fireworks'
```

无需等待后端，改一个字段值即可在真机上预览全部效果组合。

### 10.7 Mock数据覆盖的验证场景

| 验证场景 | 对应mock数据 | 预期效果 |
|---------|-------------|---------|
| 16种玩法切换 | mock-config 的 display.mode | 组件路由正确加载对应子组件 |
| 6套主题色 | mock-config 的 effect_theme | CSS变量切换，颜色正确 |
| 4种稀有度光效 | mock-prizes 的 rarity 字段 | common蓝/rare紫/epic橙/legendary金 |
| 3种中奖动画 | mock-config 的 win_animation | simple弹窗/card_flip翻转/fireworks烟花 |
| 单抽流程 | mock-draw-result count=1 | 单个结果展示 |
| 连抽流程 | mock-draw-result count=3/5/10 | 多个结果逐个揭晓 |
| 多活动多页面 | mock-campaigns 的 placement | 不同页面显示不同活动入口 |
| size适配 | mock-campaigns 的 size | full/medium/small/mini 四种尺寸 |

---

**文档版本**: 1.9.0
**创建日期**: 2026-02-11
**更新日期**: 2026-02-11（新增架构决策1.3：渲染器选择 — 全局WebView，清理Skyline残留配置；原1.3-1.7顺延为1.4-1.8）
**依据文档**: 《多活动抽奖系统-三端协作方案》v1.2.0 + 《抽奖UI设计方案-行业对比与技术可行性分析》v1.0.0
**适用项目**: 天工餐厅积分抽奖微信小程序前端
