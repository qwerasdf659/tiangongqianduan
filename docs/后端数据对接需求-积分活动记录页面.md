# 🔧 后端数据对接需求 - 积分活动记录页面

> **前端页面**: `pages/records/trade-upload-records/trade-upload-records`
> **对接日期**: 2026-02-15
> **优先级**: 🔴 高（直接影响用户看到的数据展示）

---

## 🚨 问题1：消费记录 `created_at` 字段返回了非字符串类型

**当前现象**: 消费记录列表中，时间显示为 `[object Object]`

**涉及API**: `GET /api/v4/shop/consumption/me`

**问题原因**: 后端返回的 `created_at` 字段不是字符串类型，前端直接绑定模板时显示为 `[object Object]`

**期望格式**: `created_at` 应为 ISO 8601 字符串格式

```json
{
  "success": true,
  "data": {
    "records": [
      {
        "consumption_record_id": 1,
        "created_at": "2026-02-15 14:30:00",
        "status": "approved",
        "consumption_amount": 50,
        "points_awarded": 5,
        "merchant_notes": "备注内容"
      }
    ]
  }
}
```

**需要后端确认**:
- `created_at` 字段当前实际返回的数据类型是什么？
- 是否是 Sequelize 的 Date 对象未序列化？
- 请确保返回格式为 `"YYYY-MM-DD HH:mm:ss"` 字符串

---

## 🚨 问题2：交易记录API字段名确认

**涉及API**: `GET /api/v4/assets/transactions`

**前端已按以下字段名对接，请后端确认或纠正**:

| 前端使用的字段名 | 含义 | 类型 | 示例值 |
|---|---|---|---|
| `transaction_id` | 交易ID | string | "TXN_20260215_001" |
| `transaction_title` | 交易标题 | string | "兑换商品：测试商品" |
| `transaction_type` | 交易类型 | string | "earn" 或 "consume" |
| `points_amount` | 积分数量（正数） | number | 100 |
| `business_type` | 业务类型 | string | "lottery"/"upload"/"exchange"/"trade"/"checkin" |
| `description` 或 `transaction_description` | 描述 | string | "每日签到奖励" |
| `transaction_time` 或 `created_at` | 交易时间 | string | "2026-02-15 14:30:00" |

**需要后端确认**:
1. 以上字段名是否正确？
2. `transaction_type` 的值是 `earn`/`consume` 还是 `income`/`expense`？
3. `points_amount` 是始终为正数，还是消费时为负数？
4. `business_type` 支持哪些枚举值？

**期望响应格式**:

```json
{
  "success": true,
  "data": {
    "transactions": [
      {
        "transaction_id": "TXN_20260215_001",
        "transaction_title": "每日签到奖励",
        "transaction_type": "earn",
        "points_amount": 10,
        "business_type": "checkin",
        "description": "签到获得10积分",
        "created_at": "2026-02-15 08:00:00"
      }
    ],
    "pagination": {
      "page": 1,
      "page_size": 20,
      "total": 50,
      "hasMore": true
    }
  }
}
```

---

## ⚠️ 问题3：缺少交易统计数据API

**当前现象**: 积分活动记录页面的统计面板（可用积分/冻结积分/总资产）数据来自 `GET /api/v4/assets/balance`

**前端已使用的统计API**: `GET /api/v4/assets/balance?asset_code=POINTS`

**需要后端确认该API返回格式**:

```json
{
  "success": true,
  "data": {
    "asset_code": "POINTS",
    "available_amount": 1500,
    "frozen_amount": 200,
    "total_amount": 1700
  }
}
```

**可选增强**: 如果业务需要展示"总收入"和"总支出"统计，建议后端在交易记录API中增加 `stats` 字段：

```json
{
  "success": true,
  "data": {
    "transactions": [...],
    "stats": {
      "total_income": 5000,
      "total_expense": 3500,
      "net_income": 1500,
      "transaction_count": 45
    },
    "pagination": {...}
  }
}
```

---

## 📋 前端已完成的工作

1. ✅ 交易记录字段映射：将后端 `points_amount`/`transaction_title`/`transaction_type` 等字段映射到模板显示
2. ✅ 消费记录时间安全处理：增加了 `created_at` 非字符串类型的安全转换
3. ✅ UI主题色统一：从蓝色(#4facfe)修正为项目橙色(#FF6B35)
4. ✅ 统计面板改为余额汇总：使用 `GET /api/v4/assets/balance` 获取可用/冻结/总资产

---

## 🎯 后端需要回复的清单

- [ ] 确认消费记录API `created_at` 字段是否能改为字符串格式
- [ ] 确认交易记录API的实际字段名列表
- [ ] 确认 `GET /api/v4/assets/balance` 的实际返回格式
- [ ] （可选）是否计划在交易记录API中增加统计数据

