# 后端对接需求 — 前端 D3 分页统一与字段适配

> **日期**：2026-03-24
> **提交方**：微信小程序前端
> **状态**：前端代码已完成修改，等待后端确认

---

## 一、D3 分页参数统一（前端已完成适配）

### 1.1 前端已完成的修改

前端已将所有 API 请求中的分页参数 `limit` 统一改为 `page_size`，具体涉及以下接口：

| API 路径 | 原参数 | 改后参数 | 修改文件 |
|---|---|---|---|
| `GET /api/v4/lottery/history` | `limit` | `page_size` | `utils/api/lottery.ts` |
| `GET /api/v4/system/feedback/my` | `limit` | `page_size` | `utils/api/system.ts` |
| `GET /api/v4/system/chat/sessions/:id/messages` | `limit` | `page_size` | `utils/api/system.ts` |
| `GET /api/v4/market/listings` | `limit` | `page_size` | `utils/api/market.ts` |
| `GET /api/v4/market/my-listings` | `limit` | `page_size` | `utils/api/market.ts` |
| `GET /api/v4/market/my-orders` | `limit` | `page_size` | `utils/api/market.ts` |
| `GET /api/v4/console/customer-service/sessions` | `limit` | `page_size` | `utils/api/console.ts` |
| `GET /api/v4/console/customer-service/sessions/:id/messages` | `limit` | `page_size` | `utils/api/console.ts` |
| `GET /api/v4/console/lottery-user-analysis/history/:user_id` | `limit` | `page_size` | `utils/api/console.ts` |
| `GET /api/v4/user/ad-campaigns` | `limit` | `page_size` | `utils/api/ad-campaigns.ts` |

### 1.2 需要后端确认的事项

请后端确认以下路由是否已完成 D3 修改（从 query 中读 `page_size` 而非 `limit`）：

| 路由文件 | 接口 | 需确认 |
|---|---|---|
| `routes/v4/lottery/campaigns.js` 或 `history.js` | `GET /api/v4/lottery/history` | 是否已从 query 读 `page_size`？ |
| `routes/v4/system/feedback.js` | `GET /api/v4/system/feedback/my` | 是否已从 query 读 `page_size`？ |
| `routes/v4/system/chat.js` | 会话消息历史 | ✅ 技术债务文档已确认已改 |
| `routes/v4/market/listings.js` | 挂单列表 + 我的挂单 | ✅ 技术债务文档已确认已改 |
| `routes/v4/market/sell.js` 或 `orders.js` | `GET /api/v4/market/my-orders` | 是否已从 query 读 `page_size`？ |
| `routes/v4/console/customer-service/` | 客服会话列表 + 消息历史 | 是否已从 query 读 `page_size`？ |
| `routes/v4/console/lottery-user-analysis/` | 管理员查用户抽奖历史 | 是否已从 query 读 `page_size`？ |
| `routes/v4/user/ad-campaigns.js` | 我的广告活动列表 | 是否已从 query 读 `page_size`？ |

### 1.3 未修改的接口（保持 `limit`）

以下接口前端仍使用 `limit` 参数，因为它们不是分页参数，而是 "top N" 语义：

| API 路径 | 参数 | 原因 |
|---|---|---|
| `GET /api/v4/market/price/recent-trades` | `limit` | "最近成交 N 条"，非分页 |

### 1.4 响应分页字段确认

前端 `typings/api.d.ts` 中 `Pagination` 接口定义为：

```typescript
interface Pagination {
  page: number       // 当前页码
  page_size: number  // 每页条数
  total: number      // 记录总数
  total_pages: number // 总页数
}
```

请后端确认所有分页响应的 `pagination` 对象字段名是否与上述一致（特别是是否已将 `limit` 响应字段改为 `page_size`）。

---

## 二、需要后端提供的 API（前端已编写调用代码，等待后端实现）

### 2.1 客服快捷回复模板 API

**文件位置**：`packageAdmin/customer-service/customer-service.ts` L70-87

**现状**：前端临时硬编码了 5 条快捷回复模板，注释标注等待后端 API 就绪

**需要后端提供**：

```
GET /api/v4/console/customer-service/gm-tools/templates
数据来源: system_configs 表 config_key='cs_reply_templates'

期望响应:
{
  "success": true,
  "data": {
    "templates": [
      { "id": 1, "title": "欢迎", "content": "您好！很高兴为您服务..." },
      ...
    ]
  }
}
```

### 2.2 全局氛围主题配置 API

**文件位置**：`utils/api/system.ts` `getAppThemeConfig()`

**现状**：前端已编写调用代码，API 注释标注"需要后端实现"

**需要后端提供**：

```
GET /api/v4/system/config/app-theme
数据来源: system_configs 表 config_key='app_theme'

期望响应:
{
  "success": true,
  "code": "APP_THEME_CONFIG_SUCCESS",
  "data": { "theme": "gold_luxury" }
}
```

### 2.3 C2C担保码相关 API（Phase 4）

**文件位置**：`utils/api/market.ts` `getEscrowStatus()` / `confirmDelivery()`

**现状**：前端已编写完整的调用代码和参数校验，注释标注"需要后端 Phase 4 实施完成"

**需要后端确认**：
- `GET /api/v4/market/trade-orders/:trade_order_id/escrow-status` — 是否已实现？
- `POST /api/v4/market/trade-orders/:trade_order_id/confirm-delivery` — 是否已实现？

### 2.4 交易申诉 API（文档 5.4）

**文件位置**：`utils/api/market.ts` `createTradeDispute()` / `getTradeDispute()`

**需要后端确认**：
- `POST /api/v4/market/trade-orders/:trade_order_id/dispute` — 是否已实现？
- `GET /api/v4/market/trade-orders/:trade_order_id/dispute` — 是否已实现？

---

## 三、前端发现的字段对齐问题

### 3.1 `category-cascade` 组件事件字段已统一

前端 `category-cascade` 组件触发的 `change` 事件 detail 字段已从 `categoryDefId` 改为 `categoryId`，与后端 `categories` 表的 `category_id` 主键对齐。

### 3.2 `getRecentTrades` 响应字段未知格式

前端代码（`packageTrade/trade/market/market.ts` L365-367）对响应做了双重兼容：

```typescript
const tradesList = Array.isArray(tradesResponse.data)
  ? tradesResponse.data
  : tradesResponse.data.trades || []
```

**请后端确认** `GET /api/v4/market/price/recent-trades` 的实际响应格式：
- A: `data` 直接是数组 `[{ trade_order_id, price_amount, ... }]`
- B: `data` 是对象 `{ trades: [...] }`

确认后前端将移除不需要的兼容分支。

---

## 四、前端已确认正常的机制

| 机制 | 状态 | 说明 |
|---|---|---|
| APIClient 请求封装 | ✅ 正常 | `utils/api/client.ts`，统一 `wx.request` 封装 |
| JWT 双 Token 认证 | ✅ 正常 | `access_token` + `refresh_token`，过期自动刷新 |
| 认证头处理 | ✅ 正常 | `Authorization: Bearer {token}`，401 自动刷新重试 |
| 错误处理 | ✅ 正常 | 覆盖 400/401/403/404/409/429/500/503 等状态码 |
| 维护模式 | ✅ 正常 | 503 状态码检测 + 全局维护弹窗 |
| Idempotency-Key | ✅ 正常 | 放在 Header 中（POST 抽奖/购买/上架等操作） |
| `utils/index.ts` 统一导出 | ✅ 正常 | 分类导出，无循环依赖 |
| Mock 数据 | ✅ 无 | 业务代码中无任何 mock 数据 |
| `category_def_id` 残留 | ✅ 无 | `.ts` 文件中无 `category_def` / `category_def_id` 引用 |
| 分页参数统一 | ✅ 已完成 | 所有分页 API 使用 `page_size`（D3 对齐） |

---

## 五、前端代码质量检测结果

| 工具 | 结果 |
|---|---|
| ESLint | **0 errors**, 23 warnings（均为预存的 `.js` 编译产物 `unused vars`，非本次引入） |
| Prettier | **所有修改文件格式正确** |
| TypeScript 类型 | `typings/api.d.ts` 分页类型已使用 `page_size` |
