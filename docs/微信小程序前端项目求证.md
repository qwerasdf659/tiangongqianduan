# 微信小程序前端项目求证清单（按真实代码核实）

> **归属**：微信小程序前端项目 → 后端数据库项目
> **核实日期**：2026-06-06 北京时间
> **核实方式**：逐条对照当前仓库真实代码（非引用历史报告），工具实测
> **业务背景**：餐厅积分抽奖系统，已下线用户间(C2C)交易，改为用户↔官方(B2C)单向「道具商城」
> **范围**：列出"前端无法自行决定、必须由后端/数据库或运营确认/提供"的事项，并标注纯前端可自决项

---

## 一、问题核实结论总表（基于真实代码）

| 编号 | 问题 | 真实代码核实 | 结论 | 归属 |
|---|---|---|---|---|
| 一 | 同一订单两个售后/申诉入口并存 | `packageTrade/**` 整个子包已删除，`app.json` 分包无 `packageTrade`；旧 C2C 申诉链路不存在 | 已消除 | — |
| 二 | 旧申诉页字段/状态机用旧语义 | `packageTrade/trade/dispute/`、`utils/api/market.ts`、`auction.ts` 均已随 C2C 下线删除 | 已消除 | — |
| 三 | 意见反馈入口缺失（孤儿页） | `pages/user/user.ts` `menuItems` 无"意见反馈"；`feedback` 页跳转仅在其子页互跳 | ✅ 已修复（2026-06-07） | 纯前端 |
| 四 | 兑换订单(redemption)无售后入口 | `create.ts` 白名单已收窄为 `['consumption']`，`exchange-order-detail` 无"申请售后"入口 | 入口仍缺（白名单描述需更新） | 依赖后端求证 |
| 五 | feedback 页 .ts/.js 并存 | `feedback/list/detail` 三页 `.js` 为空壳 `Page({})`，`disputes` 三页只有 `.ts` | ✅ 已修复（2026-06-07） | 纯前端 |

> **说明**：原文档前半部分（问题一、二）写于 C2C 下线改造**之前**，所依赖的 `packageTrade` 子包现已整体删除，故这两项已不再存在，本次精简移除。问题四的入口确实仍缺，但白名单现状已是 `['consumption']`（非旧文档所述的 `['trade','consumption','auction']`），本文已更正。



---

## 二、纯前端已完成项（2026-06-07 落地，工具实测通过）

> 以下为纯微信小程序前端改动，不涉及 API/后端逻辑。已过 ESLint(0) + Prettier(0) + `tsc --noEmit`(0)。

### 问题三：意见反馈孤儿页 → 已补入口

- **原现象**：`packageUser/feedback/{feedback,list,detail}` 三页与 API 均已写好，但 `pages/user/user.ts` 的 `menuItems` 无"意见反馈"项，用户无任何入口可进入。
- **影响**：合规要求的"在线客服 + 意见反馈 + 我的售后"三入口缺一，`feedbacks` 表 C 端写入通道实质不可达。
- **已处置**：在 `pages/user/user.ts` 的 `menuItems` 中"我的售后"与"联系客服"之间新增"意见反馈"项（`id:'my-feedback'`，`type:'page'`，`url:'/packageUser/feedback/feedback'`，图标 `iconClass:'icon-edit'` + `tdIcon:'edit-1-filled'`，均为项目已有图标）；同步更新类注释的功能菜单列表。

### 问题五：feedback 三页 `.js` 空壳 → 已删除

- **原现象**：项目用 TS 编译插件（`useCompilerPlugins:["typescript","sass"]`）。`packageUser/feedback/` 下 `feedback/list/detail` 同时存在 `.ts` 与空壳 `.js`（`Page({ data:{} })`，无业务逻辑）；`packageUser/disputes/` 三页只有 `.ts`。
- **影响**：冗余空壳 `.js` 可能被开发者工具优先加载，造成"改了 .ts 不生效"或新旧混跑的排查困难。
- **已处置**：删除 `packageUser/feedback/{feedback,list,detail}.js` 三个空壳（经全局检索确认无 `require` 引用），统一由 `.ts` 编译，与 `disputes` 三页一致。



---

## 三、P0 — 阻塞道具商城上线（必须后端/运营先做）

> 道具直购走现有 `exchange` 域：`GET /api/v4/exchange/items` + `POST /api/v4/exchange/`；前端货架(exchange-shelf)已对接，完全依赖接口返回，不硬编码。

| 编号 | 求证项 | 现状 | 需后端/运营提供 |
|---|---|---|---|
| Q-P0-1 | 道具商品（`item_type='prop'`）真实数据是否已配置 | admin 页面已就位，prop 商品 0 条 | 运营在 admin 建 `item_type='prop'` 模板 + 兑换商品。前端按 `GET /exchange/items` 返回渲染，不硬编码 |
| Q-P0-2 | 道具的 `star_stone` 渠道价是否已配置 | `exchange_channel_prices` 计价全为 `red_core_shard`，0 条 `star_stone` | 运营配置道具商品 `cost_asset_code='star_stone'` 渠道价，否则用户看到道具却无法用星石购买 |
| Q-P0-3 | 道具直购订单如何与"可退实物单"区分 | 道具单建单即 `completed`、即时到账、禁退款（`PROP_NO_REFUND`）；实物单仍 `pending`+可退 | `GET /api/v4/exchange/orders` 返回里前端用哪个字段判断"不可退道具单"？`item_type='prop'`、`source`、还是 `refundable` 布尔？前端据此决定是否显示"取消/退款"按钮，不自行猜测 |



### 以物易物（B2C 官方合成）C 端字段契约缺失

> 后端称已上线 `BarterService` + 路由 `GET /api/v4/exchange/barter/recipes`、`POST /api/v4/exchange/barter`，但面向小程序的 C 端字段契约未提供，前端不凭空猜测请求体/响应字段。

| 编号 | 求证项 | 现状 | 需后端提供 |
|---|---|---|---|
| B-1 | 配方列表接口 C 端字段契约 | 仅知路径，字段未知 | 响应 `data` 结构：配方所需旧物 `item_template_id`/数量、产出 `exchange_item_id`/`item_name`/`image`、是否启用。前端按后端原名 snake_case 直接渲染 |
| B-2 | 提交接口请求体与幂等键 | 仅知路径 | 请求体字段名（`recipe_code` 还是 `recipe_id`？旧物 `item_id`/数量）、是否走 Header `Idempotency-Key`、成功响应结构（产出物、`order_no`、`source='barter'`） |
| B-3 | 错误码枚举 | 未知 | 旧物不属本人/已核销/方向向上被拒/配方停用 等场景的 `code`，前端透传后端中文 message |
| B-4 | 配方真实数据是否已配置 | admin 已就位，0 条配方 | 运营在 admin「以物易物配方」填真实配方后告知。前端完全依赖接口返回，无硬编码 |
| B-5 | 入口归属确认 | 待定 | 入口挂"我的仓库"还是兑换页新增 Tab？产品决策 |

### 抽奖概率公示 / "中奖资格与消费挂钩" C 端只读接口缺失（合规硬项）

> 合规要求"抽奖规则页"补"中大奖概率区间+上限+保底"与"中奖资格与消费挂钩"的人话披露。后端目前只有管理后台侧成长等级/倍数接口，尚未开放 C 端只读公示接口。前端不硬编码概率数字与合规措辞。

| 编号 | 求证/决策项 | 现状 | 需后端/产品提供 |
|---|---|---|---|
| L-1 | 是否开放 C 端"概率公示"只读接口 | 尚未开放 | 是否新增只读接口返回"概率区间/上限/保底说明/消费挂钩说明"文案？前端按返回展示 |
| L-2 | 公示文案口径（合规文本） | 拟"中大奖概率 0–8%、动态浮动、设保底"+"高价值奖品需达消费/积分门槛" | 文案需律师复核后由后端下发或后台配置，前端不硬编码 |
| L-3 | 成长等级是否对 C 端可见、倍数是否暴露 | 倍数可能涉运营策略泄密 | 产品/合规决定是否展示"我的成长等级"，倍数是否脱敏，需后端给字段与脱敏口径 |
| L-4 | 保底 UI 是否恢复展示 | `lottery-activity.ts` 的 `showPityUI` 总开关当前关闭，后端保底引擎仍静默运行 | 产品确认是否展示"保底进度"。若展示，确认 `config` 接口 `pity_info`/`pity_enabled` 是否对 C 端下发 |

> 补充（2026-06-09）：`文案合规改造方案.md`（路线B）已定案用活动 `config.rules_text`（动态定性表述，不写死固定比例）作为"概率/规则公示"载体，前端 `lottery-activity.ts:525` 已读取 `config.rules_text` 并在 `lottery-modes.wxml:351` 的"回馈规则说明"区展示。故 L-1/L-2 可能无需新增独立公示接口——请后端确认：是否就以 `rules_text` 为唯一公示口径，L-1 的独立只读接口可关闭？

### 高价值实物 `value_tier` 分层 + 复合门槛 C 端对接缺失（合规硬项 / 路线B 前提）

> 依据 `文案合规改造方案.md` 第七节 + 模块C：高价值实物不得"固定碎片直兑"，须按 `value_tier` 分层并套"成长等级 + 多资产 + 消耗道具"复合门槛，对外做成"会员解锁/会员尊享权益"。后端模块C 已落地（`item_templates.value_tier` 字段、`exchange_redeem_requirement` 表、`CoreService._assertRedeemRequirement` 兑换校验、门槛 CRUD 路由）。但**前端全仓零引用 `value_tier`/门槛字段**，且这些数据是否对 C 端下发未知，前端不凭空猜测。

| 编号 | 求证/决策项 | 现状 | 需后端/产品提供 |
|---|---|---|---|
| V-1 | 兑换商品接口是否下发 `value_tier` | 后端已给 `item_templates` 加 `value_tier enum('low','mid','high')`；前端 `exchange-detail`/`exchange-shelf` 价格区只用 `cost_amount`+`_priceLabel`，零引用 `value_tier` | 确认 `GET /api/v4/exchange/items`、`/items/:id` 响应是否含 `value_tier`；前端据此对 high 档做差异化"会员解锁"展示，不按价格自行猜测 |
| V-2 | 复合门槛是否有 C 端只读出口 | 后端 `exchange_redeem_requirement`（最低成长等级 + `extra_cost_assets` + `required_consume_items`）仅在兑换扣费时校验，C 端无法提前获知门槛 | 是否开放只读接口或在商品详情下发 `redeem_requirement`：最低成长等级、额外消耗资产组合、需消耗的指定道具及数量？前端据此在详情页渲染"解锁条件"清单，不硬编码 |
| V-3 | 门槛不满足的错误码枚举 | 已知实物侧 `STONE_BUY_VALUABLE_FORBIDDEN`；门槛校验失败（等级不够/消耗道具不足/多资产余额不足）抛什么 `code` 未知 | 提供各失败场景的 `code` 与中文 message，前端透传后端 message、按 `code` 决定提示，不自拟文案 |
| V-4 | "会员解锁"展示所需成长等级字段 | 门槛依赖 `user_growth_levels`，前端要展示"当前等级 X，需达 Y 解锁"需当前等级 + 目标等级字段 | 确认 C 端可读的当前成长等级字段（`level_key`/`level_name`）与门槛目标等级口径；与 L-3 一并裁决成长等级 C 端可见性与脱敏 |
| V-5 | 确认不向 C 端下发现金标价 | 实测前端兑换页已无 ¥ 现金标价（用积分/碎片 `cost_amount`），`reference_price_points` 前端零引用，符合路线B 去现金锚点 | 确认后端不把 `reference_price_points` 作为"市价/参考价"下发 C 端；若仍下发，请明确前端应忽略（避免重建现金锚点） |



---

## 四、P1/P2 — 契约字段、状态与枚举对齐（前端按返回写代码，需后端书面确认口径）

### 兑换页与官方竞价

| 编号 | 求证项 | 前端现状 | 需后端确认 |
|---|---|---|---|
| Q-P1-1 | `GET /api/v4/system/config/exchange-page` 的 `tabs` 是否从源头去掉 `market` | 前端已移除"交易市场"Tab，并防御性过滤 `key === 'market'`（`pages/exchange/exchange.ts:190`） | 配置 JSON 的 `tabs` 是否应直接删 market Tab（配置即权威）？`market_filters`、`card_display.market_cta_text` 前端已不消费，可否一并移除 |
| Q-P1-2 | 官方竞价 `/api/v4/exchange/bid/*` 字段口径 | bid-panel 已对接，用后端原名不做映射 | 列表/详情：`bid_product_id`/`exchange_item_id`/`start_price`/`current_price`/`min_bid_increment`/`price_asset_code`/`status`(7态)/`start_time`/`end_time`/`bid_count`/`winner_user_id`；出价 `POST /exchange/bid` body `{bid_product_id,bid_amount}`+Header `Idempotency-Key`；历史是 `is_winning` 还是 `is_highest` |
| Q-P1-3 | 竞价货币是否恒为 `star_stone` | 竞价锁星石、碎片不参与；前端面板拟仅允许星石 | 确认 `bid_products.price_asset_code` 是否恒为 `star_stone`，前端据此不展示其它币种 |

### C2C 下线后的残留确认

| 编号 | 求证项 | 前端现状 | 需后端确认 |
|---|---|---|---|
| Q-P1-4 | 背包 `allowed_actions` 是否还下发 `sell` | C2C 下线后前端不渲染"上架"按钮 | `GET /api/v4/backpack/` 的 `allowed_actions` 是否已移除 `sell`？若仍下发前端会忽略，建议后端同步去掉 |
| Q-P1-5 | C2C 通知类型 / WebSocket 事件是否已停推 | 前端已删处理逻辑与类型 | 通知类型 `listing_created`/`listing_sold`/`listing_withdrawn`/`listing_expired`/`purchase_completed`/`trade_complete_seller`/`trade_complete_buyer` 是否停写 `user_notifications`；事件 `auction_outbid`/`auction_won`/`auction_lost`/`auction_new_bid` 是否停推 |
| T-1 | 背包是否仍存在 `item_type='tradable_item'` 物品 | "我的仓库"仍有「可交易」Tab + `categoryStats.tradable_item`（`inventory` 三端均有） | 去交易化后背包是否还有 `tradable_item` 物品？仍有则保留该 Tab（仅展示无挂卖动作）；已无则前端删 Tab + 分类统计。需后端给 item_type 取值集，前端不擅删（删错会隐藏物品） |

### 售后申诉 order_type

| 编号 | 求证项 | 前端现状 | 需后端确认 |
|---|---|---|---|
| O-1 | `trade_disputes.order_type` 已收窄为 `('redemption','consumption')` | 前端 `create.ts`/`disputes.ts`/`detail.ts`/`system.ts` 文案映射已收敛为 `redemption/consumption`，删除已废除的 `trade`/`auction` | 确认后端枚举即 `redemption/consumption`；若仍接受其它值请告知 |
| O-2（问题四） | 兑换订单 `redemption` 售后入口何时开放 | `create.ts` 白名单当前仅 `['consumption']`（唯一已接通入口）；`redemption` 已在文案映射预留但**入口未接通**，`exchange-order-detail` 无"申请售后"入口 | 兑换订单发起售后时 `order_id` 用什么主键（`RedemptionOrder` 主键为 UUID）、是否仅 `fulfilled` 可申诉？确认后前端在白名单加 `redemption` + 在 `exchange-order-detail` 补入口 |
| O-3（问题三附带） | 意见反馈分类枚举不一致 | 前端 `feedback.ts` 用 `general/technical/account/payment/suggestion/complaint`（25–30 行）；**意见反馈入口已于 2026-06-07 开通**，枚举对齐成为唯一前端待确认项 | 后端 `feedbacks.category` 实际接受哪组枚举？前端 `general/account/payment` 是否会被拒？需对齐后前端调整 |

### 历史数据

| 编号 | 求证项 | 前端现状 | 需后端确认 |
|---|---|---|---|
| D-1 | 历史 C2C 订单是否需要只读查看 | 前端已删"我的挂单/我的交易订单"入口 | 上线前是否有用户产生过 C2C 订单需可查？需要则后端提供只读历史接口；不需要则前端不再提供任何 C2C 历史入口（当前即此状态） |

### 去"抽奖/中奖"叙事改造后的后端待确认项（2026-06-09）

> 依据 `微信小程序前端对接文档-去餐厅化与去抽奖叙事.md`，前端用户可见层已全部改为"回馈"口径（结果弹窗、tab、导航标题、分享文案、平台名"天工平台"），技术层字段名/API/code 按文档照后端原样保留。以下两项前端已落地规避方案，但口径最终由后端确认。

| 编号 | 求证项 | 前端现状 | 需后端确认 |
|---|---|---|---|
| N-1 | 积分记录聚合是否需保留"抽奖"关键词匹配（存量历史数据口径） | `packageUser/points-detail/points-detail.ts:583` 聚合积分记录时用 `businessType === 'lottery_consume' \|\| desc.includes('回馈') \|\| desc.includes('抽奖') \|\| desc.includes('lottery')` 多关键词匹配。其中 `desc.includes('抽奖')` 是为识别**后端存量历史记录**（老数据 `description` 字段含"抽奖"字样）。注：这是**数据识别逻辑**（按后端返回的 description 文本聚合），非 UI 兼容层 | `lottery_transactions`/积分流水表的存量历史记录 `description` 字段是否已统一刷为"回馈"口径？① 若已统一：前端可去掉 `desc.includes('抽奖')` 分支，仅留"回馈/lottery_consume/lottery"；② 若未统一（历史数据仍是"抽奖消耗"等）：前端需保留该分支，否则历史记录会被漏聚合。请后端明确存量数据是否已迁移 |
| N-2 | `reward_tier_text` 字段文案是否统一为"回馈"口径 | 抽奖结果接口 `POST /api/v4/lottery/draw` 仍下发 `reward_tier_text`，当前值为"低档奖励/中档奖励/高档奖励/保底奖励"（含"奖励"字样、未统一为回馈口径）。前端已按对接文档 5.3 **不直接展示该字段**，改读 `reward_tier` 枚举（`high/mid/low/fallback`）自行映射为"尊享回馈/优享回馈/常享回馈/基础礼遇" | 后端是否计划将 `reward_tier_text` 值统一为回馈口径？① 在统一前：前端维持"读 `reward_tier` 自映射、忽略 `reward_tier_text`"的现状，不受影响；② 若后端后续统一：请同步告知新值，前端确认是否切换为直接展示。属后端待统一项，**不阻塞前端** |

### 协议页（用户协议 / 隐私政策）正文接口缺失（合规硬项 / 阻塞前端，2026-06-09）

> 登录弹窗的《用户协议》《隐私政策》入口需展示协议正文。协议正文为**法务文本**，前端不硬编码、不编造，统一由后端接口下发。经核实后端当前**无任何协议内容接口**。前端已建好协议页框架（`packageUser/agreement/agreement`，已注册 `app.json`，已接通 login-popup 入口并按 `type` 区分两类文档），并预置 API 调用 `API.getAgreementDocument(doc_type)`（`utils/api/system.ts`）。在后端接口就绪前，页面**明确报错提示 + 重新加载按钮**（符合"缺失必需数据应明确报错、不静默降级、不展示假内容"红线），不会再出现点击无反应。

| 编号 | 求证项 | 前端现状 | 需后端/法务提供 |
|---|---|---|---|
| A-1 | 协议正文只读接口是否提供 | 前端已调用 `GET /api/v4/system/agreement/:doc_type`（`doc_type` 取 `user_agreement`/`privacy_policy`），后端尚未实现，调用返回失败时前端展示"协议内容获取失败/暂未配置"并提供重试 | 后端新增只读接口 `GET /api/v4/system/agreement/:doc_type`。期望响应 `data` 结构（字段 snake_case 照后端原样）：`title`（文档标题）、`updated_at`（更新日期，北京时间字符串）、`sections[]`（正文段落数组 `{ heading?: string, text: string }`）。建议正文存 `system_configs`/专表，由后台维护 |
| A-2 | 协议正文内容与运营主体落定 | 前端页面已固定展示品牌"天工平台" + 运营主体"东莞市墨珩数字科技有限公司"（对接文档第六节口径），正文区留空待接口下发 | 用户协议、隐私政策正文需**法务/运营提供并经律师复核**后录入后台。前端不编写、不硬编码任何协议条款文本 |
| A-3 | 隐私政策合规项（小程序必备） | 入口已就位（《用户协议》+《隐私政策》） | 确认隐私政策是否覆盖微信小程序必备披露项（收集的个人信息类型、用途、第三方 SDK、数据存储与权限），与微信平台《隐私保护指引》对齐 |



---

## 五、数据敏感（前端已遵循，列出供后端知悉，非求证）

- **`BUDGET_POINTS`（预算账户）**：前端不请求、不展示（DataSanitizer 屏蔽）。抽奖"中奖资格与消费挂钩"仅以人话规则页披露、不暴露数字。
- **资产转换方向**：前端仅提供"碎片/源晶 → 星石"单向入口，不提供反向（后端 `CONVERT_DIRECTION_FORBIDDEN` 兜底）。
- **星石计价边界**：前端按后端返回的 `cost_asset_code` 展示，不自行判断某商品能否用星石买，由后端 `AssetProductGuard` 校验（`STONE_BUY_VALUABLE_FORBIDDEN`）。
- **去现金锚点（路线B）**：前端兑换页不展示 `reference_price_points` 现金参考价，仅展示积分/碎片 `cost_amount`；高价值实物的价值由 `value_tier` + 复合门槛体现（见 V 系列），不以现金或固定碎片数锚定。
- **抽奖商业机密字段**：奖品列表/抽奖结果接口已脱敏，前端零引用 `win_probability`/`win_weight`/`stock_quantity`/`cost_points`/`max_daily_wins`。



---

## 六、技术框架与业务背景（对齐真实配置）

- **业务模式**：餐厅积分抽奖系统。已下线用户间(C2C)交易，改为用户↔官方(B2C)单向「道具商城」。核心域：抽奖（`packageLottery`，多种玩法）、兑换/资产转换/官方竞价（`packageExchange`，走 `exchange` + `exchange/bid` 域）、用户中心与售后（`packageUser`）。
- **技术栈**（`package.json` / `project.config.json` 实证）：微信小程序原生 + TypeScript + Sass + MobX（`mobx-miniprogram`）+ Skyline 渲染；UI 用 TDesign（`tdesign-miniprogram`）；WebSocket 用 `weapp.socket.io`；编译插件 `useCompilerPlugins:["typescript","sass"]`；版本 v5.2.0。
- **技术红线**（`.cursorrules`）：不用 mock，只对接后端真实数据；前端适配后端、字段用后端原名不做映射层；商业机密字段前端不请求不展示。
- **分包结构**（`app.json`）：`packageLottery`/`packageExchange`/`packageUser`/`packageAdmin`/`packageAd`/`packageDIY`，已无 `packageTrade`（C2C 整体下线时删除）。

---

## 七、回复方式

> 请逐条在"编号"后标注结论（提供契约 / 已配置 / 由产品定 / 不开放）。
>
> - **纯前端项（已于 2026-06-07 完成）**：问题三（补意见反馈入口）、问题五（删 feedback 空壳 `.js`）。
> - **真正阻塞前端的关键**：Q-P0-1/2/3（道具真实数据 + 退款区分字段）、B-1~B-4（以物易物 C 端字段契约 + 真实配方）、L-1/L-2（概率公示 C 端接口 + 合规文案授权）、V-1~V-4（高价值实物 `value_tier` 分层 + 复合门槛 C 端下发与"会员解锁"展示）、A-1/A-2（协议正文只读接口 + 法务正文录入，协议页已建待接口供数据）。
> - **确认即可**：Q-P1 系列、T-1（item_type 取值集）、O-1/O-2/O-3（售后枚举与入口）、D-1（历史数据）、V-5（确认不下发现金参考价）、N-1（积分流水存量 description 是否已统一为"回馈"口径）、N-2（`reward_tier_text` 是否统一回馈口径，不阻塞前端）、A-3（隐私政策合规披露项对齐）。

