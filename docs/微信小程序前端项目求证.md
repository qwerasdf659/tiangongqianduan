# 问题一：同一订单出现两个售后/申诉入口（待决策）

> **创建日期**：2026-06-05
> **归属**：微信小程序前端项目
> **性质**：本次"自助发起售后"接入引入的并存问题，需产品/后端拍板后清理
> **状态**：⏳ 待决策（前端未擅自删除旧入口，避免影响既有流程）

---

## 一、问题现象（基于真实代码核对）

本次按"四类订单每笔都挂申请售后"接入了**新售后入口**（走 `POST /system/disputes`），
但项目**原有的交易/拍卖申诉入口**（走旧 `marketplace` 端点）仍然保留。
结果在两个页面上，同一张卡片/同一个订单会**同时出现两个入口**，分别指向**两套不同的后端接口**。

### 1.1 交易订单页 `packageTrade/trade/my-orders/`

文件：`packageTrade/trade/my-orders/my-orders.wxml`

| 入口文案 | 显示条件 | 绑定方法 | 实际请求 | 引入来源 |
|---------|---------|---------|---------|---------|
| **发起申诉** | `item.status === 'completed'`（119–129 行） | `onOpenDispute` | 跳 `packageTrade/trade/dispute/dispute?trade_order_id=...` → 旧 `marketplace/trade-orders/:id/dispute` | 旧有功能 |
| **申请售后** | 常驻每张卡片，不分状态（138–147 行） | `onApplyAfterSale` | 跳 `packageUser/disputes/create?order_type=trade&order_id=...` → 新 `POST /system/disputes` | 本次新增 |

→ **`completed` 状态的交易订单会同时看到"发起申诉" + "申请售后"两个按钮。**

### 1.2 拍卖详情页 `packageTrade/trade/auction-detail/`

文件：`packageTrade/trade/auction-detail/auction-detail.wxml`

| 入口文案 | 显示条件 | 绑定方法 | 实际请求 | 引入来源 |
|---------|---------|---------|---------|---------|
| **发起争议** | `auction.status === 'settled' && isWinner`（200–208 行） | `onCreateDispute` | 跳 `dispute?source=auction&auction_listing_id=...` → 旧 `marketplace/auctions/:id/dispute` | 旧有功能 |
| **对该拍卖申请售后** | `!isSeller` 常驻，不分状态（211–214 行） | `onApplyAfterSale` | 跳 `packageUser/disputes/create?order_type=auction&order_id=...` → 新 `POST /system/disputes` | 本次新增 |

→ **已结算且中标的拍卖，会同时看到"发起争议" + "对该拍卖申请售后"两个入口。**

> 注：消费记录页（`trade-upload-records`）原本**没有**申诉入口，本次只新增了"申请售后"一个，**不存在重复**。

---

## 二、为什么会这样（根本原因）

1. **两套申诉系统并存**：
   - 旧系统：`packageTrade/trade/dispute/` 页面 + `marketplace/trade-orders|auctions/:id/dispute` 端点，
     状态机为 `submitted/processing/arbitrated`、主键字段 `dispute_id`。
   - 新系统：方案A 重构后的 `POST/GET /system/disputes`，状态机 `open/reviewing/arbitrating/resolved/rejected`、
     主键字段 `trade_dispute_id`。
2. **本次按你的决策"保留旧页（keep_both）+ 新入口另起"**，所以两者并存，没有合并。
3. 旧入口是"按状态条件显示"（completed / settled+winner），新入口是"常驻不分状态"，
   在旧入口的触发状态下两者就**重叠**了。

---

## 三、影响

- **用户认知负担**：同一订单两个相似按钮（"发起申诉"vs"申请售后"、"发起争议"vs"申请售后"），用户不知点哪个。
- **数据分裂风险**：两个按钮写入两套后端表/流程，同一订单的申诉可能一部分进旧 `customer_service_issues`、
  一部分进新 `trade_disputes`，客服侧难以统一跟踪。
- **与方案A目标冲突**：方案A 的目标是"纠纷统一进 `trade_disputes`"，双入口违背这一收敛方向。

---

## 四、处置方案（三选一，需你拍板）

| 方案 | 做法 | 前端改动量 | 适用前提 |
|------|------|-----------|---------|
| **A. 隐藏旧入口，只留新"申请售后"** | 注释/移除 my-orders 的"发起申诉"块、auction-detail 的"发起争议"块；新入口统一承接 | 小（删两段 WXML + 对应方法可保留待删） | 推荐——符合方案A收敛方向；但需先确认新 `/system/disputes` 对 trade/auction 已完全可用 |
| **B. 维持双入口（现状）** | 不动 | 0 | 仅作过渡，等后端 Q4 答复期间临时并存 |
| **C. 隐藏新入口，仅在不重叠状态显示** | 新"申请售后"仅在旧入口**不显示**的状态下出现（如交易非 completed、拍卖非 settled 才显示） | 中（加显示条件） | 想兼顾"每笔可申诉"又不重叠时 |

> **依赖项**：方案A/C 是否可行，取决于后端**求证文档 Q4** 的答复——
> 即旧 `marketplace/trade-orders|auctions/:id/dispute` 端点是否仍可用、是否随 `TradeDisputeService` 换表而变。
> 若后端确认旧端点已废弃/已迁移到 `/system/disputes`，则应选 **A**（彻底删旧入口 + 评估旧 dispute 页去留）。

---

## 五、涉及文件清单（处置时改这里）

| 文件 | 涉及行 | 旧入口元素 |
|------|--------|-----------|
| `packageTrade/trade/my-orders/my-orders.wxml` | 119–129 | `<view class="order-actions" wx:if="completed">` 的"发起申诉"按钮 |
| `packageTrade/trade/my-orders/my-orders.ts` | `onOpenDispute` 方法 | 跳旧 dispute 页 |
| `packageTrade/trade/auction-detail/auction-detail.wxml` | 200–208 | `<view class="bottom-bar-inner" wx:elif="settled && isWinner">` 的"发起争议"按钮 |
| `packageTrade/trade/auction-detail/auction-detail.ts` | `onCreateDispute` 方法 | 跳旧 dispute 页 |
| `packageTrade/trade/dispute/`（旧申诉页整体） | — | 若选 A 且后端确认旧端点废弃，可整页评估下线 |

> 前端等你选定 A / B / C 后再动手，不擅自删除旧入口。

---

# 问题二：旧交易/拍卖申诉页的字段与状态机不符合方案A新标准（待清理）

> **创建日期**：2026-06-05
> **归属**：微信小程序前端项目
> **性质**：旧申诉链路未对齐方案A新字段/状态机
> **状态**：⏳ 待决策（与问题一同源，若问题一选 A 删旧链路则一并消除）

## 一、问题现象（基于真实代码核对）

旧申诉页 `packageTrade/trade/dispute/dispute.ts` 仍使用一套**与方案A完全不同**的字段与状态机：

| 维度 | 旧申诉页（实际代码） | 方案A新标准（文档 4.5 / 3.1） | 是否符合 |
|------|---------------------|------------------------------|---------|
| 主键字段 | `dispute_id` | `trade_dispute_id` | ❌ 不符 |
| 证据字段 | `evidence_urls` | `evidence` | ❌ 不符 |
| 状态机 | `submitted / processing / arbitrated` | `open / reviewing / arbitrating / resolved / rejected` | ❌ 不符 |
| 仲裁结果 | `refund / partial_refund / rejected`（独立 `result` 字段） | 统一并入 `status` + `resolution` 文本 | ❌ 不符 |

涉及代码：
- `packageTrade/trade/dispute/dispute.ts`：`DISPUTE_STATUS_MAP`（37–41 行）用旧状态、`DISPUTE_RESULT_MAP`（44–48 行）用旧结果、`response.data.dispute_id` 判断（128 行）。
- `utils/api/market.ts`：`createTradeDispute`/`getTradeDispute`（788/837 行）请求体用 `evidence_urls`。
- `utils/api/auction.ts`：`createAuctionDispute`（366 行）请求体用 `evidence_urls`。

## 二、影响

- 违反方案A "前端一律适配后端、直接改用后端字段名（不做映射兼容）" 的硬性技术标准。
- 旧页与新页（`packageUser/disputes/*`）字段/状态各一套，维护时易混淆，且两套状态文案对同一业务给出不同表述。

## 三、处置建议

与问题一绑定：若问题一选 **A（删旧链路）**，则本问题随旧页 `packageTrade/trade/dispute/` 与旧 dispute API 一并下线，自动消除；无需单独改造旧页字段（不值得为将废弃的页做适配）。

---

# 问题三：「意见反馈」入口缺失——方案A要求的"三入口"只实现了两个

> **创建日期**：2026-06-05
> **归属**：微信小程序前端项目
> **性质**：功能页已存在但无任何导航入口（孤儿页）
> **状态**：⏳ 待决策（是否补入口）

## 一、问题现象（基于真实代码核对）

方案A 第八节要求小程序提供"在线客服 + 意见反馈 + 我的售后申诉"**三个入口**。实际核对：

| 入口 | 页面 | 用户能否进入 | 入口位置 |
|------|------|-------------|---------|
| 在线客服 | `packageUser/chat/chat` | ✅ 能 | "我的"→联系客服→在线聊天（`pages/user/user.ts` `onContactService`） |
| 我的售后申诉 | `packageUser/disputes/disputes` | ✅ 能 | "我的"→我的售后（`pages/user/user.ts` 157–165 行菜单项） |
| **意见反馈** | `packageUser/feedback/feedback` | ❌ **不能** | **无任何入口** |

## 二、根本原因

`packageUser/feedback/{feedback,list,detail}` 三页与 API（`submitFeedback`/`getMyFeedbacks`/`getFeedbackDetail`）**都已写好**，
但全局检索 `/packageUser/feedback/feedback` 的跳转，**只存在于反馈自己的子页之间**（list↔detail↔feedback 互跳、分享 onShareAppMessage）。
用户中心菜单（`pages/user/user.ts` 的 menuItems）**没有"意见反馈"项**——意见反馈页成了**进不去的孤儿页**。

## 三、影响

- 方案A "三入口" 缺一，用户无法提交意见反馈（`feedbacks` 表 C 端写入通道在小程序侧实质不可达）。
- 已开发完成的反馈页（含历史列表、详情、图片附件）处于"已建未用"状态，投入未转化为可用功能。

## 四、处置建议

在 `pages/user/user.ts` 的 `menuItems` 增加一项"意见反馈"（`type: 'page'`，`url: '/packageUser/feedback/feedback'`），
位置建议与"联系客服""我的售后"相邻。改动量小（加一个菜单项对象）。

> 待确认：反馈分类前端用的是 `general/technical/account/payment/suggestion/complaint`（`feedback.ts` 24–31 行），
> 与方案A 实测 `feedbacks.category` 后端枚举 `technical/feature/bug/complaint/suggestion/other` **不一致**。
> 这属于需向后端求证的字段契约（前端 `general/account/payment` 后端是否接受），不在本次纯前端核对范围内，建议并入行情/兑换求证清单一并问后端。

---

# 问题四：兑换订单（redemption）在小程序无售后入口，未对齐后端二期能力

> **创建日期**：2026-06-05
> **归属**：微信小程序前端项目
> **性质**：后端已支持的售后类型，小程序未开放
> **状态**：⏳ 待决策（依赖后端契约求证）

## 一、问题现象（基于真实代码核对）

方案A（3.1 / 4.5 / 附录二「二期-B」）后端 `trade_disputes.order_type` 支持四类：`trade / redemption / consumption / auction`，
且二期已补 redemption 自助申诉归属校验（查 `RedemptionOrder`、仅 `fulfilled` 可申诉）。但小程序侧：

| 订单类型 | 是否有"申请售后"入口 | 入口位置 |
|---------|---------------------|---------|
| trade（交易） | ✅ 有 | `my-orders.wxml` 138–147 行 |
| auction（拍卖） | ✅ 有 | `auction-detail.wxml` 211–214 行、`my-auction-bids.wxml` |
| consumption（消费） | ✅ 有 | `trade-upload-records.wxml` 318 行 |
| **redemption（兑换）** | ❌ **无** | 兑换订单页 `packageExchange/exchange-orders`、`exchange-order-detail` 均无售后入口 |

并且 `packageUser/disputes/create.ts` 的白名单 `ALLOWED_ORDER_TYPES = ['trade','consumption','auction']`（46 行）**主动排除了 `redemption`**
（注释写"兑换订单 exchange 待后端确认 order_type 后再开"）。

## 二、影响

- 兑换订单用户在小程序里**没有任何售后申诉通道**，尽管后端二期已就绪。
- 前后端能力不对齐：后端建好了 redemption 售后，前端未开放，能力闲置。

## 三、处置建议（依赖后端求证）

1. 向后端求证：兑换订单发起售后时 `order_id` 用什么主键（`RedemptionOrder` 主键为 UUID）、是否仅 `fulfilled` 状态可申诉。
2. 确认后：在 `create.ts` 白名单加入 `redemption` + 文案映射；在 `packageExchange/exchange-order-detail` 增加"申请售后"入口（跳 `disputes/create?order_type=redemption&order_id=...`）。

> 第 1 步的 order_id/可申诉状态属于后端契约，需求证；建议并入行情/兑换求证清单一并问后端。

---

# 问题五：`packageUser/feedback/` 同时存在 .ts 与编译产物 .js（建议核查清理）

> **创建日期**：2026-06-05
> **归属**：微信小程序前端项目
> **性质**：新旧编译产物可能混跑的工程隐患
> **状态**：⏳ 待核查

## 一、问题现象（基于真实代码核对）

项目使用 TS 编译插件（`project.config.json` 的 `useCompilerPlugins: ["typescript"]`）。
但 `packageUser/feedback/` 下 `feedback`/`list`/`detail` **同时存在 `.ts` 与 `.js`** 两份；
而新建的 `packageUser/disputes/` 三页只有 `.ts`、无 `.js`。

## 二、影响

冗余的旧 `.js` 可能导致开发者工具优先加载过时的 `.js` 而非最新 `.ts`，造成"改了 .ts 不生效"或新旧逻辑混跑的排查困难。

## 三、处置建议

核对 `feedback` 三页的 `.js` 是否为历史遗留编译产物；确认后删除冗余 `.js`，统一由 `.ts` 编译，保持与 disputes 三页一致。

---

# 附：问题汇总与是否符合方案A技术标准

| 编号 | 问题 | 严重度 | 是否符合方案A标准 | 依赖后端求证 |
|------|------|--------|------------------|-------------|
| 一 | 同一订单两个售后/申诉入口并存 | 高 | ❌ 违反"不留兼容层、旧路由直接删" | 是（旧端点去留，见Q4） |
| 二 | 旧申诉页字段/状态机用旧语义 | 中 | ❌ 违反"直接用后端字段名" | 否（随问题一处置） |
| 三 | 意见反馈入口缺失（三入口缺一） | 中 | ❌ 不满足"三入口"要求 | 部分（反馈分类枚举需求证） |
| 四 | 兑换订单无售后入口 | 中 | ❌ 未对齐后端二期能力 | 是（order_id/可申诉状态） |
| 五 | feedback 页 .ts/.js 并存 | 低 | ⚠️ 工程隐患 | 否 |

> **已达标部分**：新版 `/system/disputes` 链路（API + `packageUser/disputes/*` 三页 + 字段 snake_case + 新状态机）符合文档 4.5 标准；
> `GET /system/chat/issues` 工单接口前端从未调用、用户中心无"我的工单"入口，符合 P6"工单不向用户暴露"。
# 道具商城（B2C 单向）对接 — 需后端/数据库开发确认与提供的事项

> **性质**：微信小程序前端 → 后端数据库项目的"求证 / 索要信息"清单
> **创建**：2026年06月06日 北京时间
> **依据**：`docs/合规整改执行清单.md` §11.4（给微信小程序前端的对接契约）+ §10.18-C（三端归属）
> **范围**：本文件只列"前端无法自行决定、必须由后端/数据库或运营拍板/提供"的事项。前端已完成的 C2C 下线改造见文末"前端已完成"小节。
> **前提**：合规整改要求关闭用户间(C2C)交易，改为用户↔官方(B2C)单向"道具商城"，`/api/v4/marketplace/*` 整域后端已返回 410。本次前端按此**移除全部 C2C 入口/页面/API**，并对接已存在的 `exchange` 域（直购/兑换）+ `exchange/bid`（官方竞价）。

---

## 一、需后端确认的接口契约（阻塞项）

### 1. `/api/v4/marketplace/*` 整域 410 状态（前端已据此移除所有 C2C 请求）

前端已**删除**所有指向 `/api/v4/marketplace/*` 的 API 方法与页面（挂卖、购买他人挂单、C2C 拍卖、担保码、行情/价格发现、市场分析、C2C 申诉）。请后端确认：

| 项 | 需后端确认 |
|---|---|
| `/api/v4/marketplace/*` 是否对小程序平台（`x-platform: wechat_mp`）稳定返回 **410 + `code=MARKETPLACE_DEPRECATED`** | 前端已不再请求该域，此确认仅为防止旧缓存版本误请求时拿到非预期响应 |
| C2C 相关 **WebSocket 事件**（`auction_outbid`/`auction_won`/`auction_lost`/`auction_new_bid`）是否已停止推送 | 前端已移除这些事件的监听与类型；若后端仍推送，前端将静默忽略，但建议后端同步停推 |
| C2C 相关 **通知类型**（`listing_created`/`listing_sold`/`listing_withdrawn`/`listing_expired`/`purchase_completed`/`trade_complete_seller`/`trade_complete_buyer`）是否已停止写入 `user_notifications` | 前端已从通知映射表删除这 7 个类型；若后端仍下发，通知列表会以默认"通知"标签展示且无跳转。需后端确认是否停写或保留为历史只读 |

### 2. 兑换页面配置接口 `GET /api/v4/system/config/exchange-page` 的 `tabs` 与 `market_filters`

前端兑换页（`pages/exchange/exchange`）已**移除"交易市场(market)"Tab**，仅保留：商品兑换（exchange）、资产转换（exchange-rate）。前端处理方式：

- 读取 `tabs` 时已**主动过滤掉 `key === 'market'` 的 Tab**（防御性，避免后端配置仍残留 market Tab 时又渲染出来）。
- 前端已**不再消费** `market_filters`（`type_filters`/`category_filters`/`sort_options`）和 `card_display.market_cta_text` 字段，并从前端类型定义中移除。

**需后端确认/处理**：

| 项 | 需后端确认 |
|---|---|
| `exchange-page` 配置的 `tabs` 数组是否应**从源头移除 `market` Tab**（而非依赖前端过滤） | 建议后端配置直接去掉 market Tab，保持"配置即权威"。前端过滤仅为兜底 |
| `market_filters`、`card_display.market_cta_text` 是否可从配置 JSON 中移除 | 这两个字段现已无前端消费方；保留不影响功能，但建议清理以保持前后端一致 |

### 3. 道具直购（B2C）— 道具商品如何在 `exchange` 域呈现（需后端/运营提供真实数据）

合规清单 §11.4-B + §11.6 约定：道具用 `item_type='prop'` 登记进 `exchange_items`，星石（`star_stone`）直购，走现有 `GET /api/v4/exchange/items` + `POST /api/v4/exchange/`。前端兑换货架（exchange-shelf）已对接该链路。需后端/运营确认：

| 编号 | 待确认项 | 现状（据 §11.6） | 需提供 |
|---|---|---|---|
| P-1 | **道具类商品（`item_type='prop'`）是否已在 `exchange_items` 配置真实数据** | §11.6：admin 页面已就位，**0 条 prop 商品待运营建** | 运营在 admin `物品模板管理` 建 `item_type='prop'` 模板 + `兑换商品管理` 建商品。前端不硬编码任何道具，**完全依赖 `GET /exchange/items` 返回** |
| P-2 | **道具的 `star_stone` 渠道价是否已配置** | §11.6：`exchange_channel_prices` 59 条计价全为 `red_core_shard`，**0 条 star_stone 渠道价待填** | 运营配置道具商品 `cost_asset_code='star_stone'` 渠道价。前端按返回的 `cost_asset_code` + `cost_amount` 展示，不自行换算 |
| P-3 | **道具直购的退款语义（F1 选 A）前端是否需要特殊处理** | §11.6/§10.16-D：道具单建单即 `completed`、即时到账、**禁退款**（`PROP_NO_REFUND`） | 请确认：前端兑换订单列表对 `item_type='prop'`/`source='exchange'` 的即时完成单，是否**不展示**"取消/退款"按钮。需后端在订单对象中给出可区分"实物单(可退) vs 道具单(不可退)"的字段（如 `item_type` 或 `refundable` 布尔） |

### 4. 官方竞价（B2O）`/api/v4/exchange/bid/*` — 字段与状态确认

前端竞价面板（exchange-shelf 的 bid-panel）对接已存在的 `/api/v4/exchange/bid/*`。需后端确认字段口径（前端直接用后端原名，不做映射）：

| 接口 | 前端依赖字段 | 需后端确认 |
|---|---|---|
| `GET /api/v4/exchange/bid/products` | `bid_product_id`、`exchange_item_id`、`start_price`、`current_price`、`min_bid_increment`、`price_asset_code`(=`star_stone`)、`status`(7态)、`start_time`、`end_time`、`bid_count`、`winner_user_id` | 字段名与类型是否与此一致 |
| `POST /api/v4/exchange/bid` | body `{ bid_product_id, bid_amount }`，header `Idempotency-Key` | 中标款销毁、落标解冻退回的口径是否不变 |
| `GET /api/v4/exchange/bid/history` | `bid_record_id`、`bid_product_id`、`bid_amount`、`is_winning`/`is_highest` | 出价记录字段口径确认 |

> ⚠️ **竞价货币锁 `star_stone`**（§11.2 决策13）：前端竞价面板应仅允许 `star_stone` 出价。请确认 `bid_products.price_asset_code` 是否恒为 `star_stone`（碎片不参与竞价），以便前端不展示其他币种选项。

---

## 二、需后端/运营拍板的事项（前端不硬编码）

| 编号 | 待确认项 | 说明 | 需提供 |
|---|---|---|---|
| D-1 | **"我的订单"是否需要承接原 C2C 交易订单的历史查看** | 前端"我的挂单"入口已删除；"我的订单"现仅指向兑换订单（`/packageExchange/exchange-orders`） | 若用户有历史 C2C 订单需可查，请后端确认是否提供只读历史接口；否则前端不再提供任何 C2C 历史入口 |
| D-2 | **背包物品"上架到市场"功能确认移除** | 前端已从"我的仓库"（背包）移除"上架"按钮与 `sellToMarket`/`sellFungibleAssets` 调用 | 确认背包物品 `allowed_actions` 中**不再下发 `sell`**（C2C 已下线）。若后端仍下发 `sell`，前端已忽略不渲染，但建议后端同步移除 |
| D-3 | **兑换得到的实物/道具实例 `is_tradable=0`** | §11.4-C：兑换实例不可交易，前端不显示"转让/挂卖" | 前端已无任何挂卖入口；确认后端实例 `is_tradable=0`，前端仅展示"使用/核销" |

---

## 三、数据敏感与防抓包提示（前端遵循，列出供后端知悉）

- **`BUDGET_POINTS`（预算账户）**：前端**不请求、不展示**（§11.4-D / DataSanitizer 屏蔽）。抽奖"中奖资格与消费挂钩"以人话规则页披露，不暴露数字。
- **资产转换方向**：前端仅提供"碎片/源晶 → 星石"单向入口，**不提供** "星石 → 碎片/源晶" 反向入口（后端 `CONVERT_DIRECTION_FORBIDDEN` 拦截）。
- **星石计价边界**：前端按后端返回的 `cost_asset_code` 展示，**不自行判断**某商品能否用星石买；由后端 `AssetProductGuard` 校验（星石不可买实物/券，后端 `STONE_BUY_VALUABLE_FORBIDDEN` 拦截）。前端仅透传后端错误提示。

---

## 四、前端已完成（本次 C2C 下线改造，无需后端动作，仅供对账）

> 以下为**纯微信小程序前端**已落地的改动，均依据 §11.4 契约，字段用后端原名、无映射层、无 mock 数据。

### 已删除（C2C 整体下线）
- **API 模块**：`utils/api/market.ts`（C2C 交易市场全部接口）、`utils/api/auction.ts`（C2C 拍卖全部接口）、`utils/auction-helpers.ts`（C2C 拍卖辅助）。
- **整个 `packageTrade` 子包**：market（交易市场）、listing-detail、my-listings、my-orders、dispute、auction-hall、auction-detail、auction-create、my-auctions、my-auction-bids 共 10 个 C2C 页面 + price-chart（行情图）+ escrow（担保码）组件。
- **`packageExchange` 内 C2C 组件**：exchange-market（C2C 市场 Tab 组件）、price-chart、escrow-code-dialog。
- **类型定义**（`typings/api.d.ts`）：MarketListing、MyListing、EscrowCodeInfo、价格发现系列（PriceTrendPoint/VolumeTrendPoint/PriceSummary/RecentTrade）、市场分析系列（PricingAdvice/MarketOverview/AssetRanking/PriceHistoryPoint）、C2C 拍卖系列（AuctionStatus/AuctionItemSnapshot/AuctionListing/AuctionBid/MyAuctionBidRecord/AuctionDetailResponse）、C2C WebSocket 事件（4 个）、NotificationType 中的 C2C 取值。
- **图标工具**（`utils/image-helper.ts`）：`getTradeCooldown`、`formatCooldownRemaining`、`getListingDisplayImage`、`getListingDisplayName`（仅 C2C 使用，已删）。
- **积分明细**（`points-detail.ts`）：C2C `business_type` 标签（order_freeze_buyer 等 6 项）。

### 已迁移（非 C2C，从 packageTrade 迁出）
- `我的仓库/背包`：`packageTrade/trade/inventory` → `packageUser/backpack/inventory`（走 `/backpack/*`，已剥离"上架到市场"C2C 功能）。
- `积分活动记录`：`packageTrade/records/trade-upload-records` → `packageUser/records/trade-upload-records`（走 `/assets/transactions` + `/shop/consumption/me`，无 C2C）。

### 已改造（去 C2C 引用）
- `store/trade.ts` → `store/backpack.ts`（去掉 marketListings/myListings，仅留背包库存）；`app.ts` 同步更新。
- `pages/exchange/exchange`：移除 market Tab（.ts/.wxml/.json 三端同步）。
- `pages/user/user.ts`：菜单去掉"我的挂单"，更新"我的仓库/积分记录"跳转路径。
- `packageUser/notifications`：通知类型/跳转映射去 C2C。
- `pages/lottery/lottery.ts`、`packageExchange/exchange-detail.ts`、`asset-conversion.ts`：背包/记录跳转路径更新。
- `app.json`：删除 `packageTrade` 子包，`packageUser` 新增 2 页，修正 `preloadRule`。
- `package.json`：lint 脚本去掉 `packageTrade` glob。

### 质量验收（真实工具运行结果）
- **ESLint**：本次修改的全部文件 **0 error / 0 warning**（项目其余预存 lint 问题在未改动文件，与本次无关）。
- **tsc --noEmit**：**全项目 0 类型错误**（确认无悬空引用到已删除的 C2C 类型/模块）。
- **Prettier**：本次修改文件全部通过格式检查。

---

> **一句话**：前端已按 §11.4 契约完成 C2C 全量下线 + 迁移到 `exchange` 域。**剩余阻塞点集中在"道具真实数据待运营在 admin 填写"（P-1/P-2）与"道具退款字段区分"（P-3）**，这些属后端/运营职责，前端已就绪等待真实数据。
# 抽奖前端对接 — 需后端/数据库开发确认与提供的事项

> **性质**：微信小程序前端 → 后端数据库项目的"求证 / 索要信息"清单
> **创建**：2026年06月05日 北京时间
> **依据**：`docs/微信小程序抽奖对接契约.md` + `docs/抽奖管理干预接入缺口诊断.md`
> **范围**：本文件只列"前端无法自行决定、必须由后端/数据库或运营拍板"的事项。前端代码已按契约完成的改造见文末"前端已完成"小节。

---

## 一、需后端确认的接口字段口径（阻塞项）

### 1. 奖品对外标识字段 = `id`（已按契约实现，需后端复核真实返回）

前端已把奖品标识字段从旧的 `lottery_prize_id` 全量改为契约规定的通用 `id`。请后端确认以下三个接口的奖品对象**实际下发**的就是 `id`（而非 `lottery_prize_id` / `lottery_campaign_prize_id`）：

| 接口 | 前端读取字段 | 需后端确认 |
|---|---|---|
| `GET /api/v4/lottery/campaigns/:code/prizes`（奖品列表） | `prize.id` | DataSanitizer 是否已把内部主键映射为 `id` |
| `POST /api/v4/lottery/draw`（抽奖结果 `data.prizes[].id`） | `prize.id` | 抽奖结果路由是否同样对外输出 `id` |
| `GET /api/v4/lottery/history`（历史 `prize.id`） | `prize.id` | 历史记录嵌套奖品对象口径 |

> ⚠️ 若任一接口仍返回 `lottery_prize_id`/`lottery_campaign_prize_id` 而非 `id`，前端的奖品定位（九宫格索引、集卡命中、老虎机滚轮去重）会拿到 `undefined`，请务必统一为 `id`。

### 2. 奖品列表接口的字段范围（需后端书面确认"哪些字段一定会下发"）

前端"奖品列表"接口当前依赖以下字段渲染，请后端确认**每个活动都会稳定下发**：

- `id`、`prize_name`、`prize_type`、`prize_value`、`rarity_code`、`sort_order`、`image`
- `image` 结构：`{ url, thumbnail_url, source }`，无图时为 `null`

需明确：

1. **奖品列表接口是否含 `reward_tier`？** 契约 §2.1 标注列表已脱敏移除 `reward_tier`，但抽奖结果 §2.2 含 `reward_tier`。前端已据此把 `reward_tier` 设为"仅抽奖结果含"。请确认列表接口确实不含。
2. **`is_fallback`（兜底奖品角标）列表接口是否下发？** 前端九宫格/转盘/各玩法预览区用它显示"保底"角标。若列表不下发，该角标将不显示，请确认是否符合预期。

### 3. `image.source` 取值枚举

前端注释按契约写为 `material_icon` / `media` / `placeholder`。请确认后端实际只会返回这三种取值（前端不依赖 source 做逻辑分支，仅作展示与排障）。

---

## 二、需运营/产品拍板的真实数据（前端不硬编码，等接口返回）

> 以下为契约 §六"成长等级制度"与 §四待拍板项，属**业务数据**，前端不设默认值，缺失时按接口返回展示或明确报错。

| 编号 | 待确认项 | 现状 | 需提供 |
|---|---|---|---|
| R-1 | **成长等级 C 端公示接口是否开放** | 契约 §四：后端目前只有管理后台侧成长等级/倍数接口，**尚未开放 C 端只读公示接口** | 是否需要小程序展示"我的成长等级 / 升级享更高中奖率"？需要的话请后端新增 C 端只读接口并给出**字段与脱敏口径** |
| R-2 | **成长等级阈值** | 后端 `user_growth_levels` 当前为占位起步阈值（青铜0/白银100000/黄金500000/钻石2000000） | 运营按真实业务确认后由管理后台调整；前端只展示接口返回值，**不硬编码** |
| R-3 | **各等级中奖率倍数是否对 C 端可见** | 契约 §四提示"倍数"可能涉及运营策略泄密 | 由你决定是否对外暴露倍数；如暴露需走数据脱敏评估 |
| R-4 | **概率公示页档位口径** | 契约 D8 已拍板=甲：四档 `high ≤ 8% / mid / low / fallback` | 若小程序需展示概率公示页，请后端提供 C 端公示数值接口（后台数值=公示数值，合规要求） |

---

## 三、需后端确认是否提供的字段（影响前端是否清理死代码）

### `stock_quantity` / 售罄状态 — 当前前端有"已抢光"展示逻辑

- 契约 §2.1 明确：`stock_quantity` 属已脱敏移除的商业机密字段，**不下发**。
- 但前端 13 个玩法组件的 wxml 仍保留 `is_sold_out`（基于 `stock_quantity` 计算）的"已抢光/抢光"遮罩展示。因后端不下发 `stock_quantity`，该逻辑**永不触发**（属安全的死代码）。

**需你拍板**：

1. **方案A（推荐）**：确认后端永久不下发库存 → 前端清理全部 `is_sold_out` 死代码（涉及 13 个玩法的 .ts/.wxml/.scss，改动较大但符合"删除无用代码"原则）。
2. **方案B**：后端后续会下发 `stock_quantity` 用于"已抢光"展示 → 前端保留该逻辑，并请后端补充字段口径（哪个接口、字段名、是否脱敏）。

> 当前前端先保留该逻辑不动（避免半截清理导致 wxml 悬空引用）。请你选 A 或 B，我再据此处理。这是商业机密暴露面相关决策，由你定。

---

## 四、安全 / 防抓包提示（需你决策的潜在泄密点）

按规则"涉及可能泄密的功能要及时上报由你决定"，以下为排查结论：

- ✅ **已合规**：奖品列表/抽奖结果接口已脱敏，前端拿不到也不依赖 `win_probability`（中奖概率）、`win_weight`（权重）、`stock_quantity`（库存）、`cost_points`、`max_daily_wins` 等商业机密字段。前端代码中**零引用**这些字段。
- ⚠️ **待你确认**：若开放 R-1 成长等级公示或 R-4 概率公示，"倍数/各档概率"会通过 C 端接口暴露给用户（可被抓包）。是否公示、公示到什么粒度，属业务/合规决策，请你拍板。

---

## 五、前端已完成（按契约改造，无需后端动作，仅供对账）

- 奖品标识字段全链路（TS 逻辑层 + WXML 模板层 + 类型定义）由 `lottery_prize_id` 统一改为 `id`。
- 抽奖结果"100% 出奖、无未中奖语义"已确认：前端无"很遗憾未中奖"分支。
- 被后端删除的暗箱能力（`force_win`/`force_lose`/`probability_adjust`/`user_queue`/强制中奖/概率调整）：前端**零引用**，无需移除。
- 无 mock / 模拟 / 假业务数据；API 路径、认证头（`Authorization: Bearer <JWT>`）、Token 完整性校验与自动刷新、幂等键（`Idempotency-Key` 走 Header）均符合契约。
- 类型定义 `Prize` 已对齐契约：`image` 结构改为 `{ url, thumbnail_url, source }`，移除与契约不符的 `primary_media`/`primary_media_id`/`status`，`reward_tier` 标注为"仅抽奖结果含"。

---

## 六、本次任务完整性复查结论（工具实测，非预设）

> 复查时间 2026年06月05日 北京时间。以下为工具实际执行结果，非人工估算。

### 6.1 改造彻底性核对

| 复查项 | 结果 |
|---|---|
| 全代码区搜 `lottery_prize_id` / `lottery_campaign_prize_id` / `prize_id` 残留 | 代码区**零残留**，仅 `typings/api.d.ts` 一条故意保留的警告注释 |
| 新链路 `prize.id` 消费点是否全通 | 3 处 TS 逻辑（图片报错日志、集卡命中判断、老虎机滚轮去重）+ 14 个 wxml 的 `wx:key="id"` 全部一致 |
| 删除的 `Prize.status` / `primary_media` / `primary_media_id` 是否产生悬空引用 | packageLottery 内**无任何读取**；`pages/lottery` 的 `primary_media` 是广告 banner（`carouselItems`/`ad_campaign_id`），与奖品对象无关，未受影响 |
| 保留字段 `prize_type` / `prize_value` / `material_*` 一致性 | 仍在用，已设为可选字段，前后一致 |
| 是否擅自新建文件 / 脚本 / 工具类 | 仅新建本对接文档 1 份，无新增模块 / 脚本 / 助手文件 |

### 6.2 质量门禁（命令实测结果）

| 命令 | 结果 |
|---|---|
| `npx tsc --noEmit`（全项目类型检查） | **exit 0**，零类型错误 |
| `npx eslint`（改动文件 + `store/**` + `config/**`） | **exit 0**，零问题 |
| `npx prettier --check`（改动的 .ts + 类型定义） | 全部通过 |

> **结论：本次抽奖对接前端改造已彻底执行完毕，无半截工程、无悬空引用、无未完成的代码改动。**

---

## 七、本次任务"挂起、等你拍板"才能继续的事项

> 不是没做，而是按规则（业务数据由后端权威、可能泄密由你决定）前端无权单方决定。三项即第一~三节，汇总如下，便于你集中回话：

1. **第三节 `stock_quantity` / "已抢光"死代码**：选方案 A（前端清理 13 个玩法的 `is_sold_out` 死代码）或方案 B（后端会补库存字段则保留）。
2. **第一节-2 奖品列表字段范围**：列表是否含 `is_fallback`、是否确实不含 `reward_tier`，需后端书面确认。
3. **第二节 R-1 / R-4 成长等级与概率公示**：后端尚未开 C 端只读接口，是否开放、是否暴露倍数/概率，属业务+合规决策。

---

## 八、检查中发现的"非本次任务"其它问题（仅上报，未改动）

> 以下均不属于"抽奖对接"范围，我未擅自改动。如需处理请单独指示，我会先按"先查现有、再改扩展、避免新增技术债"的原则评估后再动。

| 编号 | 问题 | 证据 | 性质 / 建议 |
|---|---|---|---|
| X-1 | `typings/store.d.ts` 的 `LotteryStore` 是**无运行时实现的死类型** | `store/` 目录仅有 diy/audit/points/helpers/trade/user，无 `lottery.ts`；`lottery-activity.ts` 注释明确"架构决策：不依赖 lotteryStore" | 可清理的死代码定义。删除前需全局确认无 import，建议单独评估 |
| X-2 | 存量 ESLint 错误 10 个（非抽奖、非本次改动文件） | `components/login-popup/login-popup.ts`（8 个 `curly` 缺花括号 + 2 个未用变量）、`packageDIY/sub/xr-bead-scene/xr-bead-scene.ts`、`packageDIY/sub/xr-bead-viewer/xr-bead-viewer.ts`、`utils/auth-helper.ts` | 导致 `npm run lint` 整体失败；多数可 `--fix` 自动修，但超出本次范围 |
| X-3 | 存量 Prettier 格式问题（非本次改动文件） | 如 `packageLottery/.../shared/result-modal/result-modal.wxml`、`sub/pinball/pinball.ts` 等被 `format:check` 标记 | git 核实非本次改动，属仓库历史格式问题 |
| X-4 | `app.json` 工作区有未提交改动（git 标记 `M`） | 会话开始前即存在，当前 `git diff app.json` 为空（已暂存或还原），非本次改动 | 仅提示，未触碰 |

---

> **回复方式**：请逐条在"编号"后标注结论（确认 / 需调整 / 由运营定）。
> - **阻塞前端继续的关键三项**：第三节 `stock_quantity` 决策、第一节-2 字段范围、第二节 R-1 / R-4 公示决策。
> - **第八节 X-1~X-4 为本次范围外的存量问题**，需你单独决定是否要我处理。

---

# 需向后端数据库项目求证事项总清单（C2C 下线后汇总）

> **创建**：2026年06月06日 北京时间
> **归属**：微信小程序前端项目 → 后端数据库项目
> **性质**：前端**无法自行决定、必须由后端确认或提供**的事项集中清单。本节是对前文各分散小节的统一汇总与补全（含前文未覆盖的新增项），便于一次性转交后端。
> **优先级**：P0=阻塞道具商城可用 · P1=契约字段/状态对齐 · P2=边界/历史/枚举确认。

## P0 — 阻塞道具商城上线（必须后端/运营先做）

| 编号 | 求证项 | 实测现状 | 需后端/运营提供 |
|---|---|---|---|
| Q-P0-1 | **道具商品（`item_type='prop'`）真实数据是否已配置** | §11.6 实测 `exchange_items` 中 prop 商品 **0 条** | 运营在 admin 建 `item_type='prop'` 模板 + 兑换商品。前端已对接 `GET /api/v4/exchange/items`，完全依赖返回，**不硬编码** |
| Q-P0-2 | **道具的 `star_stone` 渠道价是否已配置** | `exchange_channel_prices` 59 条计价**全为 `red_core_shard`，0 条 `star_stone`** | 运营配置道具商品 `cost_asset_code='star_stone'` 渠道价，否则用户看到道具却无法用星石购买 |
| Q-P0-3 | **道具直购订单如何与"可退实物单"区分** | F1 选 A：道具单建单即 `completed`、即时到账、禁退款（`PROP_NO_REFUND`）；实物单仍 `pending`+可退 | `GET /api/v4/exchange/orders` 返回对象里，前端用**哪个字段**判断"不可退道具单"？`item_type='prop'`、`source`、还是给一个 `refundable` 布尔？前端据此决定是否显示"取消/退款"按钮，**不自行猜测** |

## P1 — 契约字段与状态对齐（前端按返回写代码，需后端书面确认口径）

| 编号 | 求证项 | 前端现状 | 需后端确认 |
|---|---|---|---|
| Q-P1-1 | **`GET /api/v4/system/config/exchange-page` 的 `tabs` 是否从源头去掉 `market`** | 前端已移除"交易市场"Tab，并防御性过滤 `key === 'market'` | 后端配置 JSON 的 `tabs` 是否应直接删掉 market Tab（配置即权威）？`market_filters`、`card_display.market_cta_text` 前端已不消费，是否可一并移除 |
| Q-P1-2 | **官方竞价 `/api/v4/exchange/bid/*` 字段口径** | 前端 bid-panel 已对接，用后端原名不做映射 | 确认列表/详情字段：`bid_product_id`/`exchange_item_id`/`start_price`/`current_price`/`min_bid_increment`/`price_asset_code`/`status`(7态)/`start_time`/`end_time`/`bid_count`/`winner_user_id`；出价 `POST /exchange/bid` body `{bid_product_id,bid_amount}`+header `Idempotency-Key`；历史是 `is_winning` 还是 `is_highest` |
| Q-P1-3 | **竞价货币是否恒为 `star_stone`** | 决策13 竞价锁星石、碎片不参与；前端竞价面板拟仅允许星石 | 确认 `bid_products.price_asset_code` 是否恒为 `star_stone`，前端据此不展示其它币种选项 |
| Q-P1-4 | **背包 `allowed_actions` 是否还下发 `sell`** | C2C 下线后前端不渲染"上架"按钮 | `GET /api/v4/backpack/` 物品的 `allowed_actions` 是否已移除 `sell`？若仍下发前端会忽略，但建议后端同步去掉 |
| Q-P1-5 | **C2C 通知类型 / WebSocket 事件是否已停推** | 前端已删除处理逻辑与类型 | 通知类型 `listing_created`/`listing_sold`/`listing_withdrawn`/`listing_expired`/`purchase_completed`/`trade_complete_seller`/`trade_complete_buyer` 是否停写 `user_notifications`；WebSocket 事件 `auction_outbid`/`auction_won`/`auction_lost`/`auction_new_bid` 是否停推 |

## P2 — 售后/申诉订单类型、历史数据、枚举边界

| 编号 | 求证项 | 实测现状 | 需后端确认 |
|---|---|---|---|
| Q-P2-1 | **售后申诉 `order_type` 白名单收敛** | C2C 下线后，`trade`（原 my-orders）/`auction`（原 auction-detail）入口页已随 packageTrade 删除，这两类**已无前端入口可传入**；`packageUser/disputes/create.ts` 白名单仍含 `['trade','consumption','auction']` | C2C 下线后 `trade`/`auction` 售后是否还保留？还是收敛为仅 `consumption`？（这关系到前端是否要收窄白名单，与"问题四"redemption 决策一并定） |
| Q-P2-2 | **兑换订单 `redemption` 售后是否开放**（承接前文"问题四"） | 前端白名单**主动排除了 `redemption`**（注释"待后端确认 order_type 后再开"） | 兑换订单发起售后时 `order_id` 用什么主键（`RedemptionOrder` 主键为 UUID）、是否仅 `fulfilled` 状态可申诉？确认后前端在 `create.ts` 白名单加 `redemption` + 在 `packageExchange/exchange-order-detail` 补"申请售后"入口 |
| Q-P2-3 | **旧 dispute 端点是否已废弃**（承接前文"问题一/Q4"） | 前端已删旧 C2C 申诉链路，统一走 `/api/v4/system/disputes` | 旧 `marketplace/trade-orders\|auctions/:id/dispute` 是否随 C2C 410 一并下线？确认后前端彻底无旧链路残留 |
| Q-P2-4 | **历史 C2C 订单是否需要只读查看** | 前端已删"我的挂单/我的交易订单"入口 | 上线前是否有用户产生过 C2C 订单需可查？需要则后端提供只读历史接口；不需要（测试数据清档）则前端不再提供任何 C2C 历史入口（当前即此状态） |
| Q-P2-5 | **意见反馈分类枚举不一致**（承接前文"问题三"） | 前端用 `general/technical/account/payment/suggestion/complaint`；方案A 实测后端 `feedbacks.category`=`technical/feature/bug/complaint/suggestion/other` | 后端实际接受哪一组枚举？前端 `general/account/payment` 是否会被拒？需对齐字段契约后前端调整 |

## 数据敏感（前端已遵循，列出供后端知悉，非求证）

- **`BUDGET_POINTS`（预算账户）**：前端不请求、不展示（DataSanitizer 屏蔽）；抽奖"中奖资格与消费挂钩"仅以人话规则页披露、不暴露数字。
- **资产转换方向**：前端只提供"碎片/源晶 → 星石"单向入口，不提供反向（后端 `CONVERT_DIRECTION_FORBIDDEN` 兜底）。
- **星石计价边界**：前端按后端返回的 `cost_asset_code` 展示，不自行判断某商品能否用星石买，由后端 `AssetProductGuard` 校验（`STONE_BUY_VALUABLE_FORBIDDEN`）。

> **优先级提示**：**Q-P0-1/2/3 是真正卡住道具商城可用的关键**（前两项是运营填数据，第三项是需后端给前端一个区分字段）。其余多为"确认即可"。请逐条在编号后标注结论。
# 微信小程序前端 → 后端数据库项目：以物易物 / 概率公示 / 售后枚举 求证清单

> **性质**：微信小程序前端 → 后端数据库项目的"求证 / 索要信息"清单
> **创建**：2026年06月06日 北京时间
> **归属**：微信小程序前端项目
> **依据**：`docs/合规整改执行清单.md` §10.15 Step 17 / §10.19-C（以物易物）、§四-A（抽奖概率公示）、`docs/微信小程序抽奖对接契约.md`、`docs/微信小程序前端项目求证.md`
> **范围**：本清单只列"前端无法自行决定、必须由后端/数据库或运营提供契约/数据/决策"的事项。前端已能独立完成的清理（售后 order_type 枚举对齐、注释纠偏）本次已落地，见文末"前端本次已完成"小节。
> **原则**：前端严格按后端真实返回写代码、不硬编码业务规则默认值、不做字段映射层、不使用 mock 数据。以下事项缺少后端契约/数据时，前端不擅自推测实现。

---

## P0 — 以物易物（B2C 官方合成）C 端对接契约缺失（阻塞前端开发）

> 合规清单 §11.6 称后端已上线 `BarterService` + 路由 `GET /api/v4/exchange/barter/recipes`、`POST /api/v4/exchange/barter`。
> 但**面向小程序的 C 端字段契约尚未提供**，前端无法在"不推测 API 字段"的前提下落地入口。前端拒绝凭空猜测请求体/响应字段。

| 编号 | 求证项 | 现状 | 需后端提供 |
|---|---|---|---|
| B-1 | **以物易物配方列表接口的 C 端字段契约** | 仅知路径 `GET /api/v4/exchange/barter/recipes`，字段未知 | 响应 `data` 结构：配方对象含哪些字段（如 `recipe_code`、所需旧物 `item_template_id`/数量、产出商品 `exchange_item_id`/`item_name`/`image`、是否启用）。前端按后端原名 snake_case 直接渲染，不做映射 |
| B-2 | **提交以物易物接口的请求体与幂等键** | 仅知路径 `POST /api/v4/exchange/barter` | 请求体字段名（如 `recipe_code` 还是 `recipe_id`？需传哪些旧物 `item_id`？数量？）、是否走 Header `Idempotency-Key`（前端按 RESTful 惯例放 Header）、成功响应 `data` 结构（产出物、订单号 `order_no`、`source='barter'`） |
| B-3 | **错误码枚举** | 未知 | 旧物不属于本人/已核销/方向向上(破烂换高价值被拒)/配方停用 等场景的 `code`，前端据此透传后端中文 `message` 给用户 |
| B-4 | **配方真实数据是否已配置** | §11.6 实测 `system_settings` 无 `barter_recipes`，admin 页面已就位但**0 条配方待运营填** | 运营在 admin「以物易物配方」Tab 填入真实配方后告知。前端完全依赖接口返回，无任何硬编码配方 |
| B-5 | **入口归属确认** | 前端拟在哪挂入口待定 | 以物易物入口建议挂在"我的仓库"（用旧物换新物，语义贴近背包）还是兑换页新增 Tab？此为产品决策，请产品/后端确认期望位置 |

> **前端待办（解除 B-1~B-4 后即可执行）**：在 `utils/api/backpack.ts`（或新建 `utils/api/barter.ts`）封装 `getBarterRecipes`/`submitBarter` → 在 `utils/index.js` 统一导出 → 新增入口页面/组件，按后端字段渲染。

---

## P0 — 抽奖概率公示 / "中奖资格与消费挂钩" C 端只读接口缺失（合规硬项，阻塞）

> 合规清单 §四-A 要求："抽奖规则页"补"中大奖概率区间+上限+保底"与"中奖资格与消费挂钩"的人话披露（《规范促销行为暂行规定》强制）。
> `docs/微信小程序抽奖对接契约.md` §四 / §六明确：**后端目前只有管理后台侧成长等级/倍数接口，尚未开放 C 端只读公示接口**。
> 前端**不能硬编码"0–8%""消费越多档位越高"等合规文案**（违反"业务规则不得前端默认值"+ 合规文案需律师复核），故此项卡在后端接口与文案授权。

| 编号 | 求证/决策项 | 现状 | 需后端/产品提供 |
|---|---|---|---|
| L-1 | **是否开放 C 端"抽奖规则/概率公示"只读接口** | 契约 §四：尚未开放 C 端只读公示接口 | 是否新增面向小程序的只读接口返回"概率区间/上限/保底说明/消费挂钩说明"文案？前端按返回展示，不自拟文案 |
| L-2 | **公示文案口径（合规文本）** | §四-A 拟："中大奖概率 0–8%、动态浮动、设有保底"+"抽中高价值奖品需达到相应消费/积分门槛" | 文案需经律师复核后由后端下发（或运营在后台配置），前端**不硬编码**任何概率数字与合规措辞 |
| L-3 | **成长等级是否对 C 端可见、倍数是否暴露** | 契约 §六 / R-1~R-3：倍数可能涉及运营策略泄密 | 由产品/合规决定：是否展示"我的成长等级 / 升级享更高中奖率"？若展示，倍数是否脱敏？需后端给字段与脱敏口径 |
| L-4 | **保底 UI 是否恢复展示** | 前端 `lottery-activity.ts` 的 `showPityUI` 总开关当前关闭（保底文案/进度隐藏），后端保底引擎仍在静默运行 | 产品确认是否在 C 端展示"保底进度/设有保底"。若展示，确认后端 `config` 接口 `pity_info`/`pity_enabled` 字段是否对 C 端下发 |

> **数据敏感提示（前端已遵循）**：`BUDGET_POINTS`（预算账户）前端不请求、不展示（DataSanitizer 屏蔽）。"中奖资格与消费挂钩"只能以"人话规则"披露、绝不暴露预算数字。是否公示、公示粒度属业务+合规决策，请产品拍板。

---

## P1 — 背包"可交易(tradable_item)"分类是否保留（需后端确认 item_type 取值集）

| 编号 | 求证项 | 现状 | 需后端确认 |
|---|---|---|---|
| T-1 | **去交易化后 `GET /api/v4/backpack/` 是否仍返回 `item_type='tradable_item'` 的物品** | 前端"我的仓库"仍有「可交易」分类 Tab + `categoryStats.tradable_item`；后端 `allowed_actions` 已不再下发 `sell`（前端无挂卖入口） | 全系统去交易化后，背包是否还存在 `item_type='tradable_item'` 的物品？<br>① 若**仍有**：前端保留该 Tab（仅展示分类，无挂卖动作），合理；<br>② 若**已无/将无**：前端删除「可交易」Tab + `tradable_item` 分类统计（去除死分类）。<br>前端不擅自删除——删错会导致这类物品对用户隐藏（违反"展示效果不擅改"），故需后端确认 item_type 取值集 |

> 备注：前端本次已先纠正 `inventory.ts` 中过时的 `allowed_actions` 注释（删除 `"sell"` 表述），但 Tab 去留依赖后端 item_type 取值集确认，未擅动 UI。

---

## P1 — 售后申诉 order_type 收敛（前端本次已对齐到后端权威，列出供后端复核）

| 编号 | 求证项 | 前端本次处理 | 需后端确认 |
|---|---|---|---|
| O-1 | **`trade_disputes.order_type` 已收窄为 `('redemption','consumption')`** | 前端已将 `create.ts`/`disputes.ts`/`detail.ts`/`system.ts` 注释的 order_type 文案映射收敛为 `redemption/consumption`，**删除已废除的 `trade`/`auction`** 文案与默认值 | 确认后端 `trade_disputes.order_type` 实际枚举即 `redemption/consumption`（合规清单 §11.7 已声明收窄）。若仍接受其他值请告知 |
| O-2 | **兑换订单 `redemption` 售后入口何时开放**（承接求证清单 Q-P2-2） | 前端 `create.ts` 白名单当前仅 `['consumption']`（唯一已接通入口）；`redemption` 已在文案映射中预留但**入口未接通** | 兑换订单发起售后时 `order_id` 用什么主键（`RedemptionOrder` 主键为 UUID）、是否仅 `fulfilled` 状态可申诉？确认后前端在白名单加 `redemption` + 在 `packageExchange/exchange-order-detail` 补"申请售后"入口 |

---

## 前端本次已完成（纯前端，无需后端动作，仅供对账）

> 以下为本次**纯微信小程序前端**已落地的改动，均依据后端权威契约，字段用后端原名、无映射层、无 mock 数据。已过 ESLint(0) + Prettier(0) + `tsc --noEmit`(0)。

1. **售后 order_type 枚举对齐**：`packageUser/disputes/create.ts`、`disputes.ts`、`detail.ts`、`utils/api/system.ts` 的 `ORDER_TYPE_MAP` 与白名单/注释，由旧 C2C 口径 `trade/redemption/consumption/auction` 收敛为后端权威的 `redemption/consumption`；`create.ts` 白名单收敛为 `['consumption']`（唯一已接通入口）；列表默认值由废除的 `'trade'` 改为 `'consumption'`。
2. **背包注释纠偏**：`packageUser/backpack/inventory/inventory.ts` 删除过时的 `allowed_actions` 含 `"sell"` 表述（C2C 下线后后端不再下发 sell）。
3. **竞价注释纠偏**：`utils/api/backpack.ts` `placeBid` 注释由 `is_tradable=true` 更正为 `is_biddable=true`（决策13，竞价白名单解耦，仅 star_stone 可竞价）。
4. **措辞统一**：`packageExchange/components/exchange/exchange-shelf/_view-toggle.scss` 注释"交易市场"→"道具商城"。

---

> **回复方式**：请逐条在"编号"后标注结论（提供契约 / 已配置 / 由产品定 / 不开放）。
> - **真正阻塞前端的关键**：B-1~B-4（以物易物 C 端字段契约 + 真实配方数据）、L-1/L-2（概率公示 C 端接口 + 合规文案授权）。
> - **确认即可**：T-1（item_type 取值集）、O-1/O-2（售后枚举与 redemption 入口）。
