---
description: 天工积分小程序 Typography Token 体系规则
globs: ["**/*.scss", "**/*.wxss"]
---

# Typography Token 使用规则

本项目使用 Design Token 体系管理字体样式。所有新增或修改的样式必须引用 Token 变量，禁止写死具体数值。

## 字体族

- 中文/正文：不需要显式声明，全局已设置系统字体栈
- 数字（积分、价格、倒计时）：`font-family: var(--tg-font-number);`

## 字号层级（从大到小）

| Token | 值 | 用途 |
|-------|------|------|
| `--tg-font-size-display-lg` | 52rpx | 活动超大标题、营销数字 |
| `--tg-font-size-display` | 44rpx | 活动大标题 |
| `--tg-font-size-headline` | 36rpx | 区域标题（砸金蛋标题等） |
| `--tg-font-size-title` | 28rpx | 小标题、按钮主文字 |
| `--tg-font-size-body` | 26rpx | 正文、描述 |
| `--tg-font-size-caption` | 24rpx | 标签（"可用积分"） |
| `--tg-font-size-small` | 22rpx | 奖品名称、角标 |
| `--tg-font-size-mini` | 20rpx | tab文字、时间戳 |
| `--tg-font-size-micro` | 18rpx | 角标、折扣标签 |
| `--tg-font-size-nano` | 16rpx | 极小装饰文字、缩略编号 |

数字专用字号：
| Token | 值 | 用途 |
|-------|------|------|
| `--tg-font-size-number-lg` | 36rpx | 大数字（积分余额） |
| `--tg-font-size-number-md` | 28rpx | 中数字（按钮积分） |
| `--tg-font-size-number-sm` | 24rpx | 小数字（角标数量） |

## 字重

四级字重，按需使用：
- `var(--tg-font-weight-regular)` = 400 — 正文、辅助标签、说明文字
- `var(--tg-font-weight-medium)` = 500 — 副标题、卡片标题、按钮副文字、描述
- `var(--tg-font-weight-semibold)` = 600 — 区域标题、按钮主文字、强调文字
- `var(--tg-font-weight-bold)` = 700 — 大标题、核心数字、核心CTA按钮

字重选择原则：
- 标题和正文之间必须有至少一级字重差（如标题 600，正文 400）
- 同一卡片内避免出现两个 700，用 600 + 500 制造层次
- 数字始终用 700（视觉锚点）

## 字间距（letter-spacing）

| Token | 值 | 用途 |
|-------|------|------|
| `--tg-letter-spacing-tight` | 1rpx | 正文、数字、小字 |
| `--tg-letter-spacing-normal` | 2rpx | 区域标题、欢迎语、标签 |
| `--tg-letter-spacing-wide` | 4rpx | 大标题、按钮主文字（如"三连敲"） |

字间距使用原则：
- 中文标题必须加字间距（至少 `--tg-letter-spacing-normal`），让文字从容大气
- 按钮文字用 `--tg-letter-spacing-wide`，提升高级感
- 数字用 `--tg-letter-spacing-tight` 或 0，保持紧凑可读
- 英文/数字不宜过大字间距

## 颜色

三级文字颜色：
- `var(--tg-text-primary)` #1A1A1A — 标题、数字、重要信息
- `var(--tg-text-secondary)` #666666 — 标签、说明文字
- `var(--tg-text-tertiary)` #999999 — 辅助信息、禁用态

## 禁止事项

- 禁止写死 `font-size: 28rpx`，用 `var(--tg-font-size-title)` 代替
- 禁止写死 `color: #333`，用 `var(--tg-text-primary)` 代替
- 禁止写死 `font-weight: 700`，用 `var(--tg-font-weight-bold)` 代替
- 禁止写死 `font-weight: 600`，用 `var(--tg-font-weight-semibold)` 代替
- 禁止写死 `letter-spacing: 2rpx`，用 `var(--tg-letter-spacing-normal)` 代替
- 禁止对数字使用系统字体，必须加 `font-family: var(--tg-font-number)`

## 各场景推荐搭配

```scss
// 活动大标题（如 banner 文字）
.activity-title {
  font-size: var(--tg-font-size-display);
  font-weight: var(--tg-font-weight-bold);
  color: var(--tg-text-primary);
  letter-spacing: var(--tg-letter-spacing-wide);
}

// 区域标题（如"选一个金蛋砸开试试手气"）
.section-title {
  font-size: var(--tg-font-size-headline);
  font-weight: var(--tg-font-weight-semibold);
  color: var(--tg-text-primary);
  letter-spacing: var(--tg-letter-spacing-normal);
}

// 按钮主文字（如"三连敲"）
.btn-label {
  font-size: var(--tg-font-size-title);
  font-weight: var(--tg-font-weight-semibold);
  letter-spacing: var(--tg-letter-spacing-wide);
}

// 按钮副文字（如"90积分"）
.btn-sub {
  font-family: var(--tg-font-number);
  font-size: var(--tg-font-size-small);
  font-weight: var(--tg-font-weight-medium);
  letter-spacing: var(--tg-letter-spacing-tight);
}

// 积分数字
.points-value {
  font-family: var(--tg-font-number);
  font-size: var(--tg-font-size-number-lg);
  font-weight: var(--tg-font-weight-bold);
  color: var(--tg-text-primary);
  font-variant-numeric: tabular-nums;
  letter-spacing: var(--tg-letter-spacing-tight);
}

// 辅助标签（如"可用积分"）
.label {
  font-size: var(--tg-font-size-small);
  font-weight: var(--tg-font-weight-regular);
  color: var(--tg-text-tertiary);
  letter-spacing: var(--tg-letter-spacing-normal);
}

// 欢迎语/用户名
.welcome {
  font-size: var(--tg-font-size-title);
  font-weight: var(--tg-font-weight-semibold);
  color: var(--tg-text-primary);
  letter-spacing: var(--tg-letter-spacing-normal);
}

// 消耗提示（如"消耗 30 积分/次"）
.cost-hint {
  font-size: var(--tg-font-size-caption);
  font-weight: var(--tg-font-weight-medium);
  color: var(--tg-text-secondary);
  letter-spacing: var(--tg-letter-spacing-tight);
}
```

## Mixin 快捷方式（需要 @import 'styles/typography'）

```scss
@include tg-number(36rpx);   // 数字
@include tg-heading(32rpx);  // 标题
@include tg-body(26rpx);     // 正文
@include tg-caption(24rpx);  // 辅助
@include tg-small(22rpx);    // 小字
```

## Token 定义位置

- 变量声明：`styles/td-overrides.scss`
- 字体文件 + Mixin：`styles/_typography.scss`
