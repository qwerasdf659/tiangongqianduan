<!-- pages/exchange/exchange.wxml — 兑换中心页面壳 -->
<!-- 组件化架构: exchange-shelf（商品兑换Tab）+ exchange-market（交易市场Tab）+ asset-conversion（资产转换Tab，后端tab key仍为exchange-rate） -->
<!-- D13决策: 使用 hidden 替代 wx:if，保留组件状态（筛选/分页/搜索/滚动位置） -->
<view class="exchange-container">
  <!-- 页面级加载状态（初始化期间tabs为空，显示loading遮罩避免短暂空白） -->
  <loading-spinner wx:if="{{loading}}" text="加载中..." />

  <!-- 水平排列导航模块（后端 exchange_page 配置下发） -->
  <view class="nav-module horizontal" hover-class="none" wx:if="{{!loading}}">
    <view class="nav-buttons-row" hover-class="none">
      <view
        wx:for="{{tabs}}"
        wx:key="key"
        class="nav-item nav-item-horizontal {{currentTab === item.key ? 'active' : ''}}"
        data-tab="{{item.key}}"
        bindtap="onTabChange"
        hover-class="none"
      >
        <view class="nav-content" hover-class="none">
          <view class="icon-wrapper" hover-class="none">
            <icon class="nav-icon" type="{{item.icon}}" size="20" />
          </view>
          <view class="text-wrapper" hover-class="none">
            <text class="nav-text">{{item.label}}</text>
          </view>
        </view>
        <view class="nav-status current" wx:if="{{currentTab === item.key}}">当前</view>
      </view>
    </view>
  </view>

  <!-- 商品货架组件（view 包装器控制 hidden，组件始终存活保留状态） -->
  <view class="tab-content-wrapper" hidden="{{loading || currentTab !== 'exchange'}}">
    <exchange-shelf
      active="{{currentTab === 'exchange'}}"
      pointsBalance="{{totalPoints}}"
      frozenPoints="{{frozenPoints}}"
      assetBalances="{{assetBalances}}"
      theme="{{cardTheme}}"
      effects="{{effects}}"
      viewMode="{{viewMode}}"
      spaceId="{{currentSpace}}"
      refreshToken="{{_shelfRefreshToken}}"
      bind:exchange="onExchangeSuccess"
      bind:pointsupdate="onPointsUpdate"
      bind:viewmodechange="onViewModeChange"
      bind:autherror="onAuthError"
    />
  </view>

  <!-- 交易市场组件（view 包装器控制 hidden，组件始终存活保留状态） -->
  <view class="tab-content-wrapper" hidden="{{loading || currentTab !== 'market'}}">
    <exchange-market
      active="{{currentTab === 'market'}}"
      pointsBalance="{{totalPoints}}"
      assetBalances="{{assetBalances}}"
      theme="{{cardTheme}}"
      effects="{{effects}}"
      viewMode="{{viewMode}}"
      refreshToken="{{_marketRefreshToken}}"
      marketTypeFilters="{{marketTypeFilters}}"
      marketCategoryFilters="{{marketCategoryFilters}}"
      marketSortOptions="{{marketSortOptions}}"
      bind:purchase="onPurchaseSuccess"
      bind:pointsupdate="onPointsUpdate"
      bind:viewmodechange="onViewModeChange"
      bind:autherror="onAuthError"
    />
  </view>

  <!-- 资产转换组件（view 包装器控制 hidden，组件始终存活保留状态） -->
  <view class="tab-content-wrapper" hidden="{{loading || currentTab !== 'exchange-rate'}}">
    <asset-conversion
      active="{{currentTab === 'exchange-rate'}}"
      pointsBalance="{{totalPoints}}"
      refreshToken="{{_exchangeRateRefreshToken}}"
      bind:conversionsuccess="onConversionSuccess"
    />
  </view>

  <!-- 🔧 全屏维护遮罩（503 SYSTEM_MAINTENANCE 时激活） -->
  <maintenance-overlay />
</view>
